# Funciones SQL (`db.fn`)

Registro de funciones SQL traducidas al dialecto del motor, accesibles como
`db.fn.*`. Devuelven fragmentos SQL (texto de confianza) para incrustar en
`Query` o `Filter.raw`.

::: encino_orm.SqlFunctions

::: encino_orm.Weekday
