# Relaciones

Descriptores de las relaciones 1:1 (`Reference`) y 1:N (`HasMany`). Se registran
vía `_references_def` / `_has_many_def` o en tiempo de ejecución con
`add_reference()` / `add_has_many()`.

::: encino_orm.model.Reference

::: encino_orm.model.HasMany
