# Filtros (`Filter`)

El árbol de condiciones componible usado por `search()`, `count()`, `paginate()`
y ahora por `Model.has_many(...)`. Se combina con `&` (AND), `|` (OR) y `~` (NOT).

::: encino_orm.model.Filter

::: encino_orm.model.col
