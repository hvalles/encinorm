---
phase: 01-safety-net-ci-gates-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .git-blame-ignore-revs
  - .github/workflows/ci.yml
  - encino_orm/*.py
  - encino_orm/**/*.py
  - tests/*.py
autonomous: true
requirements: [CI-03]
must_haves:
  truths:
    - "`uv run ruff format --check encino_orm tests` exits 0 with no diff"
    - "`uv run ruff check encino_orm tests` exits 0, and exits non-zero the moment a new violation is introduced"
    - "The format-only commit is recorded in `.git-blame-ignore-revs`, so `git blame` skips it"
    - "A `lint` job exists in `ci.yml`, is a merge blocker, and carries no `continue-on-error`"
    - "Ruff suppressions are scoped to named files, each names the phase that lifts it, and no generic `pytest.raises(Exception)` or private-state assertion is rewritten in this phase (D-09)"
  artifacts:
    - path: "pyproject.toml"
      provides: "`[tool.ruff]` configuration + a version-pinned ruff dev dependency"
      contains: "line-length = 100"
    - path: ".git-blame-ignore-revs"
      provides: "Blame exclusion for the mechanical format commit"
    - path: ".github/workflows/ci.yml"
      provides: "Blocking lint job"
      contains: "lint"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "ruff reads `[tool.ruff]` from the repo root"
      pattern: "ruff (check|format)"
    - from: ".git-blame-ignore-revs"
      to: "the ruff format commit"
      via: "commit SHA line"
      pattern: "[0-9a-f]{40}"
---

## Phase Goal

> ROADMAP Phase 1 goal (verbatim): "CI can actually fail. Lint, format, types, per-engine coverage, a required-engine switch and pool characterization tests all exist and block merge, so that every later 'fix verified' claim is backed by a signal that would have caught the `COUNT(*)` bug."

**As a** maintainer hardening `encino_orm` for production use, **I want to** every quality gate to block merge and to have been proven to fail when it should, **so that** every later "fix verified" claim in this milestone is backed by a signal that would have caught the `COUNT(*)` bug.

> **MVP-mode discrepancy note:** the ROADMAP `**Goal:**` line is not in `**As a** … **I want to** … **so that** …` form. The user story above is *derived* from it and is unconfirmed. Run `/gsd mvp-phase 1` to normalize the ROADMAP goal; re-run `/gsd-plan-phase 1` if the derived framing is wrong.

<objective>
Turn the already-installed but unconfigured `ruff` into a real, blocking lint + format gate, landed as three bisectable mechanical commits (format first, then the `encino_orm/` rules commit, then the `tests/` rules commit) so that none is unreviewable.

Purpose: this is the first of five gates and the one that establishes the landing discipline for the rest of the phase — a mechanical commit isolated from the rules commits, with the mechanical commit excluded from `git blame`. It is also the only plan that reformats the tree, so it must land first: every later plan's new file must already be format-clean under this configuration.
Output: pinned `ruff`, `[tool.ruff]` config in `pyproject.toml`, a format-only commit recorded in `.git-blame-ignore-revs`, a clean `ruff check`, and a blocking `lint` CI job.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-VALIDATION.md
@pyproject.toml
@.github/workflows/ci.yml
@.gitignore

**Source of truth for every numeric value in this plan:** `01-RESEARCH.md` §Code Examples ("Ruff configuration") and §Standard Stack. All baselines were measured on this machine on 2026-09-17 with ruff 0.16.8.

**Measured baselines (do not re-derive, do re-confirm):**

| Scope | Stage | Ruleset | line-length | Errors |
|-------|-------|---------|-------------|--------|
| `ruff format --check` | pre-format | — | 100 | 61 of 101 files would be reformatted |
| `encino_orm/` | pre-format | full proposed | 100 | 128 |
| `encino_orm/` | pre-format | full proposed | 88 | 195 |
| `encino_orm/` | **post-format, after all scoped ignores** | full proposed | 100 | **69** (23 auto-fixable) |
| `tests/` | pre-format | narrow `E,F,I,UP,PT,B` (ignoring S101) | 100 | 102 |
| `tests/` | **post-format, after the scoped ignores** | full proposed | 100 | **63** (18 auto-fixable) |
| `tests/` | **post-format, after the scoped ignores** | narrow `E,F,I,UP,B` | 100 | **21** (16 auto-fixable) |
| `encino_orm/` + `tests/` | pre-format | full proposed | 100 | 1479 (1044 are `S101` in tests) |

**Read the table by stage.** The 128/102 figures are the *pre-`ruff format`* baselines quoted in RESEARCH; Task 2 reformats 61 files, and Tasks 3/4 must be sized against the **post-format** residuals (**69 source, 63 tests** — 132 findings total). Without the `S311`/`S324` scoped ignores the source residual is 72; those two entries are part of Task 3's config.

**Residuals that must NOT be suppressed and how each is handled** (measured post-format at the full ruleset):
- `F821` ×1 — `encino_orm/model/query_builder.py:283`, `Undefined name 'Records'` in `paginate(...) -> "Records"` while the module imports `Records` lazily inside the method body. **Real fix**, via a `TYPE_CHECKING` import (erased at runtime, so the deferred-import contract in `AGENTS.md` is preserved). Never a `# noqa`.
- `B008` ×7 — `encino_orm/http/routes.py` ×2 and `encino_orm/security/guard.py` ×5 (`Depends(...)` / `require(...)` in FastAPI dependency-injection defaults). Scoped `per-file-ignores`, lifted in Phase 6 (CFG-02).
- `S311` ×1 (`encino_orm/base.py`, `random` drives jittered backoff) and `S324` ×2 (`encino_orm/model/cached.py`, `encino_orm/model/filter.py`, `sha1` cache keys) — scoped `per-file-ignores` with a Spanish comment, **not** `# nosec`.
- Tests-side `S608` ×4 (`tests/test_sql_functions.py`), `S105` ×1 (`tests/test_security.py:28`, the test-only `SECRET` fixture) and `B008` ×2 (`tests/test_security.py:211`) — scoped `per-file-ignores`.

**Existing repo facts this plan must not break:**
- `pyproject.toml:54` already declares `ruff>=0.16.8` in `[dependency-groups].dev` — no new dependency, only a version pin.
- `.git-blame-ignore-revs` does **not** exist (verified).
- `tests/conftest.py` sets `WindowsSelectorEventLoopPolicy` on win32 — untouched by this plan.
- The suite is green today: `510 passed, 0 skipped`, `-W error` clean.

<interfaces>
Existing `ci.yml` structure the new `lint` job must match (house style, verified in the file):

- Triggers: `on: push.branches=[main]`, `on: pull_request`
- Top level: `permissions: contents: read`, `concurrency: { group: ci-${{ github.ref }}, cancel-in-progress: true }`
- Job shape: explicit `name:`, `runs-on: ubuntu-latest`, `timeout-minutes:`
- Setup steps used by every job:
  - `uses: actions/checkout@v5`
  - `uses: astral-sh/setup-uv@v5` with `with: { python-version: ..., enable-cache: true }`
  - `run: uv sync --extra http --extra security --extra graphql`
- The `test` job owns the MySQL + PostgreSQL service containers. New gate jobs do **not** need them.

`pyproject.toml` sections that exist today and must be preserved: `[build-system]`, `[tool.hatch.build.targets.wheel]`, `[project]`, `[project.optional-dependencies]`, `[project.urls]`, `[project.scripts]`, `[tool.pytest.ini_options]`, `[dependency-groups]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade uv, pin ruff exactly, add the ruff base config</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - `pyproject.toml` (the file being modified — note `[dependency-groups].dev` at lines 46-55 and that `[tool.ruff]` does not exist)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md` §Standard Stack (the `ruff` row and the "Pin `ruff` exactly" paragraph) and §Environment Availability (uv 0.8.22 is too old; `uv audit` is absent)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md` §`pyproject.toml` (modify) — "Sections to add"
    - `uv.lock` (the file being modified — only the lock resolution for ruff changes)
  </read_first>
  <action>
    Upgrade the local uv to at least 0.12.15 (`uv self update 0.12.15`, then confirm with `uv --version`). If `uv self update` is unavailable because uv was installed through pip, upgrade with `pip install -U uv` and record which path was used. This upgrade is a hard prerequisite for CI-07's `uv audit` in plan 01-02.

    In `pyproject.toml`, change the dev dependency `ruff>=0.16.8` to an exact pin `ruff==0.16.8`. Rationale to record as a comment: ruff's formatter output is versioned, so an unpinned ruff lets CI resolve a newer release whose formatting differs, producing a spurious `ruff format --check` failure on an unrelated PR. This pin must land BEFORE the format commit or the format commit's content is not reproducible.

    Add the `[tool.ruff]` top-level table (before `[tool.ruff.lint]`, which arrives in Task 3) with `target-version = "py310"` (must match `requires-python = ">=3.10"`; it drives the `UP` rule rewrites) and `line-length = 100`. The line length is load-bearing and must land BEFORE the format commit: at 88 the baseline is 195 source errors versus 128 at 100, and `ruff format` output itself depends on it. Add `[tool.ruff.lint.isort]` with `known-first-party = ["encino_orm"]`.

    Do NOT add `[tool.ruff.lint]` `select` in this task — selecting rules here would make Task 3 non-bisectable and could make the format commit land on a red tree.

    Comments in this repo are Spanish prose explaining *why* (`AGENTS.md` §Comments). Write the pin rationale and the line-length rationale as Spanish comments above the relevant keys.

    Run `uv lock` afterwards so `uv.lock` reflects the exact ruff pin.
  </action>
  <verify>
    <automated>uv --version && uv run ruff --version && uv run python -c "import sys,pathlib; tomllib=__import__('tomllib' if sys.version_info>=(3,11) else 'tomli'); d=tomllib.loads(pathlib.Path('pyproject.toml').read_text(encoding='utf-8')); assert d['tool']['ruff']['line-length']==100; assert d['tool']['ruff']['target-version']=='py310'; dev=d['dependency-groups']['dev']; assert any(x=='ruff==0.16.8' for x in dev), dev" && uv lock --check</automated>
  </verify>
  <acceptance_criteria>
    - `uv --version` prints a version `>= 0.12.15`
    - `uv run ruff --version` prints exactly `ruff 0.16.8`
    - `pyproject.toml` contains the literal strings `ruff==0.16.8`, `line-length = 100`, `target-version = "py310"`, and `known-first-party = ["encino_orm"]`
    - `pyproject.toml` does NOT yet contain a `[tool.ruff.lint]` `select` key
    - `uv lock --check` exits 0
    - `uv run pytest -q` still reports `510 passed` (no behavior change from the pin)
  </acceptance_criteria>
  <done>uv is ≥ 0.12.15, ruff is pinned to 0.16.8, and the ruff base config (line-length 100, target py310, isort first-party) is committed with its rationale as Spanish comments.</done>
</task>

<task type="auto">
  <name>Task 2: Land the ruff format-only commit and register it in .git-blame-ignore-revs</name>
  <files>encino_orm/*.py, encino_orm/**/*.py, tests/*.py, .git-blame-ignore-revs</files>
  <read_first>
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md` §Code Examples ("`.git-blame-ignore-revs`") — the exact three-step procedure
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md` §Established Patterns ("Commits mecánicos aislados con `.git-blame-ignore-revs` para el commit de formato")
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md` §Shared Patterns ("Isolated, bisectable mechanical commits")
    - `.gitignore` (the file being read, not modified — confirm `.git-blame-ignore-revs` is not ignored)
    - `tests/conftest.py` and one representative source file such as `encino_orm/pool.py` (to observe what `ruff format` changes — quote normalization and line wrapping only)
  </read_first>
  <action>
    Run `uv run ruff format encino_orm tests`. This reformats 61 of 101 files. Commit it ALONE as a mechanical change — the commit message must state it is formatting-only and contains no logic changes (Spanish, matching repo commit style, e.g. `style: ruff format (mecánico, sin cambios de lógica)`). Do not stage the `pyproject.toml`/`uv.lock` changes from Task 1 into this commit — those already landed separately.

    Immediately after the format commit, capture its SHA with `git rev-parse HEAD` and append it as a line to a new `.git-blame-ignore-revs` file, then commit that file separately. Add a short Spanish comment header above the SHA line explaining that this is the mechanical format commit. Finally run `git config blame.ignoreRevsFile .git-blame-ignore-revs` so the exclusion applies locally (GitHub honors the file automatically).

    Hard constraint: this commit must contain NO semantic change. If `ruff format` output looks like it alters behavior anywhere, stop and report — do not "fix" it inside this commit.

    Note: `.planning/phases/.../01-RESEARCH.md` §Open Questions item 5 asks that `mkdocs build --strict` be run once after the format commit to confirm docstrings were not disturbed. Run it if mkdocs is installed; if it is not installed locally, record that it was skipped and why.
  </action>
  <verify>
    <automated>uv run ruff format --check encino_orm tests && uv run pytest -q && git log --oneline -3 && git show --stat --format=%H HEAD~1 && python -c "import pathlib,re; t=pathlib.Path('.git-blame-ignore-revs').read_text(encoding='utf-8'); assert re.search(r'^[0-9a-f]{40}$', t, re.M), t"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff format --check encino_orm tests` exits 0 and prints no "would reformat" lines
    - `uv run pytest -q` reports `510 passed` — identical to the pre-format baseline
    - `.git-blame-ignore-revs` exists and contains at least one 40-hex-character line equal to the SHA of the format commit
    - `git config blame.ignoreRevsFile` prints `.git-blame-ignore-revs`
    - The format commit touches only `.py` files under `encino_orm/` and `tests/` (verify with `git show --name-only --format= <format-commit-sha>`)
    - `git status --porcelain` is clean apart from any intentionally untracked planning artifacts
  </acceptance_criteria>
  <done>`ruff format` is a no-op against the working tree, the suite is still 510-passed, and the format commit is excluded from blame via `.git-blame-ignore-revs`.</done>
</task>

<task type="auto">
  <name>Task 3: Land the source ruleset commit — fix the encino_orm/ findings</name>
  <files>pyproject.toml, encino_orm/*.py, encino_orm/**/*.py</files>
  <read_first>
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md` §Code Examples ("Ruff configuration") — the exact `[tool.ruff.lint]` block, `per-file-ignores` and the measured error counts; §Common Pitfalls 5 and 6 (`PT011`/`B017` vs D-09, and `S608` scoping)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md` §Implementation Decisions D-09 (no rewriting generic `pytest.raises(Exception)` or private-state assertions in this phase) and §the agent's Discretion (rule selection)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md` §`pyproject.toml` (modify)
    - `pyproject.toml` (the file being modified — append `[tool.ruff.lint]` and `[tool.ruff.lint.per-file-ignores]` after the `[tool.ruff]` table added in Task 1)
    - `encino_orm/model/query_builder.py` (lines ~275-290 — the `paginate` signature and the function-local `from .records import Records` that make the `F821` fire)
    - `AGENTS.md` §Import Organization and §Comments (the deferred-import contract and the Spanish-comment convention the new config must respect)
  </read_first>
  <action>
    Add `[tool.ruff.lint]` with `select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM", "PERF", "FURB", "ASYNC", "RUF", "S", "PT"]`. Do NOT enable `ANN`, `D` or `PL` in this milestone — record that decision as a Spanish comment with the rationale (101 files / 507 tests; enabling them wholesale buries the real findings).

    Add `[tool.ruff.lint.per-file-ignores]` with exactly these entries, each group carrying a Spanish comment naming the phase that lifts it. TOML forbids duplicate keys, so a file needing two rule families gets ONE entry listing both:

    - `"tests/**" = ["S101", "PT011", "B017", "ARG001", "ARG002"]`. Comment must state D-09: generic `pytest.raises(Exception)` and private-state assertions are NOT rewritten in Phase 1, so `PT011`/`B017` are ignored here and lifted in Phase 4. Do not drop `PT` entirely — `PT012`/`PT018`/`PT006` stay active.
    - `"tests/test_security.py" = ["B008", "S105"]`. Comment: `Depends(...)`/`require(...)` in FastAPI dependency-injection defaults (2 findings) and the test-only `SECRET` constant (1 finding) are framework idiom and a fixture, not defects; lifted in Phase 6 (CFG-02).
    - `"tests/test_sql_functions.py" = ["S608"]`. Comment: the test interpolates SQL on purpose to exercise the portable fragments.
    - `"encino_orm/sqlite.py"`, `"encino_orm/mysql.py"`, `"encino_orm/mariadb.py"`, `"encino_orm/postgresql.py"`, `"encino_orm/mssql.py"`, `"encino_orm/oracle.py"`, `"encino_orm/base.py"`, `"encino_orm/transfer.py"`, `"encino_orm/model/model.py"` → `["S608"]`. Comment must state that Phase 2 (DIAL-02) centralizes and validates these, and that the rest of `S` stays active.
    - `"encino_orm/http/routes.py" = ["S102", "B008"]` and `"encino_orm/graphql/schema.py" = ["S102"]`. Comment must point at CFG-03 (Phase 6) as the phase that removes the `exec()` generation and at CFG-02 (Phase 6) for the 2 `B008` `Depends(...)` defaults.
    - `"encino_orm/security/guard.py" = ["B008"]` (5 findings). Comment: FastAPI's documented dependency-injection idiom; lifted in Phase 6 (CFG-02).

    Fix the `encino_orm/` findings. Measured at this configuration **after** Task 2's `ruff format`: **69 findings**, 23 auto-fixable. Run `uv run ruff check --fix encino_orm` first, then hand-fix the remaining 46. Expected categories: `RUF013` ×13 (implicit `Optional` → explicit annotation), `UP045` ×13 (`Optional[X]` → `X | None`), `RUF022` ×6 (`__all__` ordering), `I001` ×6, `UP031` ×5, `B905` ×5 (`zip(..., strict=...)`), `B904` ×3, `PERF203` ×3, `SIM108` ×2, plus 13 singles including `F821`.

    Two findings need a scoped `per-file-ignores` entry rather than a fix, each with a Spanish comment stating the value is not security-relevant (do NOT use `# nosec`):
    - `S311` in `encino_orm/base.py` — `random` drives jittered deadlock-backoff waits.
    - `S324` in `encino_orm/model/cached.py` and `encino_orm/model/filter.py` — `sha1` builds cache keys, not a security boundary.

    One finding must be a REAL fix, never a suppression: `F821` (`Undefined name 'Records'`) at `encino_orm/model/query_builder.py:283`, where `paginate(...) -> "Records"` annotates with a name the module imports lazily inside the method body. Add `from typing import TYPE_CHECKING` to the module imports and an `if TYPE_CHECKING:` block importing `Records` from `.records`. A `TYPE_CHECKING` import is erased at runtime, so the deferred-import contract in `AGENTS.md` §Import Organization (the core must not acquire hard dependencies of optional layers at import time) is preserved. Do NOT add a `# noqa: F821`.

    Do NOT add `# noqa` anywhere to reach green: `RUF100` (active via `RUF`) fails unused `noqa` directives, and the repo's baseline for both `# noqa` and `# type: ignore` is zero.

    Commit this as its own bisectable commit (rules + `encino_orm/` fixes), separate from the format commit (Task 2) and from the tests commit (Task 4).
  </action>
  <verify>
    <automated>uv run ruff check encino_orm && uv run ruff format --check encino_orm && uv run pytest -q && uv run python -c "import sys,pathlib; tomllib=__import__('tomllib' if sys.version_info>=(3,11) else 'tomli'); d=tomllib.loads(pathlib.Path('pyproject.toml').read_text(encoding='utf-8')); s=set(d['tool']['ruff']['lint']['select']); assert {'E','W','F','I','UP','B','C4','SIM','PERF','FURB','ASYNC','RUF','S','PT'} <= s, s; assert 'ANN' not in s and 'D' not in s and 'PL' not in s, s; ig=d['tool']['ruff']['lint']['per-file-ignores']; codes={r for rules in ig.values() for r in rules}; assert {'S608','S102','B008'} <= codes, codes; assert {'PT011','B017'} <= set(ig['tests/**']); assert set(ig['encino_orm/http/routes.py']) == {'S102','B008'}; assert all('F821' not in v for v in ig.values()); assert 'S311' in ig['encino_orm/base.py'] and 'S324' in ig['encino_orm/model/cached.py']"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check encino_orm` exits 0 with no output
    - `uv run ruff format --check encino_orm` exits 0
    - `uv run pytest -q` still reports `510 passed` (the rule fixes must not change behavior)
    - `pyproject.toml` `[tool.ruff.lint].select` contains all fourteen families and none of `ANN`/`D`/`PL`
    - `[tool.ruff.lint.per-file-ignores]` contains a `tests/**` entry including `PT011` and `B017`, `S608` scoped to the dialect modules plus `tests/test_sql_functions.py`, `B008` scoped to `encino_orm/http/routes.py`, `encino_orm/security/guard.py` and `tests/test_security.py`, `S311`/`S324` scoped to their named files, and **no `F821` suppression anywhere**
    - `encino_orm/model/query_builder.py` imports `Records` under `TYPE_CHECKING` and contains no `# noqa`
    - The SUMMARY records the measured `encino_orm/` count before the fixes (69), the auto-fixed count (23) and the hand-fixed count (46)
  </acceptance_criteria>
  <done>`ruff check encino_orm` is clean at the full ruleset, every source finding is either fixed or scoped with a documented Spanish comment, and the suite is still 510 passed.</done>
</task>

<task type="auto">
  <name>Task 4: Land the tests ruleset commit and add the blocking lint CI job</name>
  <files>tests/*.py, .github/workflows/ci.yml</files>
  <read_first>
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md` §Code Examples ("Ruff configuration") — the measured counts and the `per-file-ignores` block; §Common Pitfalls 5 (`PT011`/`B017` vs D-09)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md` §Implementation Decisions D-09 and §the agent's Discretion (rule selection)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md` §`.github/workflows/ci.yml` (modify)
    - `.github/workflows/ci.yml` (the file being modified — match its house style: explicit `name:` on job and steps, `timeout-minutes:`, `permissions: contents: read`)
    - `pyproject.toml` (read-only here — the `[tool.ruff.lint]` block landed in Task 3; do not change the ruleset)
  </read_first>
  <action>
    Fix the `tests/` findings. Measured at this configuration **after** Task 2's `ruff format`: **63 findings**, 18 auto-fixable. Run `uv run ruff check --fix tests` first, then hand-fix the remaining 45. Expected categories: `RUF012` ×25 (mutable class defaults → annotate with `typing.ClassVar`), `PT018` ×12 (split `assert a and b` into two asserts — behavior-preserving, and NOT covered by D-09, which protects only generic `pytest.raises(Exception)` and private-state asserts), `I001` ×9, `F401` ×7, `E501` ×5 (the lines `ruff format` cannot wrap), plus `SIM117`/`SIM105`/`S110`/`RUF059`/`SIM300`.

    Do NOT shrink this by changing the ruleset. In particular do NOT narrow the tests-side selection: ruff has no per-directory `select`, so narrowing would require an all-ignoring `per-file-ignores` entry. For reference, the measured alternative (narrow `E,F,I,UP,B`, post-format) is **21** findings; the full ruleset is kept because `PT011`/`B017` are already ignored per D-09 and the remaining `PT018` splits are legitimate and behavior-preserving.

    Do NOT add `# noqa` to reach green (`RUF100` is active and the repo baseline is zero).

    Add a `lint` job to `.github/workflows/ci.yml` that is a merge blocker:
    - Trigger: unchanged for now (`workflow_call` arrives in plan 01-04, which owns CI-08).
    - Job `name:` in the house style, `runs-on: ubuntu-latest`, `timeout-minutes:` set explicitly.
    - Steps: `actions/checkout@v5`, then `astral-sh/setup-uv@v5` with `with: { version: "0.12.15", enable-cache: true }`, then `uv sync --group dev`, then `uv run ruff check encino_orm tests`, then `uv run ruff format --check encino_orm tests`.
    - Also add `version: "0.12.15"` to the existing `setup-uv` step in the `test` job so the whole workflow pins one uv version (research assumption A1 — confirm the input name against the action's README before committing; if the input is named differently, use the documented name and record it).
    - HARD RULE: never add `continue-on-error: true` to this job or any gate job in this phase.

    Commit this as its own bisectable commit (tests fixes + CI job), separate from the format and source-rules commits.
  </action>
  <verify>
    <automated>uv run ruff check encino_orm tests && uv run ruff format --check encino_orm tests && uv run pytest -q && uv run python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8')); print('ci.yml is valid YAML')" && uv run python -c "import pathlib; t=pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8'); assert 'continue-on-error' not in t; assert 'lint' in t; assert 'ruff check' in t"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check encino_orm tests` exits 0 with no output
    - `uv run ruff format --check encino_orm tests` exits 0
    - `uv run pytest -q` still reports `510 passed` (the rule fixes must not change behavior)
    - `.github/workflows/ci.yml` parses as valid YAML (`yaml.safe_load` succeeds — an invalid file would silently disable every gate) and contains a `lint` job that runs `ruff check` and `ruff format --check`; the file contains the string `continue-on-error` zero times
    - The count of `# noqa` occurrences in the repo is still **0**
    - The SUMMARY records the measured tests-side count before the fixes (63), the auto-fixed count (18) and the hand-fixed count (45), plus the measured narrow-ruleset alternative (21)
    - **Induced-failure proof (must be performed and recorded):** append a temporary unused import to a scratch copy of a source module (or use `ruff check --select F401 --isolated` on a temp file), confirm `uv run ruff check` exits non-zero, then revert. `git status --porcelain` must be clean afterwards.
  </acceptance_criteria>
  <done>`ruff check` and `ruff format --check` are both clean across `encino_orm/` and `tests/` at the full ruleset, and a blocking, YAML-valid `lint` job exists in `ci.yml` with no `continue-on-error`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → CI | A commit that passes locally must be the same commit CI gates; an unpinned formatter breaks this equivalence |
| workflow YAML → merge protection | The gate is only real if the job is blocking and cannot be softened |
| ruff config → source tree | `per-file-ignores` is a suppression surface; an over-broad entry silently disables a rule family |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Repudiation / Elevation | `.github/workflows/ci.yml` `lint` job | mitigate | The `lint` job is blocking, has an explicit `timeout-minutes`, and `continue-on-error` is forbidden — verified by an acceptance criterion that greps the workflow file for the string. The gate is proven by an induced failure (unused import → non-zero exit), not by a green run. |
| T-01-04 | Tampering / Supply Chain | `astral-sh/setup-uv@v5`, `ruff==0.16.8` | mitigate | Pin the uv `version:` input on `setup-uv` so CI cannot resolve a future uv with different behavior, and pin ruff exactly so `ruff format --check` output is reproducible. Full action-SHA pinning is explicitly deferred (RESEARCH §Known Threat Patterns); record that deferral as a workflow comment rather than silently omitting it. |
| T-01-05 | Repudiation | `# noqa` directives in `encino_orm/` and `tests/` | mitigate | `RUF` is in the selected set, so `RUF100` fails any unused `noqa`. Baseline is zero `# noqa` today; the acceptance criteria require `ruff check` to exit 0 without blanket suppression. |
| T-01-06 | Tampering | `[tool.ruff.lint.per-file-ignores]` | accept | Over-broad `per-file-ignores` could hide a real finding. Accepted because every entry is scoped to named files/modules, carries a Spanish comment naming the follow-up phase (Phase 2 DIAL-02, Phase 4 PT011/B017, Phase 6 CFG-02/CFG-03), and `S`/`PT` remain active everywhere else. The `B008` entries are the newest suppression surface (7 source + 2 test findings) and are the only ones whose rule family is suppressed for a framework idiom rather than a data-safety boundary — hence the explicit CFG-02 lift comment. `F821` is deliberately NOT suppressed: it is fixed with a `TYPE_CHECKING` import, asserted absent from every ignore list. |
| T-01-15 | Tampering | `.github/workflows/ci.yml` / `release.yml` YAML validity | mitigate | A workflow file that is syntactically invalid can be silently ignored by GitHub while every substring check in the acceptance criteria still passes. Every task that edits a workflow file must parse it with `yaml.safe_load` in its verify, so an invalid edit fails locally instead of disabling the gate in CI. |

**No package installs occur in this plan** (ruff is already a dev dependency; uv is already the project's tool), so no package-legitimacy checkpoint is required here. The supply-chain checkpoint for the newly added dev dependencies lives in plan 01-02.
</threat_model>

<verification>
Run all of the following from the repo root and require every one to exit 0:

- `uv --version` → ≥ 0.12.15
- `uv run ruff --version` → `ruff 0.16.8`
- `uv run ruff check encino_orm tests`
- `uv run ruff format --check encino_orm tests`
- `uv run pytest -q` → `510 passed`
- `uv lock --check`
- `uv run python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8'))"` → parses (an invalid `ci.yml` would silently disable every gate while all substring checks still pass)
- `git config blame.ignoreRevsFile` → `.git-blame-ignore-revs`
- `git log --oneline -5` → shows the format commit, the blame-file commit, the source-rules commit, and the tests-rules commit as four separate commits

**Induced-failure demonstration (phase success criterion 3, lint half):** introduce a deliberate `F401` violation in a scratch module, confirm `uv run ruff check` exits non-zero, revert, confirm the tree is clean. A green run alone does not prove the gate.
</verification>

<success_criteria>
- `ruff` is pinned to 0.16.8 and configured with `line-length = 100`, `target-version = "py310"`, the fourteen-family `select`, and documented scoped ignores (including the `B008`/`S105`/`S608`/`S311`/`S324` entries, each with a Spanish comment naming the lifting phase)
- The `F821` in `encino_orm/model/query_builder.py` is fixed with a `TYPE_CHECKING` import, not suppressed
- The format-only commit is isolated, recorded in `.git-blame-ignore-revs`, and does not change the test outcome; the source-rules and tests-rules fixes land as two further separate, bisectable commits
- `ruff check` and `ruff format --check` are clean across `encino_orm/` and `tests/`
- A blocking `lint` job exists in `ci.yml` with no `continue-on-error`, the file parses as valid YAML, and the gate has been proven to fail on an induced violation
- The suite is still `510 passed` — zero behavior change from this plan
</success_criteria>

<output>
Create `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-01-SUMMARY.md` when done.

The summary MUST record: the uv version actually installed and the upgrade path used (`uv self update` vs pip), the exact SHA of the format commit, the measured `ruff check` counts before the fixes (pre-format 128 source / 102 tests; post-format 69 source / 63 tests) and after (0/0), the final `select` list, every `per-file-ignores` entry with its justification and its lifting phase, the `F821` `TYPE_CHECKING` fix, and the induced-failure transcript for the lint gate.

**Deliberate deviation from `AGENTS.md` §Code Style (W7 — must be recorded here and in `AGENTS.md`):** `line-length = 100` is a conscious departure from the convention text "practical line limit near 88 chars … Match the surrounding file rather than imposing a new limit". The measured rationale is in the baselines table above: at 88 the pre-format source baseline is 195 findings versus 128 at 100, and `ruff format`'s output itself depends on the setting, so 100 produces a materially smaller mechanical reformat diff (61 of 101 files either way, but far fewer wrap-induced churn lines and rule findings). The config value wins; the `AGENTS.md` §Code Style line has been updated to name the config as the source of truth so convention and config no longer contradict.
</output>
