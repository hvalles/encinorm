---
phase: 01-safety-net-ci-gates-test-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
  - .gitignore
  - tests/test_mysql.py
  - tests/test_postgresql.py
  - tests/test_mariadb.py
  - tests/test_mssql.py
  - tests/test_oracle.py
  - tests/test_redis_cache.py
  - tests/test_pytest_config.py
autonomous: true
requirements: [CI-05, CI-06]
must_haves:
  truths:
    - "An unregistered marker fails collection under `--strict-markers`, and an emitted warning fails the suite"
    - "`pytest -m \"integration\"` selects a non-zero count and `-m \"not integration\"` deselects it — the command documented in `README.md:130` is real, not a no-op"
    - "Coverage is collected per matrix leg into a distinct file, combined across legs, and a drop below the ratchet floor fails the job"
    - "The floor is pinned against the CI-equivalent baseline (83%), not the local six-engine baseline (88%)"
    - "The threshold is a low global no-drop ratchet plus a per-dialect floor explicitly deferred to Phase 2 — Phase 1 builds the mechanism only (D-04, D-05, D-06)"
    - "`filterwarnings` starts with exactly `\"error\"` and an empty, commented allowlist, so a new warning fails the suite (D-07)"
    - "All four markers are registered and `integration` is applied so the command documented in `README.md:130` works (D-08)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Hardened `[tool.pytest.ini_options]`, `[tool.coverage.run]` and `[tool.coverage.report]`"
      contains: "[tool.coverage.run]"
    - path: "tests/test_pytest_config.py"
      provides: "Regression guards proving each hardening knob is both configured and functional"
      exports: ["TestPyprojectHardening", "TestStrictMarkersBehaviour", "TestWarningsAsErrors", "TestMarkerSelectionRegression"]
    - path: ".github/workflows/ci.yml"
      provides: "Per-leg `COVERAGE_FILE`, hidden-file artifact upload, and a `coverage` combine job"
      contains: "coverage combine"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "`coverage report` applies `fail_under` from `[tool.coverage.report]`"
      pattern: "coverage report"
    - from: ".github/workflows/ci.yml (test job)"
      to: ".github/workflows/ci.yml (coverage job)"
      via: "`actions/upload-artifact` with `include-hidden-files: true` → `actions/download-artifact`"
      pattern: "include-hidden-files"
    - from: "tests/test_*.py engine classes"
      to: "pyproject.toml `markers`"
      via: "applied `integration` / `optional_engine` markers, enforced by `--strict-markers`"
      pattern: "pytest.mark.(integration|optional_engine)"
---

## Phase Goal

> ROADMAP Phase 1 goal (verbatim): "CI can actually fail. Lint, format, types, per-engine coverage, a required-engine switch and pool characterization tests all exist and block merge, so that every later 'fix verified' claim is backed by a signal that would have caught the `COUNT(*)` bug."

**As a** maintainer hardening `encino_orm` for production use, **I want to** every quality gate to block merge and to have been proven to fail when it should, **so that** every later "fix verified" claim in this milestone is backed by a signal that would have caught the `COUNT(*)` bug.

> **MVP-mode discrepancy note:** the ROADMAP `**Goal:**` line is not in user-story form. The story above is derived and unconfirmed. Run `/gsd mvp-phase 1` to normalize it.

<objective>
Harden the pytest harness so it cannot silently pass on a warning, an unregistered marker, or a non-strict xfail — and wire `pytest-cov` so coverage is collected per matrix leg, combined across legs, and gated by a no-drop ratchet.

Purpose: CI-06 removes three ways the suite could report green while verifying less than it claims (warnings swallowed, markers inert, xfails non-strict). CI-05 builds the *mechanism* for per-engine coverage — per D-06 the per-dialect floor is defined in Phase 2, when the `dialects/` seam exists to apply it to.
Output: hardened `[tool.pytest.ini_options]`, applied `integration`/`optional_engine` markers, `[tool.coverage.*]` with `parallel = true` and `fail_under = 82`, a `coverage` combine job, and `tests/test_pytest_config.py` proving each knob works.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-VALIDATION.md
@pyproject.toml
@.github/workflows/ci.yml
@tests/test_pool.py
@README.md

**Source of truth:** `01-RESEARCH.md` §Code Examples ("pytest configuration", "Marker application map"), §Pattern 3 ("Coverage combine across matrix legs"), §Common Pitfalls 4 (`pytest-asyncio` loop-scope ambiguity), §Pitfall 2b (the coverage floor must use the CI-equivalent number), §Open Questions item 4 (the `-m "integration"` regression guard).

**Coverage baselines (measured 2026-09-17, coverage 7.16.1, branch coverage):**
- **88%** local, with all six engines running in Docker
- **83%** CI-equivalent, i.e. with the optional-engine test files excluded (only MySQL + PostgreSQL, 475 tests)
- `oracle.py` alone falls from 59% to 16% when the optional-engine tests are excluded

The floor MUST be pinned from the 83% figure. A floor set at the local 88% fails on the first CI run.

**Verified safe today (so enabling the knobs is free):** 0 xfails, 0 `@pytest.mark.skip`, 0 `skipif`, 0 `importorskip`, 0 warnings under `-W error`, and the only marker in use is `pytest.mark.asyncio` (306 occurrences).

**`-m` counts after marker application:** `integration` = 41, `optional_engine` = 13 (a subset of `integration`), `-m "not integration"` = 469, `-m "not optional_engine"` = 497, total = 510.

**Gotcha (verified):** `--strict-markers` does NOT validate `-m` expressions. `pytest --strict-markers -m "integration"` silently deselected all 510 tests today with no error. The marker only becomes meaningful once it is *applied* to tests — which is why Task 2 exists.

**Gotcha (verified):** `actions/upload-artifact` excludes hidden files by default. `.coverage.py3.10` is a hidden file, so `include-hidden-files: true` is mandatory or `coverage combine` finds nothing.

<interfaces>
`tests/conftest.py` today (preserve exactly; plan 01-04 extends it, this plan does not touch it):
- `asyncio.set_event_loop_policy(asyncio.WindowsSelectorEventLoopPolicy())` on win32
- fixtures `db` (returns `SqliteDb()`) and `connected_db` (connect `:memory:`, yield, close)

`pyproject.toml` `[tool.pytest.ini_options]` today — four keys only:
`asyncio_mode = "auto"`, `testpaths = ["tests"]`, `pythonpath = ["."]`.

Engine test file layout the marker map targets (class names and line numbers verified):

| File | Unit-only classes (do NOT mark) | Live classes (mark) |
|------|--------------------------------|---------------------|
| `tests/test_mysql.py` | — (every class is live → module-level `pytestmark`) | `TestMysqlLifecycle:41`, `TestMysqlBuildersAndQueries:110`, `TestMysqlMigrations:211` |
| `tests/test_postgresql.py` | `TestPostgresInternal:17` | `TestPostgresLifecycle:86`, `TestPostgresBuildersAndQueries:132`, `TestPostgresMigrations:228` |
| `tests/test_mariadb.py` | the two module-level unit tests (`test_mariadb_is_mysql_subclass`, `_ddl_map_`) | `TestMariadbLifecycle:49` |
| `tests/test_mssql.py` | `TestMssqlInternal:24` | `TestMssqlLifecycle:140` |
| `tests/test_oracle.py` | `TestOracleInternal:29` | `TestOracleLifecycle:108` |
| `tests/test_redis_cache.py` | — (every class is live → module-level `pytestmark`) | `TestRedisCacheBackend:43`, `TestCachedModelRedis:56` |

Preserving the unmarked unit classes matters: those 28 optional-engine *unit* tests pass without any live engine and must keep contributing coverage in the required-engine CI job.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden [tool.pytest.ini_options] and fix any resulting failures</name>
  <files>pyproject.toml</files>
  <read_first>
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md` §Code Examples ("pytest configuration") — the exact hardened block and the verified-safe list; §Common Pitfalls 4; §Assumptions Log A4
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md` §Implementation Decisions D-07 (commented narrow allowlist), D-08 (register the four markers) and D-09 (config + minimal fixes, separate bisectable commits)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md` §`pyproject.toml` (modify)
    - `pyproject.toml` (the file being modified — replace the four-key `[tool.pytest.ini_options]` block)
    - `README.md` (line ~130 documents `uv run pytest -m "not integration"` — the command D-08 makes real)
  </read_first>
  <action>
    Replace `[tool.pytest.ini_options]` with the hardened block. Keep the three existing keys (`asyncio_mode = "auto"`, `testpaths = ["tests"]`, `pythonpath = ["."]`) and add:
    - `asyncio_default_fixture_loop_scope = "function"` and `asyncio_default_test_loop_scope = "function"` — both confirmed valid ini keys in pytest-asyncio 1.4.0. Setting both explicitly removes the loop-scope ambiguity where a session-scoped loop would let a leaked pool or the `set_default_db` singleton survive across tests.
    - `addopts = "-ra --strict-markers"`.
    - `xfail_strict = true`.
    - `filterwarnings = ["error"]` with a Spanish comment above it stating the allowlist policy: entries may only be added with a written justification naming the module and the reason; a new warning must fail. **Start the allowlist empty** — the current suite is verified warning-free under `-W error`. If a Python 3.12/3.13 matrix leg later fails on a version-specific `DeprecationWarning`, add a narrowly-scoped commented `ignore::DeprecationWarning:<module>` entry rather than weakening the global `"error"` rule.
    - `markers` with exactly four entries, each with a Spanish description: `integration` (requires a live engine; MySQL/PostgreSQL in CI, optional engines locally), `optional_engine` (an engine that is NOT required in Phase 1 — MariaDB/Redis/MSSQL/Oracle; see D-02), `concurrency` (pool/race tests; excluded from xdist runs), `benchmark` (measured by pytest-codspeed; excluded from normal runs).

    Then run the full suite. Per D-09 this task may apply *minimal* fixes for incompatibilities the hardening surfaces — unregistered markers, warnings, non-strict xfails — but must NOT rewrite generic `pytest.raises(Exception)` assertions or private-state assertions. Any such fix must be its own bisectable commit. If the suite is green with no fixes needed (research says it should be), record that explicitly in the SUMMARY — do not manufacture a fix.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run python -c "import sys,pathlib; tomllib=__import__('tomllib' if sys.version_info>=(3,11) else 'tomli'); p=tomllib.loads(pathlib.Path('pyproject.toml').read_text(encoding='utf-8'))['tool']['pytest']['ini_options']; assert '--strict-markers' in p['addopts'], p; assert p['xfail_strict'] is True; assert p['filterwarnings'][0]=='error', p['filterwarnings']; assert p['asyncio_default_fixture_loop_scope']=='function'; assert p['asyncio_default_test_loop_scope']=='function'; m=p['markers']; names={x.split(':')[0].strip() for x in m}; assert {'integration','optional_engine','concurrency','benchmark'} <= names, names; assert p['asyncio_mode']=='auto' and p['testpaths']==['tests'] and p['pythonpath']==['.']"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` reports `510 passed` (the hardening itself changes no outcome)
    - `pyproject.toml` `[tool.pytest.ini_options]` contains `addopts` with `--strict-markers`, `xfail_strict = true`, `filterwarnings` whose first element is exactly `"error"`, both loop scopes set to `"function"`, and all four markers registered with descriptions
    - `[tool.pytest.ini_options]` still contains `asyncio_mode = "auto"`, `testpaths = ["tests"]`, `pythonpath = ["."]`
    - The `filterwarnings` allowlist is empty apart from the `"error"` entry, with a Spanish policy comment above it
    - Any minimal fix applied is in its own commit and is described in the SUMMARY; the count of `pytest.raises(Exception)` occurrences in `tests/` is unchanged from the pre-task baseline
  </acceptance_criteria>
  <done>The pytest config is hardened with all six knobs and all four markers, the allowlist is empty with a documented policy, and the suite is still 510 passed with no assertion rewrites.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the integration and optional_engine markers per the marker map</name>
  <files>tests/test_mysql.py, tests/test_postgresql.py, tests/test_mariadb.py, tests/test_mssql.py, tests/test_oracle.py, tests/test_redis_cache.py</files>
  <read_first>
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md` §Code Examples ("Marker application map") — the exact per-file/per-class table and the totals
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md` §Implementation Decisions D-02 (optional engines use an explicit marker) and D-08 (apply `integration` so the README command works)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md` §`tests/conftest.py` (modify) — the "Marker application style" paragraph
    - `tests/test_mysql.py`, `tests/test_postgresql.py`, `tests/test_mariadb.py`, `tests/test_mssql.py`, `tests/test_oracle.py`, `tests/test_redis_cache.py` (the files being modified — read only the import block and the class declarations; the `interfaces` block in `<context>` lists the exact classes and line numbers)
  </read_first>
  <action>
    Apply the markers exactly per the map in the `<interfaces>` block above. Two styles, and the distinction is load-bearing:

    - **Module-level `pytestmark`** only where every class in the file needs a live engine: `tests/test_mysql.py` → `pytestmark = pytest.mark.integration`; `tests/test_redis_cache.py` → `pytestmark = [pytest.mark.integration, pytest.mark.optional_engine]`. Place it immediately after the imports, before the first class.
    - **Class-level decorators** in the mixed files, so the unit-only classes keep running without an engine:
      - `tests/test_postgresql.py`: `@pytest.mark.integration` on `TestPostgresLifecycle`, `TestPostgresBuildersAndQueries`, `TestPostgresMigrations`. `TestPostgresInternal` stays unmarked.
      - `tests/test_mariadb.py`: `@pytest.mark.integration` and `@pytest.mark.optional_engine` on `TestMariadbLifecycle`. The two module-level unit tests stay unmarked.
      - `tests/test_mssql.py`: `@pytest.mark.integration` and `@pytest.mark.optional_engine` on `TestMssqlLifecycle`. `TestMssqlInternal` stays unmarked.
      - `tests/test_oracle.py`: `@pytest.mark.integration` and `@pytest.mark.optional_engine` on `TestOracleLifecycle`. `TestOracleInternal` stays unmarked.

    Do NOT mark the unit-only classes — the 28 optional-engine unit tests they contain pass without any live engine and must keep contributing coverage in the required-engine CI job.

    Do NOT change any skip behaviour in this task. The `pytest.skip(...)` calls stay exactly as they are; plan 01-04 replaces them with the required-engine switch. This plan only adds markers.

    This is a mechanical marker-application edit: no test body, assertion or fixture changes.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest --collect-only -q -m "integration" | tail -1 && uv run pytest --collect-only -q -m "optional_engine" | tail -1 && uv run pytest --collect-only -q -m "not integration" | tail -1 && uv run pytest --collect-only -q -m "not optional_engine" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --collect-only -q -m "integration"` reports exactly **41** tests collected
    - `uv run pytest --collect-only -q -m "optional_engine"` reports exactly **13** tests collected
    - `uv run pytest --collect-only -q -m "not integration"` reports exactly **469** tests collected
    - `uv run pytest --collect-only -q -m "not optional_engine"` reports exactly **497** tests collected
    - `uv run pytest -q` (no `-m`) still reports `510 passed` — the markers change selection, not outcomes
    - `TestPostgresInternal`, `TestMssqlInternal`, `TestOracleInternal` and the two `test_mariadb` module-level unit tests carry no `integration` marker (grep the class declarations and confirm)
    - The number of `pytest.skip` calls in `tests/` is unchanged from the pre-task baseline (this task adds markers only)
  </acceptance_criteria>
  <done>The four `-m` selections return exactly 41 / 13 / 469 / 497 collected tests, the unit-only classes remain unmarked, and the full suite is still 510 passed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pytest-cov with the combine mechanism, the ratchet floor, and the config regression guards</name>
  <files>pyproject.toml, .github/workflows/ci.yml, .gitignore, tests/test_pytest_config.py</files>
  <read_first>
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-RESEARCH.md` §Pattern 3 ("Coverage combine across matrix legs") — the exact `[tool.coverage.run]` / `[tool.coverage.report]` sketch, the `COVERAGE_FILE` env, the `include-hidden-files: true` gotcha, and the `coverage` job; §Common Pitfalls 2 and 2b; §Assumptions Log A3
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md` §Implementation Decisions D-04, D-05, D-06 (mechanism now, per-dialect floor in Phase 2, no-drop ratchet)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-PATTERNS.md` §`tests/test_ci_harness.py` (test, unit + file-I/O) — the `main(argv) -> int` / `tmp_path` / `pytest.raises` conventions to reuse for the regression guards; §`pyproject.toml` (modify)
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-VALIDATION.md` §Per-Task Verification Map rows `01-03-01`, `01-03-02`, `01-03-03`, `01-03-04`
    - `pyproject.toml` (the file being modified — append `[tool.coverage.run]` and `[tool.coverage.report]` after `[tool.pytest.ini_options]`)
    - `.github/workflows/ci.yml` (the file being modified — the `test` job gains the coverage env and artifact upload; a new `coverage` job is appended after the three gate jobs from plans 01-01/01-02)
    - `.gitignore` (the file being modified — the existing entry is the exact `.coverage`, which does NOT match coverage's per-leg artifacts; add the glob form)
    - `README.md` (line ~130 — the documented `uv run pytest -m "not integration"` command)
  </read_first>
  <action>
    Add `[tool.coverage.run]` with `source_pkgs = ["encino_orm"]`, `branch = true`, `parallel = true`. `parallel = true` is REQUIRED: each matrix leg must write its own data file or the legs overwrite each other.

    Add `[tool.coverage.report]` with `precision = 1`, `show_missing = true`, `fail_under = 82`, and `exclude_also = ["if TYPE_CHECKING:", "raise NotImplementedError", "@overload"]`.

    The floor value is `82`, one point under the measured CI-equivalent baseline of 83%. Above it, write a Spanish comment recording: the measurement date, the 88% local figure and why it must not be used (all six engines run locally; only MySQL + PostgreSQL run in CI; `oracle.py` falls 59% → 16% when the optional-engine tests are excluded), and that Phase 2 raises the floor once the per-dialect seam exists (D-04/D-06). Do NOT set the floor from a local run.

    In `.github/workflows/ci.yml`:
    - In the `test` job, add `COVERAGE_FILE: .coverage.py${{ matrix.python-version }}` to the existing `env:` map on the test step and extend the run command to `uv run pytest -q --junitxml=junit.xml --cov=encino_orm --cov-branch --cov-report=`. Do NOT add `-m "not optional_engine"`, `ENCINO_ORM_REQUIRE_ENGINES`, or the JUnit gate step — plan 01-04 owns those; adding them here would make that plan's induced-failure proof impossible to attribute.
    - Add an `actions/upload-artifact@v4` step to the `test` job with `if: always()`, `name: coverage-py${{ matrix.python-version }}`, `path: .coverage.py${{ matrix.python-version }}*`, and `include-hidden-files: true`. The last input is mandatory: `.coverage.*` is a hidden file and is silently excluded without it.
    - Add a `coverage` job with `needs: [test]`, `runs-on: ubuntu-latest`, explicit `timeout-minutes:`, and steps: `actions/checkout@v5` → `astral-sh/setup-uv@v5` with `with: { version: "0.12.15", enable-cache: true }` → `uv sync --group dev` → `actions/download-artifact@v4` with `pattern: coverage-py*` and `merge-multiple: true` → `uv run coverage combine` → `uv run coverage report`. The report step applies `fail_under` from `pyproject.toml`.
    - Add `[tool.coverage.paths]` ONLY if the first real CI run reports "No data to combine" (research assumption A3). Do not add it speculatively.

    Add a `.coverage*` line to `.gitignore`. This is load-bearing and the existing file does **not** cover it: `.gitignore` has an exact `.coverage` entry, but `parallel = true` makes coverage write per-leg artifacts named `.coverage.py3.10.<host>.<pid>.<rand>` (and the local verification below writes `.coverage.local`), none of which the exact entry matches. A substring check for `.coverage` passes vacuously today — the plan must add the glob and assert the literal `.coverage*` is present (W4).

    Create `tests/test_pytest_config.py` with four guard classes, all using the repo's existing conventions (plain `Test*` classes, `tmp_path`, `subprocess`, and the `tomllib`/`tomli` compatibility shim — `try: import tomllib` / `except ModuleNotFoundError: import tomli as tomllib` — because the declared Python 3.10 floor has no stdlib `tomllib` and `tomli` is present only transitively via pytest on <3.11; no new dependency):
    - `TestPyprojectHardening` — reads `pyproject.toml` through the `tomllib`/`tomli` shim (NOT a bare `import tomllib`, which raises `ModuleNotFoundError` on 3.10 and would fail collection on the local venv and the CI 3.10 matrix leg) and asserts each knob from Task 1 is present with the exact value (addopts contains `--strict-markers`, `xfail_strict is True`, `filterwarnings[0] == "error"`, both loop scopes `"function"`, and the four marker names are registered).
    - `TestStrictMarkersBehaviour` — writes a probe test file into `tmp_path` containing `@pytest.mark.marker_que_no_existe` on a trivial test, runs it with `subprocess.run([sys.executable, "-m", "pytest", "-c", "pyproject.toml", str(probe), "-q", "--no-header", "-p", "no:cacheprovider"], cwd=<repo root>, capture_output=True, text=True)`, and asserts the return code is non-zero. This proves the knob is functional, not merely present. Note in a Spanish comment that `-c pyproject.toml` is what makes the probe inherit the repo's `addopts`.
    - `TestWarningsAsErrors` — same subprocess shape with a probe that calls `warnings.warn("probe")` inside a test, asserting a non-zero return code.
    - `TestMarkerSelectionRegression` — the guard for RESEARCH §Open Questions item 4: run `--collect-only -q -m "integration"` against the repo and assert the collected count is strictly greater than zero, and run `-m "not integration"` and assert its count is strictly less than the no-`-m` total. This is the one-line check that would have caught the current no-op README command; without it the marker application can silently revert.

    Keep the subprocess count to three (one probe each for markers and warnings, plus the marker-selection pair). Add a Spanish comment if the added wall time pushes the suite past the ~8 s feedback budget from `01-VALIDATION.md`, so the cost is visible.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest tests/test_pytest_config.py -q && COVERAGE_FILE=.coverage.local uv run pytest -q -m "not optional_engine" --cov=encino_orm --cov-branch --cov-report= && uv run coverage combine && uv run coverage report && uv run python -c "import sys,pathlib; tomllib=__import__('tomllib' if sys.version_info>=(3,11) else 'tomli'); d=tomllib.loads(pathlib.Path('pyproject.toml').read_text(encoding='utf-8')); r=d['tool']['coverage']; assert r['run']['parallel'] is True; assert r['run']['branch'] is True; assert r['run']['source_pkgs']==['encino_orm']; assert r['report']['fail_under']==82, r['report']; c=pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8'); assert 'include-hidden-files: true' in c; assert 'coverage combine' in c; assert 'continue-on-error' not in c; g=pathlib.Path('.gitignore').read_text(encoding='utf-8'); assert '.coverage*' in g, 'the exact .coverage entry does not match per-leg artifacts'" && uv run python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8')); print('ci.yml is valid YAML')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` reports `510 passed`; `uv run pytest tests/test_pytest_config.py -q` passes
    - `COVERAGE_FILE=.coverage.local uv run pytest -q -m "not optional_engine" --cov=encino_orm --cov-branch --cov-report=` followed by `uv run coverage combine` and `uv run coverage report` exits 0 and prints a total at or above 82%
    - The local CI-equivalent total is recorded in the SUMMARY (expected ≈83%) together with the 88% all-engines figure and the reason the floor uses 83%
    - `pyproject.toml` has `[tool.coverage.run]` with `parallel = true` and `branch = true`, and `[tool.coverage.report]` with `fail_under = 82` and a Spanish comment naming the 83% measurement, the 88% local figure and Phase 2
    - `.github/workflows/ci.yml` parses as valid YAML and contains `COVERAGE_FILE: .coverage.py${{ matrix.python-version }}`, `--cov-report=`, `include-hidden-files: true`, a `coverage` job with `needs: [test]`, and both `coverage combine` and `coverage report`
    - `.github/workflows/ci.yml` contains the string `continue-on-error` zero times
    - `.gitignore` contains the literal `.coverage*` (the exact `.coverage` entry alone is insufficient — the per-leg artifacts are `.coverage.py3.10.<host>.<pid>.<rand>`)
    - **Induced-failure proof (must be performed and recorded):** temporarily raise `fail_under` to `99`, run `uv run coverage report`, confirm it exits non-zero with a "coverage is below" style message, then restore `fail_under = 82`. Record both exit codes in the SUMMARY.
    - **Induced-failure proof (marker guard):** temporarily remove the module-level `pytestmark` from `tests/test_mysql.py`, confirm `uv run pytest tests/test_pytest_config.py -q` fails, then restore it.
  </acceptance_criteria>
  <done>Coverage is collected per leg into `.coverage.py<version>`, combined in a dedicated job, gated at 82% with the CI-equivalent baseline documented, and `tests/test_pytest_config.py` proves both the presence and the function of every pytest hardening knob and the `-m "integration"` selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest config → suite outcome | A knob that is configured but inert (or a marker declared but never applied) manufactures confidence |
| test harness → CI job | The harness decides what runs; an over-broad deselect silently removes coverage |
| matrix legs → aggregated coverage number | Aggregation across legs is the only place a dialect gap could be hidden |
| `tmp_path` probe → subprocess pytest | Probe files are written outside the repo and executed by a child pytest process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Repudiation / Elevation | `[tool.pytest.ini_options]`, `[tool.coverage.report]` | mitigate | Every knob is enforced twice: once as a config assertion and once as a behavioural probe in `tests/test_pytest_config.py`. `filterwarnings = ["error"]` with an empty allowlist means a new warning cannot be swallowed. `xfail_strict = true` prevents an xfail from silently passing. `--strict-markers` prevents a typo'd marker from being inert. |
| T-01-06 | Tampering | `[tool.coverage.report].fail_under` | mitigate | The floor is pinned at 82 against the measured CI-equivalent 83%, not the local 88%, with the measurement and the reason recorded as a Spanish comment. The gate is proven by an induced failure (floor raised to 99 → non-zero exit). Per-dialect floors are explicitly deferred to Phase 2 (D-06) rather than faked here. |
| T-01-07 | Repudiation | marker application in `tests/test_*.py` | mitigate | `TestMarkerSelectionRegression` asserts `-m "integration"` selects a non-zero count and `-m "not integration"` a smaller count. Without it, deleting the module-level `pytestmark` would silently revert `README.md:130`'s documented command to a no-op — exactly the failure mode this phase exists to eliminate. Proven by an induced failure. |
| T-01-08 | Information Disclosure | `actions/upload-artifact` of `.coverage.py*` | accept | Coverage data files contain source line numbers and file paths, no secrets and no runtime data. `permissions: contents: read` is preserved at the workflow level; no new secret is introduced. |
| T-01-04 | Tampering / Supply Chain | `astral-sh/setup-uv@v5` in the new `coverage` job | mitigate | The `coverage` job reuses the pinned `version: "0.12.15"` input so the combine step runs on the same uv as the rest of the workflow. |

**No package installs occur in this plan** — `pytest-cov` and `coverage` were added to `[dependency-groups].dev` in plan 01-02, behind that plan's blocking-human legitimacy checkpoint.
</threat_model>

<verification>
Run all of the following from the repo root:

- `uv run pytest -q` → `510 passed`
- `uv run pytest --collect-only -q -m "integration"` → 41 collected
- `uv run pytest --collect-only -q -m "optional_engine"` → 13 collected
- `uv run pytest --collect-only -q -m "not optional_engine"` → 497 collected
- `uv run pytest --collect-only -q -m "not integration"` → 469 collected
- `COVERAGE_FILE=.coverage.local uv run pytest -q -m "not optional_engine" --cov=encino_orm --cov-branch --cov-report=` → exit 0
- `uv run coverage combine` → exit 0
- `uv run coverage report` → exit 0, total ≥ 82%
- `uv run pytest tests/test_pytest_config.py -q` → pass
- `grep -c 'continue-on-error' .github/workflows/ci.yml` → `0`
- `uv run python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8'))"` → parses (W6)
- `uv run python -c "import pathlib; assert '.coverage*' in pathlib.Path('.gitignore').read_text(encoding='utf-8')"` → passes; the exact `.coverage` entry alone would pass a naive substring check while leaving the real per-leg artifacts untracked (W4)

**Induced-failure demonstrations (phase success criteria 3, coverage half; and the marker guard):**
1. `fail_under = 99` → `uv run coverage report` exits non-zero → restore `82`.
2. Remove `pytestmark` from `tests/test_mysql.py` → `uv run pytest tests/test_pytest_config.py -q` fails → restore.
3. A probe test with an unregistered marker → non-zero exit under `--strict-markers`.
4. A probe test emitting a warning → non-zero exit under `filterwarnings = ["error"]`.

After the local coverage run, `git status --porcelain` must be clean (`.coverage*` ignored).
</verification>

<success_criteria>
- `[tool.pytest.ini_options]` is hardened with `--strict-markers`, `xfail_strict`, `filterwarnings = ["error"]` (empty commented allowlist), both loop scopes, and the four declared markers
- The `integration` / `optional_engine` markers are applied per the map, producing exactly 41 / 13 / 469 / 497 selections, and the unit-only classes remain unmarked
- Coverage is collected per leg, combined across legs, and gated by a no-drop ratchet pinned at 82 against the 83% CI-equivalent baseline, with the reasoning recorded in the config
- Every knob is proven both configured and functional by `tests/test_pytest_config.py`, including the `-m "integration"` regression guard
- `.gitignore` contains the literal `.coverage*` glob (not just the exact `.coverage` entry) and `ci.yml` still parses as valid YAML
- The suite is still `510 passed` — zero behavior change from this plan
</success_criteria>

<output>
Create `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-03-SUMMARY.md` when done.

The summary MUST record: whether any minimal fix was needed for the hardening (and if so, which commit); the exact collected counts for the four `-m` selections; the local CI-equivalent coverage total, the all-engines total, and the chosen floor with its justification; both induced-failure transcripts; and the added wall time of `tests/test_pytest_config.py` relative to the ~8 s feedback budget.
</output>
