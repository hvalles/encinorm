---
phase: 06-config-optional-layer-hygiene
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - tests/test_http_openapi.py
  - tests/__snapshots__/test_http_openapi.ambr
  - encino_orm/http/routes.py
autonomous: true
requirements: [CFG-03]
must_haves:
  truths:
    - "El snapshot OpenAPI `.ambr` se captura y commitea CONTRA el `exec()` actual ANTES del rewrite, y el rewrite lo deja BYTE-IDÉNTICO (Success Criterion 3)."
    - "Tras el rewrite, `encino_orm/http/routes.py` no contiene ninguna llamada a `exec()` y el handler se construye con `inspect.Signature` asignada a `__signature__`."
    - "Los path params siguen validando: un PK `int` con valor no numérico devuelve 422, uno válido 200, y la PK compuesta (dos segmentos) también (Success Criterion 3, mitad 'path parameters still validate')."
    - "El `operationId` de OpenAPI no cambia porque `handler.__name__ == handler.__qualname__ == 'handler'` (Pitfall 3)."
    - "El comportamiento HTTP se preserva: 200/201/404/422, PUT y DELETE idénticos (verificado empíricamente en research)."
    - "No se cambia ningún status code, ruta ni operación: el contrato observable es idéntico."
  artifacts:
    - path: "tests/test_http_openapi.py"
      provides: "Snapshot OpenAPI (syrupy) + validación de path params simple y compuesta"
      contains: "openapi"
    - path: "tests/__snapshots__/test_http_openapi.ambr"
      provides: "OpenAPI congelado capturado ANTES del rewrite (guardián del criterio 3)"
      contains: "operationId"
    - path: "encino_orm/http/routes.py"
      provides: "`_build_path_handler` con closures + `__signature__` (sin `exec`); `create`/`list_` con `Annotated`"
      contains: "__signature__"
  key_links:
    - from: "encino_orm/http/routes.py::_build_path_handler"
      to: "fastapi"
      via: "FastAPI lee `inspect.signature(handler)` → `__signature__` para derivar path params y `Depends`"
      pattern: "__signature__"
    - from: "tests/test_http_openapi.py"
      to: "tests/__snapshots__/test_http_openapi.ambr"
      via: "syrupy compara `json.dumps(app.openapi(), sort_keys=True)` sin `--snapshot-update`"
      pattern: "snapshot"
    - from: "encino_orm/http/routes.py::register_crud"
      to: "encino_orm/http/routes.py::_build_path_handler"
      via: "get/put/delete reciben el handler generado"
      pattern: "_build_path_handler"
---

<objective>
Sustituir la generación con `exec()` de los handlers `get`/`put`/`delete` de `http/routes.py` por closures con una `inspect.Signature` explícita en `__signature__`, como refactor que preserva el contrato, custodiado por un snapshot OpenAPI capturado ANTES del rewrite.

Hoy `_build_path_handler` (`routes.py:25-74`) concatena declaración y cuerpo y los ejecuta con `exec(src, ns)` (:73); la firma (`sig` en :30) codifica el nombre y el tipo `int`/`str` de cada PK más `data`/`physical`/`db=Depends(get_db)`. CFG-03 elimina el `exec()` conservando exactamente esa firma: FastAPI la lee vía `inspect.signature` (`__signature__` se devuelve tal cual) y los tipos reales evitan `__globals__` frágil y el `S102`.

Purpose: CFG-03 es la mitad de codegen del goal ("generated handlers stop being built with `exec()`"). Además permite a 06-05 retirar el `per-file-ignore` `S102` de `routes.py` y la entrada del ratchet mypy `encino_orm.http.routes`.

Output: `tests/test_http_openapi.py` + `.ambr` congelado contra el `exec()` actual, `_build_path_handler` con closures + `__signature__`, y `create`/`list_` migrados a `Annotated` (behavior-preserving).

**Orden estricto de snapshots (Pitfall 2 — restricción dura).** Task 1 ESCRIBE el test y genera el `.ambr` contra el código `exec()` ACTUAL (`uv run pytest tests/test_http_openapi.py --snapshot-update`) y lo commitea. Task 2 reescribe `routes.py` y corre el test SIN `--snapshot-update`: debe pasar con diff CERO en el `.ambr`. Si el commit del rewrite incluye cambios en `tests/__snapshots__/*.ambr`, el guardián no prueba nada y el criterio 3 queda invalidado.

**Por qué `exec()` NO está solo en HTTP (Pitfall 9).** El otro sitio es `graphql/schema.py:103` (`_pk_resolver`), que posee `06-04` con su propio guardián (snapshot SDL). Este plan NO toca `graphql/schema.py`.

**Orden interno de la fase (explícito).** Wave 2, `depends_on: [06-01, 06-02]` — el ROADMAP exige que Level 4 (config) preceda a Level 5 (codegen) para que las firmas públicas cambien UNA sola vez. `06-03` (HTTP) y `06-04` (GraphQL) son disjuntos en ficheros y corren en paralelo en la misma wave.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-config-optional-layer-hygiene/06-RESEARCH.md
@encino_orm/http/routes.py
@encino_orm/http/__init__.py
@encino_orm/http/errors.py
@encino_orm/model/model.py
@tests/test_crud.py
@tests/test_pk.py
@tests/test_sql_snapshots.py

<interfaces>
<!-- Anclajes verificados contra el árbol actual. No explorar más. -->

`encino_orm/http/routes.py` (114 líneas):
- `_cursor(model, db, **fields)` :9-12 — helper inyectado en el namespace generado.
- `_path_type(model, field)` :15-18 — `int` si `_base_type(annotation)` es `int`, si no `str`.
- `_path_suffix(model)` :21-22 — `"/" + "/".join("{"+f+"}" for f in model._primary_key)`.
- `_build_path_handler(model, get_db, op)` :25-74 — firma generada: `get` = `(pk..., db=Depends(get_db))`; `put` = `(pk..., data: model, db=Depends(get_db))`; `delete` = `(pk..., physical: bool = False, db=Depends(get_db))`. `exec(src, ns)` :73; `ns` con `_cursor`, `model`, `HTTPException`, `Depends`, `get_db` :66-72. Cuerpos en :34-63 (404 si `not obj._exists`; PUT copia `data.model_dump(exclude_unset=True)` saltando `id/enabled/created_at/updated_at`; DELETE devuelve `{**pk_dict, 'deleted': True}`).
- `register_crud(router, model, prefix, *, get_db)` :77-114 — `create` :86-90 (`status_code=201`) y `list_` :92-105 son funciones normales con `db=Depends(get_db)` (origen de los 2 `B008`); `get`/`put`/`delete` usan `_build_path_handler` (:107-114).

`encino_orm/http/__init__.py::create_crud` :13-35 — NO se cambia su firma (Open Question 2: YAGNI). `get_db` se deriva de `session(pool)` si no se pasa.

`tests/test_crud.py` :45-49 — `_make_app` con `FastAPI()` + `install_error_handlers` + `app.include_router(create_crud(d, models, prefix="/api"))`. Patrón a reutilizar en el test de snapshot.

`tests/test_pk.py` :178-199 — PK compuesta `Membership` con `tenant_id` (int) + `code` (str); `GET /api/memberships/7/admin` → 200 y `/api/memberships/abc/2` → 422 (patrón de validación).

`tests/test_sql_snapshots.py` :12-24 — flujo de actualización de syrupy y política de snapshots; el `.ambr` va en el MISMO commit que el test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `tests/test_http_openapi.py` + `.ambr` capturado CONTRA el `exec()` actual (commit separado)</name>
  <read_first>
    - `06-RESEARCH.md` §Common Pitfalls 2 (el snapshot capturado después bendice la regresión) y §Validation Architecture punto 3
    - `tests/test_sql_snapshots.py` :1-31 (flujo `--snapshot-update`, `.ambr` en el mismo commit)
    - `tests/test_crud.py` :1-49 (`_make_app`, modelos `Region`/`Agente`/`Ciudad`)
    - `tests/test_pk.py` :178-214 (PK compuesta y su validación 422)
    - `encino_orm/http/routes.py` completo (114 líneas)
  </read_first>
  <files>tests/test_http_openapi.py, tests/__snapshots__/test_http_openapi.ambr</files>
  <action>
    Crea `tests/test_http_openapi.py` SIN tocar todavía `routes.py` (el código sigue siendo `exec()`).
    - Define modelos SQLite `:memory:` reutilizando el patrón de `test_crud.py` (`Region`, `Agente`) y uno de PK compuesta reutilizando el patrón de `test_pk.py` (`Membership` con `tenant_id: int` + `code: str`).
    - Construye la app con `FastAPI()` + `install_error_handlers(app)` + `app.include_router(create_crud(db, models, prefix="/api"))`. NO iteres `app.routes` (starlette 1.6 envuelve en `_IncludedRouter`): usa `app.openapi()`.
    - `test_openapi_snapshot(snapshot)`: asserta `snapshot == json.dumps(app.openapi(), indent=2, sort_keys=True)` (o una proyección normalizada equivalente si el JSON completo resultara inestable entre versiones de Python — Assumption A2; deja escrito en el docstring qué se congela).
    - `test_path_params_siguen_validando`: con PK simple, `GET /api/agentes/abc` → 422 y `GET /api/agentes/1` → 200/404 (según exista); con PK compuesta, `GET /api/memberships/abc/2` → 422 y un caso válido → 200/404. Inserta una fila para el caso 200.
    - `test_contrato_operaciones`: verifica que las rutas y métodos del router no cambian (`POST /api/agentes/` → 201; `GET /api/agentes/` → 200; `PUT`/`DELETE` de PK) usando `httpx.ASGITransport`.
    - Genera el snapshot con `uv run pytest tests/test_http_openapi.py --snapshot-update` y REVISA el `.ambr` (debe contener los `operationId` y los parámetros de path). Commitea test + `.ambr` JUNTOS y ANTES del rewrite; deja constancia del commit en el SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_http_openapi.py -q && uv run ruff check tests/test_http_openapi.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_http_openapi.py` existe con `grep -c "def test_"` >= 3
    - `tests/__snapshots__/test_http_openapi.ambr` existe y contiene `operationId`
    - `grep -c "exec" encino_orm/http/routes.py` >= 1 (el rewrite AÚN no ha ocurrido: el snapshot es contra el `exec()`)
    - `uv run pytest tests/test_http_openapi.py -q` pasa SIN `--snapshot-update` en la verificación final
    - El `.ambr` está commiteado ANTES de cualquier cambio en `encino_orm/http/routes.py` (evidencia: `git log --oneline -- tests/__snapshots__/test_http_openapi.ambr` muestra un commit anterior al del rewrite)
  </acceptance_criteria>
  <done>El snapshot OpenAPI está congelado contra el código `exec()` actual, con tests de validación de path params simple y compuesta; el `.ambr` y el test van en un commit anterior al rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `_build_path_handler` → closures + `__signature__` (sin `exec`) + `Annotated` en `create`/`list_`</name>
  <read_first>
    - `06-RESEARCH.md` §Pattern 3 (ejemplo HTTP verificado: OpenAPI idéntica) y §Common Pitfalls 3 (operationId), 4 (`**kwargs` sin firma), 5 (`Depends` como default, no anotación)
    - `encino_orm/http/routes.py` :25-74 (cuerpos exactos a replicar) y :86-105 (`create`/`list_`)
    - `tests/__snapshots__/test_http_openapi.ambr` (el contrato congelado)
  </read_first>
  <files>encino_orm/http/routes.py</files>
  <behavior>
    - `_build_path_handler(model, get_db, "get")` devuelve un callable con `__signature__` que lista cada PK (mismo orden y tipo `int`/`str` que `_path_type`) y `db` con `default=Depends(get_db)`.
    - `put` añade `data: model` antes de `db`; `delete` añade `physical: bool = False` antes de `db`.
    - `handler.__name__ == handler.__qualname__ == "handler"`.
    - El handler no contiene `exec`; sus freevars incluyen `model` (y `HTTPException`).
    - `GET /api/agentes/abc` → 422; `GET /api/agentes/1` → 200/404; PK compuesta inválida → 422.
    - El OpenAPI generado es idéntico al `.ambr` congelado (diff cero).
  </behavior>
  <action>
    Reescribe `_build_path_handler` en `encino_orm/http/routes.py`:
    - Importa `from inspect import Parameter, Signature` y `from typing import Annotated` a nivel de módulo (stdlib).
    - Mantén `from fastapi import Depends, HTTPException` DENTRO de la función (contrato de importación diferida).
    - Construye tres closures `async def handler(...)` (una por `op`) que replican EXACTAMENTE los cuerpos de :34-63: `get` → `_cursor(model, db, **kwargs).load()`, 404 si `not obj._exists`, devuelve `obj`; `put` → carga, 404, copia `data.model_dump(exclude_unset=True)` saltando `id/enabled/created_at/updated_at`, `await obj.update()`, recarga; `delete` → carga, 404, `await obj.delete(physical=physical)`, devuelve `{**pk_dict, 'deleted': True}`. Los kwargs de PK y `db` se reciben por nombre (usa `**kwargs` internamente y `kwargs.pop("db")`), pero la firma OBSERVABLE se fija con `__signature__`.
    - Construye la `Signature` con `Parameter`: por cada PK `Parameter(f, Parameter.POSITIONAL_OR_KEYWORD, annotation=_path_type(model, f))`; para `put` añade `Parameter("data", Parameter.POSITIONAL_OR_KEYWORD, annotation=model)`; para `delete` añade `Parameter("physical", Parameter.POSITIONAL_OR_KEYWORD, default=False, annotation=bool)`; por último `Parameter("db", Parameter.POSITIONAL_OR_KEYWORD, default=Depends(get_db))` SIN `annotation` (Pitfall 5). Asigna `handler.__signature__ = Signature(params)`, `handler.__name__ = "handler"`, `handler.__qualname__ = "handler"` (Pitfall 3: preserva el `operationId`).
    - Migra `create` (:87) y `list_` (:93-99) a `db: Annotated[..., Depends(get_db)]` para eliminar los 2 `B008` (behavior-preserving; el snapshot OpenAPI ya cubre el cambio). NO cambies `status_code=201`, `response_model`, el prefijo ni las firmas de `register_crud`/`create_crud`.
    - Corre el test SIN `--snapshot-update`. Si el `.ambr` cambia, DETENTE: el contrato se ha alterado y el rewrite debe corregirse, no el snapshot.
  </action>
  <verify>
    <automated>uv run pytest tests/test_http_openapi.py -q && uv run pytest tests/test_crud.py tests/test_pk.py tests/test_pagination_limits.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "^\s*exec\(" encino_orm/http/routes.py` devuelve 0
    - `grep -c "__signature__" encino_orm/http/routes.py` devuelve >= 1
    - `grep -c "Signature(" encino_orm/http/routes.py` devuelve >= 1
    - `grep -c "__name__ = \"handler\"" encino_orm/http/routes.py` devuelve >= 1
    - `grep -c "Annotated" encino_orm/http/routes.py` devuelve >= 2
    - `git diff --name-only` NO incluye `tests/__snapshots__/test_http_openapi.ambr` (diff cero en el snapshot)
    - `uv run pytest tests/test_http_openapi.py -q` pasa sin `--snapshot-update`
    - `uv run pytest tests/test_crud.py tests/test_pk.py tests/test_pagination_limits.py -q` pasa
    - `uv run ruff check --isolated --select S102,B008 encino_orm/http/routes.py` sale 0 (ignores retirables por 06-05; `--isolated` evita que el `per-file-ignore` activo enmascare el probe — W6)
  </acceptance_criteria>
  <done>`_build_path_handler` construye handlers con closures + `__signature__` (sin `exec`), preserva el contrato OpenAPI byte a byte y la validación de path params; `create`/`list_` usan `Annotated`.</done>
</task>

<task type="auto">
  <name>Task 3: No-regresión y cierre de CFG-03 (HTTP)</name>
  <read_first>
    - `06-RESEARCH.md` §Validation Architecture punto 3 y §Runtime State Inventory (los snapshots SQL NO cambian)
    - `tests/test_http_openapi.py` (el guardián del criterio 3)
  </read_first>
  <files>tests/test_http_openapi.py</files>
  <action>
    Corre la suite no-integration completa y confirma que no hay regresiones:
    - `uv run pytest -q -m "not integration and not optional_engine"`.
    - `uv run pytest tests/test_crud.py tests/test_pk.py tests/test_pagination_limits.py tests/test_http_openapi.py -q`.
    - Confirma que `tests/__snapshots__/test_sql_snapshots.ambr` NO cambió (el rewrite no toca SQL): `git diff --stat tests/__snapshots__/test_sql_snapshots.ambr` vacío.
    - Confirma que `encino_orm/http/__init__.py` NO cambió de firma (`git diff --stat encino_orm/http/__init__.py` vacío salvo que el executor haya necesitado algo; lo esperado es vacío).
    - Registra en el SUMMARY el commit del `.ambr` (anterior) y el commit del rewrite (posterior) como evidencia del orden de snapshots.
    Si aparece un fallo en `test_pagination_limits.py` o `test_crud.py`, NO toques el snapshot: corrige el rewrite.
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration and not optional_engine" && git diff --stat tests/__snapshots__/test_sql_snapshots.ambr</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa
    - `git diff --stat tests/__snapshots__/test_sql_snapshots.ambr` vacío
    - `git diff --stat tests/__snapshots__/test_http_openapi.ambr` vacío desde el commit de Task 1
    - `grep -Ec "^\s*exec\(" encino_orm/http/routes.py` devuelve 0
    - `uv run ruff check encino_orm tests` sale 0
    - El SUMMARY registra los dos commits (snapshot → rewrite) y el diff cero del `.ambr`
  </acceptance_criteria>
  <done>CFG-03 (HTTP) cerrado: contrato OpenAPI idéntico antes/después, path params validando, sin `exec()` en `routes.py` y sin cambios en los snapshots SQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot `.ambr` -> verificación del contrato | un snapshot capturado después del rewrite "bendice" una regresión y el criterio 3 queda vacío |
| `__signature__` -> FastAPI | la firma declarada decide path params, validación y `operationId` |
| closure -> namespace generado | los nombres (`_cursor`, `model`, `HTTPException`, `Depends`, `get_db`) deben seguir resolviéndose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | El `.ambr` se regenera tras el rewrite y oculta un cambio de contrato | mitigate | Orden estricto: Task 1 commitea el `.ambr` contra `exec()`; Task 2 corre SIN `--snapshot-update` y exige diff cero; aceptación `git diff --name-only` no incluye el `.ambr` |
| T-06-03-02 | Spoofing / Tampering | La closure se llama distinto y cambia el `operationId` de OpenAPI | mitigate | `handler.__name__ = handler.__qualname__ = "handler"` (Pitfall 3); el snapshot lo verifica |
| T-06-03-03 | Tampering | La closure con `**kwargs` no declara los path params y la ruta deja de validar | mitigate | `Signature` explícita con cada PK en orden y tipo de `_path_type`; tests 422 de PK simple y compuesta |
| T-06-03-04 | Tampering | `Depends` puesto como `annotation` en vez de `default` desactiva la inyección | mitigate | `Parameter("db", ..., default=Depends(get_db))` sin anotación (Pitfall 5); test 200 con `db` inyectado |
| T-06-03-05 | Tampering | Cambio de status code/validación en `create`/`list_` al migrar a `Annotated` | mitigate | Migración behavior-preserving cubierta por el mismo snapshot OpenAPI + `test_crud`/`test_pk` |
| T-06-03-06 | Tampering | El rewrite toca SQL y rompe los snapshots de dialecto | mitigate | `git diff --stat tests/__snapshots__/test_sql_snapshots.ambr` vacío |
| T-06-03-SC | Tampering | instalaciones/actualizaciones de paquetes | n/a | Este plan no instala ni actualiza dependencias (`syrupy` ya está fijado) |
</threat_model>

<verification>
- `uv run pytest tests/test_http_openapi.py -q` verde sin `--snapshot-update`
- `uv run pytest tests/test_crud.py tests/test_pk.py tests/test_pagination_limits.py -q` verde
- `uv run pytest -q -m "not integration and not optional_engine"` verde
- `grep -Ec "^\s*exec\(" encino_orm/http/routes.py` == 0
- `grep -c "__signature__" encino_orm/http/routes.py` >= 1
- `git diff --stat tests/__snapshots__/test_sql_snapshots.ambr` vacío
- `uv run ruff check encino_orm tests` sale 0
- Evidencia en el SUMMARY: commit del `.ambr` (antes) y commit del rewrite (después), con diff cero
</verification>

<success_criteria>
- CFG-03 (HTTP) cubierto: los handlers `get`/`put`/`delete` se construyen con closures + `__signature__`, sin `exec()`
- El OpenAPI generado es idéntico antes y después del rewrite (Success Criterion 3) y los path params siguen validando
- El `operationId`, los status codes y las rutas no cambian
- El snapshot se capturó antes del rewrite y nunca se regeneró después
- Los snapshots SQL de dialecto no cambian
</success_criteria>

<output>
Create `.planning/phases/06-config-optional-layer-hygiene/06-03-SUMMARY.md` when done
</output>
