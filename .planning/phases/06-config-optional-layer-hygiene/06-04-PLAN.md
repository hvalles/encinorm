---
phase: 06-config-optional-layer-hygiene
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - tests/test_graphql_namespace.py
  - tests/__snapshots__/test_graphql_namespace.ambr
  - encino_orm/graphql/schema.py
autonomous: true
requirements: [CFG-03, CFG-04]
must_haves:
  truths:
    - "La SDL `.ambr` se captura y commitea CONTRA el `exec()` actual de `_pk_resolver` ANTES del rewrite, y el rewrite la deja BYTE-IDÉNTICA (CFG-03 GraphQL)."
    - "Tras el rewrite, `graphql/schema.py` no contiene ninguna llamada a `exec()`; los resolvers `get`/`update`/`delete` se construyen con closures + `__signature__`."
    - "`build_schema` usa un módulo sintético por build registrado en `sys.modules` y borrado en `try/finally`; el namespace de `encino_orm.graphql.schema` NO se muta (Success Criterion 4)."
    - "Dos llamadas sucesivas a `build_schema` no acumulan tipos en el namespace del módulo real ni dejan entradas huérfanas en `sys.modules`."
    - "Ambos schemas siguen ejecutando: query con relación, query con filtro autorreferente (`and`/`or`/`not`) y mutation (Success Criterion 4, mitad funcional)."
    - "El comportamiento GraphQL se preserva: `get`/`update`/`delete`/not-found idénticos (verificado empíricamente en research)."
  artifacts:
    - path: "tests/test_graphql_namespace.py"
      provides: "Snapshot SDL + test de no-mutación del namespace + funcionalidad de dos builds"
      contains: "build_schema"
    - path: "tests/__snapshots__/test_graphql_namespace.ambr"
      provides: "SDL congelada capturada ANTES del rewrite de `_pk_resolver`"
      contains: "type"
    - path: "encino_orm/graphql/schema.py"
      provides: "`_pk_resolver` con closures + `__signature__` (sin `exec`) y `build_schema` con namespace por build"
      contains: "ModuleType"
  key_links:
    - from: "encino_orm/graphql/schema.py::_pk_resolver"
      to: "strawberry"
      via: "Strawberry deriva los argumentos GraphQL de la firma del resolver (`__signature__`)"
      pattern: "__signature__"
    - from: "encino_orm/graphql/schema.py::build_schema"
      to: "strawberry.types.lazy_type::LazyType.resolve_type"
      via: "`importlib.import_module(nombre)` encuentra el módulo sintético en `sys.modules`"
      pattern: "sys.modules"
    - from: "encino_orm/graphql/types.py::build_type"
      to: "encino_orm/graphql/schema.py::build_schema"
      via: "recibe `module_name` del build y anota `strawberry.lazy(module_name)`"
      pattern: "module_name"
---

<objective>
Cerrar CFG-03 en GraphQL (`_pk_resolver` con `exec()` → closures + `__signature__`, guardado por un snapshot SDL) y CFG-04 (`build_schema` con namespace por build, sin mutar el namespace del módulo real).

Dos defectos en `graphql/schema.py`: (1) `_pk_resolver` (:50-104) genera los resolvers `get`/`update`/`delete` con `exec(src, ns)` (:103), y la firma generada (`sig`, :54) codifica los tipos de los argumentos GraphQL; (2) `build_schema` (:164-194) hace `setattr(module, model.__name__, typ)` (:178) y `setattr(module, f"{model.__name__}Filter", ftype)` (:185) sobre `sys.modules[__name__]`, acumulando tipos entre builds (verificado empíricamente: el segundo build añade `Other`/`OtherFilter` al módulo real).

Purpose: CFG-03/CFG-04 completan el goal ("generated handlers stop being built with `exec()`"; "no mutable module-level global ...") y permiten a 06-05 retirar el `per-file-ignore` `S102` de `graphql/schema.py` y las entradas del ratchet mypy `encino_orm.graphql.*` (5 `name-defined` son artefacto del `exec()`).

Output: `tests/test_graphql_namespace.py` + SDL `.ambr` congelada contra el `exec()` actual, `_pk_resolver` con closures + `__signature__`, `build_schema` con módulo sintético por build y `try/finally`, y test de no-mutación del namespace.

**Orden estricto de snapshots (Pitfall 2 — restricción dura).** Task 1 genera el `.ambr` contra el `exec()` ACTUAL y lo commitea. Task 2 reescribe `_pk_resolver` y corre el test SIN `--snapshot-update`: diff cero en el `.ambr`. Task 3 (namespace) también debe dejar el `.ambr` intacto.

**Riesgo residual (Assumption A1 / Pitfall 6).** Borrar el módulo sintético de `sys.modules` tras `strawberry.Schema(...)` es seguro para los caminos ejercitados (relaciones, `has_many`, filtros autorreferentes, mutations, `as_str()`); una ruta no ejercitada (p. ej. el optimizador de queries) podría resolver un `LazyType` después del `del`. Mitigación: el test cubre `as_str()` + los 4 tipos de operación; si aparece un fallo, conservar el módulo registrado (coste: una entrada de `sys.modules` por build) en vez de romper.

**Pitfall 9 (el `exec()` de GraphQL no se olvida).** El ROADMAP atribuye el `S102` de `graphql/schema.py` a CFG-03 pero el dueño natural del archivo es `06-04`. Este plan posee TODO `graphql/schema.py` (exec + namespace) y `06-03` solo HTTP, de modo que ambos corren en paralelo (Wave 2) sin compartir ficheros. Desviación respecto a la lectura literal del ROADMAP, documentada aquí y en el ROADMAP.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-config-optional-layer-hygiene/06-RESEARCH.md
@encino_orm/graphql/schema.py
@encino_orm/graphql/types.py
@encino_orm/graphql/filters.py
@encino_orm/graphql/resolvers.py
@tests/test_graphql.py
@tests/test_pk.py
@tests/test_sql_snapshots.py

<interfaces>
<!-- Anclajes verificados contra el árbol actual. No explorar más. -->

`encino_orm/graphql/schema.py` (194 líneas):
- `_list_resolver` :19-31, `_count_resolver` :34-39 — closures normales (sin `exec`); NO se tocan.
- `_pk_arg_type(model, field)` :42-47 — `strawberry.ID` para `id`, si no `DATATYPE_TO_TYPE.get(dt, str)`.
- `_pk_resolver(model, gtype, op, itype=None)` :50-104 — `pk = list(model._primary_key)`; `type_names = [f"_pk_t{i}" ...]` :53; `sig` :54; `kwargs` con `int({f})` para `id` :55; decl/body :57-86; `exec(src, ns)` :103; `ns` :89-102 (`__name__`, `Info`, `Optional`, `strawberry`, `db_session`, `cursor`, `model`, `gtype`, `itype`, `NotFoundError`, `_pk_t{i}`). Contratos: `get` → `resolver(info: Info, {pk}: {_pk_t{i}}) -> Optional[gtype]`; `update` → `resolver(info, {pk}, data: itype) -> gtype`; `delete` → `resolver(info, {pk}) -> bool`. La coerción `id=int(id)` vive DENTRO del cuerpo (:55), no en la firma.
- `_get_resolver` :107-108, `_update_resolver` :123-124, `_delete_resolver` :127-128 — delegan en `_pk_resolver`.
- `_create_resolver` :111-120 — closure normal (sin `exec`); NO se toca.
- `_build_query` :131-143, `_build_mutation` :146-161 — NO se tocan.
- `build_schema(models, *, auto_camel_case=False)` :164-194 — `import sys` local (:169); `module_name = __name__` (:171); `module = sys.modules[module_name]` (:172); `build_type(model, module_name)` (:176) + `setattr(module, model.__name__, typ)` (:178); `build_filter_input(model, module_name)` (:183) + `setattr(module, f"{model.__name__}Filter", ftype)` (:185); `strawberry.Schema(query=..., mutation=..., config=StrawberryConfig(...))` (:190-194).

`encino_orm/graphql/types.py` :41,46 y `filters.py` :108 — usan `strawberry.lazy(module_name)`; `LazyType.resolve_type()` hace `importlib.import_module(self.module).__dict__[self.type_name]`.

`tests/test_graphql.py` :10-37 — modelos `Agente`/`Region` con `_references_def`/`_has_many_def`; fixture `schema = build_schema([Region, Agente])`; `schema.execute(..., context_value={"db": db})`.

`tests/test_pk.py` :202-214 — `build_schema([Membership, AuditLog])` con PK compuesta.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `tests/test_graphql_namespace.py` + SDL `.ambr` capturada CONTRA el `exec()` actual (commit separado)</name>
  <read_first>
    - `06-RESEARCH.md` §Common Pitfalls 2 (snapshot después bendice la regresión) y §Validation Architecture punto 4
    - `tests/test_sql_snapshots.py` :1-31 (flujo `--snapshot-update`)
    - `tests/test_graphql.py` :1-37 (modelos con relación y `_has_many_def`)
    - `encino_orm/graphql/schema.py` completo (194 líneas)
  </read_first>
  <files>tests/test_graphql_namespace.py, tests/__snapshots__/test_graphql_namespace.ambr</files>
  <action>
    Crea `tests/test_graphql_namespace.py` SIN tocar `schema.py` (el código sigue siendo `exec()`).
    - Reutiliza los modelos `Region`/`Agente` de `tests/test_graphql.py` (con `_has_many_def` y `_references_def`) más un tercer modelo para el segundo build.
    - `test_sdl_snapshot(snapshot)`: `assert build_schema([Region, Agente]).as_str() == snapshot` (SDL congelada).
    - `test_dos_builds_sucesivos_funcionan`: construye `s1 = build_schema([Region, Agente])` y `s2 = build_schema([Region, Agente])`; ejecuta en AMBOS una query con relación (`{ agentes { agente region { region } } }`), una con filtro autorreferente (`and`/`or`/`not`) y una mutation (`agente_create`), con `context_value={"db": db}` sembrado; asserta `errors is None` en los cuatro caminos por schema.
    - `test_operaciones_pk_siguen_funcionando`: `get` (existente y ausente), `update` y `delete` vía `schema.execute` con PK simple y compuesta (patrón de `test_pk.py`).
    - Genera el snapshot con `uv run pytest tests/test_graphql_namespace.py --snapshot-update` y REVISA el `.ambr`. Commitea test + `.ambr` JUNTOS y ANTES del rewrite; deja constancia del commit en el SUMMARY.
    - NO incluyas todavía la aserción de no-mutación del namespace (esa es de Task 3, que la escribirá primero en rojo).
  </action>
  <verify>
    <automated>uv run pytest tests/test_graphql_namespace.py -q && uv run ruff check tests/test_graphql_namespace.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_graphql_namespace.py` existe con `grep -c "def test_"` >= 3
    - `tests/__snapshots__/test_graphql_namespace.ambr` existe y contiene la SDL (p. ej. `type Region`)
    - `grep -c "exec" encino_orm/graphql/schema.py` >= 1 (el rewrite AÚN no ha ocurrido)
    - `uv run pytest tests/test_graphql_namespace.py -q` pasa SIN `--snapshot-update`
    - El `.ambr` está commiteado ANTES de cualquier cambio en `encino_orm/graphql/schema.py` (evidencia: `git log --oneline -- tests/__snapshots__/test_graphql_namespace.ambr`)
  </acceptance_criteria>
  <done>La SDL está congelada contra el `exec()` actual y los caminos funcionales (relación, filtro autorreferente, mutation, operaciones PK) están cubiertos; el `.ambr` va en un commit anterior al rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `_pk_resolver` → closures + `__signature__` (sin `exec`)</name>
  <read_first>
    - `06-RESEARCH.md` §Pattern 3 (ejemplo GraphQL verificado: SDL idéntica)
    - `encino_orm/graphql/schema.py` :42-104 (contratos y cuerpos a replicar)
    - `tests/__snapshots__/test_graphql_namespace.ambr` (la SDL congelada)
  </read_first>
  <files>encino_orm/graphql/schema.py</files>
  <behavior>
    - `_pk_resolver(model, gtype, "get")` devuelve un resolver con `__signature__` = `(info: Info, {pk}: {_pk_arg_type})` y `return_annotation=Optional[gtype]`.
    - `update` añade `data: itype` y `return_annotation=gtype`; `delete` devuelve `bool`.
    - El resolver no contiene `exec`; `resolver.__name__ == "resolver"`.
    - La coerción `id=int(id)` se hace en el cuerpo (replica :55).
    - `build_schema([Region, Agente]).as_str()` es idéntico al `.ambr` congelado (diff cero).
    - `get`/`update`/`delete`/not-found se comportan igual que antes.
  </behavior>
  <action>
    Reescribe `_pk_resolver`:
    - Importa `from inspect import Parameter, Signature` a nivel de módulo (stdlib).
    - Calcula `pk = list(model._primary_key)` y `arg_types = {f: _pk_arg_type(model, f) for f in pk}`.
    - Define un helper `_cast(kw)` que replica la coerción `id=int(id)` del cuerpo generado (solo para `f == "id"`).
    - Construye tres closures `async def resolver(...)` (una por `op`) que replican EXACTAMENTE los cuerpos de :57-86: `get` → `async with db_session(info) as conn: obj = await cursor(model, conn, **_cast(kw)).load(); return obj if obj._exists else None`; `update` → carga, `NotFoundError(model._table)` si no existe, copia `strawberry.asdict(data)` saltando `None`, `await obj.update()`, recarga; `delete` → carga, `False` si no existe, `await obj.delete()`, `True`. Usa `**kwargs` interno para las PK.
    - Construye la `Signature`: `Parameter("info", Parameter.POSITIONAL_OR_KEYWORD, annotation=Info)` + un `Parameter(f, ..., annotation=arg_types[f])` por PK; para `update`, `Parameter("data", ..., annotation=itype)`; `return_annotation` = `Optional[gtype]` (get), `gtype` (update), `bool` (delete). Pasa OBJETOS de tipo reales (no strings) para no necesitar `__globals__`. Asigna `resolver.__signature__` y `resolver.__name__ = "resolver"`.
    - Elimina el `ns` y el `exec(src, ns)`; elimina `type_names`/`sig`/`kwargs`/`decl`/`body`.
    - Corre el test SIN `--snapshot-update`. Si la SDL cambia, corrige el rewrite (nombres/tipos/orden de los parámetros), NO el snapshot.
  </action>
  <verify>
    <automated>uv run pytest tests/test_graphql_namespace.py -q && uv run pytest tests/test_graphql.py tests/test_pk.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "^\s*exec\(" encino_orm/graphql/schema.py` devuelve 0
    - `grep -c "__signature__" encino_orm/graphql/schema.py` devuelve >= 1
    - `grep -c "Signature(" encino_orm/graphql/schema.py` devuelve >= 1
    - `grep -c "resolver.__name__" encino_orm/graphql/schema.py` devuelve >= 1
    - `git diff --name-only` NO incluye `tests/__snapshots__/test_graphql_namespace.ambr` (diff cero)
    - `uv run pytest tests/test_graphql_namespace.py -q` pasa sin `--snapshot-update`
    - `uv run pytest tests/test_graphql.py tests/test_pk.py -q` pasa
    - `uv run ruff check --isolated --select S102 encino_orm/graphql/schema.py` sale 0 (ignore retirable por 06-05; `--isolated` evita que el `per-file-ignore` activo enmascare el probe — W6)
  </acceptance_criteria>
  <done>`_pk_resolver` construye los resolvers con closures + `__signature__` (sin `exec`), preserva la SDL byte a byte y las operaciones `get`/`update`/`delete`/not-found.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `build_schema` con namespace por build + test de no-mutación del módulo</name>
  <read_first>
    - `06-RESEARCH.md` §Pattern 4 (namespace por build, verificado: 0 mutaciones, 0 fugas, 2 schemas funcionales) y §Common Pitfalls 6 (borrar antes de resolver)
    - `encino_orm/graphql/schema.py` :164-194 (`build_schema` actual)
    - `encino_orm/graphql/types.py` :41,46 y `filters.py` :108 (los `strawberry.lazy(module_name)`)
  </read_first>
  <files>encino_orm/graphql/schema.py, tests/test_graphql_namespace.py</files>
  <behavior>
    - `before = set(vars(encino_orm.graphql.schema))`; tras `build_schema([Region, Agente])` dos veces, `set(vars(...)) == before` (cero mutaciones).
    - Tras los dos builds, `[k for k in sys.modules if "_build_" in k] == []` (cero fugas).
    - Los dos schemas siguen ejecutando relación, filtro autorreferente y mutation.
    - La SDL sigue siendo idéntica al `.ambr`.
  </behavior>
  <action>
    **A) `build_schema` con módulo sintético por build.** Sustituye `module_name = __name__` / `module = sys.modules[__name__]` / los dos `setattr(module, ...)` por:
    - `name = f"encino_orm.graphql._build_{next(_build_counter)}"` con `_build_counter = itertools.count(1)` a nivel de módulo (importa `itertools` y `types` a nivel de módulo).
    - `mod = types.ModuleType(name)`; `mod.__package__ = __package__`; `sys.modules[name] = mod`.
    - En un `try`, construye `build_type(model, name)` y `setattr(mod, model.__name__, typ)`, luego `build_filter_input(model, name)` y `setattr(mod, f"{model.__name__}Filter", ftype)`, y devuelve `strawberry.Schema(...)`.
    - En un `finally`, `del sys.modules[name]` (Pitfall 6: registrar ANTES de construir el schema y borrar DESPUÉS de que `strawberry.Schema(...)` retorne; el `SchemaConverter` resuelve y cachea los `LazyType` durante la construcción).
    - Elimina `module_name = __name__`, `module = sys.modules[module_name]` y los `setattr(module, ...)`. Conserva `auto_camel_case` y `StrawberryConfig`.

    **B) Test de no-mutación (TDD: escríbelo primero, debe fallar con el código actual).** Añade a `tests/test_graphql_namespace.py`:
    - `test_build_schema_no_muta_el_namespace_del_modulo`: `import encino_orm.graphql.schema as schema_mod`; `before = set(vars(schema_mod))`; `build_schema([Region, Agente])`; `build_schema([Region, Agente])`; asserta `set(vars(schema_mod)) == before`.
    - `test_build_schema_no_fuga_modulos_sinteticos`: tras los dos builds, `assert not [k for k in sys.modules if "_build_" in k]`.
    - `test_dos_schemas_siguen_ejecutando`: ejecuta relación + filtro autorreferente + mutation en ambos schemas (si no estaba ya en Task 1, amplíalo aquí).
    Si el `del sys.modules[name]` provoca `ModuleNotFoundError` en algún camino no ejercitado, NO borres el test: conserva el módulo registrado (fallback documentado en el docstring) y registra la causa en el SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_graphql_namespace.py -q && uv run pytest tests/test_graphql.py tests/test_pk.py tests/test_pagination_limits.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ModuleType" encino_orm/graphql/schema.py` devuelve >= 1
    - `grep -c "sys.modules\[name\] = mod" encino_orm/graphql/schema.py` devuelve 1
    - `grep -c "del sys.modules\[name\]" encino_orm/graphql/schema.py` devuelve 1
    - `grep -c "setattr(module" encino_orm/graphql/schema.py` devuelve 0 (ya no muta el módulo real)
    - `grep -c "def test_build_schema_no_muta_el_namespace_del_modulo" tests/test_graphql_namespace.py` devuelve 1
    - `uv run pytest tests/test_graphql_namespace.py -q` pasa (incluido el test de no-mutación)
    - `uv run pytest tests/test_graphql.py tests/test_pk.py tests/test_pagination_limits.py -q` pasa
    - `git diff --name-only` NO incluye `tests/__snapshots__/test_graphql_namespace.ambr` (la SDL no cambió con el aislamiento)
  </acceptance_criteria>
  <done>`build_schema` usa un módulo sintético por build, no muta el namespace de `encino_orm.graphql.schema`, no deja fugas en `sys.modules` y ambos schemas siguen funcionando (Success Criterion 4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot SDL -> verificación del contrato | un snapshot capturado después del rewrite bendice una regresión |
| `__signature__` del resolver -> Strawberry | la firma declarada decide los argumentos y el tipo de retorno GraphQL |
| módulo sintético -> `sys.modules` | un módulo huérfano contamina el proceso; borrarlo antes de resolver rompe el lazy |
| namespace del módulo real -> builds sucesivos | acumular tipos hace que un nombre apunte al tipo del último build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | El `.ambr` se regenera tras el rewrite y oculta un cambio de SDL | mitigate | Task 1 commitea el `.ambr` contra `exec()`; Tasks 2/3 corren SIN `--snapshot-update` y exigen diff cero |
| T-06-04-02 | Tampering | El módulo sintético queda huérfano en `sys.modules` | mitigate | `try/finally` con `del sys.modules[name]`; test `test_build_schema_no_fuga_modulos_sinteticos` |
| T-06-04-03 | Tampering | Dos builds sucesivos interfieren (el nombre `Region` apunta al tipo del último build) | mitigate | Namespace por build + test `test_build_schema_no_muta_el_namespace_del_modulo` y funcional de dos schemas |
| T-06-04-04 | Tampering | La firma del resolver deriva (nombres/tipos/orden) y cambia la SDL | mitigate | `Signature` explícita con objetos de tipo reales y `return_annotation`; snapshot SDL |
| T-06-04-05 | Denial of Service | `del sys.modules` antes de que Strawberry resuelva los `LazyType` | mitigate | Registrar ANTES de `strawberry.Schema(...)` y borrar en `finally` DESPUÉS; cubrir `as_str()` + 4 tipos de operación |
| T-06-04-06 | Tampering | Ruta no ejercitada de Strawberry resuelve un `LazyType` tras el `del` (Assumption A1) | accept + mitigate | Aceptado como riesgo MEDIO; mitigación: cobertura amplia y fallback documentado (conservar el módulo registrado) si aparece `ModuleNotFoundError` |
| T-06-04-SC | Tampering | instalaciones/actualizaciones de paquetes | n/a | Este plan no instala ni actualiza dependencias (`syrupy` ya está fijado) |
</threat_model>

<verification>
- `uv run pytest tests/test_graphql_namespace.py -q` verde sin `--snapshot-update`
- `uv run pytest tests/test_graphql.py tests/test_pk.py tests/test_pagination_limits.py -q` verde
- `uv run pytest -q -m "not integration and not optional_engine"` verde
- `grep -Ec "^\s*exec\(" encino_orm/graphql/schema.py` == 0
- `grep -c "ModuleType" encino_orm/graphql/schema.py` >= 1 y `grep -c "del sys.modules\[name\]" encino_orm/graphql/schema.py` == 1
- `grep -c "setattr(module" encino_orm/graphql/schema.py` == 0
- `uv run ruff check encino_orm tests` sale 0
- Evidencia en el SUMMARY: commit del `.ambr` (antes) y commits de rewrite/namespace (después), con diff cero en la SDL y `set(vars(module))` idéntico antes/después
</verification>

<success_criteria>
- CFG-03 (GraphQL) cubierto: `_pk_resolver` se construye con closures + `__signature__`, sin `exec()`, con SDL idéntica antes/después
- CFG-04 cubierto: `build_schema` usa un namespace por build y no muta el namespace del módulo (Success Criterion 4)
- Dos llamadas sucesivas a `build_schema` no acumulan estado ni dejan fugas en `sys.modules`
- Los dos schemas siguen ejecutando relaciones, filtros autorreferentes y mutations
- El snapshot SDL se capturó antes del rewrite y nunca se regeneró después
</success_criteria>

<output>
Create `.planning/phases/06-config-optional-layer-hygiene/06-04-SUMMARY.md` when done
</output>
