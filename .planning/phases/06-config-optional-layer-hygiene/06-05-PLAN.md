---
phase: 06-config-optional-layer-hygiene
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - docs/trust-boundaries.md
  - docs/reference/filter.md
  - docs/reference/sql.md
  - docs/reference/db.md
  - docs/reference/context.md
  - docs/integrations.md
  - docs/design/9-singleton.md
  - docs/getting-started.md
  - README.md
  - mkdocs.yml
  - encino_orm/model/filter.py
  - tests/test_trust_boundaries.py
  - CHANGELOG.md
  - pyproject.toml
  - .github/workflows/ci.yml
  - uv.lock
autonomous: true
requirements: [CFG-05]
must_haves:
  truths:
    - "`docs/trust-boundaries.md` documenta `Filter.raw`, `Query` y `db.fn.*` con ejemplos SEGUROS e INSEGUROS explícitos (Success Criterion 5)."
    - "Ningún parser HTTP (`http/parsing.py`) ni GraphQL (`graphql/filters.py`) puede emitir `Filter.raw`: un test de regresión lo congela (el conjunto de operadores es cerrado)."
    - "`mkdocs build --strict` pasa con la página nueva y su entrada de nav (referencias/nav sin romper)."
    - "`CHANGELOG.md` documenta en `[Unreleased]` los cambios de CFG-01…CFG-05 con comportamiento viejo/nuevo y nota de ownership hacia Fase 8 (`08-04`)."
    - "Los `per-file-ignores` `S102`/`B008` y las entradas del ratchet mypy asignadas a Fase 6 se retiran SOLO cuando `ruff check .`/`mypy encino_orm` prueban que el código ya no los necesita; un residual ajeno se conserva con la causa escrita."
    - "El cap de `PyJWT` sube a `>=2.8,<2.15` en un commit aislado, con `uv lock` regenerado, `uv audit` (sin ignores) y `pip-audit` verdes; solo entonces se retiran los 5 `--ignore` GHSA de `ci.yml`."
  artifacts:
    - path: "docs/trust-boundaries.md"
      provides: "Fronteras de confianza de `Filter.raw`/`Query`/`db.fn.*` con ejemplos seguros e inseguros"
      contains: "Filter.raw"
    - path: "tests/test_trust_boundaries.py"
      provides: "Regresión: los parsers HTTP/GraphQL no emiten `raw`; la página conserva los tres encabezados y un bloque inseguro"
      contains: "raw"
    - path: "encino_orm/model/filter.py"
      provides: "Docstring de `Filter.raw` con el contrato de frontera de confianza"
      contains: "raw"
    - path: "CHANGELOG.md"
      provides: "Entradas `[Unreleased]` de CFG-01…CFG-05 (cambios incompatibles documentados)"
      contains: "ConnectionRegistry"
    - path: "pyproject.toml"
      provides: "Ignorés retirados y cap `PyJWT>=2.8,<2.15`"
      contains: "PyJWT>=2.8,<2.15"
    - path: ".github/workflows/ci.yml"
      provides: "Job `deps` sin los 5 `--ignore` GHSA"
      contains: "uv audit"
  key_links:
    - from: "docs/trust-boundaries.md"
      to: "encino_orm/model/filter.py::Filter.raw"
      via: "la prosa documenta el mismo contrato que el docstring"
      pattern: "Filter.raw"
    - from: "tests/test_trust_boundaries.py"
      to: "encino_orm/http/parsing.py::_OP_MAP"
      via: "afirma que ningún operador del parser produce `raw`"
      pattern: "_OP_MAP"
    - from: "pyproject.toml::[tool.ruff.lint.per-file-ignores]"
      to: "encino_orm/http/routes.py / encino_orm/graphql/schema.py / encino_orm/security/guard.py"
      via: "los ignorés se retiran porque el código ya no contiene `exec()` ni `Depends(...)` en defaults"
      pattern: "per-file-ignores"
---

<objective>
Cerrar CFG-05 (documentación de fronteras de confianza), documentar en `CHANGELOG.md` los cambios de la fase, y retirar la deuda de gates/dependencias que la fase deja saldada, siendo el ÚNICO dueño de los ficheros compartidos (`pyproject.toml`, `CHANGELOG.md`, `mkdocs.yml`, `ci.yml`).

`Filter.raw` (`model/filter.py:113-115`) no tiene docstring y su fragmento se reemite verbatim (`_rebind_raw`, :218-224); `Query` documenta su contrato (`query.py:8-47`); `SqlFunctions` (`sql.py:33-42`) ya declara "texto de confianza". No existe una página que explique la frontera completa con ejemplos. Además, `pyproject.toml` conserva supresiones que nombran Fase 6 (`S102`/`B008`, ratchet mypy) y `ci.yml` conserva 5 `--ignore` GHSA con la nota "se levantarán en la Fase 6 (CFG-02)".

Purpose: CFG-05 cierra el criterio 5; el CHANGELOG cumple el constraint de PROJECT.md ("los cambios incompatibles deben documentarse en `CHANGELOG.md`"); la limpieza de gates evita que las supresiones derroten el chequeo (objetivo declarado en `pyproject.toml`).

Output: `docs/trust-boundaries.md` + docstring de `Filter.raw` + referencias cruzadas + nav; `tests/test_trust_boundaries.py`; entradas de `CHANGELOG.md`; `pyproject.toml` con los ignorés retirados (o residuales con causa) y `PyJWT>=2.8,<2.15`; `ci.yml` sin los 5 `--ignore`; `uv.lock` regenerado.

**Propiedad única de ficheros compartidos (Pitfall 8).** `06-01`…`06-04` NO editan `pyproject.toml`, `CHANGELOG.md`, `mkdocs.yml` ni `ci.yml`. Este plan los posee en exclusiva y corre en Wave 3 (tras el código), de modo que no hay merge conflicts por concurrencia.

**Restricción dura de snapshots.** Este plan NO toca `tests/__snapshots__/test_http_openapi.ambr` ni `tests/__snapshots__/test_graphql_namespace.ambr` ni `tests/__snapshots__/test_sql_snapshots.ambr`. Si `ruff`/`mypy`/`uv lock` exigieran re-tocar un snapshot, DETENTE: es una señal de que el código de Wave 2 cambió de contrato y debe corregirse allí.

**Fail-closed del bump de dependencia (Pitfall 10).** `uv audit` (feed OSV) y `pip-audit` (feed PyPA) son fuentes independientes. El cap SOLO sube si AMBOS quedan limpios sin ignores y `tests/test_security.py` sigue verde; si algo falla, se restaura el cap `>=2.8,<2.13` y se conservan los `--ignore` con la causa escrita (estado actual, sin regresión).
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-config-optional-layer-hygiene/06-RESEARCH.md
@encino_orm/model/filter.py
@encino_orm/query.py
@encino_orm/sql.py
@encino_orm/http/parsing.py
@encino_orm/graphql/filters.py
@docs/reference/filter.md
@docs/reference/sql.md
@docs/reference/db.md
@docs/reference/context.md
@docs/integrations.md
@docs/design/9-singleton.md
@mkdocs.yml
@pyproject.toml
@.github/workflows/ci.yml
@CHANGELOG.md
@tests/test_sql_snapshots.py

<interfaces>
<!-- Anclajes verificados contra el árbol actual. No explorar más. -->

`encino_orm/model/filter.py`:
- `Filter.raw(sql, params)` :113-115 — SIN docstring; devuelve `Filter("RAW", sql, list(params))`.
- `_rebind_raw` :218-224 — reindexa `{n}` con `re.sub` y reemite el fragmento VERBATIM; no valida `sql`.
- `_safe_field` :26-29 y `_qual` :212-216 — validan SOLO nombres de campo, no el fragmento RAW.

`encino_orm/query.py` :8-47 — docstring con contrato y las 2 limitaciones declaradas (literal de cadena con `{n}`; inmutabilidad de atributo no profunda).

`encino_orm/sql.py` :33-42 — `SqlFunctions` docstring: "Devuelven **fragmentos SQL** (texto de confianza)"; `_COLUMN_RE` :12 y `_col` :47-52 validan identificadores.

`encino_orm/http/parsing.py` :9-20 (`_OP_MAP`) y :23-32 (`_apply_op`) — conjunto CERRADO de operadores; no puede producir `raw`.
`encino_orm/graphql/filters.py` :124-149 (`_apply_op`) — conjunto CERRADO; no puede producir `raw`.

`docs/reference/filter.md` (8 líneas), `sql.md`, `db.md` (27 líneas), `context.md` (14 líneas) — stubs de `mkdocstrings`.
`docs/integrations.md` :107-122 — ejemplo `guard.SECRET = ...` / `guard.GET_DB = ...` (pasa a marcarse deprecado).
`docs/design/9-singleton.md` :52-101 — reproduce el `_default_db` global como diseño (debe apuntar al registry).
`mkdocs.yml` :51-83 — nav; la sección "Desarrolladores" (:67-70) o "Guía" (:53-56) es el punto de anclaje.

`pyproject.toml`:
- :26 `security = ["fastapi>=0.110", "PyJWT>=2.8,<2.13"]`.
- :163-167 `"encino_orm/http/routes.py" = ["S102","B008"]`, `"encino_orm/graphql/schema.py" = ["S102"]`.
- :170 `"encino_orm/security/guard.py" = ["B008"]`.
- :182-184 `"tests/test_security.py" = ["B008","S105"]`.
- :257 `"encino_orm.security.models"`; :266 `"encino_orm.graphql.*"`; :276-279 `"encino_orm.http.routes"`, `"encino_orm.http.parsing"`, `"encino_orm.security.guard"` (ratchet mypy).

`.github/workflows/ci.yml` :230-253 — job `deps` con 5 `--ignore GHSA-...` (:241-245) y `uv lock --check` (:228).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CFG-05 — `docs/trust-boundaries.md` + docstring de `Filter.raw` + referencias cruzadas + nav + test de regresión</name>
  <read_first>
    - `06-RESEARCH.md` §6 (fronteras de confianza), §Code Examples "Fronteras de confianza — texto a documentar" (los 6 ejemplos seguros/inseguros) y §Validation Architecture punto 5
    - `encino_orm/model/filter.py` :113-115 y :218-224
    - `encino_orm/http/parsing.py` :9-32 y `encino_orm/graphql/filters.py` :124-149
    - `docs/reference/filter.md`, `sql.md`, `db.md`, `context.md`, `docs/integrations.md` :107-122, `docs/design/9-singleton.md` :52-101, `mkdocs.yml` :51-83
    - `README.md` :38 y `docs/getting-started.md` :90-100 (ambos enseñan `set_default_db` como camino normal; W4)
  </read_first>
  <files>docs/trust-boundaries.md, docs/reference/filter.md, docs/reference/sql.md, docs/reference/db.md, docs/reference/context.md, docs/integrations.md, docs/design/9-singleton.md, docs/getting-started.md, README.md, mkdocs.yml, encino_orm/model/filter.py, tests/test_trust_boundaries.py</files>
  <action>
    **A) `docs/trust-boundaries.md` (nuevo).** Escribe una página con tres secciones tituladas exactamente `## Filter.raw`, `## Query` y `## db.fn.*` (el test las verifica). Cada sección debe incluir: qué es, qué valida hoy (con el anchor de código), un bloque SEGURO y un bloque INSEGURO claramente etiquetados (el test exige al menos un bloque inseguro). Incluye la limitación declarada de `Query` (`query.py:35-36`: un `{n}` dentro de un literal de cadena se interpreta como placeholder) y la regla "los valores SIEMPRE viajan ligados; nunca interpoles entrada no confiable en la plantilla". Usa placeholders, nunca credenciales reales.

    **B) Docstring de `Filter.raw`.** Añade a `encino_orm/model/filter.py` un docstring a `raw` que declare la frontera: el fragmento se reemite VERBATIM (`_rebind_raw`), solo se reindexan los `{n}`, y el llamador es responsable de no interpolar entrada no confiable; los valores van en `params`. Referencia `docs/trust-boundaries.md`.

    **C) Referencias cruzadas.** Añade una línea al final de `docs/reference/filter.md`, `docs/reference/sql.md`, `docs/reference/db.md` y `docs/reference/context.md` apuntando a `trust-boundaries.md` (p. ej. "Fronteras de confianza: ver [trust-boundaries.md](../trust-boundaries.md)"). Actualiza `docs/integrations.md` §"Guard en FastAPI" para mostrar el patrón `SecurityConfig`/`security_dependencies` como recomendado y marcar `guard.SECRET = ...` como DEPRECADO (deja el ejemplo legacy, anotado). Actualiza `docs/design/9-singleton.md` para que el diseño apunte a `ConnectionRegistry` en vez del global `_default_db` (nota de actualización, sin reescribir todo el documento). Añade `docs/trust-boundaries.md` a `mkdocs.yml` (sección "Desarrolladores" o "Guía"; mantén el orden y el estilo del nav).

    **C2) Banner de deprecación en README y getting-started (W4).** `README.md` (:38 lista `set_default_db` entre los mecanismos de conexión implícita) y `docs/getting-started.md` (:93-95 enseñan `set_default_db(db)` como el camino normal) siguen presentando el shim como recomendado. Añade en ambos un banner breve que (a) marque `set_default_db`/`get_default_db` como DEPRECADOS (emiten `DeprecationWarning`), (b) nombre `ConnectionRegistry` como reemplazo, y (c) enlace a `docs/reference/context.md` y/o `docs/trust-boundaries.md`. NO reescribas el quick-start a `ConnectionRegistry` si eso lo complica: basta el banner + puntero.

    **D) `tests/test_trust_boundaries.py` (nuevo).**
    - `test_parsers_http_no_emiten_raw`: recorre `_OP_MAP` de `http/parsing.py` y afirma que ningún operador construye `Filter.raw` (p. ej. `_apply_op` sobre cada op produce un filtro cuyo `to_sql()` no contiene el fragmento RAW de prueba; o inspecciona el código con `inspect.getsource` y afirma que no contiene `.raw(`). Elige la vía que no dependa de introspección frágil.
    - `test_parsers_graphql_no_emiten_raw`: mismo espíritu sobre `graphql/filters.py::_apply_op`.
    - `test_docs_trust_boundaries_presentes`: lee `docs/trust-boundaries.md` y exige los tres encabezados (`Filter.raw`, `Query`, `db.fn.*`) y al menos un bloque marcado como inseguro.
    - `test_filter_raw_reemite_verbatim`: congela que `Filter.raw("x = {0}", [1]).to_sql()` conserva el fragmento y liga el valor (documenta el contrato, no lo cambia).
    - NO añadas `Filter.raw` a ningún parser.
  </action>
  <verify>
    <automated>uv run pytest tests/test_trust_boundaries.py -q && uv run mkdocs build --strict && uv run ruff check tests/test_trust_boundaries.py encino_orm/model/filter.py</automated>
  </verify>
  <acceptance_criteria>
    - `docs/trust-boundaries.md` existe con los tres encabezados (`grep -c "Filter.raw" docs/trust-boundaries.md` >= 1, `Query` >= 1, `db.fn` >= 1)
    - `grep -c "trust-boundaries.md" mkdocs.yml` devuelve >= 1
    - `grep -c "def raw" encino_orm/model/filter.py` devuelve 1 y `grep -c "VERBATIM\|verbatim\|frontera" encino_orm/model/filter.py` devuelve >= 1 (el docstring de `raw` existe)
    - `grep -c "trust-boundaries" docs/reference/filter.md docs/reference/sql.md docs/reference/db.md docs/reference/context.md` devuelve >= 1 por fichero
    - `grep -c "SecurityConfig\|security_dependencies" docs/integrations.md` devuelve >= 1 y `grep -c "deprecad" docs/integrations.md` devuelve >= 1
    - `grep -c "ConnectionRegistry" docs/design/9-singleton.md` devuelve >= 1
    - `grep -c "ConnectionRegistry" README.md docs/getting-started.md` devuelve >= 1 por fichero (W4)
    - `grep -c "deprecad" README.md docs/getting-started.md` devuelve >= 1 por fichero (W4: banner de deprecación presente)
    - `uv run pytest tests/test_trust_boundaries.py -q` pasa
    - `uv run mkdocs build --strict` sale 0
    - `uv run ruff check tests/test_trust_boundaries.py encino_orm/model/filter.py` sale 0
  </acceptance_criteria>
  <done>CFG-05 documentado con ejemplos seguros/inseguros, docstring de `Filter.raw`, referencias cruzadas, nav actualizado, integraciones/9-singleton alineados y test de regresión que congela que los parsers no emiten `raw`.</done>
</task>

<task type="auto">
  <name>Task 2: `CHANGELOG.md` `[Unreleased]` — cambios de CFG-01…CFG-05</name>
  <read_first>
    - `CHANGELOG.md` :9-262 (sección `[Unreleased]` y el patrón de las entradas existentes, incluida la "Nota de ownership" hacia `08-04`)
    - `06-RESEARCH.md` §Runtime State Inventory (qué artefactos sobreviven) y §Assumptions Log
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    Añade a `CHANGELOG.md` §`[Unreleased]` entradas que documenten los cambios de la fase, con comportamiento viejo/nuevo y la misma nota de ownership que las entradas existentes ("esta entrada es ADITIVA y no prejuzga la enumeración de cambios incompatibles del milestone, que posee la Fase 8 (`08-04`)"). Cubre:
    - **CFG-01:** `ConnectionRegistry` reemplaza el global `_default_db`; `set_default_db`/`get_default_db` quedan DEPRECADOS (emiten `DeprecationWarning`); `resolve_db()` sin argumentos se comporta igual; la precedencia `bind`/`session`/pool no cambia. Viejo: un único default de proceso; nuevo: default inyectable por registry.
    - **CFG-02:** `SecurityConfig` frozen + `security_dependencies(config)` reemplazan los globales mutables `SECRET`/`GET_DB`; mutarlos deja de cambiar el comportamiento; el fallback a los globales emite `DeprecationWarning`; las firmas legacy siguen funcionando. Para la frase del cap de `PyJWT`, escribe el marcador explícito `PENDIENTE_BUMP` en esa entrada; Task 3 lo reemplazará por el estado DEFINITIVO — W7 (nunca dejes una frase condicional en el CHANGELOG).
    - **CFG-03:** los handlers REST `get`/`put`/`delete` y los resolvers GraphQL `get`/`update`/`delete` se construyen con closures + `__signature__` en vez de `exec()`; el contrato OpenAPI y la SDL son IDÉNTICOS (cambio de implementación, no de contrato).
    - **CFG-04:** `build_schema` usa un namespace por build y deja de mutar el namespace del módulo; dos builds dejan de interferir.
    - **CFG-05:** fronteras de confianza de `Filter.raw`/`Query`/`db.fn.*` documentadas; `Filter.raw` sigue reemitiendo el fragmento verbatim (no cambia).
    Usa las secciones `### Cambiado`/`### Corregido` existentes; NO dupliques entradas ni inventes cambios fuera de CFG-01…05. Menciona que la retirada de los globales (no solo su deprecación) es de `REL-01` (Fase 8).
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; t=pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8'); u=t.split('## [Unreleased]',1)[1].split('## [0.2.6]',1)[0]; [u.index(k) for k in ('ConnectionRegistry','SecurityConfig','__signature__','build_schema','trust-boundaries')]; print('changelog OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ConnectionRegistry" CHANGELOG.md` >= 1 (sección Unreleased)
    - `grep -c "SecurityConfig" CHANGELOG.md` >= 1
    - `grep -c "__signature__\|exec()" CHANGELOG.md` >= 1
    - `grep -c "build_schema" CHANGELOG.md` >= 1
    - `grep -c "trust-boundaries\|fronteras de confianza" CHANGELOG.md` >= 1
    - La sección `[Unreleased]` sigue siendo la primera y no se duplican entradas existentes
  </acceptance_criteria>
  <done>`CHANGELOG.md` documenta CFG-01…CFG-05 con comportamiento viejo/nuevo y ownership hacia Fase 8.</done>
</task>

<task type="auto">
  <name>Task 3: Retirar supresiones de gates + bump de `PyJWT` + `ci.yml` sin ignores + `uv lock` + auditorías (commits aislados)</name>
  <read_first>
    - `06-RESEARCH.md` §Open Questions 1 y 4, §Common Pitfalls 10 (subir el cap sin re-verificar) y §Package Legitimacy Audit
    - `pyproject.toml` :26, :163-184, :243-281
    - `.github/workflows/ci.yml` :207-253 (job `deps`)
    - `tests/test_security.py` (el estado final de 06-02: `Annotated` y constante renombrada)
  </read_first>
  <files>pyproject.toml, .github/workflows/ci.yml, uv.lock</files>
  <action>
    **A) Retirada de supresiones (commit 1).** Tras el código de Wave 2:
    - Elimina `"S102"` de `encino_orm/http/routes.py` y de `encino_orm/graphql/schema.py`; si la lista queda vacía, elimina la entrada completa (no dejes `[]`).
    - Elimina `"B008"` de `encino_orm/http/routes.py` y `encino_orm/security/guard.py`; elimina la entrada `tests/test_security.py` (`B008`/`S105`) si el rename de 06-02 la deja sin hallazgos.
    - Actualiza los comentarios de `pyproject.toml` que dicen "se levanta en Fase 6" para reflejar el estado real.
    - **Ratchet mypy:** retira las entradas `encino_orm.http.routes` y `encino_orm.security.guard`; intenta retirar `encino_orm.graphql.*`, `encino_orm.security.models` y `encino_orm.http.parsing` (esta última también nombra Fase 6 / CFG-03 en el comentario de `pyproject.toml:277` — W3). REGLA: retira una entrada SOLO si `uv run mypy encino_orm` sigue saliendo 0; si queda un residual AJENO al alcance, CONSERVA la entrada y reescribe su causa (precedente `04-04`, `pyproject.toml:249-256`). Nunca añadas `# type: ignore` inline (el conteo debe seguir en 0).
    - Verifica con `uv run ruff check .` y `uv run mypy encino_orm` (ambos 0) antes de commitear.

    **B) Bump de `PyJWT` (commit 2, AISLADO).**
    - Cambia `pyproject.toml:26` a `PyJWT>=2.8,<2.15`.
    - Ejecuta `uv lock` y `uv lock --check`.
    - Ejecuta `uv audit` SIN `--ignore` y `uv export ... | uv run pip-audit -r -` (o el equivalente del job `deps`); AMBOS deben quedar limpios.
    - Si y solo si ambos quedan limpios y `uv run pytest tests/test_security.py -q` pasa: retira los 5 `--ignore GHSA-...` de `.github/workflows/ci.yml` (:241-245) y actualiza el comentario de :230-238 para reflejar el estado nuevo.
    - Si algo falla: RESTAURA el cap `>=2.8,<2.13`, NO toques los `--ignore`, y documenta la causa en el SUMMARY y en el CHANGELOG (la entrada de CFG-02 debe decir que el cap se mantiene).
    - El commit del bump + `ci.yml` + `uv.lock` NO debe contener cambios en `tests/__snapshots__/` ni en código de Wave 2.

    **C) Cerrar el CHANGELOG con el resultado DEFINITIVO del bump (W7).** Sea cual sea el resultado de B, reemplaza el marcador `PENDIENTE_BUMP` de la entrada CFG-02 del `CHANGELOG.md` por el estado final: cap subido a `>=2.8,<2.15` (con la evidencia de `uv audit` + `pip-audit`) o cap mantenido en `>=2.8,<2.13` con la causa. La frase final no puede ser condicional ni contener el marcador.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run mypy encino_orm && uv run pytest tests/test_security.py -q && uv lock --check && uv run pytest -q -m "not integration and not optional_engine"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"S102"' pyproject.toml` devuelve 0 (scope a las comillas de las listas de ignorés; NO cuenta comentarios explicativos — minor)
    - `grep -cE '"B008"' pyproject.toml` devuelve 0 (mismo scope)
    - `grep -c "encino_orm.http.routes" pyproject.toml` devuelve 0
    - `grep -c "encino_orm.security.guard" pyproject.toml` devuelve 0
    - `grep -c "encino_orm.http.parsing" pyproject.toml` devuelve 0 (o la entrada sigue con la causa escrita; W3)
    - `grep -c "PENDIENTE_BUMP" CHANGELOG.md` devuelve 0 (W7: el CHANGELOG refleja el estado final)
    - `grep -c "PyJWT>=2.8,<2.15" pyproject.toml` devuelve 1 (o, si la auditoría falló, `PyJWT>=2.8,<2.13` sigue y el SUMMARY documenta la causa)
    - `grep -c "GHSA-" .github/workflows/ci.yml` devuelve 0 (o, si la auditoría falló, los 5 ignores siguen y la causa está escrita)
    - `uv run ruff check .` sale 0
    - `uv run mypy encino_orm` sale 0
    - `uv lock --check` sale 0
    - `uv run pytest tests/test_security.py -q` pasa
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa
    - `git diff --name-only` de cada commit no incluye `tests/__snapshots__/`
    - El SUMMARY registra los dos commits (gates / bump) y el resultado de `uv audit` + `pip-audit`
  </acceptance_criteria>
  <done>Las supresiones `S102`/`B008` y las entradas del ratchet mypy asignadas a Fase 6 se retiran (o quedan con causa escrita), el cap de `PyJWT` sube con `uv lock` y auditorías limpias, y `ci.yml` deja de ignorar los 5 GHSA; todo con commits aislados y la suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prosa de `trust-boundaries.md` -> contrato real del código | documentar una validación que el código no hace da falsa seguridad |
| `Filter.raw` -> SQL emitido | el fragmento se reemite verbatim; entrada no confiable = inyección |
| parsers HTTP/GraphQL -> `Filter` | si un parser pudiera emitir `raw`, la entrada HTTP llegaría al SQL |
| `pyproject.toml`/`ci.yml` -> gates de CI | retirar una supresión sin el arreglo debilita el gate |
| `PyJWT` -> capa `security` | subir el cap sin re-verificar advisories o semántica de decode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | La documentación afirma una frontera que el código no aplica | mitigate | Los ejemplos citan anchors reales; `test_filter_raw_reemite_verbatim` congela el comportamiento; `test_parsers_*_no_emiten_raw` prueba el cierre de los parsers |
| T-06-05-02 | Repudiation | La página de fronteras desaparece o se vacía y nadie lo nota | mitigate | `test_docs_trust_boundaries_presentes` exige los tres encabezados y un bloque inseguro; `mkdocs build --strict` falla con nav/refs rotos |
| T-06-05-03 | Information Disclosure | Un ejemplo de docs incluye un secreto real | mitigate | Solo placeholders; sin credenciales reales (constraint de seguridad del repo) |
| T-06-05-04 | Tampering | Se retira una supresión de gate sin que el código esté arreglado | mitigate | Retirada solo tras `ruff check .`/`mypy encino_orm` verdes; residual ajeno conserva la entrada con causa escrita; nunca `# type: ignore` inline |
| T-06-05-05 | Tampering / Supply chain | El bump de `PyJWT` sin regenerar `uv.lock` o sin re-verificar advisories | mitigate | Commit aislado + `uv lock --check` + `uv audit` (OSV) + `pip-audit` (PyPA) sin ignores + `tests/test_security.py` verde; fail-closed: restaurar cap y conservar ignores si algo falla |
| T-06-05-06 | Repudiation | `CHANGELOG.md` sobreafirma o duplica cambios | mitigate | Entradas con comportamiento viejo/nuevo y ownership hacia `08-04`; sin cambios fuera de CFG-01…05 |
| T-06-05-SC | Tampering | instalaciones/actualizaciones de paquetes | mitigate | El único cambio es de VERSIÓN sobre `PyJWT`, ya declarado en `uv.lock` y auditado en research ([OK], PyPI, `github.com/jpadilla/pyjwt`); no es un paquete nuevo, así que no requiere checkpoint de legitimidad. El commit aislado + doble feed es el control |
</threat_model>

<verification>
- `uv run pytest tests/test_trust_boundaries.py -q` verde
- `uv run mkdocs build --strict` sale 0
- `uv run ruff check .` sale 0
- `uv run mypy encino_orm` sale 0
- `uv lock --check` sale 0
- `uv run pytest tests/test_security.py -q` verde
- `uv run pytest -q -m "not integration and not optional_engine"` verde
- `grep -cE '"S102"\|"B008"' pyproject.toml` == 0 (scope a las comillas; no cuenta comentarios)
- `grep -c "GHSA-" .github/workflows/ci.yml` == 0 (o fail-closed documentado)
- `git diff --name-only` no incluye `tests/__snapshots__/` en ningún commit de este plan
- El SUMMARY registra: resultado de `uv audit` + `pip-audit`, entradas de gate retiradas/conservadas, y los dos commits aislados
</verification>

<success_criteria>
- CFG-05 cubierto: `Filter.raw`, `Query` y `db.fn.*` documentados con ejemplos seguros e inseguros (Success Criterion 5) y un test congela que los parsers no emiten `raw`
- `CHANGELOG.md` documenta CFG-01…CFG-05 (constraint de PROJECT.md) con ownership hacia Fase 8
- Los `per-file-ignores` `S102`/`B008` y las entradas del ratchet mypy asignadas a Fase 6 se retiran, o el residual queda con causa escrita
- El cap de `PyJWT` sube con `uv lock` regenerado y auditorías limpias (o fail-closed documentado)
- `ci.yml` deja de ignorar los 5 GHSA (o fail-closed documentado)
- Ningún snapshot de Wave 2 se modifica
</success_criteria>

<output>
Create `.planning/phases/06-config-optional-layer-hygiene/06-05-SUMMARY.md` when done
</output>
