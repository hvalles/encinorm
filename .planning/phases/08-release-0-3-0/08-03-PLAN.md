---
phase: 08-release-0-3-0
plan: 03
type: execute
wave: 5
depends_on: [08-02, 08-04, 08-06, 08-08]
files_modified:
  - pyproject.toml
  - uv.lock
  - CHANGELOG.md
autonomous: false
requirements: [REL-02]
user_setup:
  - service: pypi
    why: "Publicar 0.3.0rc1 (TestPyPI + PyPI) y luego 0.3.0 por OIDC (D-04: las publicaciones reales las dispara el usuario)"
    env_vars: []
    dashboard_config:
      - task: "Aprobar cada publicación del entorno `pypi` (rc1 y 0.3.0)"
        location: "GitHub > repo > Actions > Publish to PyPI > Review deployments"
must_haves:
  truths:
    - "El workflow de release está gateado sobre CI verde antes de publicar (job `ci` reutilizable + `needs: [ci]` + entorno `pypi`)"
    - "Las remociones de 0.3.0 (`set_default_db`/`get_default_db`, globales `SECRET`/`GET_DB`, `last_id()`) ya aterrizaron (08-06/08-08) antes de promover la versión"
    - "0.3.0rc1 se publica ANTES que 0.3.0 (REL-02), ambas por OIDC y sin token"
    - "`pyproject.toml` y `uv.lock` declaran la misma versión en cada paso (0.3.0rc1 y luego 0.3.0)"
    - "El CHANGELOG tiene heading de la versión publicada en cada corte, manteniendo el orden descendente (Unreleased → 0.3.0rc1/0.3.0 → 0.2.7 → 0.2.6)"
  artifacts:
    - path: "pyproject.toml"
      contains: 'version = "0.3.0"'
    - path: "uv.lock"
      contains: 'version = "0.3.0"'
    - path: "CHANGELOG.md"
      contains: "## [0.3.0]"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/ci.yml"
      via: "job ci reutilizable + needs"
      pattern: "needs: \\[ci\\]"
    - from: "pyproject.toml"
      to: "CHANGELOG.md"
      via: "heading de versión publicada coherente con el literal"
      pattern: "## \\[0\\.3\\.0"
---

<objective>
Publicar 0.3.0rc1 y luego promover a 0.3.0, con el workflow de release gateado sobre CI verde
(REL-02, D-02, D-03).

Purpose: el release candidate da a los usuarios una ventana para detectar rupturas antes de la
etiqueta final; el entorno `pypi` protege ambos cortes. La promoción debe ser un cambio de versión
puro (rc1 → final sin cambios de código).

Output: `0.3.0rc1` y `0.3.0` construidas y publicadas por OIDC (con checkpoint humano en cada una),
versión sincronizada en `pyproject.toml`/`uv.lock` y CHANGELOG con el heading de cada corte.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-release-0-3-0/08-CONTEXT.md
@.github/workflows/release.yml
@.github/workflows/ci.yml
@CHANGELOG.md

Este plan NO edita `release.yml` ni `tests/test_release_config.py`: los posee 08-02. Aquí solo se
VERIFICA el gate (leyendo el YAML y corriendo ese test) antes de cortar el rc1. El CHANGELOG lo
completan 08-04 (enumeración), 08-06 (remociones) y 08-08 (last_id) en waves previas; este plan solo
promueve los headings de versión. Corre en la wave MÁS TARDE de la fase (wave 5) porque depende de
que TODAS las remociones de 0.3.0 hayan aterrizado antes de publicar.

Los guards de `tests/test_release_docs.py` (creados por 08-04 y ampliados por 08-06) escanean
`CHANGELOG.md` COMPLETO (file-wide), NO el bloque `[Unreleased]`: por eso SOBREVIVEN a la promoción
de este plan (que vacía `[Unreleased]` al renombrarlo a `[0.3.0rc1]`/`[0.3.0]`). Este plan NO edita
`tests/test_release_docs.py` (no hace falta: el guard es file-wide) y por eso NO lo añade a
`files_modified`; en su lugar corre la suite COMPLETA antes de tagear, para detectar en rojo antes
de crear el tag. `ci.yml` corre esos mismos tests en CI y `publish: needs: [ci]` + el entorno `pypi`
bloquean la publicación con CI rojo: por eso un build rojo debe detectarse en local ANTES del tag.
</context>

<interfaces>
- Versión: único literal `pyproject.toml:10` (`version = "0.2.7"` tras 08-01) con espejo en
  `uv.lock` (entrada `encino-orm`). Un bump exige `uv lock` + `uv lock --check` verde.
- Gate existente: `release.yml` tiene `ci: uses: ./.github/workflows/ci.yml` y `publish: needs: [ci]`;
  08-02 añadió `environment: {name: pypi}` al job `publish`.
- Convención CHANGELOG (Keep a Changelog, español): heading `## [X.Y.Z] - YYYY-MM-DD`.
- `dist/` está gitignored; los artefactos son efímeros.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verificar el gate de CI y cortar 0.3.0rc1 (versión + lock + CHANGELOG + dry-run)</name>
  <files>pyproject.toml, uv.lock, CHANGELOG.md</files>
  <read_first>
    - .github/workflows/release.yml (completo: `permissions`, job `ci`, `needs`, `environment`, paso OIDC)
    - tests/test_release_config.py (assertions del gate y de OIDC, creadas en 08-02)
    - .github/workflows/ci.yml (líneas 1-12 y nombres de job)
    - pyproject.toml (línea 10)
    - uv.lock (entrada `encino-orm`)
    - CHANGELOG.md (línea 9: `## [Unreleased]`)
    - .planning/phases/08-release-0-3-0/08-CONTEXT.md (D-02)
  </read_first>
  <action>
    VERIFICAR el gate ANTES de cortar (Criterio de Éxito 2):
    - Correr `uv run pytest tests/test_release_config.py -q` (debe pasar; si no, parar: el gate no está
      en su sitio).
    - Confirmar en `release.yml` la presencia de `uses: ./.github/workflows/ci.yml`, `needs: [ci]`,
      `id-token: write`, `uv publish --trusted-publishing always` y `environment:` con `name: pypi`.
    - Confirmar que el entorno `pypi` (config de GitHub) exige el CI completo; si el usuario reportó
      el fallback de D-02, verificar que el fallback incluye required checks.
    No editar `release.yml`.

    CORRER LA SUITE COMPLETA antes de cortar (detección temprana de build rojo): ejecutar
    `uv run pytest -q -m "not optional_engine and not benchmark"` — el MISMO filtro que el job `test`
    de `ci.yml` (allí con `--timeout-method=signal` y cobertura, que no cambian el veredicto de
    aprobado/rojo). Si sale roja, PARAR y no cortar el rc1: `ci.yml` corre estos tests, `publish:
    needs: [ci]` y el entorno `pypi` bloquean la publicación con CI rojo, de modo que tagear sobre un
    árbol rojo hace el release indespublicable. Incluye los guards file-wide de
    `tests/test_release_docs.py`, que deben seguir verdes tras la promoción del CHANGELOG.

    CORTAR 0.3.0rc1:
    - `pyproject.toml:10` → `version = "0.3.0rc1"`.
    - `uv lock` y verificar `uv lock --check`.
    - `CHANGELOG.md`: renombrar `## [Unreleased]` a `## [0.3.0rc1] - <YYYY-MM-DD>` e insertar
      ARRIBA un `## [Unreleased]` vacío nuevo (formato Keep a Changelog). El orden resultante debe
      quedar descendente: `[Unreleased]` → `[0.3.0rc1]` → `[0.2.7]` → `[0.2.6]`. El contenido de
      rupturas/remociones ya lo completaron 08-04/08-06; aquí solo se promueve el heading. No tocar
      el bloque `[0.2.7]`.
    - `uv build` y comprobar que existe `dist/encino_orm-0.3.0rc1-py3-none-any.whl` con metadatos
      `Version: 0.3.0rc1`. No publicar.
  </action>
  <verify>
    <automated>uv run pytest tests/test_release_config.py -q && uv run pytest -q -m "not optional_engine and not benchmark" && uv lock --check && uv build && uv run python -c "import zipfile,glob; w=glob.glob('dist/encino_orm-0.3.0rc1-*.whl')[0]; z=zipfile.ZipFile(w); m=[n for n in z.namelist() if n.endswith('METADATA')][0]; print(z.read(m).decode().splitlines()[1])"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_release_config.py -q` pasa.
    - `uv run pytest -q -m "not optional_engine and not benchmark"` pasa ANTES de tagear (build verde reproducido en local; evita tagear un árbol rojo).
    - Los guards de `tests/test_release_docs.py` siguen verdes tras la promoción (son file-wide; no dependen del bloque `[Unreleased]`, que ahora está vacío).
    - `pyproject.toml` contiene `version = "0.3.0rc1"` y `uv.lock` la refleja; `uv lock --check` sale 0.
    - `CHANGELOG.md` contiene `## [0.3.0rc1]` y un `## [Unreleased]` nuevo y vacío.
    - El orden de headings es descendente: `[Unreleased]` antes de `[0.3.0rc1]`, que va antes de `[0.2.7]`, que va antes de `[0.2.6]`.
    - El wheel `encino_orm-0.3.0rc1` existe y declara `Version: 0.3.0rc1`.
  </acceptance_criteria>
  <done>El gate de CI está verificado y 0.3.0rc1 está construible, sin publicar.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Publicar 0.3.0rc1 (TestPyPI primero, luego PyPI) por OIDC</name>
  <files>pyproject.toml (solo lectura)</files>
  <read_first>
    - .planning/phases/08-release-0-3-0/08-CONTEXT.md (D-03 y D-04)
    - .github/workflows/release.yml (paso OIDC)
  </read_first>
  <action>
    La persona publica con las credenciales/entorno que la sesión no posee (D-04):
    1. Validar el rc1 en TestPyPI: `gh workflow run "Publish to TestPyPI"` y confirmar publicación por
       OIDC. Esto cierra además la validación de OIDC en TestPyPI si 08-02 la difirió por "already exists".
    2. Publicar en PyPI: crear y empujar el tag anotado `v0.3.0rc1` sobre el commit que declara
       `0.3.0rc1` (tag → `Publish to PyPI` → CI verde → aprobación del entorno `pypi`).
    3. Confirmar que `https://pypi.org/project/encino-orm/0.3.0rc1/` existe y que NINGÚN `0.3.0` final
       está publicado todavía.
    No crear ramas. No reordenar commits respecto al tag `v0.2.7`.
  </action>
  <verify>
    <automated>echo "MISSING — verificación humana: 0.3.0rc1 visible en PyPI y TestPyPI, publicada por OIDC sin token, antes de cualquier 0.3.0"</automated>
  </verify>
  <acceptance_criteria>
    - `0.3.0rc1` está publicada en PyPI (y validada en TestPyPI) por OIDC, sin `PYPI_API_TOKEN`.
    - El run de publicación tuvo el job `CI (reusable)` verde y pasó por la aprobación del entorno `pypi`.
    - `0.3.0` final NO está en PyPI en este punto.
  </acceptance_criteria>
  <done>0.3.0rc1 publicada; existe la ventana de validación previa a la final.</done>
</task>

<task type="auto">
  <name>Task 3: Promover a 0.3.0 (versión + lock + heading + dry-run)</name>
  <files>pyproject.toml, uv.lock, CHANGELOG.md</files>
  <read_first>
    - pyproject.toml (línea 10: `0.3.0rc1`)
    - uv.lock (entrada `encino-orm`)
    - CHANGELOG.md (heading `## [0.3.0rc1]`)
  </read_first>
  <action>
    Promoción pura, sin cambios de código: `pyproject.toml:10` → `version = "0.3.0"`; `uv lock`;
    `uv lock --check`. En `CHANGELOG.md`, renombrar `## [0.3.0rc1]` a `## [0.3.0] - <YYYY-MM-DD>`
    (el rc1 y la final son idénticos; opcionalmente dejar una línea "Sin cambios respecto a
    0.3.0rc1"). NO insertar otro `## [Unreleased]` (ya existe el vacío creado en Task 1) y NO tocar
    el bloque `## [0.2.7]`. Orden resultante: `[Unreleased]` → `[0.3.0]` → `[0.2.7]` → `[0.2.6]`.
    Correr la suite COMPLETA `uv run pytest -q -m "not optional_engine and not benchmark"` antes de
    tagear la final: mismo gate que Task 1, detecta un build rojo y evita un tag indespublicable. Los
    guards file-wide de `tests/test_release_docs.py` deben seguir verdes con `[Unreleased]` vacío.
    `uv build` y comprobar
    `dist/encino_orm-0.3.0-py3-none-any.whl` con `Version: 0.3.0`. Verificar que no hay diff de
    código entre el commit de rc1 y este (solo `pyproject.toml`, `uv.lock`, `CHANGELOG.md`).
  </action>
  <verify>
    <automated>uv run pytest -q -m "not optional_engine and not benchmark" && uv lock --check && uv build && uv run python -c "import zipfile,glob; w=glob.glob('dist/encino_orm-0.3.0-*.whl')[0]; z=zipfile.ZipFile(w); m=[n for n in z.namelist() if n.endswith('METADATA')][0]; print(z.read(m).decode().splitlines()[1])" && git diff --stat "v0.3.0rc1" -- encino_orm | uv run python -c "import sys; s=sys.stdin.read().strip(); print('SIN CAMBIOS DE CODIGO' if not s else s); sys.exit(1 if s else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q -m "not optional_engine and not benchmark"` pasa en el commit de promoción (build verde antes del tag final).
    - `pyproject.toml` contiene `version = "0.3.0"` y `uv.lock` la refleja; `uv lock --check` sale 0.
    - `CHANGELOG.md` contiene `## [0.3.0]` y el orden de headings sigue siendo descendente (`[Unreleased]` → `[0.3.0]` → `[0.2.7]` → `[0.2.6]`).
    - El wheel `encino_orm-0.3.0` existe y declara `Version: 0.3.0`.
    - `git diff v0.3.0rc1 -- encino_orm` no muestra cambios de código (solo versión/lock/changelog).
  </acceptance_criteria>
  <done>0.3.0 está construible y es idéntica en código a 0.3.0rc1.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Publicar 0.3.0 final por OIDC</name>
  <files>pyproject.toml (solo lectura)</files>
  <read_first>
    - .planning/phases/08-release-0-3-0/08-CONTEXT.md (D-04)
  </read_first>
  <action>
    La persona crea y empuja el tag anotado `v0.3.0` sobre el commit de promoción y aprueba el run
    de `Publish to PyPI` tras el CI verde (entorno `pypi`). Verifica
    `https://pypi.org/project/encino-orm/0.3.0/`. Confirmar que GitHub Pages (docs.yml) desplegó los
    docs actualizados y que PyPI muestra el README con la guía de pinning de 08-05.
  </action>
  <verify>
    <automated>echo "MISSING — verificación humana: encino-orm 0.3.0 en PyPI, OIDC, CI verde, docs desplegadas"</automated>
  </verify>
  <acceptance_criteria>
    - `0.3.0` está publicada en PyPI por OIDC, con `0.3.0rc1` publicado antes.
    - `0.2.7` precede a ambos y no se ha republicado.
    - Las docs de GitHub Pages reflejan `CHANGELOG`/`MIGRATION-0.3.md`/README actualizados.
  </acceptance_criteria>
  <done>REL-02 satisfecho: rc1 antes de 0.3.0, ambas gateadas por CI y por OIDC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tag `v*` → job `publish` | Cualquiera con push puede disparar; el entorno lo filtra |
| commit fuente → artefacto rc1/final | Deben ser idénticos en código |
| TestPyPI vs PyPI | URLs de publicación distintas; confundirlas publica en el registro equivocado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | Contenido de rc1 vs 0.3.0 | mitigate | La promoción solo cambia versión/lock/changelog; el verify exige `git diff v0.3.0rc1 -- encino_orm` vacío |
| T-08-03-02 | Elevation of privilege | Publicar con CI rojo | mitigate | Entorno `pypi` con required status checks (incl. `Motores pesados (MSSQL + Oracle)`) + `needs: [ci]`; verificado en Task 1 |
| T-08-03-03 | Spoofing | Tag apunta a un commit equivocado | mitigate | Tag anotado sobre el commit cuya `pyproject.toml` declara la versión; verificación de metadatos del wheel |
| T-08-03-04 | Repudiation | Sin traza de la promoción | mitigate | Heading de CHANGELOG + tag + aprobación del entorno dejan auditoría |
| T-08-03-05 | Tampering | Publicar en TestPyPI creyendo que es PyPI | mitigate | Comando con `--publish-url` explícito solo para TestPyPI; PyPI usa el default del workflow |
| T-08-03-SC | Tampering | Instalaciones de paquetes | mitigate | No se añaden dependencias; `uv` pinado a `0.12.15` |
</threat_model>

<verification>
- `uv run pytest tests/test_release_config.py -q` verde en el commit de rc1.
- `uv run pytest -q -m "not optional_engine and not benchmark"` verde en el commit de rc1 y en el de 0.3.0 (mismo filtro que el job `test` de `ci.yml`; detecta el build rojo ANTES de tagear/publicar).
- Los guards de `tests/test_release_docs.py` son file-wide y siguen verdes tras vaciar `[Unreleased]` (no se edita ese fichero en este plan).
- `uv lock --check` verde en rc1 y en 0.3.0.
- Checkpoints: `0.2.7` → `0.3.0rc1` → `0.3.0` en PyPI, en ese orden.
</verification>

<success_criteria>
- REL-02: 0.3.0rc1 publicada antes de 0.3.0.
- El gate de CI + entorno `pypi` impide publicar con CI rojo.
- No hay cambios de código entre rc1 y la final.
</success_criteria>

<output>
Create `.planning/phases/08-release-0-3-0/08-03-SUMMARY.md` when done
</output>
