---
phase: 08-release-0-3-0
plan: 07
type: execute
wave: 3
depends_on: [08-04]
files_modified:
  - docs/credits.md
  - docs/docker.md
  - docs/design/5-security.md
  - docs/design/6-from_db.md
  - docs/design/7-missing.md
  - docs/design/8-pk.md
  - docs/design/9-singleton.md
  - docs/design/a-xpress.md
  - docs/design/has_many.md
  - tests/test_docs_links.py
autonomous: true
requirements: [REL-05]
must_haves:
  truths:
    - "Ningún fichero `.md` rastreado bajo `docs/` referencia la ruta gitignored `prompts/`, así que el enlace ya no puede dar 404 en PyPI/CI"
    - "Un test de fuente recorre `docs/**/*.md` y falla nombrando el fichero infractor si reaparece `prompts/`"
    - "El contenido técnico de los documentos de diseño se conserva; solo se reescribe la cita no distribuida"
    - "`mkdocs build --strict` sigue verde"
  artifacts:
    - path: "tests/test_docs_links.py"
      provides: "Guard de enlaces muertos a prompts/ en docs/**"
      min_lines: 25
    - path: "docs/design/a-xpress.md"
      provides: "Documento de diseño sin referencias a prompts/"
    - path: "docs/credits.md"
      provides: "Créditos sin referencias a prompts/"
  key_links:
    - from: "tests/test_docs_links.py"
      to: "docs/**/*.md"
      via: "barrido parametrizado de ficheros"
      pattern: "prompts/"
---

<objective>
Eliminar la referencia muerta a `prompts/` en todos los documentos rastreados bajo `docs/` y
congelar la limpieza con un guard de fuente (REL-05, parte docs).

Purpose: `prompts/` está gitignored (`.gitignore:38`) y da 404 en PyPI/CI. El README lo arregla 08-05;
los documentos de diseño y `credits.md`/`docker.md` se arreglan aquí. El guard de este plan barre
`docs/**` (el de 08-05 barre solo el README), de modo que ningún test queda rojo esperando a otro plan.

Output: 9 documentos sin `prompts/` y `tests/test_docs_links.py` que lo impide.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-release-0-3-0/08-CONTEXT.md
@.planning/phases/08-release-0-3-0/08-PATTERNS.md
@docs/trust-boundaries.md
@.gitignore

El contenido de `prompts/` NO está disponible (gitignored): cada cita se reemplaza por prosa rastreada
equivalente (p. ej. "análisis interno no distribuido") o por un enlace relativo real a un
`docs/design/*.md` existente; no se migra contenido. No tocar el contenido técnico. Este plan NO toca
`README.md` (lo posee 08-05) ni `docs/MIGRATION-0.3.md` (lo poseen 08-04/08-06).
</context>

<interfaces>
Ficheros y líneas que citan `prompts/` (verificado por grep):
- `docs/credits.md:18` (`prompts/analisys-*.md`)
- `docs/docker.md:28` (`prompts/analisys-12.md`)
- `docs/design/5-security.md:10,37` (`prompts/analisys-04.md`)
- `docs/design/6-from_db.md:12,36` (`prompts/analisys-05.md`)
- `docs/design/7-missing.md:4` (`prompts/analisys-06.md`)
- `docs/design/8-pk.md:3` (`prompts/analisys-07.md`)
- `docs/design/9-singleton.md:3` (`prompts/analisys-07.md`)
- `docs/design/a-xpress.md:9,669` (`prompts/analisys-12.md`)
- `docs/design/has_many.md:3` (`prompts/analisys-14.md`, `prompts/27.md`)

`docs.yml:34` corre `uv run mkdocs build --strict`; el contenido de `docs/` está en la nav o es
alcanzable, así que la edición no debe romper encabezados ni enlaces relativos.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir las citas a prompts/ en los documentos de diseño y páginas de docs</name>
  <files>docs/credits.md, docs/docker.md, docs/design/5-security.md, docs/design/6-from_db.md, docs/design/7-missing.md, docs/design/8-pk.md, docs/design/9-singleton.md, docs/design/a-xpress.md, docs/design/has_many.md</files>
  <read_first>
    - docs/credits.md (línea 18)
    - docs/docker.md (línea 28)
    - docs/design/5-security.md (líneas 10, 37)
    - docs/design/6-from_db.md (líneas 12, 36)
    - docs/design/7-missing.md (línea 4)
    - docs/design/8-pk.md (línea 3)
    - docs/design/9-singleton.md (línea 3)
    - docs/design/a-xpress.md (líneas 9, 669)
    - docs/design/has_many.md (línea 3)
    - docs/trust-boundaries.md (tono y estructura de docs)
  </read_first>
  <action>
    En cada fichero listado, eliminar o reescribir cada referencia a `prompts/<fichero>`. Como el
    contenido de `prompts/` no está disponible (gitignored), reemplazar la cita por prosa rastreada
    equivalente (p. ej. "análisis interno no distribuido") o por un enlace relativo real a un
    `docs/design/*.md` ya existente; conservar la coherencia de la frase y el significado técnico.
    Ninguna de estas rutas puede quedar con la cadena `prompts/`. No tocar el contenido técnico ni
    añadir secciones nuevas. Verificar con grep que no queda ninguna aparición en `docs/`.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; bad=[str(p) for p in pathlib.Path('docs').rglob('*.md') if 'prompts/' in p.read_text(encoding='utf-8')]; assert not bad, bad; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Ningún fichero `.md` bajo `docs/` contiene la cadena `prompts/`.
    - El contenido técnico de los 9 documentos se conserva (solo cambian las citas).
    - `uv run mkdocs build --strict` sale 0.
  </acceptance_criteria>
  <done>La documentación rastreada bajo `docs/` ya no tiene enlaces muertos a `prompts/`.</done>
</task>

<task type="auto">
  <name>Task 2: Guard de enlaces muertos para docs/**</name>
  <files>tests/test_docs_links.py</files>
  <read_first>
    - docs/ (resultado de Task 1)
    - tests/test_ci_harness.py (estilo de assertions de fuente con `pathlib`)
    - .planning/REQUIREMENTS.md (línea 83: REL-05)
  </read_first>
  <behavior>
    - Para cada `.md` bajo `docs/`, el test afirma que no contiene `prompts/` y nombra el fichero infractor al fallar.
    - El barrido cubre SOLO `docs/**/*.md` (el README lo guarda 08-05 en la misma wave; incluirlo aquí crearía un deadlock de ola).
    - El test es DB-free y sin red.
  </behavior>
  <action>
    Crear `tests/test_docs_links.py` (DB-free, sin red) que enumere con `pathlib.Path('docs').rglob('*.md')`
    y use `pytest.mark.parametrize` para afirmar que ninguno contiene `prompts/`, con un mensaje de
    assert que nombre el fichero infractor. NO incluir `README.md`: la limpieza del README la hace
    08-05 en la misma wave y su guard es `tests/test_docs_hygiene.py`. No usar subprocess ni red.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs_links.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_docs_links.py -q` pasa con al menos 1 test (parametrizado sobre todos los ficheros de `docs/`).
    - El test falla si se reintroduce `prompts/` en cualquier `.md` de `docs/`.
    - El test NO barre `README.md` (evita el deadlock con 08-05).
    - `uv run ruff check tests/test_docs_links.py` y `uv run ruff format --check tests/test_docs_links.py` salen 0.
  </acceptance_criteria>
  <done>La limpieza de `prompts/` en docs queda congelada por test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs rastreados → PyPI/CI | Enlaces a rutas no distribuidas dan 404 |
| docs → usuario que migra | Una cita rota reduce la confianza en la documentación |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | Enlace muerto a `prompts/` en `docs/**` | mitigate | Reescritura de citas + guard parametrizado que barre `docs/**` |
| T-08-07-02 | Denial of service | Nav/build de MkDocs roto | mitigate | Solo se editan citas; `uv run mkdocs build --strict` en el verify |
| T-08-07-03 | Information disclosure | Citas internas filtradas | accept | Las citas se sustituyen por prosa rastreada; no se expone contenido de `prompts/` |
| T-08-07-SC | Tampering | Instalaciones de paquetes | mitigate | No se añaden dependencias; sin subprocess ni red en los tests |
</threat_model>

<verification>
- `uv run pytest tests/test_docs_links.py -q` verde.
- `uv run python -c "..."` de Task 1 confirma cero `prompts/` en `docs/`.
- `uv run mkdocs build --strict` verde.
</verification>

<success_criteria>
- REL-05 (parte docs): la documentación rastreada no tiene enlaces muertos a `prompts/` y el guard lo impide.
</success_criteria>

<output>
Create `.planning/phases/08-release-0-3-0/08-07-SUMMARY.md` when done
</output>
