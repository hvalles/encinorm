---
phase: 08-release-0-3-0
plan: 05
type: execute
wave: 3
depends_on: [08-04]
files_modified:
  - README.md
  - .env.example
  - tests/test_docs_hygiene.py
autonomous: true
requirements: [REL-05]
must_haves:
  truths:
    - "El README indica pinnear con `~=0.2.6` (no `>=`) y explica la diferencia para la ventana 0.x"
    - "El README advierte que las credenciales de desarrollo son SOLO para local y existe `.env.example` con cabecera de aviso"
    - "El README ya no referencia la ruta gitignored `prompts/` (el barrido de `docs/**` lo cubre 08-07)"
    - "El README enlaza `docs/MIGRATION-0.3.md` (creada en 08-04)"
  artifacts:
    - path: "README.md"
      provides: "Guía de pinning, aviso de credenciales dev y enlace de migración"
      contains: "~=0.2.6"
    - path: ".env.example"
      provides: "Variables ENCINO_ORM_* con cabecera dev-only"
      min_lines: 15
    - path: "tests/test_docs_hygiene.py"
      provides: "Guard de pinning, credenciales dev y enlace muerto del README"
      min_lines: 40
  key_links:
    - from: "README.md"
      to: "docs/MIGRATION-0.3.md"
      via: "tabla de documentación"
      pattern: "MIGRATION-0\\.3\\.md"
    - from: ".env.example"
      to: "docs/docker.md"
      via: "valores documentados en la tabla de variables de entorno (el archivo replica los defaults dev; no requiere tarea de wiring)"
      pattern: "ENCINO_ORM_"
---

<objective>
Cerrar la guía de usuario del README para el release: pinning `~=0.2.6`, aviso de credenciales
solo-dev con `.env.example`, enlace a la guía de migración y eliminación de la referencia muerta a
`prompts/` en el README (REL-05).

Purpose: `>=0.2.6` instala 0.3.0 con rupturas sin aviso (pip no trata las 0.x como especiales en
`>=`). El pin `~=` es la única defensa práctica durante la ventana 0.x. Las credenciales de
`docker-compose.yml` son de juguete pero no están marcadas, y el README apunta a contenido gitignored
que da 404 en PyPI/CI.

Output: README actualizado, `.env.example` nuevo y un guard de higiene del README. El barrido de
`docs/**` (incluida la limpieza de `prompts/` en los documentos de diseño) vive en 08-07.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-release-0-3-0/08-CONTEXT.md
@.planning/phases/08-release-0-3-0/08-PATTERNS.md
@README.md
@docs/docker.md
@.gitignore

La ruta `prompts/` está gitignored (`.gitignore:38`) y da 404 en PyPI/CI. `.env` está ignorado
exactamente (`.gitignore:33`), así que `.env.example` es commiteable sin tocar `.gitignore`.
`prompts/analisys-07.md` NO está disponible (gitignored): la referencia se reemplaza por un puntero
rastreado, no se migra contenido. Los `docs/design/*.md` y `docs/credits.md`/`docs/docker.md` que
citan `prompts/` se limpian en 08-07; este plan NO los toca.
</context>

<interfaces>
- README: el bloque `## Instalación` (líneas 50-64) usa `pip install -e .`; la tabla de extras
  (68-76) es el patrón de tabla a imitar. El bloque `## Documentación` (116-126) es donde va el
  enlace a `MIGRATION-0.3.md`. El bloque de estado (10-16) contiene el enlace muerto
  `prompts/analisys-07.md`. `## Versionado` (148-153) ya declara la inestabilidad 0.x.
- `.env.example` (nuevo): fuente de valores = tabla de `docs/docker.md:220-227` y
  `docker-compose.yml:1-40`. Cabecera obligatoria de credenciales SOLO para desarrollo local.
- `tests/test_docs_hygiene.py` (nuevo): guard SOLO del README y de `.env.example`. El barrido de
  `docs/**` lo aporta `tests/test_docs_links.py` (08-07).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: README — pinning, aviso dev, versión, enlace de migración y enlace muerto</name>
  <files>README.md</files>
  <read_first>
    - README.md (completo: líneas 1-16 estado/enlace muerto, 50-78 instalación/extras, 116-126 documentación, 148-153 versionado)
    - docs/getting-started.md (redacción de instalación/pinning)
    - docs/MIGRATION-0.3.md (creada en 08-04: destino del enlace)
    - .planning/phases/08-release-0-3-0/08-PATTERNS.md (sección README.md)
  </read_first>
  <action>
    En `README.md`:
    - Bloque `## Instalación`: añadir la guía de pinning con el token literal `encino-orm~=0.2.6`
      y explicar en español por qué `~=` y no `>=` durante la ventana 0.x (pip no trata las 0.x como
      especiales para `>=`, así que `>=0.2.6` auto-instala las rupturas de 0.3.0). Mantener los
      ejemplos actuales de extras.
    - Bloque de estado (líneas 10-16): sustituir la referencia a `prompts/analisys-07.md` por un
      puntero rastreado (p. ej. `CHANGELOG.md` y `docs/MIGRATION-0.3.md`); NINGUNA aparición de la
      cadena `prompts/`.
    - Añadir un aviso de que las credenciales de `docker-compose.yml`/`.env.example` son SOLO para
      desarrollo local (nunca producción), coherente con `## Versionado`.
    - Tabla `## Documentación` (116-126): añadir la fila de `docs/MIGRATION-0.3.md`
      ("Guía de migración 0.3", con qué rupturas cubre).
    - Actualizar el header `# encino_orm · v0.2.6` y el texto "Estado: experimental (v0.2.6)" a
      `0.3.0`. Mantener el README como readme válido de PyPI (`readme = "README.md"`).
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; t=pathlib.Path('README.md').read_text(encoding='utf-8'); assert '~=0.2.6' in t; assert 'prompts/' not in t; assert 'MIGRATION-0.3.md' in t; assert 'v0.3.0' in t; assert 'SOLO para desarrollo' in t or 'solo para desarrollo' in t.lower(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contiene `~=0.2.6`, `MIGRATION-0.3.md` y `v0.3.0`.
    - `README.md` no contiene `prompts/` ni una recomendación de `>=0.2.6`.
    - Existe una frase que marca las credenciales dev como solo-desarrollo.
  </acceptance_criteria>
  <done>El README guía el pinning, avisa de las credenciales dev, enlaza la migración y ya no tiene el enlace muerto.</done>
</task>

<task type="auto">
  <name>Task 2: Crear .env.example con cabecera dev-only</name>
  <files>.env.example</files>
  <read_first>
    - docs/docker.md (líneas 213-234: tabla de variables y ejemplo de export)
    - docker-compose.yml (líneas 1-40: credenciales dev y servicios)
    - .gitignore (líneas 29-41: `.env` exacto, `prompts/`)
    - .planning/phases/08-release-0-3-0/08-PATTERNS.md (sección `.env.example`)
  </read_first>
  <action>
    Crear `.env.example` en la raíz con una cabecera explícita de "credenciales SOLO para desarrollo
    local (valores desechables; nunca uses esto en producción)" y las variables del namespace
    `ENCINO_ORM_*` de `docs/docker.md:220-227`: MYSQL (HOST/PORT/USER/PASSWORD/DB), MARIADB,
    POSTGRES, MSSQL (incluidas DRIVER y TRUST_CERT), ORACLE (incluida SERVICE) y REDIS_URL, con los
    valores dev de la tabla. Comentarios en español. NO usar secretos reales. `.env.example` es
    commiteable (`.env` está ignorado exactamente; `.env.example` no matchea). No añadir tareas de
    wiring: el archivo solo replica los defaults documentados en `docs/docker.md`.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; t=pathlib.Path('.env.example').read_text(encoding='utf-8'); assert 'ENCINO_ORM_MYSQL_HOST' in t and 'ENCINO_ORM_MARIADB_HOST' in t and 'ENCINO_ORM_POSTGRES_HOST' in t and 'ENCINO_ORM_MSSQL_HOST' in t and 'ENCINO_ORM_ORACLE_HOST' in t and 'ENCINO_ORM_REDIS_URL' in t; assert 'desarrollo' in t.lower(); print('OK', len(t.splitlines()))"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` existe con las variables de MySQL, MariaDB, PostgreSQL, MSSQL, Oracle y Redis y una cabecera dev-only.
    - `.env.example` no contiene secretos reales ni valores de producción.
  </acceptance_criteria>
  <done>Las credenciales dev están ejemplificadas y avisadas en un archivo rastreado.</done>
</task>

<task type="auto">
  <name>Task 3: Guard de higiene del README y de .env.example</name>
  <files>tests/test_docs_hygiene.py</files>
  <read_first>
    - README.md (resultado de Task 1)
    - .env.example (resultado de Task 2)
    - tests/test_release_docs.py (guard de 08-04; no duplicar la parte de CHANGELOG/MIGRATION)
    - .planning/REQUIREMENTS.md (línea 83: REL-05)
  </read_first>
  <behavior>
    - `README.md` contiene `~=0.2.6`, no contiene `>=0.2.6` ni `prompts/`, y enlaza `docs/MIGRATION-0.3.md`.
    - `README.md` marca las credenciales de desarrollo como solo-dev.
    - `.env.example` existe, declara `ENCINO_ORM_MYSQL_HOST`, `ENCINO_ORM_MARIADB_HOST`, `ENCINO_ORM_POSTGRES_HOST`, `ENCINO_ORM_MSSQL_HOST`, `ENCINO_ORM_ORACLE_HOST` y `ENCINO_ORM_REDIS_URL`, y lleva aviso dev-only.
    - El guard NO barre `docs/**` (esa prohibición vive en `tests/test_docs_links.py`, 08-07).
  </behavior>
  <action>
    Crear `tests/test_docs_hygiene.py` (DB-free, sin red) con assertions de fuente por `pathlib`
    que congelen REL-05 para el README y `.env.example`: token de pinning presente y ausencia de
    `>=0.2.6`/`prompts/` en `README.md`, presencia del enlace a `docs/MIGRATION-0.3.md`, aviso
    dev-only en `README.md`, y existencia y contenido mínimo de `.env.example`. No incluir un barrido
    de `docs/**`: la limpieza y su guard corren en 08-07, así que incluirlo aquí dejaría el test rojo
    hasta entonces. No usar subprocess ni red.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs_hygiene.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_docs_hygiene.py -q` pasa con al menos 5 tests verdes.
    - El test falla si se reintroduce `prompts/` en el README, si se quita el pin `~=0.2.6` o si desaparece `.env.example`.
    - El test NO barre `docs/**` (evita el deadlock con 08-07).
    - `uv run ruff check tests/test_docs_hygiene.py` y `uv run ruff format --check tests/test_docs_hygiene.py` salen 0.
  </acceptance_criteria>
  <done>REL-05 (README + credenciales dev) queda congelado por test sin acoplarse al barrido de docs de 08-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README/docs → usuario que instala | Una guía de instalación ambigua provoca upgrades con rupturas |
| `.env.example` → entorno del usuario | Valores copiados a producción serían un fallo de seguridad |
| README rastreado → PyPI/CI | Enlaces a rutas no distribuidas dan 404 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information disclosure | `.env.example` | mitigate | Cabecera dev-only y valores desechables de `docs/docker.md`; el guard prohíbe valores de producción |
| T-08-05-02 | Spoofing / Tampering | Upgrade silencioso a 0.3.0 | mitigate | Guía `~=0.2.6` (no `>=`); test prohíbe `>=0.2.6` en el README |
| T-08-05-03 | Tampering | Enlace muerto a `prompts/` en el README | mitigate | Guard del README sin `prompts/`; el barrido de `docs/**` lo cubre 08-07 |
| T-08-05-04 | Denial of service | README roto para PyPI (`readme = "README.md"`) | mitigate | Markdown válido y build de docs en CI; no se rompen encabezados ni tablas |
| T-08-05-SC | Tampering | Instalaciones de paquetes | mitigate | No se añaden dependencias; sin subprocess ni red en los tests |
</threat_model>

<verification>
- `uv run pytest tests/test_docs_hygiene.py -q` verde.
- `uv run python -c "..."` de Task 2 confirma `.env.example` con las seis familias de variables.
- `uv run mkdocs build --strict` verde (la nav de 08-04 sigue válida).
</verification>

<success_criteria>
- REL-05 (parte README/credenciales): README con pinning `~=0.2.6`, aviso de credenciales dev y enlace `prompts/` corregido en el README.
- `.env.example` committeado con cabecera dev-only.
</success_criteria>

<output>
Create `.planning/phases/08-release-0-3-0/08-05-SUMMARY.md` when done
</output>
