---
phase: 08-release-0-3-0
plan: 06
type: execute
wave: 3
depends_on: [08-01, 08-04]
files_modified:
  - encino_orm/context.py
  - encino_orm/__init__.py
  - encino_orm/security/guard.py
  - tests/test_registry.py
  - tests/test_singleton.py
  - tests/test_security.py
  - tests/test_deprecations_0_2_7.py
  - tests/test_removals_0_3_0.py
  - tests/test_release_docs.py
  - CHANGELOG.md
  - docs/MIGRATION-0.3.md
autonomous: true
requirements: [REL-01, REL-04]
must_haves:
  truths:
    - "`set_default_db`/`get_default_db` ya no existen en `encino_orm` (ImportError) ni en `encino_orm.context` (AttributeError); `ConnectionRegistry` es la única vía de default"
    - "Los globales mutables `SECRET`/`GET_DB` y el fallback `_legacy_config` ya no existen; la seguridad exige `SecurityConfig` vía `security_dependencies(config)`"
    - "El camino feliz de resolución de conexión (`ConnectionRegistry`, `bind`, `resolve_db(registry=...)`) y de seguridad (`security_dependencies(SecurityConfig)`) sigue verde"
    - "Ningún test importa o invoca los shims retirados (`tests/test_singleton.py` migrado a `_registry.set_default`), así que la suite completa colecciona y pasa"
    - "El bloque `[Unreleased]` del CHANGELOG tiene EXACTAMENTE un heading `### Eliminado` (08-06 es su único owner)"
    - "CHANGELOG `[Unreleased]` y `docs/MIGRATION-0.3.md` documentan las retiradas reales (incluida `last_id()`, que implementa 08-08) con comportamiento viejo/nuevo"
    - "La nota de ownership del milestone queda resuelta (ya no hay punteros 'lo posee la Fase 8')"
  artifacts:
    - path: "encino_orm/context.py"
      provides: "Registry sin shims deprecados"
      min_lines: 60
    - path: "encino_orm/security/guard.py"
      provides: "Guards solo por SecurityConfig"
      contains: "security_dependencies"
    - path: "tests/test_removals_0_3_0.py"
      provides: "Tests de ausencia y happy path de los símbolos retirados de config/seguridad"
      min_lines: 40
    - path: "tests/test_release_docs.py"
      provides: "Guard de unicidad del heading ### Eliminado en [Unreleased]"
      contains: "### Eliminado"
    - path: "CHANGELOG.md"
      contains: "### Eliminado"
  key_links:
    - from: "tests/test_removals_0_3_0.py"
      to: "encino_orm/context.py"
      via: "ImportError/AttributeError de set_default_db/get_default_db"
      pattern: "set_default_db|get_default_db"
    - from: "tests/test_removals_0_3_0.py"
      to: "encino_orm/security/guard.py"
      via: "AttributeError de SECRET/GET_DB + happy path con SecurityConfig"
      pattern: "SECRET|GET_DB|SecurityConfig"
---

<objective>
Ejecutar la retirada real de 0.3.0 de los shims de configuración/seguridad deprecados en 0.2.7:
`set_default_db`/`get_default_db` y los globales mutables `SECRET`/`GET_DB` (con su fallback
`_legacy_config`). Documentar TODAS las retiradas de la línea 0.3.0 (incluida `last_id()`, cuya
implementación es 08-08) en CHANGELOG + MIGRATION-0.3 (REL-01, REL-04).

Purpose: 0.2.7 avisó; 0.3.0 retira. La retirada es lo que cierra REL-01 (los globales "no solo
deprecados"). El corte de 0.2.7 ya está congelado (08-01) y este plan aterriza DESPUÉS, de modo que
`v0.2.7` nunca contiene las remociones.

Output: `context.py` sin shims, `security/guard.py` sin globales mutables, tests de ausencia +
happy path, y CHANGELOG/MIGRATION con `### Eliminado` enumerando las retiradas reales.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-release-0-3-0/08-CONTEXT.md
@.planning/phases/08-release-0-3-0/08-PATTERNS.md
@CHANGELOG.md
@docs/MIGRATION-0.3.md
@encino_orm/context.py
@encino_orm/security/guard.py

Alcance de la retirada: los shims PÚBLICOS deprecados. El `ConnectionRegistry` y el `_registry` de
módulo se CONSERVAN: `resolve_db()` sin argumentos y `Model` (`model/model.py:219,592`) dependen de
ellos, y el CHANGELOG los declara el diseño vigente (no deprecado). Retirar `_registry` rompería la
resolución implícita de `Model`, que NO es una ruptura enumerada. `tests/test_deprecations_0_2_7.py`
(creado por 08-01) se retira: pineaba warnings de la línea 0.2.7 que dejan de existir con las
retiradas; su cobertura de "reset_on_release" se traslada a `tests/test_removals_0_3_0.py`.

La retirada de `last_id()` la implementa 08-08 en la wave siguiente; este plan solo la DOCUMENTA
(no la ejecuta) para que CHANGELOG/MIGRATION queden completos y con un único owner de los docs.
</context>

<interfaces>
Símbolos a retirar (definiciones actuales):
- `encino_orm/context.py:83-106` → `set_default_db(db)` y `get_default_db()` (ambos emiten
  `DeprecationWarning` y delegan en `_registry`). `ConnectionRegistry.set_default/get_default` y
  `resolve_db(registry=...)` son el reemplazo y NO se tocan.
- `encino_orm/__init__.py:2` (import) y `__all__` (`get_default_db` en `:79`, `set_default_db` en
  `:92`) → quitar ambos del import y de `__all__`.
- `encino_orm/security/guard.py:22-24` → globales `SECRET`/`GET_DB`; `:33-41` `_resolve` usa los
  globales como fallback; `:50-64` `_legacy_config`; `:115-141` `get_current_user`/`require`
  ramifican al fallback cuando no reciben argumentos. El reemplazo es
  `security_dependencies(SecurityConfig(secret, get_db))` (`:67-112`), que NO se toca.
- `tests/test_registry.py:7-16,44-66` importa y ejercita los shims → quitar imports y los tests de
  shim; conservar los de `ConnectionRegistry`, `bind` y `resolve_db(registry=...)`.
- `tests/test_security.py` (fallback legacy) → sustituir por assertions de ausencia + happy path.
- `tests/test_deprecations_0_2_7.py` → eliminar (la línea 0.2.7 ya está publicada; sus pines de
  warnings retirados son obsoletos). Re-pinear el warning de `reset_on_release='commit'` (que sigue
  vigente) en `tests/test_removals_0_3_0.py`.

Formato de retirada en CHANGELOG: 08-06 es el ÚNICO owner del heading `### Eliminado` (08-04 lo
tiene prohibido; ver 08-04 Task 1). Si ya existe, añadir a esa sección; si no, crear exactamente una.
Prosa española con "Viejo: ... nuevo: ...". `MIGRATION-0.3.md` usa pares Antes/Después.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retirar set_default_db/get_default_db y sus exports</name>
  <files>encino_orm/context.py, encino_orm/__init__.py, tests/test_registry.py, tests/test_singleton.py, tests/test_removals_0_3_0.py</files>
  <read_first>
    - encino_orm/context.py (completo: `ConnectionRegistry`, `_registry`, shims deprecados, `resolve_db`)
    - encino_orm/__init__.py (línea 2 y `__all__` líneas 40-100)
    - tests/test_registry.py (completo: tests de shims a retirar y tests de registry a conservar)
    - tests/test_singleton.py (completo: import de `set_default_db` en la línea 12 y usos bajo `pytest.warns` en 32-33, 46-47, 54-55, 75-76)
    - tests/test_deprecations_0_2_7.py (resultado de 08-01: pines a migrar/retirar)
    - encino_orm/model/model.py (líneas 210-225 y 585-595: usos de `resolve_db()` sin argumentos que deben seguir funcionando)
  </read_first>
  <behavior>
    - `from encino_orm import set_default_db` lanza `ImportError`; `encino_orm.context.set_default_db` lanza `AttributeError`.
    - `from encino_orm import get_default_db` lanza `ImportError`; `encino_orm.context.get_default_db` lanza `AttributeError`.
    - `ConnectionRegistry(db).resolve() is db`, `resolve_db(registry=ra) is db_a` y `bind` siguen funcionando.
    - `resolve_db()` sin argumentos sigue usando el `_registry` de módulo (comportamiento histórico, dependencia de `Model`).
    - `tests/test_singleton.py` no importa ni invoca `set_default_db`; sus 4 tests de default usan `_registry.set_default(...)` directamente y pasan sin warnings.
    - `tests/test_deprecations_0_2_7.py` ya no existe.
  </behavior>
  <action>
    En `encino_orm/context.py`: eliminar las funciones `set_default_db` y `get_default_db` (líneas
    83-106). Quitar `import warnings` si queda sin uso. NO tocar `ConnectionRegistry`, `_registry`,
    `bind` ni `resolve_db`. Actualizar el docstring del módulo para dejar de mencionar shims
    deprecados y aclarar que el default vive en el registry (de módulo o inyectado).

    En `encino_orm/__init__.py`: quitar `get_default_db` y `set_default_db` del import de `.context`
    (línea 2) y de `__all__` (líneas 79 y 92).

    En `tests/test_registry.py`: quitar `get_default_db`/`set_default_db` del import y eliminar
    `test_resolve_db_sin_argumentos_usa_el_registry_de_modulo` y `test_shim_deprecado_avisa`
    (ejercitan shims retirados). Conservar el resto (registry, `bind`, `resolve_db(registry=...)`,
    WR-03). Ajustar `test_resolve_db_sin_argumentos_...` si se desea mantener la cobertura del
    `_registry` de módulo usando `_registry.set_default(db_a)` directamente (sin shim) bajo
    `warnings.catch_warnings()` + `simplefilter("error")` para probar que el camino ya NO avisa.

    En `tests/test_singleton.py`: quitar `set_default_db` del import de `encino_orm` (líneas 5-13).
    Migrar sus 4 tests que usaban el shim al camino NO deprecado, con el mismo tratamiento que
    `tests/test_registry.py`: sustituir `with pytest.warns(DeprecationWarning, match="set_default_db"):
    set_default_db(connected_db)` por `_registry.set_default(connected_db)` (el `_registry` ya está
    importado en la línea 14 y el fixture `_clean_default` ya lo resetea), en
    `test_model_resolves_default_db` (32-33), `test_insert_many_without_db` (46-47),
    `test_explicit_db_wins` (54-55) y `test_bind_wins_over_default` (75-76). Dejar el resto de la
    clase intacto (incluido `test_error_without_connection`, que no usa el default).

    Crear `tests/test_removals_0_3_0.py` con: `test_set_default_db_retirado` y
    `test_get_default_db_retirado` (import falla con ImportError; atributo de módulo con
    AttributeError), `test_registry_sigue_siendo_la_via` (happy path), y
    `test_reset_on_release_commit_sigue_avisando` (re-pin del warning vigente con
    `pytest.warns(DeprecationWarning, match="reset_on_release='commit'")`), que reemplaza la
    cobertura perdida al retirar `tests/test_deprecations_0_2_7.py`.

    Eliminar `tests/test_deprecations_0_2_7.py` (git rm): pertenecía a la línea 0.2.7 ya publicada y
    sus warnings dejan de existir con las retiradas. NO debilitar `filterwarnings`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_removals_0_3_0.py tests/test_registry.py tests/test_singleton.py -q && uv run python -c "import encino_orm, encino_orm.context as c; assert not hasattr(c, 'set_default_db') and not hasattr(c, 'get_default_db'); assert not hasattr(encino_orm, 'set_default_db') and not hasattr(encino_orm, 'get_default_db'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_removals_0_3_0.py tests/test_registry.py tests/test_singleton.py -q` pasa con al menos 6 tests verdes.
    - `grep -rn --exclude-dir=__pycache__ "set_default_db\|get_default_db" encino_orm tests` sin coincidencias (barrido de repo, no solo `encino_orm/`; `--exclude-dir=__pycache__` evita falsos positivos de bytecode).
    - `tests/test_deprecations_0_2_7.py` no existe.
    - `uv run pytest -q -m "not optional_engine and not benchmark"` pasa sin warnings no capturados.
    - `uv run ruff check encino_orm tests` y `uv run ruff format --check encino_orm tests` salen 0.
  </acceptance_criteria>
  <done>`set_default_db`/`get_default_db` están retirados; `ConnectionRegistry`/`resolve_db` siguen verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retirar los globales mutables SECRET/GET_DB y el fallback legacy</name>
  <files>encino_orm/security/guard.py, tests/test_security.py, tests/test_removals_0_3_0.py</files>
  <read_first>
    - encino_orm/security/guard.py (completo: globales, `_resolve`, `_explicit_config`, `_legacy_config`, `security_dependencies`, firmas legacy)
    - tests/test_security.py (líneas 340-400: tests del fallback legacy a retirar/ajustar)
    - encino_orm/security/__init__.py (exports: confirmar si re-exporta SECRET/GET_DB)
    - .planning/phases/08-release-0-3-0/08-PATTERNS.md (sección security-global fallback: el mensaje nombra NOMBRES, nunca valores)
  </read_first>
  <behavior>
    - `encino_orm.security.guard.SECRET` y `encino_orm.security.guard.GET_DB` lanzan `AttributeError`.
    - `_legacy_config` ya no existe.
    - `security_dependencies(SecurityConfig(secret, get_db))` devuelve factorías funcionales (happy path).
    - `get_current_user()`/`require("m", "op")` sin argumentos lanzan `AuthenticationError` con un mensaje que dirige a `security_dependencies(SecurityConfig(...))` (ya no hay fallback a globales).
    - El camino explícito `get_current_user(secret=..., get_db=...)` sigue funcionando sin warning.
  </behavior>
  <action>
    En `encino_orm/security/guard.py`: eliminar los globales `SECRET`/`GET_DB` (líneas 22-24) y
    `_legacy_config` (líneas 50-64). Hacer que `_resolve(secret, get_db)` exija AMBOS valores y lance
    `AuthenticationError` con un mensaje que nombre `security_dependencies(SecurityConfig(...))` si
    falta alguno (sin leer globales). En `get_current_user` y `require`, eliminar la rama de fallback
    cuando no se pasan argumentos: sin config explícita, lanzar `AuthenticationError` accionable (el
    usuario debe construir `SecurityConfig`). Conservar el camino explícito
    `get_current_user(secret=..., get_db=...)` / `require(modelo, op, secret=..., get_db=...)` y
    `security_dependencies(config)`. Quitar `import warnings` si queda sin uso. Actualizar el
    docstring del módulo (ya no hay globales que "sobreviven por compatibilidad").

    En `encino_orm/security/__init__.py`: si re-exporta `SECRET`/`GET_DB`, quitarlos (verificado: no
    aparecen; confirmar con grep antes de editar).

    En `tests/test_security.py`: retirar/reescribir los tests del fallback legacy de globales para
    afirmar la AUSENCIA (`AttributeError` sobre `guard.SECRET`/`guard.GET_DB`) y el happy path con
    `SecurityConfig`; conservar el resto.

    En `tests/test_removals_0_3_0.py`: añadir `test_secret_get_db_retirados` (AttributeError),
    `test_legacy_config_retirado` y `test_security_config_happy_path` (construir
    `security_dependencies(SecurityConfig(...))` y verificar que las factorías se crean).
  </action>
  <verify>
    <automated>uv run pytest tests/test_removals_0_3_0.py tests/test_security.py -q && uv run python -c "import encino_orm.security.guard as g; assert not hasattr(g, 'SECRET') and not hasattr(g, 'GET_DB') and not hasattr(g, '_legacy_config'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_removals_0_3_0.py tests/test_security.py -q` pasa con al menos 8 tests verdes.
    - `uv run python -c "import encino_orm.security.guard as g; assert not hasattr(g, 'SECRET') and not hasattr(g, 'GET_DB') and not hasattr(g, '_legacy_config'); print('OK')"` sale 0.
    - `grep -n "^SECRET\|^GET_DB" encino_orm/security/guard.py` sin coincidencias (no quedan asignaciones de módulo).
    - `uv run pytest -q -m "not optional_engine and not benchmark"` pasa sin warnings no capturados.
    - `uv run ruff check encino_orm tests` y `uv run ruff format --check encino_orm tests` salen 0.
  </acceptance_criteria>
  <done>Los globales mutables de seguridad están retirados; la seguridad solo se configura con `SecurityConfig`.</done>
</task>

<task type="auto">
  <name>Task 3: Documentar las retiradas reales en CHANGELOG y MIGRATION-0.3</name>
  <files>CHANGELOG.md, docs/MIGRATION-0.3.md, tests/test_release_docs.py</files>
  <read_first>
    - CHANGELOG.md (líneas 40-70 y 116-339: entradas `CAMBIO DE COMPORTAMIENTO` y notas de ownership)
    - docs/MIGRATION-0.3.md (creada por 08-04: estructura Antes/Después)
    - tests/test_release_docs.py (creado por 08-04: añadir aquí el guard de unicidad de `### Eliminado`)
    - .planning/REQUIREMENTS.md (líneas 79-83: REL-01, REL-04)
  </read_first>
  <action>
    En `CHANGELOG.md`, dentro del bloque `## [Unreleased]` (que 08-03 promueve a `[0.3.0]`), 08-06 es
    el ÚNICO owner del heading `### Eliminado` (08-04 tiene prohibido crearlo). Si el bloque ya
    contiene un `### Eliminado`, AÑADIR las entradas a esa sección existente; si no existe, crear
    EXACTAMENTE UNA. Nunca crear un segundo heading. La sección enumera las retiradas REALES de
    0.3.0 con comportamiento viejo/nuevo:
    - `set_default_db()`/`get_default_db()` → `ConnectionRegistry.set_default()/get_default()`.
    - Globales mutables `SECRET`/`GET_DB` y el fallback legacy → `SecurityConfig` +
      `security_dependencies(config)`.
    - `Db.last_id()`/`PoolDb.last_id()` post-hoc → `execute_insert(qry)` / retorno de
      `Model.insert()` (lo implementa 08-08; documentarlo como retirada de la línea 0.3.0).
    Reescribir las "Nota de ownership: ... la Fase 8 (08-04)" ya cumplidas por su redacción final;
    NO dejar punteros pendientes. Documentar una vez la elección de la palabra (`### Eliminado`).
    Mantener el estilo de prosa español, sin `python-semantic-release`.

    En `docs/MIGRATION-0.3.md`, añadir/ajustar la sección de retiradas con pares "Antes (0.2.6)" /
    "Después (0.3.0)" y la acción de migración para cada símbolo retirado, coherente con el CHANGELOG.

    En `tests/test_release_docs.py` (creado por 08-04), añadir `test_eliminado_heading_unico`: extraer
    el bloque `[Unreleased]` con la regex `^## \[Unreleased\].*?(?=^## \[|\Z)` y afirmar que el número
    de headings `### Eliminado` es EXACTAMENTE 1 (`len(re.findall(r'^### Eliminado\\s*$', b, re.M)) == 1`).
    Este guard es el que impide que 08-04 y 08-06 dupliquen el heading. No debilitar los guards de
    08-04 ni añadir dependencias.
  </action>
  <verify>
    <automated>uv run pytest tests/test_release_docs.py -q && uv run python -c "import re,pathlib; t=pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8'); m=re.search(r'^## \[Unreleased\].*?(?=^## \[|\Z)', t, re.M|re.S); assert m; b=m.group(0); assert len(re.findall(r'^### Eliminado\s*$', b, re.M)) == 1, 'debe haber EXACTAMENTE un heading ### Eliminado'; assert 'set_default_db' in b and 'SECRET' in b and 'last_id' in b; g=pathlib.Path('docs/MIGRATION-0.3.md').read_text(encoding='utf-8'); assert 'set_default_db' in g and 'last_id' in g and 'SECRET' in g; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - El bloque `[Unreleased]` de `CHANGELOG.md` contiene EXACTAMENTE un heading `### Eliminado` y nombra `set_default_db`/`get_default_db`, `SECRET`/`GET_DB` y `last_id`.
    - `tests/test_release_docs.py` incluye `test_eliminado_heading_unico` (assert de exactamente 1 heading en `[Unreleased]`) y pasa.
    - No quedan notas "Nota de ownership: ... Fase 8" pendientes en las entradas enumeradas.
    - `docs/MIGRATION-0.3.md` documenta las mismas retiradas con pares Antes/Después.
    - `uv run mkdocs build --strict` sale 0.
  </acceptance_criteria>
  <done>CHANGELOG y MIGRATION-0.3 reflejan las retiradas reales de 0.3.0 con viejo/nuevo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API pública → usuario que migra | Retirar símbolos rompe imports; la documentación debe anticiparlo |
| globales de seguridad → guard | Un global mutable reasignable es un vector de elevación de privilegio |
| CHANGELOG/MIGRATION → usuario | Una retirada no documentada es una migración rota silenciosa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Elevation of privilege | `SECRET`/`GET_DB` globales mutables | mitigate | Se eliminan por completo; la config es `SecurityConfig` inmutable inyectada |
| T-08-06-02 | Spoofing | Default de conexión mutable | mitigate | Se retira `set_default_db`; el default vive en un `ConnectionRegistry` de instancia o de módulo |
| T-08-06-03 | Denial of service | Retirar `_registry` por error rompe `Model` | mitigate | El plan prohíbe tocar `_registry`/`resolve_db` sin argumentos; verificado por tests de `test_registry.py` |
| T-08-06-04 | Repudiation | Retirada no documentada | mitigate | `### Eliminado` en CHANGELOG + pares Antes/Después en MIGRATION-0.3, con verify de fuente |
| T-08-06-05 | Tampering | Migración silenciosa a 0.3.0 | mitigate | La retirada aterriza DESPUÉS del tag `v0.2.7` (08-01), que conserva los shims con warning |
| T-08-06-SC | Tampering | Instalaciones de paquetes | mitigate | No se añaden dependencias; sin subprocess ni red en los tests |
</threat_model>

<verification>
- `uv run pytest -q -m "not optional_engine and not benchmark"` verde.
- `grep -rn --exclude-dir=__pycache__ "set_default_db\|get_default_db\|_legacy_config" encino_orm tests` sin coincidencias (repo-wide; excluye bytecode).
- `uv run python -c "import encino_orm.security.guard as g; assert not hasattr(g,'SECRET') and not hasattr(g,'GET_DB')"` sale 0.
- CHANGELOG `[Unreleased]` con EXACTAMENTE un `### Eliminado`; `docs/MIGRATION-0.3.md` coherente; `mkdocs build --strict` verde.
</verification>

<success_criteria>
- REL-01 (retirada): los shims/globales deprecados dejan de existir en la línea 0.3.0.
- REL-04: la enumeración documenta las retiradas con viejo/nuevo, sin notas de ownership pendientes.
- El camino feliz de conexión y seguridad sigue verde.
</success_criteria>

<output>
Create `.planning/phases/08-release-0-3-0/08-06-SUMMARY.md` when done
</output>
