---
phase: 08-release-0-3-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - encino_orm/model/query_builder.py
  - encino_orm/sql.py
  - tests/test_deprecations_0_2_7.py
  - tests/test_query_builder.py
  - tests/test_benchmarks.py
  - pyproject.toml
  - uv.lock
  - CHANGELOG.md
autonomous: false
requirements: [REL-01]
user_setup:
  - service: pypi
    why: "Publicación real de 0.2.7 (D-04: la sesión no maneja credenciales de publicación)"
    env_vars:
      - name: PYPI_API_TOKEN
        source: "PyPI > Account Settings > API tokens (mecanismo vigente; se elimina en 08-02)"
    dashboard_config:
      - task: "Aprobar el workflow Publish to PyPI disparado por el tag v0.2.7"
        location: "GitHub > repo > Actions > Publish to PyPI > Review deployments"
must_haves:
  truths:
    - "Cada ruptura 0.3.0 nombrada por el ROADMAP (implicit commit on release, unvalidated identifiers, mutable SECRET/GET_DB, post-hoc last_id()) queda cubierta: emite un DeprecationWarning en runtime o está documentada como fallo cerrado sin warning posible"
    - "La suite completa pasa con filterwarnings=['error']: todo warning nuevo está pinado con pytest.warns"
    - "pyproject.toml y uv.lock declaran 0.2.7 de forma consistente (uv lock --check verde)"
    - "CHANGELOG.md tiene una sección [0.2.7] que documenta las deprecaciones de esta línea"
  artifacts:
    - path: "tests/test_deprecations_0_2_7.py"
      provides: "Pines de los cuatro DeprecationWarning de las rupturas 0.3.0"
      min_lines: 40
    - path: "encino_orm/model/query_builder.py"
      provides: "DeprecationWarning para identificadores cualificados en posiciones de expresión"
      contains: "DeprecationWarning"
    - path: "CHANGELOG.md"
      contains: "## [0.2.7]"
    - path: "pyproject.toml"
      contains: 'version = "0.2.7"'
  key_links:
    - from: "tests/test_deprecations_0_2_7.py"
      to: "encino_orm/model/query_builder.py"
      via: "pytest.warns sobre _safe_column cualificado"
      pattern: "pytest\\.warns\\(DeprecationWarning"
    - from: "encino_orm/sql.py"
      to: "warnings"
      via: "SqlFunctions._col tolerante a puntos"
      pattern: "warnings\\.warn"
---

<objective>
Cortar 0.2.7 como tag sobre `main` en el commit de congelación posterior a este plan, con
`DeprecationWarning` en runtime por cada ruptura de 0.3.0, y publicarla primero (D-01, D-03).

Purpose: SemVer exige deprecar en un *minor* antes de romper. 0.2.7 es el aviso: describe el
comportamiento viejo mientras todavía existe y nombra el reemplazo. `PYPI_API_TOKEN` sigue vigente
para esta publicación (la migración a OIDC es 08-02). Las remociones NO ocurren en este plan.

Output: warnings en runtime (incluido el que falta para "unvalidated identifiers"), tests que los
pinean bajo `filterwarnings=['error']`, versión 0.2.7 en `pyproject.toml` + `uv.lock`, sección
`[0.2.7]` en `CHANGELOG.md`, y un checkpoint de publicación.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-0-3-0/08-CONTEXT.md
@.planning/phases/08-release-0-3-0/08-PATTERNS.md
@.planning/phases/08-release-0-3-0/08-DISCUSSION-LOG.md
@CHANGELOG.md

Hallazgo crítico del pattern map: de las cuatro rupturas nombradas en `ROADMAP.md:392`,
tres ya tienen warning de runtime (`pool.py:118-127` implicit commit, `security/guard.py:50-64`
SECRET/GET_DB, `base.py:22-33` last_id) y la cuarta — "unvalidated identifiers" — NO lo tiene.
Este plan añade la que falta.
</context>

<interfaces>
Sitios de warning EXISTENTES (verificar presencia y pinear, no duplicar):

- `encino_orm/base.py::_warn_last_id_deprecated` → `warnings.warn("last_id() está deprecado; usa execute_insert(qry) o el retorno de Model.insert()", DeprecationWarning, stacklevel=2)`.
- `encino_orm/security/guard.py::_legacy_config` → `warnings.warn("los globales SECRET/GET_DB están deprecados; usa SecurityConfig y security_dependencies(config)", DeprecationWarning, stacklevel=2)`. El mensaje nombra NOMBRES de globales, nunca sus valores (Pitfall 12).
- `encino_orm/pool.py` (`PoolDb.__init__`, dentro de `if reset_on_release == "commit":`) → warning enganchado a la POLÍTICA, no a `in_transaction()`.

Sitios SIN warning (allowlist tolerante a puntos, `_COLUMN_RE = r"^[A-Za-z_][A-Za-z0-9_.]*$"`):

- `encino_orm/model/query_builder.py::_safe_column(expr)` — acepta `mm.agente` (y `mm.*`).
- `encino_orm/sql.py::SqlFunctions._col(name)` — acepta `tabla.columna`.

Otros identificadores de esta plan:
- `filterwarnings = ["error"]` en `pyproject.toml:62-72`: cualquier warning NO pinado falla la suite.
- `uv.lock:793` replica la versión del proyecto (`version = "0.2.6"`): un bump exige `uv lock`.
- `.gitignore:38` ignora `prompts/`; `prompts/` no participa en este plan.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir el warning que falta (identificadores cualificados) y pinear las cuatro rupturas</name>
  <files>encino_orm/model/query_builder.py, encino_orm/sql.py, tests/test_deprecations_0_2_7.py, tests/test_query_builder.py, tests/test_benchmarks.py</files>
  <read_first>
    - encino_orm/model/query_builder.py (líneas 1-45: `_COLUMN_RE`, `_safe_column`, docstring de la allowlist distinta)
    - encino_orm/sql.py (líneas 1-55: `_COLUMN_RE` y `SqlFunctions._col`)
    - encino_orm/base.py (líneas 20-35: `_warn_last_id_deprecated`)
    - encino_orm/security/guard.py (líneas 44-64: `_legacy_config`)
    - encino_orm/pool.py (líneas 110-130: warning de `reset_on_release='commit'`)
    - encino_orm/dialects/identifiers.py (allowlist estricta: base del texto del warning)
    - CHANGELOG.md (líneas 116-339: entradas `CAMBIO DE COMPORTAMIENTO` del milestone)
    - tests/test_security.py (líneas 359-376: patrón `pytest.warns` del fallback legacy)
    - tests/test_pool.py (líneas 394-406: patrón `pytest.warns` de `last_id()` y de la política)
    - tests/test_query_builder.py (líneas 370-430: usos de columnas cualificadas)
    - tests/test_benchmarks.py (líneas 120-135: `select/group_by/order_by` cualificados)
    - pyproject.toml (líneas 62-72: `filterwarnings = ["error"]`)
  </read_first>
  <behavior>
    - `_safe_column("mm.agente")` emite EXACTAMENTE un `DeprecationWarning` y devuelve `"mm.agente"`.
    - `_safe_column("agente")` no emite warning; `_safe_column("*")` y `_safe_column("mm.*")` tampoco.
    - `_safe_column("x; DROP TABLE t --")` sigue lanzando `ValueError` y NO emite warning.
    - `SqlFunctions("sqlite")._col("tabla.col")` emite un `DeprecationWarning` y devuelve `"tabla.col"`.
    - `SqlFunctions("sqlite")._col("col")` no emite warning; `_col("d; DROP")` lanza `ValueError`.
    - Los tres warnings existentes siguen emitiéndose exactamente una vez cada uno.
  </behavior>
  <action>
    Cerrar el hueco del pattern map. La ruptura "unvalidated identifiers" (la validación estricta
    rechaza nombres que 0.2.6 aceptaba) no tiene warning de runtime. El único punto donde todavía se
    ACEPTA una forma legacy de identificador es la allowlist tolerante a puntos en
    `encino_orm/model/query_builder.py::_safe_column` y `encino_orm/sql.py::SqlFunctions._col`.

    En AMBOS sitios: cuando el nombre aceptado contiene un punto y NO es `*` ni termina en `.*`,
    emitir `warnings.warn(<mensaje>, DeprecationWarning, stacklevel=2)` DESPUÉS del match exitoso y
    ANTES del return. Mensaje exacto, idéntico en los dos sitios:
    "los identificadores cualificados (con punto) están deprecados; 0.3.0 valida todo identificador
    con la allowlist estricta y los rechazará — usa la columna desnuda o el parámetro schema= explícito".
    No tocar la allowlist ni el rechazo: `x; DROP TABLE t --` sigue lanzando `ValueError` SIN warning.
    Añadir `import warnings` a `encino_orm/sql.py` si falta. Respetar `line-length = 100`.

    Crear `tests/test_deprecations_0_2_7.py` con un test por ruptura usando `pytest.warns`
    (patrón de `tests/test_security.py:362` y `tests/test_pool.py:398`):
    - `test_implicit_commit_on_release_avisa`: `PoolDb("sqlite", reset_on_release="commit")` bajo
      `pytest.warns(DeprecationWarning, match="reset_on_release='commit'")`.
    - `test_secret_get_db_globales_avisan`: fijar `guard.SECRET`/`guard.GET_DB` y llamar al camino
      legacy bajo `pytest.warns(DeprecationWarning, match="deprecad")`; restaurar globales en `finally`.
    - `test_last_id_post_hoc_avisa`: `await db.last_id()` sobre `SqliteDb` conectada a `:memory:`
      bajo `pytest.warns(DeprecationWarning, match="deprecado")`; cerrar.
    - `test_identificador_cualificado_avisa`: `QueryBuilder(Model, None).select("mm.agente")` y
      `SqlFunctions("sqlite")._col("t.c")` bajo `pytest.warns(DeprecationWarning, match="cualificados")`.
    - `test_identificador_simple_no_avisa` y `test_hostil_sigue_fallando_cerrado` con
      `warnings.catch_warnings()` + `simplefilter("error")` y `pytest.raises(ValueError)`.

    Después, correr `uv run pytest -q` para descubrir TODO sitio existente que pase por
    `_safe_column`/`_col` con punto y pinear cada uno con `pytest.warns(DeprecationWarning,
    match="cualificados")`. Candidatos conocidos: `tests/test_query_builder.py:106,162,389,399` y
    `tests/test_benchmarks.py:126-130`. NO debilitar `filterwarnings`.

    Finalmente, auditar `CHANGELOG.md` (`[Unreleased]`, líneas 116-339): enumerar las entradas
    `CAMBIO DE COMPORTAMIENTO` y dejar constancia en el CHANGELOG de `[0.2.7]` (Task 2) de cuáles
    son fallo cerrado sin warning de runtime posible (p. ej. `indexes_ddl` hostil, `QueryBuilder._table`
    no identificador: validan y lanzan antes de aceptar, así que no pueden avisar sin degradar la
    seguridad). Esa nota cierra el requisito "warning para cada ruptura o justificación escrita".
  </action>
  <verify>
    <automated>uv run pytest tests/test_deprecations_0_2_7.py -q && uv run pytest -q -x -m "not optional_engine and not benchmark"</automated>
  </verify>
  <acceptance_criteria>
    - `encino_orm/model/query_builder.py` y `encino_orm/sql.py` contienen la cadena `DeprecationWarning` y la cadena `cualificados` en el mensaje.
    - `uv run pytest tests/test_deprecations_0_2_7.py -q` pasa con al menos 6 tests verdes.
    - `uv run pytest -q -m "not optional_engine and not benchmark"` pasa sin warnings no capturados.
    - `uv run ruff check encino_orm tests` y `uv run ruff format --check encino_orm tests` salen 0.
    - `grep -c "CAMBIO DE COMPORTAMIENTO" CHANGELOG.md` es mayor o igual a 10 (auditoría base).
  </acceptance_criteria>
  <done>Las cuatro rupturas 0.3.0 tienen warning de runtime pineado bajo `filterwarnings=["error"]`, o constan como fallo cerrado justificado; la suite está verde.</done>
</task>

<task type="auto">
  <name>Task 2: Bump a 0.2.7, sincronizar el lock, documentar [0.2.7] y dry-run de build</name>
  <files>pyproject.toml, uv.lock, CHANGELOG.md</files>
  <read_first>
    - pyproject.toml (líneas 8-14: `[project]` version)
    - uv.lock (líneas 788-796: entrada del proyecto `encino-orm`)
    - CHANGELOG.md (líneas 1-11 y 341-345: formato y patrón de heading `## [0.2.6] - 2026-09-11`)
    - CHANGELOG.md (líneas 40-115: entradas `CAMBIO DE COMPORTAMIENTO` ya redactadas)
  </read_first>
  <action>
    Bump de versión en el ÚNICO literal: `pyproject.toml:10` de `version = "0.2.6"` a
    `version = "0.2.7"`. No tocar backend, extras, `[project.scripts]` ni secciones `[tool.*]`.

    Sincronizar `uv.lock`: ejecutar `uv lock` para reescribir la entrada del proyecto
    (`uv.lock:792-793`) a `version = "0.2.7"`. Verificar con `uv lock --check` (es el gate del job
    `deps` de CI; sin esto CI quedaría rojo).

    En `CHANGELOG.md`, insertar ARRIBA de `## [Unreleased]` una sección nueva
    `## [0.2.7] - <YYYY-MM-DD>` (fecha real de corte) con:
    - `### Deprecaciones` (o `### Cambiado`, siguiendo Keep a Changelog en español ya usado):
      una entrada por cada ruptura 0.3.0 con el comportamiento viejo, el reemplazo y el warning
      emitido: `reset_on_release='commit'`, globales `SECRET`/`GET_DB`, `last_id()` post-hoc e
      identificadores cualificados en posiciones de expresión / `db.fn.*`.
    - Nota explícita de que las REMOCIONES no ocurren en 0.2.7 (siguen funcionando con warning) y de
      qué rupturas son fallo cerrado sin warning de runtime posible (auditoría de Task 1).
    NO renombrar ni vaciar `## [Unreleased]`: `main` sigue hacia 0.3.0 y esa sección pertenece al
    milestone (la promueve 08-03).

    Dry-run de build y verificación del artefacto: `uv build`, comprobar que
    `dist/encino_orm-0.2.7-py3-none-any.whl` y el sdist existen, e inspeccionar los metadatos del
    wheel (`Version: 0.2.7`, `Name: encino-orm`) sin publicar. `dist/` está gitignored
    (`.gitignore:12`): no commitear artefactos.
  </action>
  <verify>
    <automated>uv lock --check && uv build && uv run python -c "import zipfile,glob; w=glob.glob('dist/encino_orm-0.2.7-*.whl')[0]; z=zipfile.ZipFile(w); m=[n for n in z.namelist() if n.endswith('METADATA')][0]; print(z.read(m).decode().splitlines()[1])"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contiene exactamente `version = "0.2.7"` una vez.
    - `uv.lock` contiene `version = "0.2.7"` para la entrada `name = "encino-orm"`; `uv lock --check` sale 0.
    - `CHANGELOG.md` contiene `## [0.2.7]` y conserva `## [Unreleased]` no vacío.
    - `dist/encino_orm-0.2.7-py3-none-any.whl` existe y su METADATA declara `Version: 0.2.7`.
    - La suite sigue verde (`uv run pytest -q -m "not optional_engine and not benchmark"`).
  </acceptance_criteria>
  <done>0.2.7 está cortada en fuente: versión sincronizada en pyproject+lock, CHANGELOG documentado y wheel construible sin publicar.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Congelar y publicar 0.2.7 (tag v0.2.7 → PyPI)</name>
  <files>CHANGELOG.md (solo lectura)</files>
  <read_first>
    - .github/workflows/release.yml (líneas 1-49: trigger por tag `v*`, job `ci` reutilizable, publish con `PYPI_API_TOKEN`)
    - .planning/phases/08-release-0-3-0/08-CONTEXT.md (D-01 y D-04)
  </read_first>
  <action>
    El ejecutor NO publica ni maneja credenciales (D-04). Antes de ceder el control debe dejar
    evidencia de que TODO lo automatizable está hecho: `uv lock --check` verde, `uv build` con
    `Version: 0.2.7`, suite verde y CHANGELOG `[0.2.7]` presente. Luego, la persona:
    1. Commitea el punto de congelación en `main` (warnings + 0.2.7 + CHANGELOG) — commit anterior a
       cualquier remoción.
    2. Crea y empuja el tag anotado `v0.2.7` apuntando a ESE commit (`git tag -a v0.2.7 -m "0.2.7"`,
       `git push origin v0.2.7`). No crear ni renombrar ramas (`branching_strategy: none`).
    3. El tag dispara `Publish to PyPI`; tras el job `CI (reusable)` verde, aprueba la publicación.
    4. Verifica en PyPI que `0.2.7` aparece ANTES de que exista cualquier artefacto 0.3.0.
    Si el token hubiera caducado, regenerar `PYPI_API_TOKEN` NO es parte de este plan: detenta el
    checkpoint y reporta (la migración a OIDC es 08-02).
  </action>
  <verify>
    <automated>echo "MISSING — verificación humana: PyPI muestra encino-orm 0.2.7 publicado tras CI verde; tag v0.2.7 existe en el commit de congelación"</automated>
  </verify>
  <acceptance_criteria>
    - El tag `v0.2.7` existe en `main` y apunta a un commit cuyo `pyproject.toml` declara `0.2.7` y cuyo `CHANGELOG.md` contiene `## [0.2.7]`.
    - El workflow `Publish to PyPI` corrió con el job `CI (reusable)` verde antes del publish.
    - `https://pypi.org/project/encino-orm/0.2.7/` existe y es la versión más reciente antes de cualquier `0.3.0*`.
  </acceptance_criteria>
  <done>0.2.7 está publicada en PyPI, las remociones siguen ausentes y `main` puede continuar hacia 0.3.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (tag commit) → PyPI | El artefacto publicado debe provenir del commit tagueado; el contenido cruza a usuarios finales |
| fuente → wheel/sdist | El build de hatchling empaqueta `encino_orm/` y metadatos |
| warning runtime → usuario | El mensaje de deprecación llega a logs de producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | `pyproject.toml` vs `uv.lock` | mitigate | Bump en el único literal + `uv lock` en el mismo commit; `uv lock --check` es gate del job `deps` |
| T-08-01-02 | Information disclosure | `security/guard.py` mensaje de warning | mitigate | El mensaje nombra los NOMBRES de los globales, nunca sus valores; el test afirma `material not in message` |
| T-08-01-03 | Spoofing | Publicación de 0.2.7 | accept | 0.2.7 usa el mecanismo vigente por token (D-03); el riesgo se cierra en 08-02 con OIDC + entorno protegido. Publicar solo tras CI verde |
| T-08-01-04 | Denial of service | `filterwarnings=["error"]` con warning nuevo | mitigate | Warning solo en formas deprecadas (punto), nunca en el camino feliz; cada sitio pinado con `pytest.warns` |
| T-08-01-05 | Tampering | Artefactos `dist/` no deben commitearse | mitigate | `dist/` en `.gitignore:12`; el build es efímero y verificable |
| T-08-01-SC | Tampering | Instalaciones de paquetes | mitigate | No se añaden dependencias; `uv` permanece pinado a `0.12.15` |
</threat_model>

<verification>
- `uv run pytest -q -m "not optional_engine and not benchmark"` verde.
- `uv lock --check` verde; `uv build` produce `encino_orm-0.2.7`.
- `grep -c "## \[0.2.7\]" CHANGELOG.md` = 1.
- Checkpoint: tag `v0.2.7` publicado y visible en PyPI antes de cualquier 0.3.0.
</verification>

<success_criteria>
- REL-01: 0.2.7 publicada con `DeprecationWarning` en runtime por cada ruptura 0.3.0 (o justificación de fallo cerrado escrita).
- Versión única y consistente en `pyproject.toml` y `uv.lock`.
- CHANGELOG documenta la línea 0.2.7 sin tocar la enumeración del milestone.
</success_criteria>

<output>
Create `.planning/phases/08-release-0-3-0/08-01-SUMMARY.md` when done
</output>
