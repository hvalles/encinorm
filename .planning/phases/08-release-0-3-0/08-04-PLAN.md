---
phase: 08-release-0-3-0
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - CHANGELOG.md
  - docs/MIGRATION-0.3.md
  - mkdocs.yml
  - tests/test_release_docs.py
autonomous: true
requirements: [REL-04]
must_haves:
  truths:
    - "Cada entrada rompiente del milestone (`CAMBIO DE COMPORTAMIENTO` en `[Unreleased]`) declara comportamiento viejo y nuevo"
    - "`docs/MIGRATION-0.3.md` existe con pares Antes/Después por cada ruptura acumulada desde 0.2.6 (Fases 2-7), no solo los shims deprecados"
    - "La guía de migración está registrada en la nav de MkDocs y `mkdocs build --strict` pasa"
    - "Un test de fuente impide que la enumeración o la guía desaparezcan o se vacíen"
  artifacts:
    - path: "docs/MIGRATION-0.3.md"
      provides: "Guía de migración con ejemplos Antes/Después"
      min_lines: 120
    - path: "CHANGELOG.md"
      provides: "Enumeración completa de rupturas con viejo/nuevo"
      contains: "CAMBIO DE COMPORTAMIENTO"
    - path: "mkdocs.yml"
      provides: "Entrada de nav para la guía de migración"
      contains: "MIGRATION-0.3.md"
    - path: "tests/test_release_docs.py"
      provides: "Guard de la enumeración y de la guía"
      min_lines: 40
  key_links:
    - from: "mkdocs.yml"
      to: "docs/MIGRATION-0.3.md"
      via: "nav Desarrolladores"
      pattern: "MIGRATION-0\\.3\\.md"
    - from: "tests/test_release_docs.py"
      to: "CHANGELOG.md"
      via: "assertions de viejo/nuevo por ruptura"
      pattern: "CAMBIO DE COMPORTAMIENTO"
---

<objective>
Completar la documentación de rupturas: `CHANGELOG.md` enumera cada cambio incompatible con
comportamiento viejo/nuevo, y `docs/MIGRATION-0.3.md` muestra ejemplos antes/después (REL-04).

Purpose: quien migra de 0.2.6 a 0.3.0 necesita saber qué cambió y cómo adaptar su código. Las
entradas `[Unreleased]` ya existen pero dicen explícitamente que la enumeración completa la posee
esta fase. `python-semantic-release` está fuera de alcance: el texto es manual.

Output: `[Unreleased]` con la enumeración completa, la guía de migración registrada en MkDocs y un
guard de fuente que la mantiene viva.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-release-0-3-0/08-CONTEXT.md
@.planning/phases/08-release-0-3-0/08-PATTERNS.md
@CHANGELOG.md
@mkdocs.yml
@docs/trust-boundaries.md

Material de origen de las rupturas: entradas `CAMBIO DE COMPORTAMIENTO` de `[Unreleased]`
(`CHANGELOG.md:40-339`), que cubren Fases 2-7 (Query, pool release/last_id, migraciones, caché,
taxonomía de errores, reciclado de conexiones, codegen, config). El alcance es TODAS las rupturas
acumuladas desde 0.2.6, no solo los shims deprecados (discreción del agente en CONTEXT).
</context>

<interfaces>
- Formato CHANGELOG: Keep a Changelog 1.1.0 en español, headings `### Añadido` / `### Cambiado` /
  `### Corregido` / `### Seguridad`; marca manual de ruptura `CAMBIO DE COMPORTAMIENTO (...)`.
- Convención de redacción existente: "Viejo: ... nuevo: ..." o "Antes, ... ahora ...", cada entrada
  nombra requisito/plan.
- Analog de la guía nueva: `docs/trust-boundaries.md` (intro en español → reglas → secciones por
  tema con bloques de código → tabla resumen). Aquí los pares son Antes/Después.
- Nav MkDocs: `mkdocs.yml:67-71` sección `Desarrolladores` (junto a `trust-boundaries.md`).
  `docs.yml` corre `mkdocs build --strict`: una entrada de nav rota falla el build, así que el
  fichero y su entrada deben aterrizar juntos.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Completar la enumeración de rupturas en CHANGELOG</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (líneas 1-11: formato y convención; líneas 40-339: `[Unreleased]` completo)
    - .planning/phases/08-release-0-3-0/08-PATTERNS.md (sección CHANGELOG.md: patrón de heading y de entrada)
    - .planning/REQUIREMENTS.md (líneas 79-83: REL-04)
  </read_first>
  <action>
    Revisar TODAS las entradas de `[Unreleased]` en `CHANGELOG.md` (líneas 40-339) y asegurar que
    cada una marcada `CAMBIO DE COMPORTAMIENTO` (o que describe un cambio incompatible aunque no
    lleve la marca) declara de forma explícita el comportamiento VIEJO y el NUEVO, en el estilo
    existente ("Viejo: ... nuevo: ..." / "Antes, ... ahora ..."). Cubrir el inventario del
    milestone: contrato de cardinalidad de `Query`, validación estricta de identificadores
    (`_table`, `join`, `indexes_ddl`), `PoolDb.acquire()` devolviendo handle, semántica de
    liberación del pool, `last_id()` post-hoc deprecado, formato de clave de caché, dominio de caché
    por PK en `CachedModel`, ledger/`migrate()` de dos fases, taxonomía de errores, reciclado
    opt-in, CFG-01…CFG-05 y el seam de dialectos PERF.

    Añadir una sección `### Eliminado` (o `### Removido`) SOLO si alguna ruptura es una retirada;
    documentar la elección de la palabra en una línea. Mantener el estilo de prosa español, sin
    `python-semantic-release` (Out of Scope, `REQUIREMENTS.md:129`). Reemplazar las notas
    "Nota de ownership: ... la Fase 8 (08-04)" ya cumplidas por la redacción final, sin perder la
    información de comportamiento viejo/nuevo.
  </action>
  <verify>
    <automated>uv run python -c "import re,pathlib; t=pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8'); m=re.search(r'^## \[Unreleased\].*?(?=^## \[|\Z)', t, re.M|re.S); assert m, 'no hay bloque [Unreleased]'; b=m.group(0); marks=re.findall(r'CAMBIO DE COMPORTAMIENTO', b); assert len(marks)>=10, len(marks); print('marcas:', len(marks))"</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` conserva la sección `## [Unreleased]` (la promueven 08-03/08-06) y NO contiene ninguna nota pendiente del tipo "no prejuzga la enumeración ... que posee la Fase 8".
    - Cada ruptura del inventario (>= 10 marcas `CAMBIO DE COMPORTAMIENTO`) incluye una frase viejo/nuevo ("Viejo:"/"nuevo:", "Antes,"/"ahora", "Antes"/"Después").
    - El bloque `[Unreleased]` se extrae desde el heading `## [Unreleased]` hasta el siguiente heading `## [`; el verify NO depende de la posición de `## [0.2.7]` (que 08-01 insertó debajo de `[Unreleased]`).
    - No se elimina ninguna entrada de `### Corregido` que describa comportamiento.
  </acceptance_criteria>
  <done>El CHANGELOG enumera cada cambio incompatible del milestone con comportamiento viejo y nuevo.</done>
</task>

<task type="auto">
  <name>Task 2: Crear docs/MIGRATION-0.3.md y registrarla en la nav de MkDocs</name>
  <files>docs/MIGRATION-0.3.md, mkdocs.yml</files>
  <read_first>
    - docs/trust-boundaries.md (completo: estructura, tono, tabla resumen, bloques de código)
    - mkdocs.yml (líneas 51-84: nav, en especial `Desarrolladores`)
    - CHANGELOG.md (`[Unreleased]`: fuente de los pares Antes/Después, resultado de Task 1)
    - .github/workflows/docs.yml (línea 34: `mkdocs build --strict`)
  </read_first>
  <action>
    Crear `docs/MIGRATION-0.3.md` (español) con la estructura de `docs/trust-boundaries.md`:
    título, intro que fije el alcance ("cambios incompatibles acumulados desde 0.2.6, Fases 2-7"),
    sección por ruptura con un par de bloques de código "Antes (0.2.6)" / "Después (0.3.0)" y una
    frase de acción ("qué hacer"), y una tabla resumen final (Ruptura | Antes | Después | Acción).
    Cubrir al menos: contrato de cardinalidad de `Query` (`with_params()`/`rebind` eliminado),
    validación de identificadores (`ValueError` en tabla/columna no identificador),
    `PoolDb.acquire()` → `PooledConnection`, semántica de liberación (`reset_on_release`),
    `last_id()` → `execute_insert`/retorno de `Model.insert`, invalidación de caché y clave por
    scope, ledger de migraciones/`reconcile_migrations()`/`resolve_migration()`, taxonomía de
    errores (`raise ... from exc`), reciclado `pre_ping`/`max_connection_lifetime`, y los shims
    deprecados de CFG-01/CFG-02. Los ejemplos usan credenciales ficticias, nunca valores reales.

    Registrar la página en `mkdocs.yml`, sección `Desarrolladores`, junto a
    `Fronteras de confianza (SQL): trust-boundaries.md`, como
    `Guía de migración 0.3: MIGRATION-0.3.md`. Añadir el fichero y la entrada de nav en el mismo
    commit y verificar `uv run mkdocs build --strict` (una nav rota falla el build).
  </action>
  <verify>
    <automated>uv run mkdocs build --strict && uv run python -c "import pathlib,re; t=pathlib.Path('docs/MIGRATION-0.3.md').read_text(encoding='utf-8'); secs=len(re.findall(r'^## ', t, re.M)); pairs=len(re.findall(r'Antes|Después', t)); assert secs>=8, secs; assert pairs>=16, pairs; assert 'MIGRATION-0.3.md' in pathlib.Path('mkdocs.yml').read_text(encoding='utf-8'); print('secciones:', secs, 'pares:', pairs)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/MIGRATION-0.3.md` existe, tiene >= 8 secciones `##` y >= 16 apariciones de "Antes"/"Después".
    - `mkdocs.yml` referencia `MIGRATION-0.3.md` en la nav.
    - `uv run mkdocs build --strict` sale 0 (sin warnings de nav).
    - La guía no contiene credenciales reales ni enlaces a `prompts/`.
  </acceptance_criteria>
  <done>La guía de migración existe, está publicada en la nav y su build es estricto.</done>
</task>

<task type="auto">
  <name>Task 3: Guard de fuente para la enumeración y la guía</name>
  <files>tests/test_release_docs.py</files>
  <read_first>
    - CHANGELOG.md (resultado de Task 1)
    - docs/MIGRATION-0.3.md (resultado de Task 2)
    - mkdocs.yml (resultado de Task 2)
    - tests/test_ci_harness.py (estilo de assertions de fuente con `pathlib`)
    - tests/test_trust_boundaries.py (precedente de guard de una página de docs, si existe)
  </read_first>
  <behavior>
    - `docs/MIGRATION-0.3.md` existe y tiene >= 8 secciones `##`.
    - Cada sección de la guía contiene al menos un par Antes/Después.
    - `mkdocs.yml` incluye `MIGRATION-0.3.md` en la nav.
    - El bloque `[Unreleased]` de `CHANGELOG.md` (extraído desde `## [Unreleased]` hasta el siguiente `## [`) tiene >= 10 marcas `CAMBIO DE COMPORTAMIENTO` y cada una aparece en una entrada con marcadores viejo/nuevo.
  </behavior>
  <action>
    Crear `tests/test_release_docs.py` (DB-free, sin red) con assertions de fuente por `pathlib`:
    existencia y tamaño de `docs/MIGRATION-0.3.md`, número de secciones, pares Antes/Después por
    sección, presencia en la nav de `mkdocs.yml`, y el contrato del CHANGELOG (extraer el bloque
    `[Unreleased]` con una regex `^## \[Unreleased\].*?(?=^## \[|\Z)` y exigir >= 10 marcas
    `CAMBIO DE COMPORTAMIENTO`, cada una dentro de una entrada que contiene al menos un marcador
    viejo y uno nuevo). NO incluir el barrido de `prompts/`: esa limpieza y su guard viven en
    08-07 (docs) y 08-05 (README), que corren después de las limpiezas correspondientes. No usar
    subprocess ni red.
  </action>
  <verify>
    <automated>uv run pytest tests/test_release_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_release_docs.py -q` pasa con al menos 5 tests verdes.
    - El test falla si se borra `docs/MIGRATION-0.3.md`, si se vacía su contenido o si se quita la entrada de `mkdocs.yml`.
    - El test NO referencia `prompts/` (esa prohibición pertenece a 08-05/08-07).
    - `uv run ruff check tests/test_release_docs.py` y `uv run ruff format --check tests/test_release_docs.py` salen 0.
  </acceptance_criteria>
  <done>La enumeración del CHANGELOG y la guía de migración quedan congeladas por test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs publicadas → usuario que migra | Una afirmación falsa sobre el comportamiento induce una migración rota |
| repo → PyPI readme | El CHANGELOG/MIGRATION pueden quedar expuestos en el README de PyPI |
| ejemplos de código → entorno del usuario | Los snippets no deben contener secretos reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information disclosure | Ejemplos de `MIGRATION-0.3.md` | mitigate | Solo credenciales ficticias de dev (`admin`, `Admin_123`); nunca secretos reales |
| T-08-04-02 | Tampering | Drift entre CHANGELOG y código | mitigate | Guard de fuente: cada `CAMBIO DE COMPORTAMIENTO` debe declarar viejo y nuevo |
| T-08-04-03 | Denial of service | Nav de MkDocs rota en `--strict` | mitigate | Fichero + entrada de nav en el mismo commit; `uv run mkdocs build --strict` en el verify |
| T-08-04-04 | Tampering | Drift entre `docs/MIGRATION-0.3.md` y el CHANGELOG | mitigate | Guard de fuente: >= 8 secciones con pares Antes/Después; guía y enumeración en el mismo commit |
| T-08-04-SC | Tampering | Instalaciones de paquetes | mitigate | No se añaden dependencias; `mkdocs` ya está en el grupo dev |
</threat_model>

<verification>
- `uv run pytest tests/test_release_docs.py -q` verde.
- `uv run mkdocs build --strict` verde.
- `CHANGELOG.md` mantiene `## [Unreleased]` sin notas de ownership pendientes.
</verification>

<success_criteria>
- REL-04: CHANGELOG enumera cada cambio incompatible con viejo/nuevo; `MIGRATION-0.3.md` muestra ejemplos Antes/Después.
- La guía está registrada en la nav y el build estricto pasa.
</success_criteria>

<output>
Create `.planning/phases/08-release-0-3-0/08-04-SUMMARY.md` when done
</output>
