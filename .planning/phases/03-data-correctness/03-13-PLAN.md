---
phase: 03-data-correctness
plan: 13
type: execute
wave: 2
depends_on: [03-11, 03-12]
gap_closure: true
files_modified:
  - CHANGELOG.md
  - docs/guide.md
  - docs/design/5-security.md
autonomous: true
requirements: [SEC-01, DATA-03, DATA-02]
must_haves:
  truths:
    - "El CHANGELOG deja de afirmar que la escritura cruzada de tenant estaba cerrada por la ronda 3 y registra SEC-01 (update/delete acotados por scope) como el fix real"
    - "Se elimina/supersede el bullet contradictorio de 'esa única entrada' de la ronda 2 en `[Unreleased]`"
    - "La guía y el diseño de seguridad documentan que las ESCRITURAS deben correr bajo el mismo `scope()` que las lecturas (WR-R3-04) y que `upsert` con claves de conflicto globales queda como residual"
    - "La afirmación sobre `last_id()` de Oracle se corrige: el fallback cubre un `last_id()` que falle, no que Oracle devuelva 0 (IN-R3-01)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "entradas veraces de SEC-01/CR-R3-01 y corrección del overclaim"
      contains: "scope"
    - path: "docs/guide.md"
      provides: "contrato de escritura bajo scope y residual de upsert"
      contains: "scope"
    - path: "docs/design/5-security.md"
      provides: "DML acotado por scope y límites"
      contains: "scope"
  key_links:
    - from: "CHANGELOG.md / docs/design/5-security.md"
      to: "encino_orm/model/model.py::_scoped_dml"
      via: "describen el DML acotado entregado por 03-11"
      pattern: "scope"
---

<objective>
Cerrar **WR-R3-03** (overclaim "escritura cruzada cerrada" + bullet contradictorio "única entrada"),
**WR-R3-04** (documentar que las escrituras deben correr bajo el mismo scope que las lecturas),
**IN-R3-01** (corregir la afirmación sobre `last_id()` de Oracle) e **IN-R3-03** (suavizar la promesa de
huella estable). Depende de `03-11` (DML acotado por scope) y `03-12` (fail-open), ambos COMPLETADOS.

Purpose: la documentación debe describir el comportamiento REAL; PROJECT.md exige registrar los cambios
incompatibles en `0.x`, y una afirmación de aislamiento de tenant que el código no cumple induce
configuraciones inseguras.

Output: `CHANGELOG.md`, `docs/guide.md` y `docs/design/5-security.md` alineados con `03-11`/`03-12`.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-data-correctness/03-REVIEW-GAPS-R3.md
@.planning/phases/03-data-correctness/03-11-PLAN.md
@.planning/phases/03-data-correctness/03-12-PLAN.md
@CHANGELOG.md
@docs/guide.md
@docs/design/5-security.md
@encino_orm/model/model.py
@encino_orm/model/cached.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corregir el CHANGELOG (overclaim + bullet contradictorio)</name>
  <read_first>
    - CHANGELOG.md (`[Unreleased]`: `### Cambiado`, `### Corregido`, y el bullet de la ronda 2 sobre "esa única entrada" ~:119)
    - .planning/phases/03-data-correctness/03-REVIEW-GAPS-R3.md (WR-R3-03: líneas exactas)
    - encino_orm/model/model.py (`_scoped_dml` entregado por 03-11)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    En `[Unreleased]` (sin duplicar encabezados):
    - Reescribe la entrada que afirma que la ronda 3 cerró "la escritura cruzada de tenant": la caché
      namespaced por `scope()` deja de ser un HABILITADOR de escritura cruzada, pero la escritura cruzada
      con claves no-PK se cierra con SEC-01 (03-11): `Model.update`/`delete` aplican el `scope()` activo
      al DML. Reformula a "el acierto de caché ya no habilita una escritura cruzada" + entrada explícita
      de SEC-01.
    - Supersede/reescribe el bullet de la ronda 2 que dice "antes de invalidar esa **única** entrada":
      ahora la invalidación cubre TODAS las filas afectadas (no debe quedar ninguna afirmación de
      "única entrada").
    - Añade a `### Corregido`: `Model.update`/`delete` aplican el `scope()` activo al WHERE (params
      ligados) — una escritura con clave no-PK ya no modifica/borra filas de otro tenant (SEC-01).
    - Añade a `### Corregido`: la invalidación post-escritura de `CachedModel` es fail-open incluso con
      valores de PK no hashables (WR-R3-02).
    - Corrige la nota de `last_id()`/Oracle (IN-R3-01): el fallback cubre un `last_id()` que FALLE, no
      que Oracle devuelva 0 (Oracle usa `RETURNING id INTO`). No nombres a Oracle como el motor del fallback.
    - Mantén la nota de ownership (entradas aditivas; la enumeración del milestone es de la Fase 8 / `08-04`).
  </action>
  <verify>
    <automated>sed -n '/## \[Unreleased\]/,/## \[0.2.6\]/p' CHANGELOG.md | grep -ci "scope"</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/## \[Unreleased\]/,/## \[0.2.6\]/p' CHANGELOG.md | grep -ci "SEC-01\|scope"` devuelve >= 1
    - `sed -n '/## \[Unreleased\]/,/## \[0.2.6\]/p' CHANGELOG.md | grep -ci "única entrada"` devuelve 0
    - `sed -n '/## \[Unreleased\]/,/## \[0.2.6\]/p' CHANGELOG.md | grep -c "### Corregido"` devuelve >= 1 (sin duplicar)
    - `sed -n '/## \[Unreleased\]/,/## \[0.2.6\]/p' CHANGELOG.md | grep -ci "Oracle.*0\|devuelve 0"` devuelve 0
  </acceptance_criteria>
  <done>El CHANGELOG describe SEC-01 y WR-R3-02, sin overclaim de "escritura cruzada cerrada" por la caché ni "única entrada".</done>
</task>

<task type="auto">
  <name>Task 2: Guía y diseño de seguridad (escrituras bajo scope + residuales)</name>
  <read_first>
    - docs/guide.md (§10 caché; y cualquier § de multi-tenancy/scope)
    - docs/design/5-security.md (§5.4 caché; y la sección de scope/multi-tenancy si existe)
    - encino_orm/model/model.py (`_scoped_dml`, `update`/`delete`)
    - .planning/phases/03-data-correctness/03-REVIEW-GAPS-R3.md (WR-R3-04, IN-R3-01, IN-R3-03)
  </read_first>
  <files>docs/guide.md, docs/design/5-security.md</files>
  <action>
    En ambos documentos (tono y formato existentes, español, sin `Args:`/`Returns:`):
    - Documenta que `Model.update`/`delete` aplican el `scope()` activo al DML (WHERE ligado): una
      escritura por clave no-PK no cruza tenants. Sin `scope()` el DML es idéntico al de siempre.
    - **WR-R3-04**: advierte que la invalidación de caché se namespacea con el `scope()` del ESCRITOR;
      un mantenimiento sin `scope()` (o con otro distinto) no invalida las entradas con scope y estas
      quedan obsoletas hasta el TTL. Recomienda que escritor y lector corran bajo el mismo `scope()`, o
      deshabilitar `CachedModel`/invalidar fuera de banda para rutas administrativas.
    - **Residual `upsert`**: `upsert` con claves de conflicto globales no se acota por `scope()`; la
      unicidad multi-tenant debe ser `(tenant, clave)`. Documenta que queda como límite conocido.
    - **IN-R3-03**: suaviza la promesa de "huella estable" de `scope()`: los filtros de scope deben ser
      deterministas (p. ej. `Filter.in_` con un `set` puede variar el orden entre procesos).
    - **IN-R3-01**: si se menciona el fallback de `last_id()`, describe que cubre un `last_id()` que
      falle, no un motor concreto.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "scope" docs/guide.md` devuelve >= 1 en la sección de multi-tenancy/caché
    - `grep -ci "scope" docs/design/5-security.md` devuelve >= 1
    - `grep -ci "upsert" docs/guide.md docs/design/5-security.md` devuelve >= 1 (residual documentado)
    - `uv run mkdocs build --strict` termina con exit code 0 (sin warnings de anclas)
  </acceptance_criteria>
  <done>La guía y el diseño de seguridad documentan el DML acotado por scope, el requisito de mismo scope en escritura, y los residuales (upsert, huella determinista).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentación -> operador | una afirmación de aislamiento no implementada induce configuraciones inseguras |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13-01 | Repudiation | CHANGELOG/docs | mitigate | se documenta SEC-01 y se corrigen los overclaims de la ronda 3 (WR-R3-03/04, IN-R3-01/03) |
| T-03-13-SC | Tampering | instalaciones pip/npm/cargo | mitigate | la fase NO instala paquetes ni añade `continue-on-error` a los gates |
</threat_model>

<verification>
- `uv run mkdocs build --strict` verde (sin warnings de anclas)
- `uv run pytest -q` verde
- `uv run ruff check encino_orm tests` + `uv run ruff format --check encino_orm tests` + `uv run mypy encino_orm` salen 0
</verification>

<success_criteria>
- WR-R3-03/WR-R3-04/IN-R3-01/IN-R3-03 cerrados: docs y CHANGELOG veraces
- SEC-01 registrado en el CHANGELOG
- Los docs compilan en modo estricto
</success_criteria>

<output>
Create `.planning/phases/03-data-correctness/03-13-SUMMARY.md` when done
</output>
