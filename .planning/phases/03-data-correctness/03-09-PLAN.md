---
phase: 03-data-correctness
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - encino_orm/migration.py
  - tests/test_migration_reconcile.py
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "La compensación pre-DDL borra la fila del ledger por la IDENTIDAD de la fila que ESTA llamada insertó (no por `{name, status}`), de modo que una fila `pending` re-publicada por otro runner sobrevive (WR-01 residual)"
    - "Si el motor no expone un `last_id()` utilizable (Oracle devuelve 0), la compensación cae al compare-and-delete `{name, status: pending}` y la limitación queda documentada como residual asignada a la Fase 4 / POOL-03"
    - "Un fallo de la propia compensación no reemplaza la excepción original del DDL: se registra un warning y se re-lanza el error raíz (IN-01)"
    - "La máquina de dos fases (D-08/D-17) se preserva: DDL que corre y falla el promote deja la fila `pending` A PROPÓSITO"
  artifacts:
    - path: "encino_orm/migration.py"
      provides: "compensación por identidad de fila + compensación que no enmascara el error raíz"
      contains: "last_id"
    - path: "tests/test_migration_reconcile.py"
      provides: "regresión del interleaving rollback+reinserción y del enmascaramiento de la excepción"
      contains: "propia"
  key_links:
    - from: "encino_orm/migration.py::_apply"
      to: "Db.last_id"
      via: "captura best-effort de la identidad de la fila insertada, antes del DDL"
      pattern: "last_id"
    - from: "encino_orm/migration.py::_apply compensación"
      to: "Db.delete"
      via: "DELETE por `{id: ledger_id}` cuando hay identidad; fallback `{name, status: pending}`"
      pattern: "db\\.delete"
---

<objective>
Cerrar los residuales de la revisión de la ronda 2 en el runner de migraciones: **WR-01 residual**
(la compensación compara por `{name, status='pending'}`, que no prueba PROPIEDAD de la fila: si el
runner A hace rollback de su fila y el runner B re-inserta `pending` antes de la compensación de A,
A borra la fila de B) y **IN-01** (un fallo de la compensación dentro del `except` enmascara la
excepción original del DDL). Además, fortalecer el test de IN-02 (aserción de comportamiento, no de
substring de docstring).

Purpose: `DATA-02` exige que un `migrate()` fallido deje un estado reconcilable. El compare-and-delete
`{name, status}` de `03-07` cerró el caso del INSERT duplicado, pero sigue borrando una fila ajena en
el interleaving rollback+reinserción. La identidad de la fila (`id`) es el discriminante correcto.

Output: `encino_orm/migration.py` con captura best-effort de `db.last_id()` y compensación por
identidad (con fallback documentado), y compensación que no reemplaza el error raíz;
`tests/test_migration_reconcile.py` con el fake `LedgerDb` extendido y regresiones RED→GREEN.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-correctness/03-CONTEXT.md
@.planning/phases/03-data-correctness/03-REVIEW-GAPS.md
@.planning/phases/03-data-correctness/03-VERIFICATION-FINAL.md
@encino_orm/migration.py
@tests/test_migration_reconcile.py

<interfaces>
<!-- Anclajes extraídos del árbol. No explorar más. -->

Estado ACTUAL de `encino_orm/migration.py` (entregado por `03-07`; a MODIFICAR):
- `_apply(db, name, qry)` (:65): `ddl_done = False`; `inserted = False`; `try: async with db.transaction():`
  INSERT `{name, status: pending, sql_text}` -> `inserted = True` -> `db.execute(qry)` -> `ddl_done = True`
  -> UPDATE a `applied`. `except Exception:` compensa si `not db.transactional_ddl and inserted and not ddl_done`
  con `db.delete(MIGRATIONS_TABLE, {"name": name, "status": STATUS_PENDING})` en una transacción nueva;
  si `ddl_done` deja warning; termina con `raise`.
- `_row_status` (:41), `_set_status` (:53), `_delete_row` (:59): usan `db.delete`/`db.update` con claves
  físicas; `name` viaja como parámetro ligado.
- El ledger tiene columna `id` (`INTEGER PRIMARY KEY AUTOINCREMENT` en SQLite; ver
  `SqliteDb._ensure_migrations_table` :242). `reconcile_migrations` (:176) ya ordena por `id`.
- `Db.last_id()` es abstracto (`base.py:130`); MySQL/MariaDB guardan `lastrowid` (`mysql.py:196`),
  Oracle devuelve `self._last_id` (0 si el driver no lo expone) (`oracle.py:316`), PoolDb lo resuelve
  por la conexión en uso (`pool.py:300`).

De `tests/test_migration_reconcile.py` (fake a mano):
- `LedgerDb(Db)` (:22): `transactional_ddl`, `fail_ddl`, `fail_promote`, `fail_duplicate_insert`;
  `ledger: dict[str, dict]` indexado por `name`; `delete_calls: list[dict]`; `transaction()` hace
  snapshot/rollback según `transactional_ddl`; `insert`/`delete`/`update` devuelven tuplas y `execute`
  las interpreta; `delete` es fiel al SQL real (todas las claves deben casar). NO tiene `last_id`.
- 4 tests `test_*propia*`/`test_*duplicado*`/`test_*compensacion*`/`test_*reintentar*` de `03-07`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regresiones RED de WR-01 residual e IN-01</name>
  <read_first>
    - tests/test_migration_reconcile.py (LedgerDb y tests de `03-07`)
    - encino_orm/migration.py (`_apply` :65, compensación :98-118)
    - .planning/phases/03-data-correctness/03-REVIEW-GAPS.md (WR-01 residual: secuencia exacta; IN-01)
  </read_first>
  <files>tests/test_migration_reconcile.py</files>
  <behavior>
    - Interleaving: el runner A inserta `pending` (id=1) y su DDL falla ANTES del commit (rollback
      elimina la fila); el runner B inserta `pending` (id=2) y commitea; la compensación de A debe
      borrar SOLO la fila con `id=1` (inexistente) y dejar intacta la fila de B (`id=2`).
    - Enmascaramiento: si la compensación falla (el backend lanza al borrar), la excepción que ve el
      llamador es la del DDL raíz, no la de la compensación.
  </behavior>
  <action>
    Escribe PRIMERO las regresiones y captura su salida RED.

    Extiende `LedgerDb`:
    - Añade un contador `_next_id` y asigna `row["id"]` en el INSERT (el ledger guarda `id`).
    - Implementa `async def last_id(self)` devolviendo el id de la última fila insertada por ESTA
      instancia (o 0 si no hubo).
    - Haz que el `delete` interprete `{"id": N}` (borra la fila cuyo `id == N`) ADEMÁS de `{name, ...}`,
      de forma fiel al SQL real (todas las claves deben casar).
    - Añade un flag `fail_compensation` (o similar) para que el DELETE de compensación lance, de modo
      que se pueda probar IN-01.

    Añade tests con prefijo `test_wr01_` y `test_in01_`:
    1. `test_wr01_compensacion_no_borra_la_fila_reinsertada`: simula el interleaving con dos instancias
       `LedgerDb` compartiendo el mismo `ledger` (o manipulando el ledger directamente) y verifica que
       tras la compensación de A la fila de B sigue en el ledger. **HOY FALLA**: el compare-and-delete
       `{name, status: pending}` borra la fila de B.
    2. `test_in01_compensacion_fallida_no_enmascara_el_error_raiz`: con `fail_ddl=True` y
       `fail_compensation=True`, `pytest.raises(RuntimeError)` captura el mensaje del DDL raíz
       (p. ej. `"DDL falló"`), NO el de la compensación.
    - No borres ni renombres los tests existentes de `03-07`.

    No toques todavía `encino_orm/migration.py`.
  </action>
  <verify>
    <automated>! uv run pytest tests/test_migration_reconcile.py -k "wr01_ or in01_" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_wr01_" tests/test_migration_reconcile.py` devuelve >= 1
    - `grep -c "def test_in01_" tests/test_migration_reconcile.py` devuelve >= 1
    - `grep -c "def last_id" tests/test_migration_reconcile.py` devuelve 1
    - `uv run pytest tests/test_migration_reconcile.py -k "wr01_ or in01_" -q` (sin `!`) termina con exit code != 0 (RED capturado)
  </acceptance_criteria>
  <done>Las regresiones existen, fallan contra el código actual y no rompen los tests de `03-07`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compensación por identidad + no enmascarar el error raíz (GREEN)</name>
  <read_first>
    - encino_orm/migration.py (`_apply` :65 y la compensación :98-118)
    - tests/test_migration_reconcile.py (regresiones y fake extendido de Task 1)
    - encino_orm/oracle.py (`last_id` :316), encino_orm/mysql.py (`last_id` :246), encino_orm/pool.py (`last_id` :300)
  </read_first>
  <files>encino_orm/migration.py</files>
  <action>
    Modifica SOLO `_apply`:

    1. **Identidad de fila (WR-01 residual):**
       - Tras `await db.execute(db.insert(...))` y `inserted = True`, captura la identidad con
         `ledger_id = 0` y `try: ledger_id = await db.last_id()` / `except Exception: ledger_id = 0`
         (best-effort: un motor sin `last_id` útil no debe romper el runner).
       - En la compensación (`not db.transactional_ddl and inserted and not ddl_done`):
         - Si `ledger_id` es utilizable (`> 0`), borra por identidad:
           `db.delete(MIGRATIONS_TABLE, {"id": ledger_id})`.
         - Si no, conserva el compare-and-delete `{"name": name, "status": STATUS_PENDING}`.
       - Comenta el porqué: `{name, status}` no prueba propiedad; el `id` sí. Nota explícita: Oracle
         (`last_id()==0`) mantiene el residual estrecho, asignado a la Fase 4 / POOL-03 (captura de
         `last_id` dentro del insert). No introduzcas un `RETURNING` específico de motor aquí.

    2. **IN-01 — no enmascarar la excepción raíz:**
       - Envuelve el cuerpo de la compensación en su propio `try/except Exception as exc:` con
         `logger.warning("no se pudo compensar la fila pending de %s: %r", name, exc)`.
       - El `raise` final del `except` exterior debe seguir re-lanzando la excepción ORIGINAL del DDL
         (un `raise` desnudo en el bloque exterior).

    Actualiza el docstring de `_apply` para describir la compensación por identidad y el fallback.
    Mantén el español y el estilo.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migration_reconcile.py tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_migration_reconcile.py -k "wr01_ or in01_" -q` pasa (GREEN)
    - `uv run pytest tests/test_migration_reconcile.py tests/test_migrations.py -q` termina con exit code 0
    - `grep -c "last_id" encino_orm/migration.py` devuelve >= 1
    - `grep -c '{"id": ledger_id}' encino_orm/migration.py` devuelve >= 1
    - `grep -c "STATUS_PENDING" encino_orm/migration.py` devuelve >= 2 (fallback preservado)
    - `grep -c "logger.warning" encino_orm/migration.py` devuelve >= 2
  </acceptance_criteria>
  <done>La compensación borra por identidad cuando el motor la expone y cae al compare-and-delete documentado cuando no; un fallo de compensación no enmascara el error raíz.</done>
</task>

<task type="auto">
  <name>Task 3: Fortalecer el test de IN-02 (comportamiento, no substring)</name>
  <read_first>
    - tests/test_migration_reconcile.py (el test que hoy asserta `"rollback_migration" in resolve_migration.__doc__`)
    - encino_orm/migration.py (`resolve_migration` :208)
  </read_first>
  <files>tests/test_migration_reconcile.py</files>
  <action>
    Sustituye la aserción de substring de docstring por una verificación de comportamiento:
    - Mantén la comprobación del texto del error de `reconcile_migrations` (ahí el texto SÍ es
      load-bearing: es la instrucción que ve el operador).
    - Para `resolve_migration`, elimina el assert sobre `__doc__` y en su lugar verifica el efecto:
      con una fila `rolling_back`, `resolve_migration(db, name, applied=True)` la deja `applied`, y
      re-emitir `rollback_migration` con un `down` válido vuelve a `rolling_back`/borra la fila según
      corresponda (comportamiento ya cubierto por `test_rolling_back_true_restaura_applied`; si ya
      existe, basta con retirar el assert de docstring y dejar el test de comportamiento existente).
  </action>
  <verify>
    <automated>uv run pytest tests/test_migration_reconcile.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "__doc__" tests/test_migration_reconcile.py` devuelve 0
    - `uv run pytest tests/test_migration_reconcile.py -q` termina con exit code 0
  </acceptance_criteria>
  <done>El test de IN-02 verifica comportamiento; no queda ninguna aserción sobre texto de docstring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner de migraciones -> ledger | la compensación borra filas; un borrado sin prueba de propiedad destruye el registro de otro proceso |
| backend -> runner | `last_id()` puede no estar disponible (Oracle); el fallback debe ser seguro y documentado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Tampering | `_apply` compensación | mitigate | borrado por identidad `{id: ledger_id}` capturada tras el INSERT; fallback `{name, status}` documentado (WR-01 residual) |
| T-03-09-02 | Repudiation | excepción enmascarada | mitigate | la compensación va en su propio `try/except` con warning; el `raise` exterior re-lanza el error raíz (IN-01) |
| T-03-09-03 | Denial of Service | `db.last_id()` no soportado | mitigate | captura best-effort en `try/except`; `0` activa el fallback sin romper el runner |
| T-03-09-SC | Tampering | instalaciones pip/npm/cargo | mitigate | la fase NO instala paquetes ni añade `continue-on-error` a los gates |
</threat_model>

<verification>
- `uv run pytest tests/test_migration_reconcile.py -k "wr01_ or in01_" -q` verde (RED capturado en el SUMMARY)
- `uv run pytest tests/test_migration_reconcile.py tests/test_migrations.py -q` verde
- `uv run pytest -q` verde (suite completa)
- `uv run ruff check encino_orm tests` + `uv run ruff format --check encino_orm tests` + `uv run mypy encino_orm` salen 0
- `grep -c "db.commit()" encino_orm/migration.py` devuelve 0 (el runner usa solo `db.transaction()`)
</verification>

<success_criteria>
- WR-01 residual cerrado para motores con `last_id()` útil (MySQL/MariaDB); fallback documentado para Oracle
- IN-01 cerrado: la compensación no enmascara el error raíz
- D-08/D-17 preservados: el DDL que corre y falla el promote deja la fila `pending` a propósito
- DATA-02 sigue cubierto por must-haves verificables
</success_criteria>

<output>
Create `.planning/phases/03-data-correctness/03-09-SUMMARY.md` when done
</output>
