---
phase: 03-data-correctness
plan: 12
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - encino_orm/model/cached.py
  - tests/test_cached_model.py
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "La invalidación post-escritura es fail-open incluso si `_union` encuentra un valor de PK no hashable: nunca propaga una excepción después del commit (D-12, WR-R3-02)"
    - "`_union` deduplica por una huella hashable (`repr` del valor), no por el valor crudo"
    - "El comportamiento de `_union` para PKs escalares (int/str) no cambia"
  artifacts:
    - path: "encino_orm/model/cached.py"
      provides: "unión de sondas fail-open y huella hashable"
      contains: "_invalidate_after_write"
    - path: "tests/test_cached_model.py"
      provides: "regresión de fail-open con valor de PK no hashable"
      contains: "unhashable"
  key_links:
    - from: "encino_orm/model/cached.py::update/delete/upsert"
      to: "_invalidate_after_write"
      via: "sustituye la llamada directa a `_union` + `_invalidate_pks`"
      pattern: "_invalidate_after_write"
---

<objective>
Cerrar **WR-R3-02**: `_union` (ronda 3) se llama FUERA de cualquier `try` y usa
`seen.add(tuple(sorted(item.items())))`; con un valor de PK no hashable (p. ej. `list`/`dict`, que
pydantic y `_from_db` admiten) lanza `TypeError` DESPUÉS de que la escritura se commiteó, violando la
garantía fail-open D-12 ("la escritura ya está commiteada: propagar daría un fallo falso al llamador").

Purpose: la invalidación de caché nunca puede fallar una escritura ya persistida. `_resolve_pk_values`
y `_invalidate_pks` ya están envueltos en `try/except`; el único eslabón sin guarda es la unión.

Output: `encino_orm/model/cached.py` con `_union` usando huella `repr` y un helper
`_invalidate_after_write` fail-open; `tests/test_cached_model.py` con la regresión.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-data-correctness/03-REVIEW-GAPS-R3.md
@encino_orm/model/cached.py
@tests/test_cached_model.py

<interfaces>
Estado ACTUAL de `encino_orm/model/cached.py` (ronda 3):
- `_union(*lists)` (:109): `fingerprint = tuple(sorted(item.items()))`; `seen.add(fingerprint)` —
  lanza con valores no hashables.
- Overrides `update` (:187), `delete` (:201), `upsert` (:214): terminan con
  `await self._invalidate_pks(self._union(pks, pks_after))` FUERA de try.
- `_invalidate_pks` (:130) ya es fail-open (try/except por clave).
- `_delete_cached` (:42) y `_resolve_pk_values` (:54) ya son fail-open.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regresión RED de fail-open con PK no hashable</name>
  <read_first>
    - tests/test_cached_model.py (modelos, `MemoryCacheBackend`, helpers de clave)
    - encino_orm/model/cached.py (`_union` :109, overrides :187/:201/:214)
    - .planning/phases/03-data-correctness/03-REVIEW-GAPS-R3.md (WR-R3-02: reproducción)
  </read_first>
  <files>tests/test_cached_model.py</files>
  <behavior>
    Un `CachedModel` con `_primary_key=("k",)` y `k: list | None` (la columna se almacena como JSON):
    - `load()` puebla la caché; un `update()` con una clave de escritura no-PK que resuelve una PK de
      tipo `list` NO debe lanzar `TypeError` después del commit; la escritura queda persistida.
  </behavior>
  <action>
    Escribe PRIMERO la regresión y captura su salida RED.

    En `tests/test_cached_model.py`:
    - Añade un modelo `ClientePkLista(CachedModel)` con `_table="clientes_pk_lista"`, `_primary_key=("k",)`,
      campo `k: list | None = Field(default=None)`, `grupo: str | None`, `nombre: str | None`, y su DDL
      (`k TEXT`, `grupo TEXT`, `nombre TEXT`, `enabled INTEGER DEFAULT 1`, `created_at`, `updated_at`) y
      fixture.
    - Añade `test_wr_r3_02_union_no_rompe_fail_open`: inserta una fila con `k=[1,2]` y `grupo="G"`;
      `await ClientePkLista(db, cache=cache, k=[1,2]).load()` (cachea bajo la PK `k`); luego
      `await ClientePkLista(db, cache=cache, grupo="G", nombre="Nuevo").update(keys=["grupo"])` y
      assert que NO lanza y que la fila quedó actualizada (`nombre == "Nuevo"`).
      **HOY FALLA** con `TypeError: unhashable type: 'list'`.
    - Si `update(keys=["grupo"])` no resolviera una PK `list` por el camino de `search`, ajusta el test
      para forzar la ruta de `_union` con valores no hashables (p. ej. PK de tipo `dict`/`list` presente
      en la sonda) sin debilitar la aserción de fail-open.

    No toques todavía `encino_orm/model/cached.py`.
  </action>
  <verify>
    <automated>! uv run pytest tests/test_cached_model.py -k wr_r3_02 -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_wr_r3_02_" tests/test_cached_model.py` devuelve 1
    - `uv run pytest tests/test_cached_model.py -k wr_r3_02 -q` (sin `!`) termina con exit code != 0 (RED capturado)
  </acceptance_criteria>
  <done>La regresión existe y falla contra el código actual (RED capturado).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `_union` hashable + invalidación post-escritura fail-open (GREEN)</name>
  <read_first>
    - encino_orm/model/cached.py (fichero modificado)
    - tests/test_cached_model.py (la regresión `test_wr_r3_02_*`)
  </read_first>
  <files>encino_orm/model/cached.py</files>
  <action>
    1. En `_union`, cambia la huella a una hashable:
       `fingerprint = tuple(sorted((k, repr(v)) for k, v in item.items()))`.
       Documenta que `repr` evita el `TypeError` de valores no hashables sin cambiar el dedupe de PKs
       escalares.
    2. Añade un helper fail-open:
       ```python
       async def _invalidate_after_write(self, *probe_lists) -> None:
           """Invalida la unión de las sondas sin poder fallar una escritura commiteada (D-12)."""
           try:
               pks = self._union(*probe_lists)
           except Exception as exc:
               logger.warning("no se pudo unir las PKs a invalidar de %s: %r", self._table, exc)
               pks = [d for lst in probe_lists if lst for d in lst]
           await self._invalidate_pks(pks)
       ```
    3. Sustituye en `update`/`delete`/`upsert` la llamada
       `await self._invalidate_pks(self._union(pks, pks_after))` por
       `await self._invalidate_after_write(pks, pks_after)`.
    4. No cambies la semántica de `_resolve_pk_values` ni `_invalidate_pks`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cached_model.py tests/test_cache_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cached_model.py -k wr_r3_02 -q` pasa (GREEN)
    - `uv run pytest tests/test_cached_model.py tests/test_cache_backend.py -q` termina con exit code 0
    - `grep -c "_invalidate_after_write" encino_orm/model/cached.py` devuelve >= 4 (definición + 3 overrides)
    - `grep -c "repr(v)" encino_orm/model/cached.py` devuelve >= 1
  </acceptance_criteria>
  <done>La invalidación post-escritura es fail-open incluso con PKs no hashables; `_union` usa huella hashable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| escritura commiteada -> invalidación de caché | una excepción en la invalidación no puede revertir ni fallar la escritura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12-01 | Denial of Service | `_union` post-escritura | mitigate | huella `repr` hashable + `_invalidate_after_write` con `try/except`; D-12 garantizado |
| T-03-12-SC | Tampering | instalaciones pip/npm/cargo | mitigate | la fase NO instala paquetes ni añade `continue-on-error` a los gates |
</threat_model>

<verification>
- `uv run pytest tests/test_cached_model.py -k wr_r3_02 -q` verde (RED capturado)
- `uv run pytest tests/test_cached_model.py tests/test_cache_backend.py -q` verde
- `uv run pytest -q` verde
- `uv run ruff check encino_orm tests` + `uv run ruff format --check encino_orm tests` + `uv run mypy encino_orm` salen 0
</verification>

<success_criteria>
- WR-R3-02 cerrado: la invalidación nunca falla una escritura commiteada
- D-12 y el resto de las garantías de la ronda 3 intactas
</success_criteria>

<output>
Create `.planning/phases/03-data-correctness/03-12-SUMMARY.md` when done
</output>
