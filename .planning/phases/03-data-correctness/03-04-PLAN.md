---
phase: 03-data-correctness
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - encino_orm/model/cache_backend.py
  - tests/test_cache_backend.py
  - docs/guide.md
  - CHANGELOG.md
autonomous: true
requirements: [DATA-04]
must_haves:
  truths:
    - "`MemoryCacheBackend` desaloja la entrada menos recientemente usada al superar `max_size` (1024 por defecto)"
    - "`get` y `set` actualizan el orden de uso (`move_to_end`)"
    - "`len(cache._store)` sigue reflejando el número de entradas (los tests existentes no se rompen)"
    - "La expiración por TTL sigue funcionando igual que antes"
    - "El contrato dev/test-only de `MemoryCacheBackend` está documentado en el docstring y en `docs/guide.md`"
    - "`CHANGELOG.md` documenta los cambios incompatibles de la fase (ledger de rollback, estado `pending`, invalidación de caché, cota LRU)"
  artifacts:
    - path: "encino_orm/model/cache_backend.py"
      provides: "`MemoryCacheBackend` acotado con LRU (`OrderedDict` + `move_to_end` + `popitem(last=False)`)"
      contains: "max_size"
    - path: "tests/test_cache_backend.py"
      provides: "tests DB-free de LRU, `max_size`, `len(_store)`, `delete` y expiración"
      contains: "lru"
    - path: "docs/guide.md"
      provides: "contrato dev/test-only + limitación multi-proceso (invalidación local)"
      contains: "dev/test"
    - path: "CHANGELOG.md"
      provides: "entradas `[Unreleased] ### Corregido` de los cuatro cambios de la fase"
      contains: "MemoryCacheBackend"
  key_links:
    - from: "encino_orm/model/cache_backend.py::MemoryCacheBackend.set"
      to: "collections.OrderedDict.popitem(last=False)"
      via: "desalojo LRU al superar `max_size`"
      pattern: "popitem\\(last=False\\)"
    - from: "encino_orm/model/cache_backend.py::MemoryCacheBackend.get"
      to: "collections.OrderedDict.move_to_end"
      via: "actualización del orden de uso en cada acceso"
      pattern: "move_to_end"
---

<objective>
Acotar `MemoryCacheBackend` con una política LRU y documentar explícitamente su contrato dev/test-only,
y cerrar la fase documentando los cambios incompatibles en `CHANGELOG.md` y `docs/guide.md`.

Purpose: `MemoryCacheBackend._store` es un `dict` sin cota y la expiración solo se evalúa al releer la
MISMA clave, así que las claves nunca releídas se acumulan indefinidamente (Pitfall 17). Además,
`docs/guide.md` no dice en ninguna parte que el backend en memoria no es apto para producción (D-14).

Output: `MemoryCacheBackend` con LRU (`max_size=1024`), tests DB-free, `docs/guide.md` §10 con el
contrato dev/test y la limitación multi-proceso, y `CHANGELOG.md` con los cambios incompatibles de la
fase.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-correctness/03-CONTEXT.md
@.planning/phases/03-data-correctness/03-RESEARCH.md
@.planning/phases/03-data-correctness/03-PATTERNS.md
@.planning/phases/03-data-correctness/03-VALIDATION.md
@encino_orm/model/cache_backend.py
@tests/test_cached_model.py
@docs/guide.md
@CHANGELOG.md

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del código vivo; no explorar más. -->

Estado actual de `encino_orm/model/cache_backend.py:11-32` (lo que se acota):
```python
class MemoryCacheBackend:
    """Backend en memoria para desarrollo y pruebas."""

    def __init__(self):
        self._store = {}

    async def get(self, key: str) -> bytes | None:
        item = self._store.get(key)
        if item is None:
            return None
        value, expires_at = item
        if expires_at is not None and time.monotonic() >= expires_at:
            self._store.pop(key, None)
            return None
        return value

    async def set(self, key: str, value: bytes, ttl: int) -> None:
        expires_at = time.monotonic() + ttl if ttl else None
        self._store[key] = (value, expires_at)

    async def delete(self, key: str) -> None:
        self._store.pop(key, None)
```

`RedisCacheBackend` (`cache_backend.py:35-59`) NO se toca: Redis ya acota y expira.

Invariante a preservar: `tests/test_cached_model.py:36` usa `len(cache._store) == 1`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LRU acotado en `MemoryCacheBackend` (D-13/D-14)</name>
  <read_first>
    - encino_orm/model/cache_backend.py (fichero modificado; `MemoryCacheBackend` 11-32 y el Protocol 5-8)
    - tests/test_cached_model.py (análogo: usa `len(cache._store)` en la línea 36 — invariante a preservar)
    - .planning/phases/03-data-correctness/03-RESEARCH.md (Pattern 4 + "Don't Hand-Roll")
    - .planning/phases/03-data-correctness/03-PATTERNS.md (sección `model/cache_backend.py`)
  </read_first>
  <files>encino_orm/model/cache_backend.py, tests/test_cache_backend.py</files>
  <action>
    En `encino_orm/model/cache_backend.py`, `MemoryCacheBackend` pasa a usar
    `collections.OrderedDict` (import de stdlib) y gana `def __init__(self, max_size: int = 1024)` que
    guarda `self._max_size = max_size` y `self._store = OrderedDict()`. `OrderedDict` conserva la
    interfaz usada hoy (`.get`, `.pop(key, None)`, `in`, `len`), así que el resto del código no cambia de
    forma.
    - `get`: tras obtener el ítem y resolver la expiración, si la entrada sigue viva llama
      `self._store.move_to_end(key)` antes de devolver el valor (marcar como recientemente usada). La
      ruta de expiración sigue haciendo `self._store.pop(key, None)` y devolviendo `None`.
    - `set`: escribe `self._store[key] = (value, expires_at)`, llama `self._store.move_to_end(key)` y
      desaloja con `while len(self._store) > self._max_size: self._store.popitem(last=False)`.
    - `delete`: sin cambios funcionales (`pop(key, None)`).
    - NO reimplementes LRU con listas/heaps ni purgues expirados antes de desalojar (descartado en D-13).
    - Documenta el contrato dev/test-only en el docstring de la clase (D-14): backend en memoria, para
      desarrollo y pruebas, NO para producción; una sola instancia por proceso y sin invalidación
      distribuida; para producción usa `RedisCacheBackend`. El docstring va en español y sin secciones
      `Args:`/`Returns:`.

    Crea `tests/test_cache_backend.py` (DB-free, sin fixtures de BD; `@pytest.mark.asyncio` explícito):
    - `test_lru_evicts_least_recently_used`: con `MemoryCacheBackend(max_size=2)`, setear A y B, leer A
      (`get`) y setear C; assertar que B fue desalojada (su `get` es `None`) y que A y C siguen.
    - `test_max_size_is_respected`: tras N sets con `max_size=3`, `len(cache._store) == 3`.
    - `test_default_max_size_is_1024`: `MemoryCacheBackend()._max_size == 1024`.
    - `test_store_len_is_preserved`: `set` de una clave → `len(cache._store) == 1` (invariante de
      `tests/test_cached_model.py`).
    - `test_delete_removes_key`: `set` + `delete` → `get` devuelve `None` y `len` es 0.
    - `test_expiration_still_works`: `set` con `ttl` y `get` tras vencer devuelve `None` y elimina la
      clave (usa `monkeypatch` sobre `time.monotonic` si hace falta; no esperes con `sleep`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache_backend.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OrderedDict" encino_orm/model/cache_backend.py` devuelve >= 2
    - `grep -c "move_to_end" encino_orm/model/cache_backend.py` devuelve >= 2 (get y set)
    - `grep -c "popitem(last=False)" encino_orm/model/cache_backend.py` devuelve 1
    - `grep -c "max_size" encino_orm/model/cache_backend.py` devuelve >= 2
    - `grep -c "dev/test" encino_orm/model/cache_backend.py` devuelve >= 1
    - `uv run pytest tests/test_cache_backend.py -x` termina con exit code 0 y ejecuta al menos 6 tests
    - `uv run pytest tests/test_cached_model.py -x` termina con exit code 0 (invariante `len(_store)`)
  </acceptance_criteria>
  <done>`MemoryCacheBackend` es acotado con LRU y su contrato dev/test-only está documentado en el docstring.</done>
</task>

<task type="auto">
  <name>Task 2: Contrato dev/test + limitación multi-proceso en `docs/guide.md` §10</name>
  <read_first>
    - docs/guide.md (fichero modificado; §10 "Caché (`CachedModel`)" 475-507, con la frase de la línea ~486)
    - encino_orm/model/cache_backend.py (el contrato recién escrito)
    - .planning/phases/03-data-correctness/03-CONTEXT.md (D-14)
  </read_first>
  <files>docs/guide.md</files>
  <action>
    En `docs/guide.md` §10 ("Caché (`CachedModel`)"):
    - Añade una nota explícita de contrato: `MemoryCacheBackend` es para desarrollo y pruebas, **no**
      para producción; está acotado con LRU (`max_size=1024` por defecto, configurable) y para
      producción se usa `RedisCacheBackend` (D-14).
    - Añade la limitación conocida de invalidación: es LOCAL al proceso; no hay pub/sub distribuido, así
      que con varios procesos/servidores una invalidación en uno no llega a los demás y las entradas
      obsoletas quedan acotadas por el TTL. Referencia `docs/design/5-security.md` §5.4 para el
      mecanismo (overrides de `update`/`delete`/`upsert` en `CachedModel`).
    - La frase existente de la línea ~486 ("invalida al actualizar/borrar") ahora es VERDADERA (la hace
      cierta 03-03); puedes mantenerla y precisar que la invalidación ocurre tras el commit y es
      fail-open (un fallo de invalidación no revierte la escritura).
    Mantén el español, el tono y el formato del documento; sin secciones `Args:`/`Returns:`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache_backend.py tests/test_cached_model.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dev/test" docs/guide.md` devuelve >= 1
    - `grep -c "1024" docs/guide.md` devuelve >= 1
    - `grep -ci "proceso" docs/guide.md` devuelve >= 1 (limitación multi-proceso)
    - `uv run pytest tests/test_cache_backend.py tests/test_cached_model.py -q` termina con exit code 0
  </acceptance_criteria>
  <done>La guía documenta el contrato dev/test-only, la cota LRU y la limitación de invalidación local.</done>
</task>

<task type="auto">
  <name>Task 3: `CHANGELOG.md` con los cambios incompatibles de la fase</name>
  <read_first>
    - CHANGELOG.md (fichero modificado; sección `[Unreleased] ### Corregido` existente — NO duplicar el encabezado)
    - .planning/phases/03-data-correctness/03-CONTEXT.md (D-06, D-01/D-02, D-09/D-15/D-16, D-13/D-14)
    - .planning/phases/03-data-correctness/03-RESEARCH.md (§State of the Art: qué cambia y por qué)
    - .planning/PROJECT.md (constraint: todo cambio incompatible debe quedar documentado)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    Extiende la sección `[Unreleased] ### Corregido` EXISTENTE de `CHANGELOG.md` (no crees un encabezado
    nuevo ni dupliques `### Corregido`) con cuatro entradas en el mismo estilo que las de Fase 2 (qué
    cambia, comportamiento viejo y nuevo, y por qué):
    1. Ledger de rollback: `rollback_migration` deja de insertar una fila `{name}:down` y ahora ejecuta
       el `down` marcando la fila `rolling_back` y **borrándola** después, de modo que re-aplicar la
       misma migración funciona (antes era un no-op silencioso). CAMBIO INCOMPATIBLE: quien consultara
       filas `:down` en `migrate_status()` ya no las verá.
    2. `migrate()` de dos fases: inserta la fila como `pending` antes del DDL y la promueve a `applied`
       después; en motores con commit implícito de DDL (MySQL/MariaDB/Oracle) un fallo deja un `pending`
       que `reconcile_migrations()` DETECTA (nunca re-ejecuta DDL) y que se resuelve con
       `resolve_migration(db, name, applied=...)`. Se añade la columna `status` al ledger, con `ALTER`
       idempotente para instalaciones existentes.
    3. `CachedModel` invalida la clave afectada tras `update`/`delete`/`upsert` (y con
       `insert_many(cache=...)`), después del commit y fail-open: antes la caché nunca se invalidaba y
       una lectura posterior podía servir una fila obsoleta hasta el TTL.
    4. `MemoryCacheBackend` pasa a estar acotado con LRU (`max_size=1024` por defecto) y se documenta
       como backend dev/test-only; antes era un `dict` sin cota y las claves nunca releídas se
       acumulaban indefinidamente.
    Nota de ownership: estas entradas son ADITIVAS y no prejuzgan la enumeración completa de cambios
    incompatibles del milestone, que posee la Fase 8 (`08-04`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache_backend.py tests/test_cached_model.py tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollback_migration" CHANGELOG.md` devuelve >= 1
    - `grep -c "reconcile_migrations" CHANGELOG.md` devuelve >= 1
    - `grep -c "MemoryCacheBackend" CHANGELOG.md` devuelve >= 1
    - `grep -c "### Corregido" CHANGELOG.md` devuelve exactamente 1
    - `uv run pytest tests/test_cache_backend.py tests/test_cached_model.py tests/test_migrations.py -q` termina con exit code 0
  </acceptance_criteria>
  <done>El CHANGELOG documenta los cuatro cambios de la fase sin duplicar encabezados ni robar la enumeración del milestone a Fase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modelo → `MemoryCacheBackend` | almacén en proceso; su cota es la defensa contra el crecimiento sin límite |
| proceso A → proceso B | la caché en memoria NO cruza procesos: la invalidación es local por construcción |
| documentación → usuario | el doc decide si un usuario despliega un backend no apto para producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Denial of Service | `MemoryCacheBackend._store` | mitigate | LRU acotado (`max_size=1024`) con desalojo O(1) vía `OrderedDict.popitem(last=False)`; sin cota, claves nunca releídas crecían sin límite (Pitfall 17) |
| T-03-04-02 | Information Disclosure | contrato del backend | mitigate | se documenta explícitamente que el backend en memoria es dev/test-only y que la invalidación no es distribuida; se remite a `RedisCacheBackend` para producción |
| T-03-04-03 | Tampering | desalojo incorrecto | mitigate | `move_to_end` en `get` y `set` mantiene el orden de uso; test dedicado de LRU que verifica que se desaloja la menos usada, no la más antigua por inserción |
| T-03-04-04 | Repudiation | cambios incompatibles sin documentar | mitigate | `CHANGELOG.md` documenta los cuatro cambios de la fase; PROJECT.md exige que todo cambio incompatible quede registrado |
| T-03-04-SC | Tampering | instalaciones npm/pip/cargo | mitigate | la fase NO instala paquetes; no se añade ningún `continue-on-error` a los jobs de gate |
</threat_model>

<verification>
- `uv run pytest tests/test_cache_backend.py tests/test_cached_model.py -q` verde
- `uv run pytest -q` verde (suite completa)
- `grep -c "### Corregido" CHANGELOG.md` devuelve 1
- `grep -rn "OrderedDict" encino_orm/model/cache_backend.py` presente
</verification>

<success_criteria>
- DATA-04: `MemoryCacheBackend` está acotado (LRU, `max_size=1024`) Y explícitamente documentado como dev/test-only con un contrato escrito
- `len(cache._store)` sigue siendo válido para los tests existentes
- `CHANGELOG.md` enumera los cambios incompatibles de la fase (ledger de rollback, estado `pending`, invalidación de caché, cota LRU)
</success_criteria>

<output>
Create `.planning/phases/03-data-correctness/03-04-SUMMARY.md` when done
</output>
