---
phase: 02-dialect-seam-engine-parity
plan: 05
type: execute
wave: 5
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - pyproject.toml
  - tests/test_sql_snapshots.py
  - tests/__snapshots__/test_sql_snapshots.ambr
  - tests/test_mariadb.py
  - tests/test_redis_cache.py
  - tools/ci/check_coverage_floors.py
  - tests/test_ci_harness.py
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - DIAL-07
  - DIAL-08
must_haves:
  truths:
    - "Los snapshots de SQL por dialecto corren SIN base de datos en el job SQLite siempre activo y fallan ante cualquier deriva de dialecto"
    - "La matriz de CI ejecuta tests de integración reales en MariaDB y Redis (motores promovidos a requeridos) y en MSSQL y Oracle (job dedicado)"
    - "Quitar un servicio de motor de un job requerido hace FALLAR el job, no omitirlo"
    - "Los pisos de cobertura por módulo se aplican sobre `coverage json` y fallan cerrado si un módulo del mapa desaparece del reporte"
    - "Ningún job de gate usa `continue-on-error`"
  artifacts:
    - path: "tests/test_sql_snapshots.py"
      provides: "Snapshots DB-free de 6 dialectos × DML + count/paginate/list_tables"
      min_lines: 60
    - path: "tests/__snapshots__/test_sql_snapshots.ambr"
      provides: "Snapshots commiteados (el gate: un snapshot ausente FALLA)"
    - path: "tools/ci/check_coverage_floors.py"
      provides: "Gate de pisos de cobertura por módulo sobre `coverage json`, fail-closed"
      exports: ["main", "FLOORS"]
    - path: ".github/workflows/ci.yml"
      provides: "Job `test` extendido (mariadb+redis) y job `engine-heavy` (mssql+oracle) + pisos en el job `coverage`"
  key_links:
    - from: "tests/test_sql_snapshots.py"
      to: "tests/__snapshots__/test_sql_snapshots.ambr"
      via: "syrupy falla si el snapshot no existe: los .ambr deben ir en el mismo commit"
      pattern: "__snapshots__"
    - from: ".github/workflows/ci.yml"
      to: "tests/conftest.py"
      via: "ENCINO_ORM_REQUIRE_ENGINES incluye los motores promovidos (el switch ya es extensible sin código)"
      pattern: "ENCINO_ORM_REQUIRE_ENGINES"
    - from: ".github/workflows/ci.yml"
      to: "tools/ci/check_coverage_floors.py"
      via: "el job `coverage` genera coverage.json y lo alimenta al script"
      pattern: "check_coverage_floors.py"
---

<objective>
Cerrar la fase con la prueba de paridad y el gate que la sostiene: snapshots de SQL por dialecto (syrupy) sin base de datos en el job SQLite siempre activo, la matriz de CI multi-motor (MariaDB + Redis como servicios en el job `test`; MSSQL + Oracle en un job dedicado con instalación explícita del driver ODBC), y el gate de pisos de cobertura por módulo prometido por la Fase 1 (D-04/D-06).

Purpose: sin esto, "paridad de motores" es una afirmación sin señal. Los snapshots detectan deriva de dialecto en segundos y sin contenedores; la matriz convierte la paridad en una prueba que puede fallar; y los pisos por módulo impiden que SQLite enmascare las rutas dialectales.

Output: `tests/test_sql_snapshots.py` + `.ambr` commiteados; `syrupy==6.1.1` en el grupo dev; job `test` con 4 motores requeridos; job `engine-heavy` con MSSQL/Oracle y el paso de instalación de `msodbcsql18`; `tools/ci/check_coverage_floors.py` cableado al job `coverage`.

**Nota de alcance (desviación documentada):** este plan lleva CUATRO tareas. Razón: el checkpoint de legitimidad del paquete `syrupy` (slopcheck `[SUS]`) debe ser una tarea de tipo `checkpoint:human-verify` ANTES de la instalación (protocolo de paquetes), y no puede fusionarse con una tarea de implementación sin degradar un gate duro a advisory. El plan queda por tanto `autonomous: false`.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md
@.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md
@.planning/phases/02-dialect-seam-engine-parity/02-PATTERNS.md
@.planning/phases/02-dialect-seam-engine-parity/02-VALIDATION.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md
@.github/workflows/ci.yml
@tools/ci/check_skips.py
@pyproject.toml
@tests/conftest.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Compuerta de legitimidad del paquete `syrupy` (slopcheck `[SUS]`, bloqueante)</name>
  <files>pyproject.toml (solo tras la aprobación: `syrupy==6.1.1` en `[dependency-groups].dev`)</files>
  <what-built>Nada todavía. Esta es la compuerta de legitimidad de paquete exigida por el protocolo: `slopcheck` marcó `syrupy` como `[SUS]` por una heurística de similitud de nombre contra `scrapy`. La investigación determinó que es un FALSO POSITIVO, pero el veredicto `[SUS]` se mantiene y el planner NO puede auto-aprobarlo.</what-built>
  <how-to-verify>
    1. Abrir `https://pypi.org/project/syrupy/` y confirmar: nombre exacto `syrupy`, versión `6.1.1`, licencia MIT, y que la página enlaza al repositorio `github.com/syrupy-project/syrupy`.
    2. Abrir `https://github.com/syrupy-project/syrupy` y confirmar: proyecto de snapshots para pytest, README con `assert actual == snapshot`, `--snapshot-update` y el directorio `__snapshots__/*.ambr`. Confirmar que NO es un paquete vacío, typosquat ni un fork abandonado.
    3. Confirmar que el mantenedor y el historial de releases son coherentes (línea 6.x publicada, no un paquete de un solo commit).
    4. Comprobar que `syrupy` no es `scrapy` ni una variante de él: son proyectos sin relación.
    5. Si cualquiera de los puntos anteriores falla, responder con el problema: se abandona `syrupy` y se replantea DIAL-07 con aserciones golden-string ampliadas (el repo ya las tiene como suelo).
  </how-to-verify>
  <resume-signal>Escribe "approved" para autorizar `uv add --dev syrupy==6.1.1`, o describe el problema encontrado.</resume-signal>
  <action>No ejecutar ninguna instalación hasta recibir la aprobación explícita. Si el usuario aprueba, la instalación y su pin exacto los realiza la tarea 2. Si el usuario reporta un problema, DETENER el plan y devolver el hallazgo al orquestador: DIAL-07 se replantea con aserciones golden-string ampliadas en vez de `syrupy`.</action>
  <verify>
    <automated>MISSING — verificación humana bloqueante. No hay comando automatizable: la compuerta es la revisión de `https://pypi.org/project/syrupy/` y `https://github.com/syrupy-project/syrupy` por el usuario. El plan NO es auto-aprobable (`workflow.auto_advance` se ignora para esta tarea).</automated>
  </verify>
  <done>El usuario responde "approved" (o equivalente explícito) tras verificar el paquete, o el plan se detiene con el problema reportado.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar `syrupy`, verificar la interacción con la config endurecida y congelar los snapshots por dialecto</name>
  <files>pyproject.toml, tests/test_sql_snapshots.py, tests/__snapshots__/test_sql_snapshots.ambr</files>
  <read_first>
    - `pyproject.toml` (`[tool.pytest.ini_options]`: `addopts = "-ra --strict-markers"`, `filterwarnings = ["error", ...]`, `markers`; `[dependency-groups].dev` con el pin comentado de `ruff==0.16.8`; `[tool.coverage.*]`).
    - `tests/test_mssql.py` (líneas 24-70) y `tests/test_oracle.py` (líneas 28-70): el patrón de adaptador SIN `connect()` — construir, llamar al builder, `_prepare()`.
    - `encino_orm/base.py` (líneas 149-178): `list_tables` y `paginate` — las dos APIs cuyo SQL hay que congelar vía spy.
    - `encino_orm/model/model.py` (líneas 784-796): `Model.count`.
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — §Pattern 4 ("Database-free per-dialect snapshots") con sus dos advertencias operativas, y §Assumptions Log A4.
    - `.planning/phases/02-dialect-seam-engine-parity/02-VALIDATION.md` — fila `02-05-01` y la fila "Snapshot update".
    - `.gitignore`: verificar que NO excluye `__snapshots__` ni `*.ambr`.
  </read_first>
  <action>
    **A) Instalar** `syrupy==6.1.1` con pin exacto en `[dependency-groups].dev`, con un comentario en español que replique el racional del pin de `ruff`: la salida de serialización de snapshots está versionada, así que sin pin una versión más nueva podría reformatear los `.ambr` y romper CI en un PR no relacionado.

    **B) Verificar la interacción con la configuración endurecida de Fase 1 ANTES de escribir la suite** (riesgo A4): `--strict-markers` está en `addopts`, así que un marker no registrado falla la COLECCIÓN. Comprobar con `uv run pytest --collect-only -q -m syrupy_snapshot` que el marker que registra el plugin no rompe. Si falla, declarar `syrupy_snapshot` explícitamente en `markers` de `pyproject.toml` con un comentario que diga que lo registra el plugin y por qué se declara igualmente. Comprobar también que `filterwarnings = ["error"]` no convierte ningún warning de syrupy en fallo en el camino feliz; si apareciera uno, NO se debilita el "error" global: se añade una entrada de allowlist acotada y comentada (módulo + motivo), como manda D-07 de Fase 1.

    **C) `tests/test_sql_snapshots.py`** (nuevo, DB-free, sin `@pytest.mark.parametrize` para mantener la convención del resto de la fase). Importar los 6 adaptadores. Para cada operación, un test que construya el SQL de los 6 dialectos y lo compare contra UN snapshot que contiene los 6 dialectos indexados por nombre, de modo que el diff muestre exactamente qué dialecto derivó:
    - `insert` plano, `insert` con `ignore_duplicated=True`, `insert` con `replace=True` (con y sin `conflict` explícito en PostgreSQL/MSSQL/Oracle).
    - `update` y `delete`.
    - `count` / `paginate` / `list_tables`: el SQL se obtiene con un **spy** — adaptador sin `connect()`, con `fetch_one`/`fetch_many` monkeypatcheados para capturar el `Query` recibido y devolver filas canónicas; se congela el `sql_template` capturado (que conserva los `{n}`), que es exactamente la clase de SQL que DIAL-03 arregló. Afirmar además que el SQL de agregado capturado contiene `AS n`.
    Usar adaptadores SIN `connect()`: los constructores solo asignan `self._connection = None`, los builders son puros y `_prepare()` solo aplica una regex (patrón ya usado en `tests/test_mssql.py` y `tests/test_oracle.py`).

    **D) Generar y COMMITEAR los `.ambr`**: ejecutar `uv run pytest --snapshot-update -m syrupy_snapshot` (o el filtro equivalente), verificar que `tests/__snapshots__/test_sql_snapshots.ambr` existe y queda versionado (`git ls-files tests/__snapshots__` no vacío; `git check-ignore` no debe marcar el fichero). Documentar el flujo de actualización en el docstring del módulo de test, en español.

    **E) Política de snapshots no usados:** aceptar el comportamiento estricto por defecto de syrupy (un snapshot huérfano falla). Un snapshot que ya no se ejecuta es peso muerto; el flujo de limpieza es `--snapshot-update` tras renombrar/borrar un test. NO añadir `--snapshot-warn-unused`. Documentar esta decisión en el docstring del módulo.

    **F) Gate, no advisory:** los snapshots son parte de la suite normal (se ejecutan en el job SQLite sin base de datos). No crear un marker `benchmark`-like ni excluirlos de `addopts`.
  </action>
  <acceptance_criteria>
    - `pyproject.toml` contiene `syrupy==6.1.1` en `[dependency-groups].dev` con comentario justificativo.
    - `uv run pytest tests/test_sql_snapshots.py -x -q` pasa sin ninguna base de datos levantada.
    - `git ls-files tests/__snapshots__` incluye `test_sql_snapshots.ambr` y `git check-ignore tests/__snapshots__/test_sql_snapshots.ambr` sale con código distinto de 0 (no ignorado).
    - El `.ambr` contiene entradas para los 6 dialectos en cada operación: `grep -c "sqlite\|mysql\|mariadb\|postgresql\|mssql\|oracle" tests/__snapshots__/test_sql_snapshots.ambr` ≥ 6.
    - Borrar temporalmente el `.ambr` y correr el test lo hace FALLAR (syrupy es sound: un snapshot ausente es un fallo, no un skip); restaurarlo después.
    - `uv run pytest --collect-only -q -m syrupy_snapshot` sale con código 0 (el marker no rompe `--strict-markers`).
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa con los snapshots incluidos.
    - `uv run ruff check encino_orm tests` sale con código 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_sql_snapshots.py -x -q</automated>
  </verify>
  <done>Los 6 dialectos están congelados en `.ambr` commiteados, la suite los verifica sin base de datos en cada corrida, y la interacción con `--strict-markers`/`filterwarnings=["error"]` quedó verificada empíricamente.</done>
</task>

<task type="auto">
  <name>Task 3: Matriz de CI multi-motor — MariaDB + Redis requeridos, job `engine-heavy` con MSSQL/Oracle</name>
  <files>.github/workflows/ci.yml, tests/test_mariadb.py, tests/test_redis_cache.py</files>
  <read_first>
    - `.github/workflows/ci.yml` (completo, 250 líneas): job `test` (servicios mysql+postgres, `timeout-minutes: 15`, `ENCINO_ORM_REQUIRE_ENGINES: mysql,postgresql`, `-m "not optional_engine"`, gate `check_skips.py`, upload de cobertura), job `coverage` (`needs: [test]`, `coverage combine`, `coverage report`).
    - `tests/conftest.py`: `required_engines()` ya parte por comas → el switch es extensible SIN cambio de código.
    - `tests/test_mariadb.py` (líneas 9-15 y 51-52): `ENCINO_ORM_MARIADB_PORT` por defecto `3307` y el marcador `optional_engine`.
    - `tests/test_redis_cache.py` (líneas 9-13): `ENCINO_ORM_REDIS_URL` y `pytestmark` con `optional_engine`.
    - `tests/test_mssql.py` (líneas 11-20) y `tests/test_oracle.py` (líneas 10-17): env vars, driver `ODBC Driver 18 for SQL Server`, `ENCINO_ORM_ORACLE_SERVICE` por defecto `XEPDB1`.
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — §Pattern 6 completo (topología, snippet de instalación ODBC verificado, desajuste de service name de Oracle, health check de MSSQL, prohibición de `continue-on-error`) y Pitfall H.
    - `.planning/phases/02-dialect-seam-engine-parity/02-VALIDATION.md` — §Manual-Only Verifications.
  </read_first>
  <action>
    **A) Job `test` (extender, sin tocar los 4 legs):**
    - Añadir servicios `mariadb` (`mariadb:11`, `MARIADB_ROOT_PASSWORD: admin`, `MARIADB_DATABASE: encino_orm_test`, puerto **`3307:3306`** — MySQL ya ocupa 3306 y el default del test es 3307) y `redis` (`redis:latest`, puerto `6379:6379`, health-cmd `redis-cli ping`, retries 12). Copiar el patrón `options: --health-cmd/--health-interval/--health-timeout/--health-retries` de los servicios existentes.
    - `uv sync` del step de dependencias: añadir `--extra cache`.
    - `ENCINO_ORM_REQUIRE_ENGINES: mysql,postgresql,mariadb,redis`.
    - Añadir las env vars `ENCINO_ORM_MARIADB_HOST/PORT/USER/PASSWORD/DB` (`127.0.0.1`, `3307`, `root`, `admin`, `encino_orm_test`) y `ENCINO_ORM_REDIS_URL=redis://127.0.0.1:6379`.
    - Mantener `-m "not optional_engine"`: MSSQL/Oracle siguen marcados `optional_engine` y se deseleccionan aquí a propósito (su job es `engine-heavy`).

    **B) Promoción de MariaDB y Redis (Pitfall H):** retirar el marcador `optional_engine` de `tests/test_mariadb.py` (clase `TestMariadbLifecycle`, línea 52) y de `pytestmark` en `tests/test_redis_cache.py` (línea 11), **en el mismo commit** que añade el servicio, las env vars y el extra. Promover sin quitar el marcador es teatro: los tests quedarían deseleccionados y CI verde sin probar nada. Conservar el marcador `integration` en ambos (lo exige `tests/test_pytest_config.py`). NO tocar los marcadores de `tests/test_mssql.py` ni `tests/test_oracle.py`.

    **C) Job `engine-heavy` (nuevo):**
    - `runs-on: ubuntu-latest`, `timeout-minutes: 30`, una sola pata (`python-version: "3.12"`): el comportamiento de dialecto no depende del intérprete y multiplicar Oracle (60-120 s de arranque) por 4 legs no añade cobertura.
    - Servicios: `mssql` (`mcr.microsoft.com/mssql/server:2022-latest`, `ACCEPT_EULA: "Y"`, `MSSQL_SA_PASSWORD: Admin_123`, puerto `1433:1433`, health-cmd `/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Admin_123 -C -Q "SELECT 1" -b -o /dev/null`, `--health-interval=10s --health-timeout=5s --health-retries=20`; si la ruta de `sqlcmd` difiere en la imagen, degradar a una comprobación de puerto TCP y anotarlo) y `oracle` (`gvenzl/oracle-free`, `ORACLE_PASSWORD: admin`, puerto `1521:1521`, health-check propio de la imagen).
    - **Paso obligatorio de instalación del driver ODBC** ANTES de pytest: `ubuntu-24.04` NO trae `unixodbc` ni `msodbcsql18`, así que `pyodbc`/`aioodbc` no pueden abrir conexión. Instalar `unixodbc-dev` + la clave y el repo de Microsoft + `msodbcsql18` con `ACCEPT_EULA=Y`, tal como está verificado en §Pattern 6 de la investigación. Sin este paso, el job falla en el import/health, no en una aserción.
    - `uv sync --extra mssql --extra oracle` (sin los extras, los drivers son inimportables y los fixtures convierten el `ImportError` en un fallo confuso).
    - Ejecutar `uv run pytest -q tests/test_mssql.py tests/test_oracle.py --junitxml=junit.xml`. **NO usar `-m "not optional_engine"` aquí**: los dos ficheros conservan el marcador (para que el job `test` los deseleccione) y filtrar por marker dejaría el job probando CERO tests — el fallo silencioso que esta fase elimina.
    - `ENCINO_ORM_REQUIRE_ENGINES: mssql,oracle`, `ENCINO_ORM_MSSQL_*` (`127.0.0.1`, `1433`, `sa`, `Admin_123`, `encino_orm_test`, driver `ODBC Driver 18 for SQL Server`, `ENCINO_ORM_MSSQL_TRUST_CERT=true`) y `ENCINO_ORM_ORACLE_HOST/PORT/SERVICE/USER/PASSWORD` con **`ENCINO_ORM_ORACLE_SERVICE=FREEPDB1`** (el compose local usa `XE`/`XEPDB1`; la imagen ligera de CI usa `FREEPDB1`).
    - Gate de skips: `uv run python tools/ci/check_skips.py junit.xml` con `if: always()`.
    - Subir la cobertura del job como artefacto `coverage-heavy` (`COVERAGE_FILE: .coverage.heavy`, `--cov=encino_orm --cov-branch --cov-report=`, `include-hidden-files: true`) para que el job `coverage` pueda medir los adaptadores MSSQL/Oracle y la Fase 7 pueda fijar sus pisos con datos reales.
    - **`continue-on-error` PROHIBIDO** en este job y en todos los de gate: un job advisory es la Fase 1 fallando otra vez (CI verde que no verifica nada). Si el tiempo de CI se vuelve un problema, la mitigación permitida es ejecutar el job solo en `push` a `main` + `workflow_dispatch`, nunca hacerlo advisory.
    - Añadir en el YAML, junto a las env vars de MSSQL/Oracle, un comentario de PROCEDENCIA: los techos ad-hoc de parámetros de MSSQL (2100) y Oracle (65535) NO están verificados empíricamente; la sonda manual descrita en `02-VALIDATION.md` §Manual-Only Verifications debe ejecutarse en este job y anotar el valor medido junto a las constantes de `dialects/strategies.py`. No convertir esa sonda en un gate advisory.

    **D) Job `coverage`:** `needs: [test, engine-heavy]`; descargar ambos artefactos con `pattern: coverage-*` y `merge-multiple: true` (sustituye al `pattern: coverage-py*` actual). Mantener `coverage combine` + `coverage report` (el `fail_under = 82` global sigue siendo el ratchet no-baja).
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contiene `3307:3306` para MariaDB y un servicio `redis`.
    - `ENCINO_ORM_REQUIRE_ENGINES: mysql,postgresql,mariadb,redis` en el job `test` y `ENCINO_ORM_REQUIRE_ENGINES: mssql,oracle` en `engine-heavy`.
    - El job `engine-heavy` contiene el paso de instalación con `msodbcsql18` y `ACCEPT_EULA=Y`, y `ENCINO_ORM_ORACLE_SERVICE: FREEPDB1`.
    - `grep -c "continue-on-error" .github/workflows/ci.yml` es 0.
    - `grep -c "optional_engine" tests/test_mariadb.py tests/test_redis_cache.py` es 0 en ambos; `grep -c "optional_engine" tests/test_mssql.py tests/test_oracle.py` es ≥ 1 en ambos.
    - `uv run python -c "import yaml,pathlib; d=yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8')); assert 'engine-heavy' in d['jobs']; assert 'mssql' in d['jobs']['engine-heavy']['services'] and 'oracle' in d['jobs']['engine-heavy']['services']"` sale con código 0 (si `pyyaml` no está disponible, validar con un parseo mínimo equivalente y anotarlo).
    - `uv run pytest -q -m "not integration and not optional_engine"` sigue pasando (la retirada de marcadores no rompe la selección local).
    - `uv run pytest tests/test_pytest_config.py -q` pasa (los 6 ficheros de motor siguen aportando tests `integration`).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_pytest_config.py -q && uv run python -c "import pathlib; t=pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8'); assert '3307:3306' in t and 'msodbcsql18' in t and 'FREEPDB1' in t and 'continue-on-error' not in t and 'engine-heavy' in t"</automated>
  </verify>
  <done>El job `test` exige y ejecuta MySQL, PostgreSQL, MariaDB y Redis; existe un job `engine-heavy` no-advisory que instala el driver ODBC y ejecuta MSSQL/Oracle como motores requeridos; el job `coverage` combina también la cobertura de los motores pesados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Gate de pisos de cobertura por módulo sobre `coverage json`</name>
  <files>tools/ci/check_coverage_floors.py, tests/test_ci_harness.py, .github/workflows/ci.yml</files>
  <read_first>
    - `tools/ci/check_skips.py` (completo, 44 líneas): el patrón exacto a replicar — solo stdlib, fallo cerrado ante un artefacto ilegible, salida `FALLO: ...` a stderr, `raise SystemExit(main(sys.argv))`.
    - `tests/test_ci_harness.py` (líneas 1-115): el patrón de test del arnés y la convención de nombres (el nombre del módulo en el nombre del método para que `-k` seleccione de verdad).
    - `.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-CONTEXT.md` — D-04 (piso global bajo + piso alto por dialecto) y D-06 (el mecanismo se monta en Fase 1, el piso por dialecto se define en Fase 2).
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — §Pattern 5 ("Per-module coverage floor enforced by a stdlib script"): esquema verificado de `coverage json`, pisos propuestos, fail-closed y normalización de rutas; Pitfall J.
    - `.github/workflows/ci.yml` (job `coverage`, líneas 217-250): dónde se cablea el script.
  </read_first>
  <behavior>
    - Un `coverage.json` con todos los módulos del mapa por encima de su piso → `main` devuelve 0 e imprime el OK.
    - Un módulo por debajo de su piso → devuelve 1 e imprime una línea `FALLO:` con el módulo, el valor medido y el piso.
    - Un módulo del mapa AUSENTE del reporte → devuelve 1 (fallo cerrado: borrar el fichero no puede volver el gate verde).
    - Un `coverage.json` ilegible o con JSON inválido → devuelve 1.
    - Rutas con separador de Windows (`encino_orm\\query.py`) se normalizan a `/` y se comparan correctamente.
    - `FLOORS` es un dict módulo → piso y contiene al menos `encino_orm/dialects/identifiers.py` (100), `encino_orm/dialects/builders.py` (95) y `encino_orm/query.py` (95).
  </behavior>
  <action>
    Crear `tools/ci/check_coverage_floors.py` con el mismo patrón que `tools/ci/check_skips.py`: solo stdlib (`json`, `sys`), docstring en español explicando por qué existe el script (`coverage.py` no soporta `fail_under` por fichero: `[report] fail_under` es un único total, verificado en la doc oficial) y por qué falla cerrado (Pitfall J: iterar el mapa con `.get()` haría pasar el gate si el módulo desaparece). Función `_norm(path)` que sustituye `\` por `/`. `FLOORS` con los tres módulos del seam y de `Query` en los pisos propuestos por la investigación, más un comentario que explique por qué NO hay pisos de adaptador todavía: en la corrida equivalente a CI `oracle.py` mide 16% porque Oracle está deseleccionado; un piso fijado antes de que existan los jobs de motor es una ficción. Indicar que esos pisos se fijan en la fase siguiente a partir del primer run verde de `engine-heavy` (cuya cobertura ya sube al job `coverage` en la tarea 3). `main(argv) -> int`: leer el JSON con `encoding="utf-8"`, normalizar las claves de `files`, recorrer `FLOORS`, acumular fallos y devolver 1 si hay alguno. Mensajes `FALLO: ...` a stderr; mensaje OK a stdout. `if __name__ == "__main__": raise SystemExit(main(sys.argv))`.

    Extender `tests/test_ci_harness.py` con tests nombrados `test_check_coverage_floors_*` (el nombre debe contener el literal del módulo para que `-k` los seleccione de verdad — precedente de Fase 1), cubriendo TODO el bloque `<behavior>` con ficheros temporales `tmp_path` y `json.dump`.

    Cablear en el job `coverage` de `.github/workflows/ci.yml`, después de `coverage report`:
    - `uv run coverage json -o coverage.json`
    - `uv run python tools/ci/check_coverage_floors.py coverage.json`
    con un comentario que explique que es el gate de pisos por módulo prometido por D-04/D-06 de Fase 1 y que el piso global (`fail_under = 82`) sigue vigente por separado.

    No añadir ignores de ruff: el script parsea JSON, no XML, así que no necesita `S314`.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_ci_harness.py -k check_coverage_floors -q` pasa y selecciona al menos 5 tests (no pasa en vacío).
    - `uv run coverage json -o coverage.json && uv run python tools/ci/check_coverage_floors.py coverage.json` sale con código 0 sobre la suite actual.
    - Borrar temporalmente `encino_orm/query.py` del `coverage.json` generado (o usar un JSON de prueba) hace que el script devuelva 1: el fail-closed está cubierto por un test.
    - `grep -c "check_coverage_floors.py" .github/workflows/ci.yml` ≥ 1 y el step está en el job `coverage`.
    - `uv run ruff check encino_orm tests tools` sale con código 0.
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_ci_harness.py -k check_coverage_floors -q</automated>
  </verify>
  <done>El gate de pisos por módulo existe, falla cerrado ante un módulo ausente, está cubierto por tests que `-k` selecciona de verdad, y está cableado al job `coverage` de CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependencias de desarrollo → entorno de build/CI | `syrupy` se instala en el grupo dev; es la única dependencia nueva de la fase (runtime: NINGUNA). |
| repositorio → runner de CI | Los workflows ejecutan contenedores de terceros y un paso de `curl | apt` para el driver ODBC de Microsoft. |
| `coverage.json` → gate de pisos | El artefacto de cobertura es la entrada del gate; un artefacto manipulado o ausente no puede convertirse en verde. |
| snapshots `.ambr` → suite | Un snapshot commiteado congela el SQL: si se actualiza sin revisar, "bendice" una regresión. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | Tampering (supply chain) | instalación de `syrupy` | mitigate | `slopcheck` marcó `[SUS]` (falso positivo por similitud con `scrapy`). Compuerta `checkpoint:human-verify` `blocking-human` ANTES de instalar; pin exacto `==6.1.1`; verificación manual de PyPI + repositorio. La compuerta nunca es auto-aprobable. |
| T-02-26 | Tampering | gate de motores degradado a no-op | mitigate | Prohibido `continue-on-error` en todos los jobs de gate; `grep -c continue-on-error == 0` es criterio de aceptación. El job `engine-heavy` ejecuta por SELECCIÓN DE FICHERO (no por marker), de modo que no puede quedarse probando cero tests. |
| T-02-27 | Repudiation | promoción de motor sin retirar el marcador | mitigate | Pitfall H: retirar `optional_engine` de MariaDB y Redis en el MISMO commit que añade servicio + env vars + extra. Un motor "requerido" cuyos tests siguen deseleccionados es CI verde sin verificación. |
| T-02-28 | Tampering | gate de cobertura que pasa en vacío | mitigate | `check_coverage_floors.py` falla cerrado si un módulo del mapa no aparece en `coverage.json` (espejo de `check_skips.py`). Cubierto por un test dedicado. |
| T-02-29 | Tampering | snapshots que bendicen una regresión | mitigate | Los `.ambr` se generan DESPUÉS de que 02-02 haya probado el SQL byte-idéntico con las aserciones golden-string intactas; el snapshot se revisa en el diff y se commitea en el mismo commit que el test. Un snapshot ausente FALLA (syrupy es sound). |
| T-02-30 | Information Disclosure | credenciales de los contenedores de CI | accept | Se usan los valores de desarrollo ya presentes en `docker-compose.yml` (`sa`/`Admin_123`, `admin`). No se introduce ningún secreto de repositorio ni se publican los logs de conexión. |
| T-02-31 | Tampering | paso `curl | apt` del driver ODBC | mitigate | Se usa la clave y el repo oficiales de `packages.microsoft.com` sobre HTTPS y `ACCEPT_EULA=Y` explícito, con el snippet verificado en la investigación. El paso solo se ejecuta en el runner efímero de CI. |
| T-02-32 | Denial of Service | Oracle tarda 60-120 s en arrancar | accept (mitigado) | Health-check de la propia imagen + `timeout-minutes: 30`. Si el coste en PR es inaceptable, la mitigación permitida es mover el job a `push` a `main` + `workflow_dispatch`; NUNCA hacerlo advisory. |
</threat_model>

<verification>
- `uv run pytest tests/test_sql_snapshots.py tests/test_ci_harness.py -q` pasa sin base de datos.
- `uv run pytest -q -m "not integration and not optional_engine"` pasa.
- `uv run coverage json -o coverage.json && uv run python tools/ci/check_coverage_floors.py coverage.json` sale con código 0.
- `uv run ruff check encino_orm tests tools` + `uv run ruff format --check encino_orm tests tools` + `uv run mypy encino_orm` pasan.
- `grep -c "continue-on-error" .github/workflows/ci.yml` es 0.
- Verificación manual (no automatizable desde el host): rama scratch quitando el servicio MariaDB del job `test` → el job debe quedar ROJO (no skip). Precedente probado en Fase 1 (run #25).
- Verificación manual: sonda del techo ad-hoc de parámetros de MSSQL y Oracle en el job `engine-heavy`, y anotación del valor medido junto a las constantes de `dialects/strategies.py`.
</verification>

<success_criteria>
- DIAL-07: los snapshots de SQL por dialecto corren sin BD en el job SQLite siempre activo y son un gate, no advisory.
- DIAL-08: la matriz cubre MariaDB + Redis como servicios requeridos y MSSQL/Oracle en un job dedicado de una sola versión de Python, con el driver ODBC instalado explícitamente y `FREEPDB1` como service name.
- El gate de pisos por módulo prometido por la Fase 1 (D-04/D-06) existe, falla cerrado y está cableado a CI.
- Ningún job de gate usa `continue-on-error`.
</success_criteria>

<output>
Create `.planning/phases/02-dialect-seam-engine-parity/02-05-SUMMARY.md` when done
</output>
