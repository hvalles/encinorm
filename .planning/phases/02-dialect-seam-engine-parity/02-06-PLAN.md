---
phase: 02-dialect-seam-engine-parity
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - encino_orm/model/query_builder.py
  - encino_orm/model/model.py
  - encino_orm/model/types.py
  - tests/test_query_builder.py
  - tests/test_identifiers.py
  - tests/test_index.py
autonomous: true
gap_closure: true
requirements:
  - DIAL-01
  - DIAL-02
must_haves:
  truths:
    - "Un alias hostil pasado a `QueryBuilder(alias=)`, a `join()` o a `join_subquery()` lanza `ValueError` ANTES de que exista SQL y sin alcanzar el driver"
    - "El nombre de columna por defecto (el nombre de campo pydantic) se valida al construir el mapa de columnas, de modo que `to_ddl`, `indexes_ddl` e `insert_many` heredan el chequeo sin tocarlos"
    - "La allowlist sigue teniendo una única definición estricta y NO se relaja (ni se añade escape hatch para alias)"
    - "Los alias legítimos (`mm`, `r`, `sq1_mm`) y las columnas legítimas siguen produciendo exactamente el mismo SQL que antes"
  artifacts:
    - path: "encino_orm/model/query_builder.py"
      provides: "Validación de alias por la allowlist estricta en los tres puntos de entrada"
      contains: "check_identifier(alias"
    - path: "encino_orm/model/model.py"
      provides: "Validación del nombre de columna por defecto en el único punto donde se construye el mapa"
      contains: "check_identifier(name, \"nombre de columna\")"
    - path: "encino_orm/model/types.py"
      provides: "Resolución validada de las columnas de índice (sin fallback crudo)"
      contains: "columna de índice"
    - path: "tests/test_query_builder.py"
      provides: "Regresión de inyección por alias (constructor, join, join_subquery) + spy de driver inalcanzable"
      contains: "TestAliasInjection"
    - path: "tests/test_identifiers.py"
      provides: "Regresión del nombre de campo no identificador en `_column_map`/`to_ddl`/`insert_many`"
      contains: "nombre de columna inválido"
    - path: "tests/test_index.py"
      provides: "Regresión del spec de índice no mapeado y no identificador"
      contains: "columna de índice"
  key_links:
    - from: "encino_orm/model/query_builder.py"
      to: "encino_orm/dialects/identifiers.py"
      via: "import del checker y llamada en __init__/join/join_subquery antes de guardar el alias"
      pattern: "check_identifier\\(alias"
    - from: "encino_orm/model/model.py"
      to: "encino_orm/dialects/identifiers.py"
      via: "_build_column_map valida el nombre por defecto (consumido por to_ddl/insert_many/indexes_ddl)"
      pattern: "check_identifier\\(name"
    - from: "tests/test_query_builder.py"
      to: "encino_orm/model/query_builder.py"
      via: "reproducción literal del payload UNION del informe de verificación"
      pattern: "UNION SELECT nombre FROM usuarios"
---

<objective>
Cerrar el GAP 1 (BLOCKER) de `02-VERIFICATION.md`: la allowlist de identificadores está single-sourced pero **no es un cuello de botella**. `QueryBuilder` interpola sus alias (`alias=`, `join()`, `join_subquery()`) sin validar — una primitiva de inyección SQL reproducible a través de un parámetro público — y `_build_column_map` nunca valida el nombre de columna **por defecto**, que fluye a `to_ddl`, `indexes_ddl` e `insert_many`.

Purpose: la cláusula 1 del goal de la fase ("Identifier validation … live in exactly one place inside the core") es una afirmación funcional, no de conteo de definiciones. Mientras exista un camino público que llega a la interpolación SQL sin pasar por `check_identifier`, la fase no cumple su objetivo.

Output: alias validados en los tres puntos de entrada de `QueryBuilder`; nombre de columna por defecto validado en el punto único donde se construye el mapa; spec de índice no mapeado validado y fail-closed; tests de regresión que reproducen los dos bypasses del informe y prueban que ahora lanzan `ValueError`.

**Requisito previo:** los planes 02-01…02-05 ya están ejecutados sobre este árbol (`710 passed`). No se listan en `depends_on` porque no se re-ejecutan; este plan extiende el seam que construyeron.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md
@.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION.md
@.planning/phases/02-dialect-seam-engine-parity/02-REVIEW.md
@.planning/phases/02-dialect-seam-engine-parity/02-PATTERNS.md
@encino_orm/dialects/identifiers.py
@encino_orm/model/query_builder.py
@encino_orm/model/model.py
@encino_orm/model/types.py
@tests/test_query_builder.py
@tests/test_identifiers.py
@tests/test_index.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validar los alias de `QueryBuilder` por la allowlist estricta (cierra CR-01)</name>
  <files>encino_orm/model/query_builder.py, tests/test_query_builder.py</files>
  <read_first>
    - `encino_orm/model/query_builder.py` — completo (296 líneas): el docstring del módulo y `_COLUMN_RE` (líneas 1-20, la allowlist DISTINTA que sí acepta puntos), `_safe_column` (27-37), `__init__` (46-61, con `self._alias = alias` sin validar en :49), `_expand_star`/`join`/`join_subquery`/`_next_subquery_alias` (88-121, con los alias guardados sin validar en :101 y :112), y `_build_base` (176-198, con la interpolación en :178, :184, :192).
    - `encino_orm/dialects/identifiers.py` — completo (28 líneas): `IDENTIFIER_RE = re.compile(r"^[A-Za-z_][A-Za-z0-9_]*$")` y `check_identifier(value, label) -> str` (devuelve el valor; mensaje `f"{label} inválido: {value!r}"`). Es la ÚNICA definición; NO se relaja.
    - `tests/test_dialect_builders.py` líneas 245-303 — `TestRechazoPorAdaptador` / `TestAdaptadoresRechazanAntesDelDriver`: el patrón de spy que prueba que el driver nunca se alcanza (`assert reached == []`).
    - `tests/test_query_builder.py` líneas 1-130 — `_RecordingDb`, los modelos `Region`/`Agente`, `test_join` (alias `"r"`), `test_join_subquery` (alias autogenerado `sq1_mm`) y `test_order_and_limit_pagination` (el camino legítimo que debe seguir intacto).
    - `.planning/phases/02-dialect-seam-engine-parity/02-REVIEW.md` — CR-01 (reproducción exacta + sketch de fix).
    - `.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION.md` — bloque `gaps[0]`, artefactos y `missing`.
  </read_first>
  <behavior>
    - `QueryBuilder(Agente, None, alias="mm WHERE 1=0 UNION SELECT nombre FROM usuarios --")` → `ValueError` con `alias inválido` en el mensaje (el payload literal del informe).
    - `QueryBuilder(Agente, None, alias="a b")`, `alias="a.b"`, `alias="1x"`, `alias=""`, `alias=None` → `ValueError` cada uno.
    - `join(Region, "a b", on)` y `join_subquery(sub, "x; DROP TABLE t --", on)` → `ValueError`; el mensaje nombra el alias ofensor con `repr()`.
    - `QueryBuilder(Agente, None)._build_base()[0] == "FROM agentes mm"` — el alias por defecto no cambia de forma.
    - `join_subquery(sub, None, on)` sigue generando `sq1_mm` y el SQL contiene `JOIN (SELECT ...) sq1_mm ON` — no se sobre-endurece el alias autogenerado.
    - Con un doble de `Db` que registra llamadas, `await QueryBuilder(Agente, fake, alias=<payload>).all()` lanza `ValueError` y la lista de llamadas al driver queda VACÍA.
    - `QueryBuilder(Agente, db).join(Region, "r", ...)` con alias duplicado sigue lanzando `DuplicateAliasError` (comportamiento y mensaje intactos).
  </behavior>
  <action>
    **A) Import.** En `encino_orm/model/query_builder.py`, añadir el import absoluto del checker compartido junto al import existente de `Query`: `from encino_orm.dialects.identifiers import check_identifier`. Es un import de paquete cruzado (convención del repo) y no crea ciclo: `dialects/identifiers.py` solo importa `re`.

    **B) Constructor.** Sustituir `self._alias = alias` por un alias validado y construir las estructuras derivadas a partir del valor VALIDADO, no del parámetro crudo:
    `alias = check_identifier(alias, "alias")`, `self._alias = alias`, `self._aliases = {alias}`, `self._alias_to_model = {alias: model_class}`.

    **C) `join()`.** Validar el alias entrante con `check_identifier(alias, "alias")` ANTES de la comprobación de duplicado (un alias hostil debe fallar como identificador inválido, no como duplicado), y usar el valor validado en `self._aliases.add(...)`, `self._alias_to_model[...]` y en el dict que se añade a `self._joins`.

    **D) `join_subquery()`.** Cuando `alias is None`, seguir generándolo con `_next_subquery_alias()` (su salida es `sq{n}_{self._alias}`, válida porque `self._alias` ya está validado). Validar el alias FINAL con `check_identifier(alias, "alias")` antes de la comprobación de duplicado y usar el valor validado en `_aliases`, `_alias_to_model` y `_joins`.

    **E) Lo que NO se toca.**
    - No tocar `_safe_column` ni `_COLUMN_RE`: esa allowlist acepta puntos A PROPÓSITO para expresiones de columna calificadas (`mm.agente`) y unificarla sería un cambio de comportamiento (ya documentado en el docstring de `dialects/identifiers.py`). Un alias es siempre un identificador simple, así que va por la allowlist ESTRICTA.
    - No añadir un escape hatch tipo `schema=` para alias: en el `FROM`/`JOIN` generado un alias legítimo es siempre un identificador desnudo; no existe caso legítimo de alias con punto o citado. Dejar un comentario en español de una línea diciéndolo, para que nadie lo "arregle" después.
    - No cambiar el mensaje ni el tipo de `DuplicateAliasError` para el caso legítimo de alias repetido.

    **F) Tests de regresión** en `tests/test_query_builder.py`, clase nueva `TestAliasInjection` (mismo estilo que el resto del fichero: `pytest.raises`, sin `@pytest.mark.parametrize`, modelos `Agente`/`Region` ya definidos arriba):
    1. `test_alias_con_inyeccion_en_constructor_lanza` — el payload LITERAL del informe: `QueryBuilder(Agente, None, alias="mm WHERE 1=0 UNION SELECT nombre FROM usuarios --")` lanza `ValueError` y `"alias inválido"` está en el mensaje.
    2. `test_alias_no_identificador_en_constructor_lanza` — bucle sobre `["a b", "a.b", "1x", "", "a-b", None]` con `pytest.raises(ValueError)`.
    3. `test_join_con_alias_inyectado_lanza` — `QueryBuilder(Agente, None).join(Region, "a b", Filter.eq("mm.region_id", col("r.id")))` lanza `ValueError` (join es puro: no necesita db).
    4. `test_join_subquery_con_alias_inyectado_lanza` — `join_subquery(sub, "x; DROP TABLE t --", Filter.eq("mm.id", col("sq1_mm.id")))` lanza `ValueError`.
    5. `test_alias_por_defecto_y_autogenerado_siguen_validos` — `QueryBuilder(Agente, None)._build_base()[0] == "FROM agentes mm"`; y con `join_subquery(sub, None, on)` el SQL de `_build_subquery`/`_build_base` contiene `sq1_mm`.
    6. `test_el_driver_nunca_se_alcanza_con_alias_hostil` — doble de `Db` que registra llamadas (añadir a `_RecordingDb` los métodos `fetch_all`/`fetch_many` que apendan a `self.queries`), `pytest.raises(ValueError)` alrededor de `await QueryBuilder(Agente, fake, alias=payload).all()`, y `assert fake.queries == []`.
    7. `test_alias_duplicado_sigue_lanzando_duplicate_alias_error` — dos `join` con el mismo alias válido → `DuplicateAliasError` (no `ValueError`).
  </action>
  <acceptance_criteria>
    - La reproducción del informe lanza: `QueryBuilder(Agente, None, alias="mm WHERE 1=0 UNION SELECT nombre FROM usuarios --")._build_base()` eleva `ValueError` (antes devolvía `'FROM agentes mm WHERE 1=0 UNION SELECT nombre FROM usuarios --'`).
    - `uv run pytest tests/test_query_builder.py -q` pasa y `-k "alias"` selecciona al menos 6 tests (no pasa en vacío).
    - Los tests existentes de `join` (alias `"r"`) y `join_subquery` (alias `sq1_mm`) pasan SIN editarse.
    - `grep -v '^#' encino_orm/model/query_builder.py | grep -c "check_identifier(alias"` ≥ 3.
    - `grep -rn "noqa" encino_orm/model/query_builder.py` no devuelve nada (invariante `noqa == 0`).
    - `uv run ruff check encino_orm tests` y `uv run mypy encino_orm` salen 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_query_builder.py -q -k "alias" && uv run python -c "from encino_orm.model import Model, QueryBuilder; exec('class T(Model):\n    _table = \'t\'\n');\nimport sys\ntry:\n    QueryBuilder(T, None, alias='mm WHERE 1=0 UNION SELECT nombre FROM usuarios --')._build_base()\nexcept ValueError as e:\n    assert 'alias inválido' in str(e), str(e)\n    print('OK: alias hostil rechazado ->', e)\nelse:\n    sys.exit('FALLO: la inyección por alias sigue aceptándose')"</automated>
  </verify>
  <done>Ningún alias llega a `FROM`/`JOIN` sin pasar por `check_identifier`; el payload UNION del informe de verificación lanza `ValueError` antes de construir SQL y sin alcanzar el driver; los alias legítimos (`mm`, `r`, `sq1_mm`) producen el mismo SQL que antes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validar el nombre de columna por defecto y cerrar el fallback crudo de `indexes_ddl` (cierra WR-06)</name>
  <files>encino_orm/model/model.py, encino_orm/model/types.py, tests/test_identifiers.py, tests/test_index.py</files>
  <read_first>
    - `encino_orm/model/model.py` líneas 212-252 — `_column_map` (caché `_COLUMN_MAPS`), `_build_column_map` (el `col = name` sin validar en :231 y el `check_identifier(col_name, ...)` del override explícito en :235), `_col`, `_field_of`, `_pk_fields`/`_pk_cols`/`_is_auto_pk`.
    - `encino_orm/model/model.py` líneas 509-559 — `insert_many`: el orden exacto es `if not rows: return 0` → `db = db or resolve_db()` → `col_map = cls._column_map()` (línea 524) → DML en línea con `cls._table` y `columns` en :556. Es el consumidor que hoy interpola identificadores sin validar.
    - `encino_orm/model/types.py` líneas 152-233 — `to_ddl` (lee `model_class._column_map()` en :169 y `_col(...)` en :179/:190) y `indexes_ddl` (los dos fallbacks crudos `col_map.get(col, col)` en :221 y `col_map.get(spec, spec)` en :223, y el `check_identifier(name, "nombre de índice")` en :226 que valida SOLO el nombre del índice).
    - `encino_orm/dialects/identifiers.py` — completo: `check_identifier` devuelve el valor validado y usa `f"{label} inválido: {value!r}"`.
    - `tests/test_identifiers.py` — completo (83 líneas): la tabla accept/reject, el test de mensaje exacto y el guard de fuente de definición única (que debe seguir pasando).
    - `tests/test_index.py` líneas 25-95 — `indexes_ddl` con `Column(name=...)` (`test_column_mapping` en :53-60) y las variantes de dirección; ninguna usa un spec desconocido.
    - `.planning/phases/02-dialect-seam-engine-parity/02-REVIEW.md` — WR-06 (reproducción con `create_model` + `to_ddl`).
  </read_first>
  <behavior>
    - Un modelo creado dinámicamente con un campo llamado `"a; DROP TABLE x --"` lanza `ValueError` con `nombre de columna inválido` desde `_column_map()`.
    - El MISMO modelo lanza desde `to_ddl(M, "sqlite")` y desde `await M.insert_many(db=<doble>, rows=[{}])` — ambos consumidores heredan el chequeo sin haber sido tocados.
    - Un modelo dinámico normal (`{"a": (str | None, None)}`) sigue mapeando: `_column_map()["a"] == "a"`.
    - `indexes_ddl` con `Index("a; DROP TABLE x --", name="idx_ok")` lanza `ValueError` con `columna de índice` en el mensaje (el nombre del índice es válido, así que el fallo es de la COLUMNA).
    - `indexes_ddl` con `Index("agente")` sobre un campo mapeado a `Column(name="nombre")` sigue produciendo `ON a (nombre)` (camino legítimo byte-idéntico).
  </behavior>
  <action>
    **A) Un único chequeo, todos los consumidores.** En `_build_column_map` (`encino_orm/model/model.py`), sustituir `col = name` por `col = check_identifier(name, "nombre de columna")`. El override explícito (`if col_name: col = check_identifier(col_name, "nombre de columna")`) se queda como está. `check_identifier` devuelve el valor, así que la forma del mapa y todos los consumidores no cambian.

    Añadir un comentario en español de dos líneas explicando POR QUÉ aquí y no en cada consumidor: el mapa es la única fuente de verdad de los nombres físicos de columna, así que validar en su construcción cierra `to_ddl` (DDL) e `insert_many` (DML en línea, fuera de `dialects/builders.py`) sin abrir dos frentes que puedan divergir.

    **B) Cerrar el último fallback crudo.** En `indexes_ddl` (`encino_orm/model/types.py`), los dos `col_map.get(..., ...)` interpolan tal cual un string del llamador cuando no es un campo mapeado. Sustituir ambos por una resolución validada: resolver contra `col_map` y, si no está, validar el spec con `check_identifier(spec, "columna de índice")` antes de añadirlo. NO se relaja ninguna allowlist: un spec que no es campo mapeado ni identificador simple ahora lanza `ValueError` (fail-closed), coherente con la política de la fase (el escape hatch para identificadores citados o expresiones es `Query` crudo, no un fallback silencioso). Aplicar la misma resolución a la rama con dirección `(col, "ASC"|"DESC")`.

    No cambiar el cuerpo de `to_ddl` (ya lee el mapa validado) y no tocar `encino_orm/sql.py:_COLUMN_RE` ni `query_builder._COLUMN_RE`.

    **C) Tests de regresión.**
    - `tests/test_identifiers.py` — clase nueva `TestNombreDeColumnaPorDefecto`:
      1. `test_column_map_rechaza_nombre_de_campo_no_identificador` — `from pydantic import create_model`; `M = create_model("M", __base__=Model, **{"a; DROP TABLE x --": (str | None, None)})`; `M._table = "t"`; `pytest.raises(ValueError)` alrededor de `M._column_map()`, con `"nombre de columna inválido"` en el mensaje y el `repr()` del nombre ofensor.
      2. `test_to_ddl_hereda_la_validacion` — el mismo modelo, `pytest.raises(ValueError)` alrededor de `to_ddl(M, "sqlite")`.
      3. `test_insert_many_hereda_la_validacion` — `@pytest.mark.asyncio`; `await M.insert_many(db=object(), rows=[{}])` lanza `ValueError` (el mapa se computa antes de tocar la db, así que `object()` como doble es suficiente y el test es DB-free).
      4. `test_modelo_dinamico_normal_sigue_mapeando` — `{"a": (str | None, None)}` → `_column_map()["a"] == "a"`.
      Nota: el guard de definición única (`test_allowlist_tiene_una_unica_definicion_en_la_fuente`) debe seguir pasando — este plan no añade ninguna copia del literal del allowlist.
    - `tests/test_index.py` — dos tests nuevos:
      5. `test_columna_de_indice_no_mapeada_no_identificadora_lanza` — modelo con `_indexes = [Index("a; DROP TABLE x --", name="idx_ok")]`; `pytest.raises(ValueError)` con `"columna de índice"` en el mensaje. El nombre del índice debe ser explícito y válido para que el chequeo del nombre no enmascare el de la columna.
      6. `test_columna_de_indice_no_mapeada_pero_identificadora_se_acepta` — `Index("no_existe", name="idx_ok2")` produce `ON t (no_existe)` (un identificador simple sigue siendo aceptable: solo se cierra el fallback que interpolaba CUALQUIER string).
  </action>
  <acceptance_criteria>
    - La reproducción de WR-06 lanza: el modelo dinámico con el campo `'a; DROP TABLE x --'` eleva `ValueError` desde `_column_map()` y desde `to_ddl()`.
    - `uv run pytest tests/test_identifiers.py tests/test_index.py -q` pasa y `-k "columna"` selecciona al menos 3 tests.
    - `tests/test_index.py::TestIndexes::test_column_mapping` pasa SIN editarse (`ON a (nombre)`).
    - `grep -v '^#' encino_orm/model/model.py | grep -c "check_identifier(name"` == 1.
    - `grep -v '^#' encino_orm/model/types.py | grep -c "col_map.get(col, col)\|col_map.get(spec, spec)"` == 0.
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa (el chequeo nuevo no rompe ningún modelo existente).
    - `uv run ruff check encino_orm tests` y `uv run mypy encino_orm` salen 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_identifiers.py tests/test_index.py -q && uv run python -c "from pydantic import create_model; from encino_orm.model import Model; from encino_orm.model.types import to_ddl; M = create_model('M', __base__=Model, **{'a; DROP TABLE x --': (str | None, None)}); M._table = 't'; ok = 0\nfor fn in (lambda: M._column_map(), lambda: to_ddl(M, 'sqlite')):\n    try:\n        fn()\n    except ValueError as e:\n        assert 'nombre de columna inválido' in str(e), str(e)\n        ok += 1\n    else:\n        raise SystemExit('FALLO: identificador no validado')\nprint('OK: mapa y DDL rechazan el nombre hostil', ok)"</automated>
  </verify>
  <done>El nombre de columna por defecto se valida en el único punto donde se construye el mapa, de modo que `to_ddl`, `insert_many` e `indexes_ddl` son fail-closed ante identificadores no válidos; ningún string del llamador llega a la DDL sin pasar por la allowlist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API pública → interpolación SQL | `QueryBuilder(alias=)` y `join(alias=)` son parámetros públicos documentados que hoy llegan crudos a `FROM`/`JOIN`. |
| definición de modelo → DDL/DML | El nombre de campo pydantic (y el spec de columna de índice) es entrada del desarrollador que hoy se interpola sin validar en `CREATE TABLE` e `INSERT` multi-fila. |
| allowlist → resto del core | `dialects/identifiers.py` es la única fuente de verdad; cualquier camino que la salte rompe la cláusula 1 del goal de la fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-34 | Tampering / Elevation of Privilege (**high**) | `QueryBuilder.__init__`, `join()`, `join_subquery()` — alias interpolado en `FROM`/`JOIN` | mitigate | `check_identifier(alias, "alias")` (allowlist estricta) en los TRES puntos de entrada, antes de guardar el alias y antes de la comprobación de duplicado; `_aliases`/`_alias_to_model`/`_joins` se construyen con el valor validado. Sin escape hatch: un alias legítimo es siempre un identificador desnudo. Test de regresión que reproduce el payload literal del informe (`'mm WHERE 1=0 UNION SELECT nombre FROM usuarios --'`) y prueba que lanza `ValueError`, más un spy que demuestra que `fetch_all`/`fetch_many` reciben CERO llamadas. |
| T-02-35 | Tampering | `Model._build_column_map` — nombre de columna por defecto → `to_ddl` / `insert_many` | mitigate | `col = check_identifier(name, "nombre de columna")` en el punto único de construcción del mapa; tests que reproducen el campo `'a; DROP TABLE x --'` y verifican `ValueError` desde `_column_map()`, `to_ddl()` e `insert_many()`. |
| T-02-36 | Tampering | `indexes_ddl` — spec de columna no mapeado interpolado crudo | mitigate | El fallback `col_map.get(spec, spec)` se sustituye por una resolución validada (`check_identifier(spec, "columna de índice")`); fail-closed ante un string arbitrario. Test con nombre de índice explícito y válido para que el chequeo del nombre no enmascare el de la columna. |
| T-02-37 | Tampering | Relajación de la allowlist para "aceptar" alias o nombres cualificados | mitigate | No se modifica `IDENTIFIER_RE` ni `_COLUMN_RE`: el patrón del allowlist está pinado por `tests/test_identifiers.py::test_identificador_re_es_allowlist_estricta` y el guard de fuente exige una única definición. `grep` de criterios de aceptación confirman que las llamadas nuevas usan `check_identifier`, no una regex propia. |
| T-02-38 | Repudiation | Falso positivo por validar solo el nombre del índice | mitigate | El test de `indexes_ddl` usa `Index(..., name="idx_ok")` para que el único fallo posible sea el de la columna; se documenta en el propio test. |
| T-02-SC | Tampering | Instalaciones de paquetes | n/a | Este plan NO instala ni actualiza dependencias (no hay `uv add`, no se toca `pyproject.toml`). La compuerta de legitimidad de paquetes no aplica. |
</threat_model>

<verification>
- `uv run pytest tests/test_query_builder.py tests/test_identifiers.py tests/test_index.py -q` pasa.
- `uv run pytest -q -m "not integration and not optional_engine"` pasa (ningún modelo existente se rompe con la validación del nombre por defecto).
- Las DOS reproducciones de `02-VERIFICATION.md` (CR-01 y WR-06) lanzan `ValueError` — comandos en los `<verify>` de cada tarea.
- `uv run ruff check encino_orm tests tools` + `uv run ruff format --check encino_orm tests tools` + `uv run mypy encino_orm` salen 0.
- `grep -rn "noqa" encino_orm/` sigue vacío (invariante de Fase 1).
- `uv run pytest -q` (suite completa con motores locales) pasa.
</verification>

<success_criteria>
- GAP 1 cerrado: la allowlist es un cuello de botella REAL para los alias de `QueryBuilder` y para los nombres de columna derivados del modelo.
- Ambas reproducciones del informe de verificación ahora lanzan `ValueError` con mensaje en español y sin alcanzar el driver.
- La allowlist no se ha relajado y sigue teniendo una única definición (guard de fuente verde).
- Los caminos legítimos (alias `mm`/`r`/`sq1_mm`, `Column(name=...)`, campos normales) producen el mismo SQL que antes.
</success_criteria>

<output>
Create `.planning/phases/02-dialect-seam-engine-parity/02-06-SUMMARY.md` when done
</output>
