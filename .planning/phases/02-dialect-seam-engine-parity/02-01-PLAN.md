---
phase: 02-dialect-seam-engine-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - encino_orm/dialects/__init__.py
  - encino_orm/dialects/identifiers.py
  - encino_orm/base.py
  - encino_orm/sqlite.py
  - encino_orm/mysql.py
  - encino_orm/model/model.py
  - encino_orm/model/types.py
  - encino_orm/transfer.py
  - encino_orm/sql.py
  - encino_orm/model/query_builder.py
  - tests/test_identifiers.py
autonomous: true
requirements:
  - DIAL-01
must_haves:
  truths:
    - "Existe exactamente UNA definición del allowlist de identificadores en todo `encino_orm/`"
    - "Todos los módulos que antes duplicaban el allowlist (6 regex + 2 checkers) importan la definición única"
    - "El comportamiento observable es idéntico: la suite existente pasa sin editar un solo test existente"
    - "Los mensajes de error de identificador inválido conservan el texto exacto en español"
  artifacts:
    - path: "encino_orm/dialects/identifiers.py"
      provides: "IDENTIFIER_RE + check_identifier: allowlist estricta y único punto de validación"
      contains: "def check_identifier"
    - path: "encino_orm/dialects/__init__.py"
      provides: "Barrel del seam de dialectos"
      exports: ["IDENTIFIER_RE", "check_identifier"]
    - path: "tests/test_identifiers.py"
      provides: "Tabla accept/reject + guard de definición única a nivel de fuente"
      min_lines: 40
  key_links:
    - from: "encino_orm/base.py"
      to: "encino_orm/dialects/identifiers.py"
      via: "Db._check_identifier delega (los 5 adaptadores que llaman self._check_identifier quedan intactos)"
      pattern: "from \\.dialects\\.identifiers import check_identifier"
    - from: "encino_orm/transfer.py"
      to: "encino_orm/dialects/identifiers.py"
      via: "se borra la copia local (regex + checker) y se importa la compartida"
      pattern: "check_identifier"
    - from: "encino_orm/model/model.py"
      to: "encino_orm/dialects/identifiers.py"
      via: "los 5 sitios `_IDENTIFIER_RE.match(...)` pasan a `check_identifier(..., label)`"
      pattern: "check_identifier\\("
---

<objective>
Centralizar `_IDENTIFIER_RE` / `_check_identifier` en un único módulo (`encino_orm/dialects/identifiers.py`) y reescribir los **6 importadores** (+2 módulos con `_COLUMN_RE` que solo reciben un comentario), como **refactor puro con CERO cambio de comportamiento**, en sus propios commits.

Purpose: crear el único cuello de botella de validación de identificadores ANTES de añadir cualquier validación nueva. Si la centralización y la validación se mezclan, el cambio de seguridad deja de ser bisecable y la validación se aplica a uno de los seis dialectos y falta en los otros cinco (ROADMAP "Hard Ordering Constraints" #2, PITFALLS.md Pitfall 10).

Output: paquete `encino_orm/dialects/` con la definición única; los **6 módulos** importándola (`base.py`, `sqlite.py`, `mysql.py`, `model/model.py`, `model/types.py`, `transfer.py`), y `sql.py`/`model/query_builder.py` conservando su `_COLUMN_RE` con un comentario que explica por qué NO se unifica; `tests/test_identifiers.py` con la tabla accept/reject y el guard de fuente que hace imposible reintroducir una copia.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md
@.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md
@.planning/phases/02-dialect-seam-engine-parity/02-PATTERNS.md
@.planning/phases/02-dialect-seam-engine-parity/02-VALIDATION.md
@encino_orm/base.py
@encino_orm/transfer.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear el módulo central de identificadores y su tabla accept/reject</name>
  <files>encino_orm/dialects/identifiers.py, encino_orm/dialects/__init__.py, tests/test_identifiers.py</files>
  <read_first>
    - `encino_orm/base.py` (líneas 1-70): la definición canónica a mover — `_IDENTIFIER_RE` en la línea 13 y `Db._check_identifier` en las líneas 60-65, con el mensaje `f"{label} inválido: {value!r}"`.
    - `encino_orm/transfer.py` (líneas 1-30): la copia duplicada (regex + checker re-implementado) que 02-01 elimina.
    - `.planning/phases/02-dialect-seam-engine-parity/02-PATTERNS.md` — sección "`encino_orm/dialects/identifiers.py` (utility, pure transform)": naming público, patrón de imports y la nota de que `sql.py:_COLUMN_RE` NO se toca.
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — Pitfall A y §Code Examples "`dialects/identifiers.py` (02-01, pure move)".
    - `tests/test_engine.py` (líneas 1-80): estilo de test puro, sin BD, con `pytest.raises`.
    - `encino_orm/model/__init__.py`: patrón de barrel (bloque de imports + `__all__` alfabético).
    - `pyproject.toml` (`[tool.pytest.ini_options]`, `[tool.ruff.lint]`): los markers ya están registrados y `--strict-markers` está activo.
  </read_first>
  <behavior>
    - `check_identifier("t", "tabla")` devuelve `"t"`; igual para `"_x"`, `"a1"`, `"T_1"`.
    - `check_identifier(v, "tabla")` lanza `ValueError` para `v` en: `"t; DROP TABLE x; --"`, `` "`x`" ``, `"a.b"`, `"1abc"`, `""`, `"a b"`, `"a-b"`, `"t;--"`, `"tabla\n"`, `None`, `123`.
    - El mensaje del `ValueError` contiene `repr(v)` (convención del repo: el valor ofensivo vía `!r`).
    - `check_identifier("t", "nombre de tabla")` produce exactamente `"nombre de tabla inválido: 't;--'"` para `"t;--"`.
  </behavior>
  <action>
    Crear el paquete `encino_orm/dialects/` con DOS archivos y un test.

    `encino_orm/dialects/identifiers.py`: importar SOLO `re` (stdlib; el contrato de importación diferida prohíbe cualquier dependencia de driver o capa opcional). Declarar `IDENTIFIER_RE = re.compile(r"^[A-Za-z_][A-Za-z0-9_]*$")` (nombre público, sin guion bajo). Declarar `check_identifier(value: str, label: str) -> str` copiando **verbatim** el cuerpo de `Db._check_identifier` de `encino_orm/base.py:60-65`: comprobación `isinstance(value, str)` + `IDENTIFIER_RE.match(value)`, `raise ValueError(f"{label} inválido: {value!r}")`, `return value`. Docstring en español explicando el POR QUÉ: es el único punto de validación de identificadores interpolados; la allowlist NO se relaja nunca para aceptar `schema.tabla` (Pitfall 10) porque ampliar la clase de caracteres reabre la superficie de inyección — el caso legítimo de nombre cualificado se cubre con un parámetro `schema=` explícito que se valida por separado en 02-02. Sin secciones `Args:`/`Returns:`.

    `encino_orm/dialects/__init__.py`: barrel que importa `IDENTIFIER_RE` y `check_identifier` desde `.identifiers` y los lista en `__all__` en orden alfabético. No re-exportar nada más en este plan (los builders llegan en 02-02).

    `tests/test_identifiers.py`: test puro sin BD, con `pytest.raises(ValueError)`. Tabla de aceptación (`"t"`, `"_x"`, `"a1"`, `"T_1"`) que comprueba el valor devuelto; tabla de rechazo (los 11 valores del bloque `<behavior>`) que comprueba `ValueError` y que `repr(v)` aparece en `str(exc.value)`. Añadir además un caso que fija el mensaje completo con un label compuesto (`"nombre de tabla"`) para blindar el texto en español. `tests/**` ya ignora `PT011` en `pyproject.toml`, así que `pytest.raises(ValueError)` es correcto.

    NO añadir ninguna llamada de validación a builders, adaptadores ni a `sync_schema`: eso es 02-02 y mezclarlo destruye la bisecabilidad. NO tocar `encino_orm/sql.py` ni `encino_orm/model/query_builder.py`. NO editar `encino_orm/__init__.py`: el seam es interno, la superficie pública sigue siendo `Db.insert/update/delete`.
  </action>
  <acceptance_criteria>
    - `encino_orm/dialects/identifiers.py` existe y contiene el literal `re.compile(r"^[A-Za-z_][A-Za-z0-9_]*$")` exactamente una vez.
    - `uv run python -c "from encino_orm.dialects.identifiers import IDENTIFIER_RE, check_identifier; assert check_identifier('t','tabla') == 't'"` sale con código 0.
    - `uv run python -c "from encino_orm.dialects import IDENTIFIER_RE, check_identifier"` sale con código 0 (el barrel re-exporta ambos símbolos).
    - Cada valor de la tabla de rechazo produce `ValueError` y `repr(valor)` está presente en el mensaje.
    - `uv run ruff check encino_orm tests` sale con código 0 (sin F401 en el nuevo paquete).
    - `uv run mypy encino_orm` sale con código 0 (los módulos nuevos NO están en ningún `ignore_errors` de `pyproject.toml`).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_identifiers.py -x -q</automated>
  </verify>
  <done>`encino_orm/dialects/identifiers.py` + `encino_orm/dialects/__init__.py` + `tests/test_identifiers.py` existen; el test pasa; ruff y mypy limpios. Ninguna validación nueva se ha conectado todavía a ningún punto de llamada.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar los 6 importadores y añadir el guard de definición única</name>
  <files>encino_orm/base.py, encino_orm/sqlite.py, encino_orm/mysql.py, encino_orm/model/model.py, encino_orm/model/types.py, encino_orm/transfer.py, encino_orm/sql.py, encino_orm/model/query_builder.py, tests/test_identifiers.py</files>
  <read_first>
    - `encino_orm/base.py` (líneas 1-70 y 135-178): `_IDENTIFIER_RE:13`, `_check_identifier:60-65`, y `list_tables:158` (contexto de que este plan NO toca el bug de `COUNT(*)`).
    - `encino_orm/sqlite.py` (líneas 1-20 y 107-130): `_IDENTIFIER_RE:13`, `columns_of:114-117`.
    - `encino_orm/mysql.py` (líneas 1-20 y 115-130): `_IDENTIFIER_RE:15`, `columns_of:121-123`.
    - `encino_orm/model/model.py` (líneas 1-35, 219-250, 744-756, 840-853): `_IDENTIFIER_RE:29` y los 5 puntos de llamada con sus mensajes actuales.
    - `encino_orm/model/types.py` (líneas 195-237): `_IDENTIFIER_RE:201`, `indexes_ddl:227-230`, y comprobar si `re` se usa para algo más.
    - `encino_orm/transfer.py` (líneas 1-30 y 100-150): la copia local y sus 4 puntos de llamada.
    - `encino_orm/sql.py` (líneas 1-50): `_COLUMN_RE:8`, que NO se toca.
    - `encino_orm/model/query_builder.py` (líneas 1-35): `_COLUMN_RE:16`, que NO se toca.
    - `.planning/phases/02-dialect-seam-engine-parity/02-PATTERNS.md` — tabla "Call-site styles to update (6 regexes, 2 checker shapes)".
  </read_first>
  <action>
    Reescribir los 6 importadores para que usen la definición única, **preservando byte a byte cada mensaje de error actual**.

    `encino_orm/base.py`: borrar `_IDENTIFIER_RE` (línea 13); añadir `from .dialects.identifiers import check_identifier`; convertir `Db._check_identifier` en un `@staticmethod` de una sola línea que delega en `check_identifier(value, label)`. Esto deja intactos los 5 adaptadores que llaman `self._check_identifier(save_point, "savepoint")` (`sqlite.py:90,97`, `mysql.py:97,104`, `postgresql.py:105,112`, `mssql.py:126,134`, `oracle.py:120,128`). **Borrar también el `import re` de `base.py`**: en ese módulo `re` se usa EXCLUSIVAMENTE para `_IDENTIFIER_RE` (verificado: no hay ninguna otra referencia a `re.` en el fichero), así que dejarlo dispara `F401`. La definición de la regex vive ahora en `dialects/identifiers.py`, que es quien importa `re`.

    `encino_orm/sqlite.py` y `encino_orm/mysql.py`: borrar `_IDENTIFIER_RE`; en `columns_of` sustituir `_IDENTIFIER_RE.match(table)` por `check_identifier(table, "nombre de tabla")`, que reproduce el mensaje actual `nombre de tabla inválido: {table!r}`. Conservar `_PLACEHOLDER_RE` y `import re` (se siguen usando).

    `encino_orm/model/model.py`: borrar `_IDENTIFIER_RE` (línea 29); importar `check_identifier`; sustituir los 5 puntos de llamada usando **exactamente** los labels que reproducen los mensajes de hoy:
    - línea 223 → `check_identifier(cls._table, "nombre de tabla")` (mensaje actual: `nombre de tabla inválido: ...`)
    - línea 235 → `check_identifier(col_name, "nombre de columna")`
    - línea 244 → `check_identifier(col, "nombre de columna")`
    - línea 752 → `check_identifier(c, "columna")` (mensaje actual: `columna inválida: ...`)
    - línea 847 → `check_identifier(col, "columna de orden")` (mensaje actual: `columna de orden inválida: ...`)

    `encino_orm/model/types.py`: borrar `_IDENTIFIER_RE` (línea 201) y el `import re` si queda sin uso (solo se usa en esa línea → F401 si se deja); sustituir `_IDENTIFIER_RE.match(name)` por `check_identifier(name, "nombre de índice")` (mensaje actual: `nombre de índice inválido: ...`).

    `encino_orm/transfer.py`: borrar el bloque de las líneas 16-23 completo (regex local + `_check_identifier` local) y el `import re` (sin uso tras el borrado); importar `check_identifier` desde `.dialects.identifiers`; renombrar los 4 puntos de llamada (`106` y `144` → label `"tabla"`; `109` y `112` → label `"columna"`).

    `encino_orm/sql.py` y `encino_orm/model/query_builder.py`: NO cambiar la regex. Añadir junto a `_COLUMN_RE` un comentario en español explicando el por qué de no unificarla: es una allowlist DISTINTA que acepta a propósito nombres cualificados con punto (`a.b`) para `db.fn.*` y para expresiones de `QueryBuilder`; unificarla con la estricta sería un cambio de comportamiento (prohibido en este plan) y relajarla sería el Pitfall 10.

    `tests/test_identifiers.py`: añadir el guard de fuente — recorrer `encino_orm/**/*.py` (excluyendo `__pycache__`) y afirmar que exactamente 1 fichero contiene el literal `re.compile(r"^[A-Za-z_][A-Za-z0-9_]*$")` y que ese fichero es `encino_orm/dialects/identifiers.py`. Usar `pathlib.Path.rglob` y normalizar separadores `\` → `/` para que el guard funcione igual en Windows y en Linux.

    Comentarios y docstrings en español explicando el POR QUÉ. Sin secciones `Args:`/`Returns:`.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import pathlib,re; p=[f for f in pathlib.Path('encino_orm').rglob('*.py') if '^[A-Za-z_][A-Za-z0-9_]*$' in f.read_text(encoding='utf-8')]; print(p)"` imprime exactamente `[WindowsPath('encino_orm/dialects/identifiers.py')]`.
    - `grep -rn "_IDENTIFIER_RE" encino_orm/` no devuelve ninguna coincidencia.
    - `grep -rn "_check_identifier" encino_orm/` devuelve solo las llamadas `self._check_identifier(...)` de los 5 adaptadores y la definición delegada en `base.py`.
    - `uv run ruff check encino_orm tests` sale con código 0 (sin F401 por imports `re` huérfanos).
    - `uv run mypy encino_orm` sale con código 0.
    - `uv run pytest tests/test_identifiers.py -x -q` pasa, incluido el guard de fuente.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_identifiers.py -x -q</automated>
  </verify>
  <done>Los 6 módulos importan la definición única; no queda ninguna copia de la regex ni del checker; `sql.py`/`model/query_builder.py` conservan su `_COLUMN_RE` con el comentario del por qué; el guard de fuente lo demuestra; ruff y mypy limpios.</done>
</task>

<task type="auto">
  <name>Task 3: Probar que el refactor es puro (suite existente sin editar + SQL generado idéntico)</name>
  <files>tests/test_identifiers.py</files>
  <read_first>
    - `tests/test_postgresql.py` (líneas 1-60), `tests/test_mssql.py` (líneas 20-70), `tests/test_oracle.py` (líneas 25-70), `tests/test_sqlite.py`, `tests/test_mysql.py`: las aserciones golden-string que son el oráculo de "SQL generado idéntico".
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — Pitfall B ("the validation commit is not actually separate, or not actually bisectable").
    - `.planning/phases/02-dialect-seam-engine-parity/02-VALIDATION.md` — fila `02-01-02`.
  </read_first>
  <action>
    Producir la evidencia de que 02-01 es un refactor puro, y dejarla registrada en el SUMMARY de este plan.

    1) Ejecutar la suite completa y confirmar que pasa. 2) Confirmar con `git diff --stat` que **ningún fichero de `tests/` preexistente fue modificado** en este plan: el único cambio bajo `tests/` debe ser el fichero NUEVO `tests/test_identifiers.py`. Si algún test preexistente aparece modificado, eso es un hallazgo: revertir la edición y arreglar el código, no el test. 3) Confirmar que el diff de este plan no contiene ninguna aserción de SQL modificada. 4) Confirmar que el mensaje de commit de este plan NO contiene la palabra "validate" ni "validación" (el commit de validación es 02-02).

    No añadir tests nuevos de comportamiento en esta tarea más allá del guard de fuente ya creado; esta tarea es de verificación, y su entregable es la evidencia.
  </action>
  <acceptance_criteria>
    - `git diff --stat HEAD~1 -- tests/` (o el rango equivalente de los commits de este plan) muestra únicamente `tests/test_identifiers.py` como añadido y ningún otro fichero de `tests/` como modificado.
    - `git diff` sobre `tests/test_postgresql.py`, `tests/test_mssql.py`, `tests/test_oracle.py`, `tests/test_mysql.py`, `tests/test_sqlite.py` está vacío.
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa (0 fallos, 0 skips no marcados).
    - El mensaje de commit del plan no contiene `validate`/`validación`/`validation`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q -m "not integration and not optional_engine"</automated>
  </verify>
  <done>La suite pasa sin haber editado un solo test existente; las aserciones golden-string de los 5 ficheros de motor están intactas; la evidencia queda en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description
|----------|------------|
| código de la librería → SQL interpolado | Los identificadores (tabla/columna/savepoint/índice) se concatenan al SQL; los valores nunca. Este plan mueve la única función que decide si un identificador es aceptable. |
| fuente del repo → superficie de validación | Un fichero con una copia local de la regex puede quedar sin migrar y validar con una política divergente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `dialects/identifiers.py` (allowlist) | mitigate | La allowlist `^[A-Za-z_][A-Za-z0-9_]*$` se mueve verbatim, sin ampliar la clase de caracteres. Relajarla para aceptar `schema.tabla` está prohibido (Pitfall 10); el caso cualificado se resolverá con un parámetro `schema=` validado aparte en 02-02. |
| T-02-02 | Tampering / Repudiation | los 6 importadores (+2 módulos con `_COLUMN_RE` que no se unifican) | mitigate | Guard de fuente en `tests/test_identifiers.py`: exactamente 1 fichero bajo `encino_orm/` contiene la regex. Reintroducir una copia rompe el test. |
| T-02-03 | Tampering | `transfer.py` (checker duplicado) | mitigate | Se borra la implementación local y se importa la compartida; sus 4 puntos de llamada conservan label y mensaje, así que un identificador derivado de introspección sigue validándose. |
| T-02-04 | Tampering | `sql.py:_COLUMN_RE` / `model/query_builder.py:_COLUMN_RE` | accept | Son allowlists distintas que aceptan punto a propósito. Unificarlas sería un cambio de comportamiento (violaría el requisito de refactor puro); se documenta con un comentario el por qué y se deja como candidato de seguimiento. |
| T-02-05 | Repudiation | commits del plan | mitigate | El commit de este plan no debe contener "validate": la validación es 02-02, en un commit separado y bisecable (ROADMAP constraint #2). |
| T-02-06 | Tampering | `# noqa` / `# nosec` rot | mitigate | El invariante de Fase 1 (`noqa == 0` en `encino_orm/`) se preserva: el plan no introduce supresiones; ruff `S` sigue activo y este plan no añade ninguna regla a per-file-ignores. |
</threat_model>

<verification>
- `uv run pytest -q -m "not integration and not optional_engine"` pasa.
- `uv run ruff check encino_orm tests` y `uv run ruff format --check encino_orm tests` pasan.
- `uv run mypy encino_orm` pasa (los módulos nuevos se chequean estrictamente).
- Guard de fuente: exactamente un fichero con la allowlist literal.
- Ningún test preexistente modificado.
</verification>

<success_criteria>
- DIAL-01 cumplido: `_IDENTIFIER_RE`/`check_identifier` viven en `encino_orm/dialects/identifiers.py` y los 6 importadores los usan (`sql.py`/`model/query_builder.py` quedan documentados como allowlist distinta, sin unificar).
- La suite existente pasa sin una sola edición de test (prueba de refactor puro).
- Existe un guard automático que impide reintroducir una copia del allowlist.
- Cero cambios de comportamiento: ningún mensaje de error ni SQL generado cambia.
</success_criteria>

<output>
Create `.planning/phases/02-dialect-seam-engine-parity/02-01-SUMMARY.md` when done
</output>
