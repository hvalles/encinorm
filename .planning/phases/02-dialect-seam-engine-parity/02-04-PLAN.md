---
phase: 02-dialect-seam-engine-parity
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - encino_orm/base.py
  - encino_orm/model/model.py
  - encino_orm/model/query_builder.py
  - tests/test_aggregates.py
  - tests/test_query_builder.py
  - tests/test_sqlite.py
  - tests/test_mysql.py
  - tests/test_mariadb.py
  - tests/test_postgresql.py
  - tests/test_mssql.py
  - tests/test_oracle.py
autonomous: true
requirements:
  - DIAL-03
  - DIAL-09
must_haves:
  truths:
    - "Los SIETE lectores de agregados devuelven resultados correctos en PostgreSQL, SQL Server y Oracle (no solo en SQLite/MySQL)"
    - "Ningún lector indexa el resultado por el texto de la expresión: todos leen `row[\"n\"]` de una expresión aliasada `AS n`"
    - "Existen tests de integración por motor para `count`/`paginate`/`list_tables`/`sync_schema`/`last_id` en los seis motores"
    - "El arreglo se aplica en el SQL, no normalizando claves en un adaptador"
  artifacts:
    - path: "encino_orm/base.py"
      provides: "`list_tables` con `SELECT COUNT(*) AS n` y lectura de `row[\"n\"]`"
      contains: "AS n FROM"
    - path: "encino_orm/model/model.py"
      provides: "`Model.count` aliasado"
      contains: "SELECT COUNT(*) AS n"
    - path: "encino_orm/model/query_builder.py"
      provides: "count/sum/avg/min/max aliasados a `n`"
      contains: "AS n"
    - path: "tests/test_aggregates.py"
      provides: "Guard unitario: el SQL generado contiene `AS n` y los agregados devuelven el valor correcto en SQLite"
  key_links:
    - from: "encino_orm/model/query_builder.py"
      to: "encino_orm/_rows.py"
      via: "los labels de MSSQL/Oracle se normalizan a minúsculas y asyncpg ya devuelve minúsculas: el alias fijo `n` es el único contrato estable"
      pattern: "row\\[\"n\"\\]"
    - from: "tests/test_postgresql.py"
      to: "encino_orm/model/model.py"
      via: "test de integración que ejercita Model.count/paginate contra PostgreSQL real"
      pattern: "count\\(|paginate\\("
---

<objective>
Arreglar el bug de la clave de resultado de los agregados en sus **SIETE** sitios — no en los tres que nombra la letra de DIAL-03 — y añadir la prueba de paridad real por motor para `count`/`paginate`/`list_tables`/`sync_schema`/`last_id`.

Purpose: `SELECT COUNT(*)` sin alias hace que cada driver devuelva un label distinto (`COUNT(*)` en SQLite/MySQL, `count` en asyncpg, minúsculas forzadas en MSSQL/Oracle vía `_rows.py:14`). El patrón correcto ya existe en `base.py:174-177` (`AS n` → `row["n"]`). Dejar `QueryBuilder.sum/avg/min/max` sin arreglar falsifica el propio objetivo de la fase ("resultados correctos en los seis motores"): fallan con el mismo `KeyError` en los mismos tres motores.

Output: los 7 sitios aliasados; `tests/test_aggregates.py` y `tests/test_query_builder.py` extendidos con el guard `AS n`; los 6 ficheros de motor con tests de integración de las cinco APIs (DIAL-09).
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md
@.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md
@.planning/phases/02-dialect-seam-engine-parity/02-PATTERNS.md
@.planning/phases/02-dialect-seam-engine-parity/02-VALIDATION.md
@encino_orm/base.py
@encino_orm/model/model.py
@encino_orm/model/query_builder.py
@encino_orm/_rows.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aliasar `AS n` en los 7 lectores de agregados + guard unitario</name>
  <files>encino_orm/base.py, encino_orm/model/model.py, encino_orm/model/query_builder.py, tests/test_aggregates.py, tests/test_query_builder.py</files>
  <read_first>
    - `encino_orm/base.py` (líneas 149-178): el sitio `list_tables:158` (el más engañoso: funciona en SQLite/MySQL y revienta en PostgreSQL) y el patrón correcto ya presente en `paginate:174-177`.
    - `encino_orm/model/model.py` (líneas 784-796): `Model.count` con `row["COUNT(*)"]`.
    - `encino_orm/model/query_builder.py` (líneas 241-280): los cinco lectores `count`/`sum`/`avg`/`min`/`max`, todos indexando por el texto de la expresión.
    - `encino_orm/_rows.py` (líneas 1-30): la normalización a minúsculas de los labels de MSSQL/Oracle — la causa raíz de por qué el alias debe ser fijo y en minúsculas.
    - `tests/test_aggregates.py` (completo): los tests existentes que se extienden.
    - `tests/test_query_builder.py`: los tests existentes de agregados del builder.
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — Pitfall C ("the COUNT(*) fix is applied to 3 sites and 4 remain broken") y Pitfall D ("the result-key fix is solved by normalizing keys in one adapter").
  </read_first>
  <behavior>
    - El SQL generado por `list_tables` contiene `SELECT COUNT(*) AS n FROM (` y el resultado se lee con `row["n"]`.
    - El SQL generado por `Model.count` es `SELECT COUNT(*) AS n FROM <tabla><where>` y se lee con `row["n"]`.
    - El SQL de `QueryBuilder.count` es `SELECT COUNT(*) AS n <resto>`; el de `sum` es `SELECT SUM(<col>) AS n <resto>`; igual para `avg`/`min`/`max` — los cinco leen `row["n"]`.
    - `QueryBuilder.sum` sigue devolviendo `0` cuando el resultado es `None`; `avg`/`min`/`max` siguen devolviendo `None` cuando no hay fila.
    - Ningún lector de agregados indexa el resultado por texto de expresión (`row["COUNT(*)"]`, `row[f"SUM({column})"]`, etc.).
    - No se introduce ningún normalizador de claves dependiente del motor en la ruta de fetch.
  </behavior>
  <action>
    Aplicar el patrón de `base.py:174-177` a los SIETE sitios, sin excepción:
    - `encino_orm/base.py` línea 158 (`list_tables`): `SELECT COUNT(*) AS n FROM ({sql})` + `row["n"] if row else 0`. Aprovechar para eliminar el paréntesis redundante y dejar el SQL consistente con `paginate`.
    - `encino_orm/model/model.py` línea 793-795 (`Model.count`): `f"SELECT COUNT(*) AS n FROM {self._table}{where}"` + `return row["n"] if row else 0`.
    - `encino_orm/model/query_builder.py`:
      - `count` (244-246): `f"SELECT COUNT(*) AS n {sql}"` + `row["n"] if row else 0`.
      - `sum` (252-255): `f"SELECT SUM({column}) AS n {sql}"` + `return row["n"] if row and row["n"] is not None else 0`.
      - `avg` (261-263), `min` (269-271), `max` (277-279): `f"SELECT AVG({column}) AS n {sql}"` (y sus análogos) + `return row["n"] if row else None`.
    El alias debe ser el mismo literal en los siete sitios: `n`, en minúsculas, para que el contrato de resultado sea independiente del motor.

    **Prohibido** arreglar esto normalizando claves en un adaptador (`_normalize_keys`, `if engine == Engine.POSTGRESQL` en la ruta de fetch): el label difiere por EXPRESIÓN, no solo por motor, y `_rows.py` ya normaliza MSSQL/Oracle. El alias en el SQL es la única solución correcta (Pitfall D).

    Extender `tests/test_aggregates.py`: (a) aserciones de que el SQL generado contiene `AS n` (capturando el `Query` enviado a un fake de `Db` o usando un `SqliteDb` real en memoria); (b) aserciones de que `count`/`sum`/`avg`/`min`/`max` devuelven el valor correcto sobre SQLite en memoria; (c) el caso `sum` sobre una tabla vacía devuelve `0` y `avg`/`min`/`max` devuelven `None`. Extender `tests/test_query_builder.py` con la misma comprobación para los agregados del builder.

    Añadir un guard de fuente (en `tests/test_aggregates.py`): recorrer `encino_orm/**/*.py` y afirmar que NO aparece ninguna de las cadenas `row["COUNT(*)"]`, `row[f"SUM(`, `row[f"AVG(`, `row[f"MIN(`, `row[f"MAX(`, ni `"COUNT(*)"]]` como clave de indexación. El guard debe ser un chequeo de texto sobre el fuente (con normalización `\` → `/`), de modo que reintroducir un lector por expresión rompa el test.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_aggregates.py tests/test_query_builder.py -x -q` pasa.
    - `grep -rn 'row\["COUNT(\*)"\]\|row\[f"SUM(\|row\[f"AVG(\|row\[f"MIN(\|row\[f"MAX(' encino_orm/` no devuelve nada.
    - `uv run python -c "import inspect, encino_orm.base as b, encino_orm.model.model as m, encino_orm.model.query_builder as q; srcs=''.join(inspect.getsource(x) for x in (b.Db.list_tables, m.Model.count, q.QueryBuilder.count, q.QueryBuilder.sum, q.QueryBuilder.avg, q.QueryBuilder.min, q.QueryBuilder.max)); assert srcs.count('AS n')>=7, srcs.count('AS n')"` sale con código 0.
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa (ninguna suite existente se rompe).
    - `uv run ruff check encino_orm tests` y `uv run mypy encino_orm` salen con código 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_aggregates.py tests/test_query_builder.py -x -q</automated>
  </verify>
  <done>Los siete lectores de agregados usan un alias fijo `n` y leen `row["n"]`; un guard unitario impide reintroducir la lectura por texto de expresión.</done>
</task>

<task type="auto">
  <name>Task 2: Integración por motor en PostgreSQL, SQL Server y Oracle (donde el bug es fatal)</name>
  <files>tests/test_postgresql.py, tests/test_mssql.py, tests/test_oracle.py</files>
  <read_first>
    - `tests/test_postgresql.py` (completo): configuración por env var, skip-guard con `engine_unavailable`, patrón de clases de integración y los tests white-box de builders.
    - `tests/test_mssql.py` (completo): idem, más el marcador `optional_engine` en la clase de integración (línea 142) — NO se retira en este plan.
    - `tests/test_oracle.py` (completo): idem (marcador en la línea 112).
    - `tests/conftest.py`: `engine_unavailable` y `required_engines` (el interruptor `ENCINO_ORM_REQUIRE_ENGINES` ya es extensible).
    - `encino_orm/model/model.py` (líneas 784-821): `count`/`paginate`; y `sync_schema` (871-924).
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — §Phase Requirements DIAL-09: "gap analysis: zero current calls to those five APIs in any non-SQLite test file".
    - `.planning/codebase/TESTING.md`: convenciones de test por motor (archivos separados, sin `@pytest.mark.parametrize`, skip-guard).
  </read_first>
  <action>
    Añadir a cada uno de los tres ficheros una clase de integración nueva (o extender la existente) con aserciones reales contra el motor para las cinco APIs. Mantener las convenciones del repo: fichero por motor, `engine_unavailable(...)` en el fixture de conexión, `@pytest.mark.integration`, `@pytest.mark.optional_engine` en la clase (estos dos motores siguen siendo opcionales para el job `test`; el job `engine-heavy` los ejecuta por selección de fichero, no por marker), `@pytest.mark.asyncio` explícito, y sin `@pytest.mark.parametrize`.

    Cobertura mínima por motor:
    - `count`: crear la tabla del test, insertar N filas conocidas, `await Model.count()` devuelve N; `await Model.count(filtro)` devuelve el subconjunto correcto. **Esta es la aserción que hoy falla con `KeyError: 'COUNT(*)'` en PostgreSQL y con la clave en minúsculas en MSSQL/Oracle.**
    - `paginate`: `Records.total` coincide con `count`, `Records.rows` tiene el tamaño de página correcto y `limit`/`page` se respetan.
    - `list_tables`: `db.list_tables()` devuelve un `Records` con `total >= 1` y contiene la tabla del test.
    - `sync_schema`: crear la tabla con una columna de menos que el modelo, llamar `sync_schema()` y comprobar `{"added": [...]}` y que la columna existe en el catálogo después.
    - `last_id`: insertar una fila y comprobar que `db.last_id()` devuelve un id coherente con la fila insertada (el contrato cambia en la Fase 4/POOL-03; aquí se fija el comportamiento actual como caracterización).
    - `QueryBuilder`: `Model.query().count()`, `.sum(col)`, `.avg(col)`, `.min(col)`, `.max(col)` devuelven los valores correctos — los cuatro últimos son los sitios que el roadmap no nombraba y que el bug también rompía.

    Reutilizar el DDL y los helpers ya presentes en cada fichero (no inventar tablas nuevas si ya existe una equivalente); limpiar la tabla en el teardown como hacen los tests actuales.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_postgresql.py tests/test_mssql.py tests/test_oracle.py -q -m "not optional_engine or optional_engine"` pasa (con los motores disponibles localmente; sin ellos, los tests de motor omiten y los unitarios pasan).
    - Cada uno de los tres ficheros contiene al menos una llamada a `count(`, una a `paginate(`, una a `list_tables(`, una a `sync_schema(` y una a `last_id(`: `grep -c "list_tables(" tests/test_postgresql.py tests/test_mssql.py tests/test_oracle.py` es ≥ 1 en los tres.
    - Cada uno de los tres ficheros contiene aserciones de `sum(`/`avg(`/`min(`/`max(` del `QueryBuilder`.
    - `uv run pytest tests/test_pytest_config.py -q` sigue pasando (los 6 ficheros de motor siguen aportando tests marcados `integration`).
    - `uv run ruff check tests` sale con código 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_postgresql.py tests/test_mssql.py tests/test_oracle.py -q</automated>
  </verify>
  <done>PostgreSQL, SQL Server y Oracle tienen cobertura de integración real de `count`/`paginate`/`list_tables`/`sync_schema`/`last_id` y de los cinco agregados del `QueryBuilder`.</done>
</task>

<task type="auto">
  <name>Task 3: Integración por motor en SQLite, MySQL y MariaDB</name>
  <files>tests/test_sqlite.py, tests/test_mysql.py, tests/test_mariadb.py</files>
  <read_first>
    - `tests/test_sqlite.py`, `tests/test_mysql.py`, `tests/test_mariadb.py` (completos): fixtures de conexión, DDL existente y patrón de clase de integración.
    - `tests/test_mariadb.py` (líneas 51-79): la clase `TestMariadbLifecycle` y su marcador `optional_engine` — NO se retira aquí (lo hace 02-05 junto con el servicio de CI, Pitfall H).
    - `tests/test_postgresql.py`: el bloque de aserciones añadido en la tarea 2, para mantener el mismo conjunto de comprobaciones por motor.
    - `.planning/phases/02-dialect-seam-engine-parity/02-VALIDATION.md` — fila `02-04-03`.
  </read_first>
  <action>
    Replicar el conjunto de comprobaciones de la tarea 2 en los tres motores restantes, respetando las particularidades de cada uno:
    - `tests/test_sqlite.py`: SQLite es el motor siempre activo, así que estas aserciones corren en **cada** corrida de CI y son la red de seguridad barata. Cubrir `count`/`paginate`/`list_tables`/`sync_schema`/`last_id` + los cinco agregados del `QueryBuilder`. `sync_schema` en SQLite no soporta `alter_types` (lanza `NotImplementedError`): asertar ese contrato explícitamente en vez de evitarlo.
    - `tests/test_mysql.py`: idem contra MySQL real (job `test` de CI, ya requerido).
    - `tests/test_mariadb.py`: idem contra MariaDB real (el servicio se añade en 02-05; hasta entonces el test omite en local sin MariaDB). Mantener `optional_engine` en la clase: retirarlo aquí rompería el job `test` (que no tiene servicio MariaDB todavía) porque los tests se seleccionarían y el gate de skips fallaría.
    - Comprobar en los tres que `Model.query().sum/avg/min/max` devuelven valores correctos: en SQLite/MySQL el bug no se manifiesta, pero el test fija el contrato y detecta una regresión introducida por el arreglo del alias.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_sqlite.py -q` pasa (SQLite está siempre disponible).
    - `uv run pytest tests/test_mysql.py tests/test_mariadb.py -q` pasa (con motores disponibles) u omite limpiamente sin ellos.
    - Los tres ficheros contienen al menos una llamada a cada una de `count(`, `paginate(`, `list_tables(`, `sync_schema(`, `last_id(`.
    - `tests/test_mariadb.py` conserva el marcador `optional_engine` (`grep -c "optional_engine" tests/test_mariadb.py` ≥ 1).
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa.
    - `uv run pytest tests/test_pytest_config.py -q` pasa.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_sqlite.py -q</automated>
  </verify>
  <done>Los seis ficheros de motor cubren las cinco APIs de DIAL-09 y los cinco agregados del `QueryBuilder`; SQLite lo verifica en cada corrida de CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver → lector de resultados | El nombre de columna que devuelve cada driver NO es un contrato estable: depende del motor y de si la expresión está aliasada. |
| tests de integración → motores reales | Los tests hablan con contenedores de CI; las credenciales son de desarrollo y no se introducen secretos nuevos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | `base.py`, `model/model.py`, `model/query_builder.py` (lectura de agregados) | mitigate | Alias fijo `AS n` + `row["n"]` en los siete sitios. Guard de fuente que falla si reaparece una lectura por texto de expresión. |
| T-02-21 | Tampering | ruta de fetch de los adaptadores | mitigate | Prohibido normalizar claves en un adaptador (`_normalize_keys`, ramas por motor en el fetch): el label difiere por expresión, no solo por motor (Pitfall D). El alias en el SQL es la única mitigación correcta. |
| T-02-22 | Repudiation | cobertura de paridad declarada sin prueba | mitigate | Los tests de integración por motor son la evidencia de DIAL-03/DIAL-09; el job `test` (MySQL, PostgreSQL, SQLite) los ejecuta en cada corrida y el job `engine-heavy` (02-05) cubre MSSQL/Oracle. |
| T-02-23 | Information Disclosure | credenciales en los tests de motor | accept | Se reutilizan las env vars `ENCINO_ORM_<ENGINE>_*` con los valores de desarrollo ya presentes en `docker-compose.yml`; no se introduce ningún secreto de repositorio. |
| T-02-24 | Denial of Service | `list_tables` / `paginate` con filtros | accept | El cambio es de forma del SQL (alias), no de semántica de filtrado; los parámetros siguen ligados y validados en el seam. Sin cambio de superficie. |
</threat_model>

<verification>
- `uv run pytest -q -m "not integration and not optional_engine"` pasa.
- `uv run pytest tests/test_sqlite.py tests/test_postgresql.py tests/test_mysql.py tests/test_mssql.py tests/test_mariadb.py tests/test_oracle.py -q` pasa (con los motores disponibles; los ausentes omiten).
- Guard de fuente: cero lecturas de agregados por texto de expresión.
- `uv run ruff check encino_orm tests`, `uv run ruff format --check encino_orm tests`, `uv run mypy encino_orm` pasan.
</verification>

<success_criteria>
- DIAL-03 cumplido en su totalidad real (7 sitios, no 3): `count`/`paginate`/`list_tables` y `sum`/`avg`/`min`/`max` devuelven valores correctos en los seis motores.
- DIAL-09 cumplido: existen tests de integración por motor para `count`/`paginate`/`list_tables`/`sync_schema`/`last_id` en los seis ficheros.
- El arreglo vive en el SQL (alias `n`), no en la capa de fetch de ningún adaptador.
</success_criteria>

<output>
Create `.planning/phases/02-dialect-seam-engine-parity/02-04-SUMMARY.md` when done
</output>
