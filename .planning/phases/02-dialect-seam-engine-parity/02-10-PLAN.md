---
phase: 02-dialect-seam-engine-parity
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - encino_orm/model/query_builder.py
  - tests/test_query_builder.py
autonomous: true
gap_closure: true
requirements:
  - DIAL-01
  - DIAL-02
must_haves:
  truths:
    - "Ningún `_table` que no sea identificador llega al `FROM`/`JOIN` que genera `QueryBuilder`: la construcción lanza `ValueError` antes de que exista una sola cadena SQL."
    - "El payload reproducido por la verificación (`_table = 't; DROP TABLE usuarios --'`) lanza `ValueError` y el driver nunca se alcanza."
    - "El camino legítimo queda byte-idéntico: `QueryBuilder(Agente, None)._build_base()[0] == 'FROM agentes mm'` y el JOIN de tabla sigue emitiendo el mismo SQL."
    - "Las posiciones de EXPRESIÓN (`select`, `group_by`, `order_by`, `sum/avg/min/max`) siguen pasando por la allowlist tolerante a puntos `_COLUMN_RE` (decisión documentada, no se unifica) y rechazan payloads hostiles."
    - "El barrido deja constancia de que `_table` era el ÚLTIMO punto de interpolación de identificador sin validar de `QueryBuilder`."
  artifacts:
    - path: "encino_orm/model/query_builder.py"
      provides: "Validación de `model_class._table` en `__init__` y de `other._table` en `join()` con el allowlist estricto"
      contains: "nombre de tabla"
    - path: "tests/test_query_builder.py"
      provides: "Regresión CR-01 del nombre de tabla con modelo dinámico hostil y spy de driver"
      contains: "DROP TABLE usuarios"
  key_links:
    - from: "encino_orm/model/query_builder.py"
      to: "encino_orm/dialects/identifiers.py"
      via: "`check_identifier` es el ÚNICO punto de validación de identificadores (ya importado en el módulo desde 02-06)"
      pattern: "check_identifier"
    - from: "encino_orm/model/query_builder.py"
      to: "encino_orm/model/model.py"
      via: "`_table` es un atributo de clase del modelo; `QueryBuilder` no dispara `_build_column_map()`, que era el único sitio que lo validaba (model.py:223-224)"
      pattern: "_table"
---

<objective>
Cerrar el GAP A / CR-01 de `02-VERIFICATION-GAPS.md` (ronda 3): **el cuello de botella de validación de identificadores sigue incompleto**. `_build_base()` interpola `self._model_class._table` (`query_builder.py:189`) y cada `join['model_class']._table` (`:195`) **sin validación**. `Model._table` solo se valida dentro de `Model._build_column_map()` (`model.py:223-224`), que `QueryBuilder` **nunca** dispara (solo llama a `_column_map()` para `select("alias.*")`).

Reproducido en la verificación: `QueryBuilder(Evil, None)._build_base()[0]` → `'FROM t; DROP TABLE usuarios -- mm'` con `Evil._table = 't; DROP TABLE usuarios --'`. La ronda anterior cerró el vector público `alias=` pero dejó la MISMA clase de defecto por la puerta del nombre de tabla — el escenario exacto de los modelos dinámicos/code-generated.

Purpose: la cláusula 1 del goal de la fase ("la validación de identificadores y la construcción DML viven en exactamente un sitio dentro del core") es literalmente falsa mientras un `_table` hostil llegue al `FROM` sin pasar por el allowlist. `check_identifier` no es un cuello de botella real si un constructor público lo esquiva.

Output: `QueryBuilder.__init__` y `QueryBuilder.join()` validan el nombre de tabla con el allowlist ESTRICTO (`check_identifier`, sin relajarlo); regresión que reproduce el payload literal del informe con un modelo dinámico hostil y un spy que prueba que el driver no se alcanza; barrido documentado del resto de puntos de interpolación del módulo (`select`/`group_by`/`order_by`/agregados) para que este sea genuinamente el último.

**Linaje de ficheros.** `02-06` ya validó los alias en estos dos ficheros; `02-10` los extiende. Como `02-06` está ejecutado y en el árbol, no hay restricción de orden: `depends_on: []` es correcto y `02-10` comparte wave con `02-11` (ficheros disjuntos).

**Disciplina de byte-identidad.** Este plan NO cambia el SQL generado para entradas válidas: solo añade rechazo fail-closed para entradas que hoy producen SQL inválido/inyectable. `tests/__snapshots__/test_sql_snapshots.ambr` y los golden strings de los adaptadores no se tocan (criterio de aceptación explícito).
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md
@.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md
@.planning/phases/02-dialect-seam-engine-parity/02-PATTERNS.md
@.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION-GAPS.md
@.planning/phases/02-dialect-seam-engine-parity/02-REVIEW-GAPS.md
@.planning/phases/02-dialect-seam-engine-parity/02-06-SUMMARY.md
@encino_orm/model/query_builder.py
@encino_orm/dialects/identifiers.py
@tests/test_query_builder.py

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del árbol; no explorar el código para rederivarlos. -->

De `encino_orm/dialects/identifiers.py` (único punto de validación, allowlist ESTRICTA — no relajar):
```python
IDENTIFIER_RE = re.compile(r"^[A-Za-z_][A-Za-z0-9_]*$")

def check_identifier(value: str, label: str) -> str:
    """Valida que `value` sea un identificador SQL seguro (evita inyección)."""
    if not isinstance(value, str) or not IDENTIFIER_RE.match(value):
        raise ValueError(f"{label} inválido: {value!r}")
    return value
```

De `encino_orm/model/query_builder.py` (estado actual, 02-06 ya importa `check_identifier`):
```python
_PLACEHOLDER = re.compile(r"\{(\d+)\}")
# Allowlist DISTINTA a propósito: acepta puntos para expresiones calificadas.
_COLUMN_RE = re.compile(r"^[A-Za-z_][A-Za-z0-9_.]*$")

def _safe_column(expr: str) -> str: ...          # usada por select/group_by/order_by/agregados

class QueryBuilder:
    def __init__(self, model_class, db=None, alias: str = "mm"):
        self._model_class = model_class
        alias = check_identifier(alias, "alias")   # :53 — ya validado por 02-06
        ...
    def join(self, other, alias: str, on: Filter) -> "QueryBuilder":
        alias = check_identifier(alias, "alias")   # :104 — ya validado por 02-06
        ...
    def _build_base(self) -> tuple[str, list]:
        sql = f"FROM {self._model_class._table} {self._alias}"                       # :189 — SIN VALIDAR
        ...
        sql += f" JOIN {join['model_class']._table} {join['alias']} ON {on_frag}"    # :195 — SIN VALIDAR
```

De `encino_orm/model/model.py` (el único sitio que hoy valida `_table`, y `QueryBuilder` no lo dispara):
```python
_table: ClassVar[str] = ""          # :131
# :223-224 dentro de _build_column_map():
if cls._table:
    check_identifier(cls._table, "nombre de tabla")
```

Patrón de modelo dinámico ya establecido en `tests/test_identifiers.py:96-100`:
```python
M = create_model("M", __base__=Model, **{"a": (str | None, None)})
M._table = "t"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validar `_table` en `QueryBuilder.__init__` y `join()` con el allowlist estricto (CR-01)</name>
  <files>encino_orm/model/query_builder.py, tests/test_query_builder.py</files>
  <read_first>
    - `encino_orm/model/query_builder.py` completo (312 líneas): `_safe_column` (:28-38), `__init__` (:47-66, con la validación de alias ya presente en :53), `join` (:99-110, con `check_identifier(alias, "alias")` en :104 y el comentario de "validar ANTES de comprobar duplicados"), `join_subquery` (:112-125, revalida el alias final en :119), `_build_base` (:187-209, con `FROM {self._model_class._table}` en :189 y `JOIN {join['model_class']._table}` en :195), `_expand_star` (:93-97, columnas desde `model_cls._column_map()`), `_render_select` (:228-234).
    - `encino_orm/dialects/identifiers.py` completo (28 líneas) — la allowlist estricta y `check_identifier(value, label)`; el mensaje es `f"{label} inválido: {value!r}"` (convención del repo, `repr()` del valor ofensivo).
    - `encino_orm/model/model.py:214-246` — `_build_column_map` (:214) con la ÚNICA validación de `_table` (:223-224, condicionada a `if cls._table:`) y `_col` (:245); entender por qué `QueryBuilder` no la dispara (solo llama a `_column_map()` para `select("alias.*")` vía `_expand_star`).
    - `tests/test_query_builder.py:1-92` — imports (`from pydantic import Field`; hay que añadir `create_model`), los dobles `_RecordingDb` (:8-26) y `_RecordingDbPaginado` (:28-51), y los modelos `Region`/`Agente` (:54-63) con `_table` válido.
    - `tests/test_query_builder.py:250-301` — `TestAliasInjection`, el precedente exacto a imitar: payload literal del informe, `pytest.raises(ValueError)` + `"alias inválido" in str(exc.value)`, y `test_el_driver_nunca_se_alcanza_con_alias_hostil` con `assert fake.queries == []`.
    - `tests/test_identifiers.py:89-124` — `TestNombreDeColumnaPorDefecto`, el patrón de modelo dinámico hostil con `create_model(__base__=Model, ...)` y asignación posterior de `_table`.
    - `.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION-GAPS.md` — el `gaps[0]` (CR-01 STILL OPEN) con la reproducción y el `missing` que exige el test de regresión.
  </read_first>
  <behavior>
    - `QueryBuilder(Evil, None)` donde `Evil._table = "t; DROP TABLE usuarios --"` lanza `ValueError` cuyo mensaje contiene `"nombre de tabla inválido"` y `repr("t; DROP TABLE usuarios --")`.
    - `QueryBuilder(Agente, None).join(Evil, "h", Filter.eq("mm.agente", col("h.agente")))` lanza `ValueError` por el `_table` del destino del JOIN (no por el alias, que es válido).
    - El JOIN de SUBQUERY sigue validado: `sub` con `_table` hostil no puede ni construirse, así que `join_subquery` no puede introducir un `_table` sin validar.
    - `QueryBuilder(Agente, None)._build_base()[0] == "FROM agentes mm"` sigue pasando (byte-identidad del camino válido) y el JOIN de tabla válido emite `JOIN regiones r ON ...`.
    - Un modelo con `_table = ""` (o no-`str`) lanza `ValueError` fail-closed en vez de emitir `FROM  mm`.
    - El driver nunca se alcanza: con `_RecordingDb`, `await QueryBuilder(Evil, fake).all()` lanza `ValueError` y `fake.queries == []`.
  </behavior>
  <action>
    **A) Validar el nombre de tabla en el constructor.** En `encino_orm/model/query_builder.py::QueryBuilder.__init__`, antes de almacenar el modelo (o inmediatamente después de la línea que valida el alias), añadir una llamada `check_identifier(model_class._table, "nombre de tabla")` (sin reasignar nada: el valor validado no se usa, solo se comprueba; mismo estilo que `Model._build_column_map`). Usar la allowlist ESTRICTA (`check_identifier`), no `_COLUMN_RE`: un nombre de tabla es siempre un identificador desnudo; no hay caso legítimo de `_table` con punto (el cualificado se resuelve con el parámetro `schema=` de los builders, `dialects/builders.py::_qualified`). La validación es INCONDICIONAL (no copiar el `if cls._table:` de `_build_column_map`): un modelo sin tabla no puede producir SQL válido, así que fallar rápido es lo correcto y cierra el hueco de `FROM  mm`. Escribir un comentario en español explicando que `_build_column_map()` es el único sitio que validaba `_table` y que `QueryBuilder` no lo dispara (por eso el defecto sobrevivió a 02-06).

    **B) Validar el nombre de tabla de cada destino de JOIN.** En `QueryBuilder.join`, añadir una llamada `check_identifier(other._table, "nombre de tabla")` (sin reasignar). Colocarlo JUNTO a la validación de alias (:104) y ANTES de la comprobación de duplicados, manteniendo el invariante que 02-06 estableció: un destino hostil debe fallar como identificador inválido, no como alias duplicado. No hace falta tocar `join_subquery`: el `_table` de la subquery ya se validó al construir ESE `QueryBuilder` (el constructor es el único punto de entrada).

    **C) No relajar ni duplicar la allowlist.** Reutilizar el `check_identifier` ya importado en :4 (`from encino_orm.dialects.identifiers import check_identifier`). No crear una regex local, no tocar `_COLUMN_RE`, no usar `_safe_column` para el nombre de tabla (es tolerante a puntos a propósito, para expresiones calificadas).

    **D) Regresión RED/GREEN en `tests/test_query_builder.py`.** Añadir una clase `TestTablaInjection` (después de `TestAliasInjection`), con:
    1. Un helper estático `_modelo_tabla_hostil()` que construye `create_model("Evil", __base__=Model, **{"nombre": (str | None, None)})` y le asigna `M._table = "t; DROP TABLE usuarios --"` (mismo patrón que `tests/test_identifiers.py:96-100`). Añadir `from pydantic import create_model` al bloque de imports (junto a `Field`).
    2. `test_tabla_con_inyeccion_en_constructor_lanza`: `pytest.raises(ValueError)` alrededor de `QueryBuilder(Evil, None)`; asertar `"nombre de tabla inválido" in str(exc.value)` y `repr("t; DROP TABLE usuarios --") in str(exc.value)`.
    3. `test_tabla_hostil_en_join_lanza`: `QueryBuilder(Agente, None)` y luego `pytest.raises(ValueError)` al hacer `.join(Evil, "h", Filter.eq("mm.agente", col("h.agente")))`. El alias `"h"` es válido a propósito, de modo que el fallo solo puede venir del `_table`.
    4. `test_tabla_no_identificador_en_constructor_lanza`: iterar `["a b", "a.b", "1x", "", "a-b", None, "t;--"]` y asertar `ValueError` para cada uno.
    5. `test_el_driver_nunca_se_alcanza_con_tabla_hostil` (async): `fake = _RecordingDb({"id": 1})`; `pytest.raises(ValueError)` alrededor de `await QueryBuilder(Evil, fake).all()`; `assert fake.queries == []`.
    6. `test_tabla_valida_sigue_byte_identica`: `QueryBuilder(Agente, None)._build_base()[0] == "FROM agentes mm"`; y con `qb = QueryBuilder(Agente, None); qb.join(Region, "r", Filter.eq("mm.region_id", col("r.id")))`, asertar que el SQL contiene `"JOIN regiones r ON"`.
    7. `test_tabla_hostil_en_subquery_no_construye`: `pytest.raises(ValueError)` al construir `QueryBuilder(Evil, None)` usado como `sub` en `join_subquery` — documentar en un comentario que la subquery se valida en su propio constructor, así que no puede inyectarse por esta vía.
    Capturar la salida RED (antes del fix) y la GREEN (después) como evidencia del SUMMARY: el RED es `ValueError` NO lanzado y `_build_base()[0] == "FROM t; DROP TABLE usuarios -- mm"`.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_query_builder.py -q -k TablaInjection` pasa y selecciona al menos 6 tests.
    - `grep -c "nombre de tabla" encino_orm/model/query_builder.py` ≥ 2 (constructor + join).
    - `grep -c "DROP TABLE usuarios" tests/test_query_builder.py` ≥ 1 (el payload literal del informe).
    - `QueryBuilder(Agente, None)._build_base()[0] == "FROM agentes mm"` sigue pasando (el test existente de `TestAliasInjection` no se edita).
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa.
    - `git diff --stat tests/__snapshots__/` está VACÍO y ningún golden string de `tests/test_{sqlite,mysql,mariadb,postgresql,mssql,oracle}.py` se edita.
    - `uv run ruff check encino_orm tests` y `uv run ruff format --check encino_orm tests` y `uv run mypy encino_orm` salen 0; el conteo de `# noqa` en `encino_orm/` sigue siendo 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_query_builder.py -q -k TablaInjection && uv run pytest -q -m "not integration and not optional_engine" && uv run python -c "import pathlib; t = pathlib.Path('encino_orm/model/query_builder.py').read_text(encoding='utf-8'); assert t.count('nombre de tabla') >= 2, t.count('nombre de tabla'); print('OK: _table validado en constructor y join')"</automated>
  </verify>
  <done>`QueryBuilder` rechaza con `ValueError` cualquier `_table` que no pase el allowlist estricto, tanto en el constructor como en cada destino de `join`, sin alcanzar el driver; el camino válido queda byte-idéntico y el payload literal del informe (`'t; DROP TABLE usuarios --'`) está cubierto por regresión.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Barrido de los puntos de interpolación restantes de `QueryBuilder` y cierre del hueco</name>
  <files>encino_orm/model/query_builder.py, tests/test_query_builder.py</files>
  <read_first>
    - `encino_orm/model/query_builder.py` — TODAS las interpolaciones a SQL, una por una: `_build_base` (:187-209: `_table`, alias, `on_frag`, sub-SQL), `_build_full` (:211-222: `GROUP BY`, `HAVING`, `ORDER BY`), `_build_subquery` (:224-226), `_render_select` (:228-234), `_add_select` (:74-91), `_expand_star` (:93-97), `group_by` (:138-140), `order_by` (:146-148), `_parse_sort` (:161-178), `count/sum/avg/min/max` (:260-297) y `exists` (:299-304).
    - `encino_orm/model/query_builder.py:16-38` — `_PLACEHOLDER`, `_COLUMN_RE` (tolerante a puntos, DELIBERADA) y `_safe_column`; el comentario de :17-20 explica por qué NO se unifica con la allowlist estricta (unificar sería un cambio de comportamiento y relajarla reabriría la superficie, Pitfall 10).
    - `encino_orm/model/filter.py` — `Filter.to_sql()`: los VALORES del filtro viajan como `{n}` ligados; solo los nombres de columna se interpolan, y pasan por `col()`/`_COLUMN_RE` (fuera del alcance de este plan, ya caracterizado en la fase).
    - `.planning/phases/02-dialect-seam-engine-parity/02-REVIEW-GAPS.md` — el veredicto del área de foco 1: "`select`/`group_by`/`order_by` go through `_safe_column` (dot-tolerant, not the strict allowlist), which is acceptable for those expression positions" — el plan debe fijar esa decisión con un test de caracterización, no cambiarla.
    - `.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION-GAPS.md` — el `missing[1]` de CR-01 y la instrucción del encargo: "sweep for any OTHER unvalidated identifier that reaches SQL interpolation through `QueryBuilder`".
  </read_first>
  <behavior>
    - Cada posición de expresión hostil lanza `ValueError`: `select("x; DROP TABLE t --")`, `select("a AS 'b'")`, `group_by("x;--")`, `order_by("x;--")`, `sort_by("x;--")`, `sum("x;--")`, `avg("x;--")`, `min("x;--")`, `max("x;--")`.
    - La allowlist de expresión sigue siendo la tolerante a puntos: `select("mm.agente")`, `group_by("mm.agente")` y `order_by("mm.agente")` siguen aceptándose (byte-identidad del SQL legítimo).
    - El `_table` validado en Task 1 es el ÚNICO punto que necesitaba la allowlist estricta: un guard de fuente comprueba que `_build_base` no interpola ningún identificador que no venga de un valor ya validado (constructor/join) o de `_safe_column`.
  </behavior>
  <action>
    **A) Inventario explícito en el código.** Añadir un comentario en español en `_build_base` (encima de la construcción del `FROM`) que declare el contrato del módulo: los únicos identificadores que llegan al SQL son (1) `_table`, validado en `__init__`/`join` con la allowlist ESTRICTA; (2) los alias, validados en `__init__`/`join`/`join_subquery`; (3) las expresiones de columna, validadas por `_safe_column` con la allowlist tolerante a puntos `_COLUMN_RE`, deliberadamente distinta. Dejar escrito que `_COLUMN_RE` NO se unifica (Pitfall 10: unificar sería un cambio de comportamiento y relajarla reabriría la superficie).

    **B) Test de caracterización del barrido.** Añadir a `TestTablaInjection` (o a una clase hermana `TestBarridoDeIdentificadores` en el mismo fichero) un test por familia de posición que demuestre el rechazo hostil y la aceptación legítima:
    1. `test_select_rechaza_expresion_hostil` / `test_select_acepta_columna_calificada`: construir `QueryBuilder(Agente, None)`, `pytest.raises(ValueError)` con `select("x; DROP TABLE t --")`; y aceptar `select("mm.agente")` (aserción sobre `_build_subquery()[0]` o `_render_select()`).
    2. `test_group_by_y_order_by_rechazan_expresion_hostil`: `group_by("x;--")` y `order_by("x;--")` lanzan `ValueError`; `group_by("mm.agente")`/`order_by("mm.agente")` se aceptan.
    3. `test_sort_by_rechaza_expresion_hostil`: `sort_by("x;--")` lanza `ValueError`; `sort_by("agente desc")` produce `"agente DESC"` (la dirección ya se valida contra `asc`/`desc`).
    4. `test_agregados_rechazan_columna_hostil`: para `sum`/`avg`/`min`/`max` con `"x;--"`, usar un **fake db** (p. ej. `_RecordingDb`) en lugar de `QueryBuilder(Agente, None)`. OJO: `sum/avg/min/max` llaman a `self._ensure_db()` **antes** de `_safe_column(column)` (`query_builder.py:267-297`), así que con `db=None` el `ValueError` lo lanzaría `_ensure_db` ("QueryBuilder sin conexión Db") y el test pasaría aunque `_safe_column` se eliminara. Por eso el test debe usar el fake db y **asertar que el mensaje contiene `"nombre de columna inválido"`**, no solo que se lanza `ValueError`.
    5. `test_alias_de_columna_hostil_rechazado`: `select("agente AS 'x'")` lanza `ValueError` (el alias de columna pasa por `_safe_column`).
    Escribir en el docstring de la clase que estas posiciones usan la allowlist tolerante a puntos POR DECISIÓN (Pitfall 10) y que el barrido confirma que `_table` era el último punto que necesitaba la estricta.

    **C) Guard de fuente (barrido verificable).** Añadir `test_build_base_no_interpola_tablas_sin_validar`: leer el fuente de `QueryBuilder._build_base` con `inspect.getsource` y asertar que el `FROM` usa `self._model_class._table` (no una variable local sin validar) y que el módulo contiene exactamente DOS llamadas `check_identifier(..., "nombre de tabla")` (constructor + join) y NINGUNA interpolación directa de un `_table` que no venga de `self._model_class`/`join['model_class']`. Mantener el guard simple y determinista (aserciones sobre substrings, sin AST salvo que aporte claridad): el objetivo es que reintroducir un `_table` sin validar rompa el test.

    **D) No cambiar comportamiento de expresión.** No tocar `_COLUMN_RE`, `_safe_column` ni las firmas de `select`/`group_by`/`order_by`/`sort_by`/agregados. Este task AÑADE evidencia y comentarios; cualquier cambio de SQL legítimo es un fallo del task.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_query_builder.py -q` pasa completo (los tests existentes de `TestQueryBuilder`, `TestSortBy`, `TestPaginacionDelegadaAlAdaptador` y `TestAliasInjection` NO se editan).
    - Existe al menos un test por familia de posición de expresión (`select`, `group_by`, `order_by`, `sort_by`, agregados, alias de columna) y todos pasan.
    - `git diff encino_orm/model/query_builder.py` NO modifica `_COLUMN_RE` ni `_safe_column` (verificable con `git diff -U0 encino_orm/model/query_builder.py` e inspección).
    - `git diff --stat tests/__snapshots__/` está VACÍO.
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa.
    - `uv run ruff check encino_orm tests` y `uv run ruff format --check encino_orm tests` y `uv run mypy encino_orm` salen 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_query_builder.py -q && uv run pytest -q -m "not integration and not optional_engine" && uv run python -c "import pathlib; t = pathlib.Path('encino_orm/model/query_builder.py').read_text(encoding='utf-8'); assert t.count('check_identifier') >= 4, t.count('check_identifier'); tt = pathlib.Path('tests/test_query_builder.py').read_text(encoding='utf-8'); assert 'x; DROP' in tt, 'el payload hostil debe vivir en los tests'; print('OK: barrido de interpolaciones documentado y con guard')"</automated>
  </verify>
  <done>El barrido está documentado en el código y fijado por tests: cada posición de interpolación de `QueryBuilder` tiene su evidencia (estricta para `_table`/alias, tolerante a puntos para expresiones), y `_table` queda confirmado como el último punto que necesitaba la allowlist estricta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modelo (atributo de clase `_table`) → SQL `FROM`/`JOIN` | `_table` es un identificador que se INTERPOLA en el SQL. Puede venir de un modelo dinámico/code-generated (`create_model`, `introspection.generate_model`), no de una constante escrita a mano. |
| llamador → `QueryBuilder(alias=)` / `join(alias=)` | Ya cerrado por 02-06; el alias es un identificador interpolado. |
| llamador → `select`/`group_by`/`order_by`/agregados | Posiciones de EXPRESIÓN; pasan por `_safe_column` (`_COLUMN_RE`, tolerante a puntos) por decisión documentada. |
| `QueryBuilder` → driver | La validación debe ocurrir ANTES de que exista SQL; un spy prueba que `_prepare`/`fetch_*` no se alcanzan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-53 | Tampering / Elevation of Privilege (**severidad ALTA**) | `QueryBuilder._build_base` interpola `self._model_class._table` sin validar (`query_builder.py:189`) | mitigate | `check_identifier(model_class._table, "nombre de tabla")` en `QueryBuilder.__init__` con la allowlist ESTRICTA (sin relajarla). Regresión que reproduce el payload literal `"t; DROP TABLE usuarios --"` en un modelo dinámico y aserta `ValueError` con `repr()` del valor; el spy `_RecordingDb` prueba `fake.queries == []` (driver nunca alcanzado). |
| T-02-54 | Tampering / Elevation of Privilege (**severidad ALTA**) | `QueryBuilder.join` interpola `join['model_class']._table` sin validar (`query_builder.py:195`) | mitigate | `check_identifier(other._table, "nombre de tabla")` en `join()`, colocado junto a la validación de alias y ANTES del chequeo de duplicados; test dedicado con alias válido y `_table` hostil (el fallo solo puede venir del `_table`). |
| T-02-55 | Tampering | Posiciones de expresión (`select`/`group_by`/`order_by`/`sort_by`/agregados) interpolan texto del llamador | mitigate | Pasan por `_safe_column` con `_COLUMN_RE` (tolerante a puntos, deliberada): rechazan payloads hostiles (`"x; DROP TABLE t --"` → `ValueError`) y aceptan `mm.agente`. Se AÑADE caracterización por familia y un guard de fuente; NO se unifica con la allowlist estricta (Pitfall 10: unificar sería un cambio de comportamiento). |
| T-02-56 | Repudiation / Integrity | Un `_table` vacío (`""`) produce `FROM  mm` (SQL inválido silencioso) | mitigate | La validación del constructor es INCONDICIONAL: `""` lanza `ValueError` fail-closed en vez de generar SQL inválido. Cubierto por el test de valores no-identificador. |
| T-02-SC | Tampering | Instalaciones de paquetes (npm/pip/cargo) | n/a | Este plan NO instala ni actualiza dependencias. |
</threat_model>

<verification>
- `uv run pytest tests/test_query_builder.py -q` pasa (con `-k TablaInjection` y completo).
- `uv run pytest -q -m "not integration and not optional_engine"` pasa (suite DB-free + snapshots).
- `git diff --stat tests/__snapshots__/` VACÍO; ningún golden string por motor editado.
- `uv run ruff check encino_orm tests` + `uv run ruff format --check encino_orm tests` + `uv run mypy encino_orm` salen 0; `# noqa` en `encino_orm/` sigue en 0.
- Reproductor de la verificación: `QueryBuilder(Evil, None)` con `Evil._table = "t; DROP TABLE usuarios --"` lanza `ValueError` (antes: `'FROM t; DROP TABLE usuarios -- mm'`).
- `QueryBuilder(Agente, None)._build_base()[0] == "FROM agentes mm"` (byte-identidad del camino válido).
</verification>

<success_criteria>
- GAP A / CR-01 cerrado: ningún `_table` no-identificador llega al `FROM`/`JOIN` de `QueryBuilder`; la validación vive en el constructor y en `join()`, con el allowlist estricto ya centralizado.
- Existe una regresión que reproduce el payload literal del informe, falla antes y pasa después, y prueba que el driver no se alcanza.
- El barrido deja constancia (comentario + tests de caracterización + guard de fuente) de que `_table` era el último punto sin validar y de por qué las posiciones de expresión usan la allowlist tolerante a puntos.
- Cero cambios de SQL para entradas válidas (snapshots y golden strings intactos).
</success_criteria>

<output>
Create `.planning/phases/02-dialect-seam-engine-parity/02-10-SUMMARY.md` when done
</output>
