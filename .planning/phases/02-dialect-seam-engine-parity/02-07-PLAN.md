---
phase: 02-dialect-seam-engine-parity
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - encino_orm/query.py
  - tests/test_query.py
  - docs/design/0-design.md
autonomous: true
gap_closure: true
requirements:
  - DIAL-05
must_haves:
  truths:
    - "`Query('SELECT 1', [1])` lanza `ValueError`: un parámetro sin placeholder ya no puede declararse y desaparecer en silencio"
    - "`{00}` compila a `%(parameter_0000)s`, la MISMA clave que el dict de params, así que el adaptador nunca lanza `KeyError: 'parameter_00000'`"
    - "`Query(sql, [])` sin placeholders sigue funcionando (migraciones, `list_tables` sin filtro, SQL crudo): el camino de compatibilidad no se rompe"
    - "El doc de diseño publicado muestra la implementación REAL de `Query` y un ejemplo de `with_params()` que se ejecuta tal como está escrito"
  artifacts:
    - path: "encino_orm/query.py"
      provides: "Contrato de cardinalidad aplicado sin carve-out + compilación desde el índice normalizado"
      contains: "int(m.group(1))"
    - path: "tests/test_query.py"
      provides: "Regresión del contrato (valores sin placeholder, {00}, compatibilidad) y guard de sincronía del doc"
      contains: "TestContractEnforcement"
    - path: "docs/design/0-design.md"
      provides: "§2.1 sincronizada con el código y ejemplo de with_params() verdadero"
  key_links:
    - from: "encino_orm/query.py"
      to: "encino_orm/sqlite.py"
      via: "el índice normalizado hace que `_to_positional` encuentre la clave en el dict de params"
      pattern: "_to_positional"
    - from: "docs/design/0-design.md"
      to: "encino_orm/query.py"
      via: "guard de fuente que exige que el sketch documentado coincida con el código"
      pattern: "indices and"
---

<objective>
Cerrar el GAP 2 (parcial) de `02-VERIFICATION.md`: el contrato de cardinalidad de `Query` está documentado (docstring + doc de diseño publicado) pero **no se aplica** en dos casos alcanzables, y el ejemplo publicado de `with_params()` es falso y enseña el patrón del descarte silencioso.

- `Query("SELECT 1", [1])` se acepta en silencio: el guard `if indices and indices != ...` cortocircuita cuando no hay placeholders, el parámetro se guarda y todos los `_prepare` lo descartan. Es el pie de la trampa para `Query("… WHERE x = %s", [v])`.
- `{00}` se valida con `int()` pero se compila con el texto CRUDO, así que el SQL apunta a `parameter_00000` mientras el dict tiene `parameter_0000`; el fallo sale como `KeyError` desde `_to_positional`/`_to_postgres`/`_to_mysql` en vez del `ValueError` limpio que promete el constructor.
- `docs/design/0-design.md` §2.1 documenta un resultado que no existe y un patrón (reutilizar un SELECT sin placeholders con valores) que el punto anterior declara inválido.

Purpose: el contrato y la documentación son parte del entregable de DIAL-05 (D-04/D-05). Un contrato documentado que no se aplica es peor que no tenerlo: normaliza el uso silenciosamente roto.

Output: guard de cardinalidad sin carve-out; compilación desde el índice normalizado; docstring actualizado; §2.1 del doc de diseño sincronizada y con un ejemplo verdadero, protegido por un guard de fuente.

**Requisito previo:** los planes 02-01…02-05 ya están ejecutados. Las decisiones D-01/D-02/D-03/D-04/D-05 siguen vigentes y este plan no las reabre.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md
@.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION.md
@.planning/phases/02-dialect-seam-engine-parity/02-REVIEW.md
@encino_orm/query.py
@tests/test_query.py
@docs/design/0-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aplicar el contrato de cardinalidad y compilar desde el índice normalizado</name>
  <files>encino_orm/query.py, tests/test_query.py</files>
  <read_first>
    - `encino_orm/query.py` — completo (130 líneas): el docstring (líneas 9-41, que declara el contrato y las limitaciones), `_PLACEHOLDER_RE` (:5), `__init__` (:53-71, con el carve-out `if indices and ...` en :56 y la compilación con `m.group(1)` crudo en :62), `with_params` (:103-108), `__eq__`/`__hash__` (:110-127).
    - `tests/test_query.py` — completo (167 líneas): `TestCompilation` (`test_no_placeholders_with_empty_fields` en :107, `test_out_of_range_raises`, `test_unused_parameter_raises`), `TestWithParams`, `TestReadOnlyCompat`, `TestEqualityAndHash`. Estos tests son el oráculo de no-regresión: ninguno debe editarse.
    - `encino_orm/sqlite.py` líneas 20-40 — `_PLACEHOLDER_RE` + `_to_positional(sql, params)` (el traductor que hoy lanza `KeyError` con `{00}`).
    - `.planning/phases/02-dialect-seam-engine-parity/02-REVIEW.md` — WR-01 y WR-02 (reproducciones exactas).
    - `.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION.md` — bloque `gaps[2]` y spot-check `WR-01/WR-02`.
  </read_first>
  <behavior>
    - `Query("SELECT 1", [1])` → `ValueError` cuyo mensaje contiene `placeholders [] no cuadran con 1 parámetros`.
    - `Query("SELECT 1", [])` y `Query("SELECT 1")` → siguen compilando a `sql == "SELECT 1"`, `params == {}`, `query == ["SELECT 1", {}]`.
    - `Query("a={00}", [7])` → `sql == "a=%(parameter_0000)s"` y `params == {"parameter_0000": 7}`; `_to_positional(q.sql, q.params)` devuelve `("a=?", [7])` SIN `KeyError`.
    - `Query("a={0} AND b={00}", [7])` → ambos placeholders compilan a `%(parameter_0000)s` y el dict tiene exactamente una clave.
    - `Query("SELECT 1", []).with_params([1])` → `ValueError` (el patrón que enseña el doc actual deja de "funcionar").
    - Todos los casos ya cubiertos siguen igual: índices dispersos, duplicados, fuera de rango, parámetro sin usar, hash/eq, inmutabilidad.
  </behavior>
  <action>
    **A) Quitar el carve-out.** En `encino_orm/query.py::__init__`, sustituir `if indices and indices != set(range(len(values))):` por `if indices != set(range(len(values))):`. `set() != set(range(0))` es `False`, así que el caso "sin placeholders y sin valores" sigue pasando sin condición especial; el caso "sin placeholders CON valores" pasa a lanzar, que es exactamente lo que promete el docstring y lo que D-04 fija. El mensaje de error actual (`f"placeholders {sorted(indices)} no cuadran con {len(values)} parámetros"`) se conserva literal: los tests existentes lo comprueban por subcadena.

    **B) Compilar desde el índice normalizado.** Sustituir `lambda m: f"%(parameter_000{m.group(1)})s"` por `lambda m: f"%(parameter_000{int(m.group(1))})s"`, de modo que validación y compilación no puedan discrepar: `{0}` y `{00}` producen el mismo placeholder y la misma clave que el dict de params. Esto elimina el `KeyError` que hoy escapa desde `_to_positional`/`_to_postgres`/`_to_mysql`.

    **C) Actualizar el docstring** para que describa lo que el código hace ahora: (1) el conjunto de índices detectados debe ser EXACTAMENTE `range(len(values))`, sin excepciones ni carve-outs; (2) los índices se normalizan con `int()` antes de compilar, así que `{0}` y `{00}` son el mismo placeholder; (3) mencionar que pasar valores a una plantilla sin `{n}` es un error de contrato (no un descarte silencioso). Mantener las dos limitaciones conocidas ya declaradas (un `{n}` dentro de un literal de cadena se interpreta como placeholder; la inmutabilidad es de atributo, no profunda).

    **D) Lo que NO se toca.** No añadir modo de params nombrados (D-04 lo descartó). No reintroducir `rebind`/`format` (D-01). No cambiar los accesores `sql`/`params`/`fields`/`query`/`sql_template` (D-02) ni `__eq__`/`__hash__` (D-03). No añadir `__setattr__` (la limitación documentada de IN-01 no es parte de este gap).

    **E) Comprobación previa obligatoria de callers.** Antes de aplicar B/C, listar los sitios que construyen `Query` sin placeholders: `grep -rn "Query(" encino_orm/`. Los legítimos son `Query(sql, [])` (ledger de migraciones, `_tables_sql` sin filtro, DDL crudo) y `Query(sql)`/`Query(sql, [])` en `migration.py`. Si aparece algún sitio que pasa valores NO vacíos a una plantilla sin `{n}`, DETENERSE y reportarlo en el SUMMARY: es un descarte silencioso en ese call site (un bug latente), no una razón para conservar el carve-out.

    **F) Tests de regresión** en `tests/test_query.py`, clase nueva `TestContractEnforcement`:
    1. `test_valores_sin_placeholders_ahora_lanza` — `pytest.raises(ValueError)` sobre `Query("SELECT 1", [1])`; el mensaje contiene `"placeholders []"` y `"1 parámetros"`.
    2. `test_sin_placeholders_y_sin_valores_sigue_pasando` — `Query("SELECT 1", [])` y `Query("SELECT 1")` compilan a `("SELECT 1", {})`.
    3. `test_indice_con_cero_inicial_compila_normalizado` — `Query("a={00}", [7])`: `sql == "a=%(parameter_0000)s"`, `params == {"parameter_0000": 7}`; y `_to_positional(q.sql, q.params) == ("a=?", [7])` importando el traductor privado directamente (`from encino_orm.sqlite import _to_positional`, patrón white-box aceptado en el repo).
    4. `test_indice_normal_y_cero_inicial_no_colisionan` — `Query("a={0} AND b={00}", [7])`: `sql == "a=%(parameter_0000)s AND b=%(parameter_0000)s"`, `len(q.params) == 1`.
    5. `test_with_params_sobre_plantilla_sin_placeholders_lanza` — `Query("SELECT 1", []).with_params([1])` lanza `ValueError` (el anti-ejemplo exacto del doc actual).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_query.py -q` pasa y `-k "cero_inicial or placeholders"` selecciona al menos 4 tests.
    - `Query("SELECT 1", [1])` lanza `ValueError`; `Query("SELECT 1", [])` sigue funcionando.
    - `grep -v '^#' encino_orm/query.py | grep -c "indices and"` == 0 y `grep -v '^#' encino_orm/query.py | grep -c "int(m.group(1))"` == 1.
    - Ningún test existente de `tests/test_query.py` se edita (solo se añade la clase nueva).
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa (ningún caller del core rompe).
    - `uv run ruff check encino_orm tests` y `uv run mypy encino_orm` salen 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_query.py -q -k "cero_inicial or placeholders or contract" && uv run python -c "from encino_orm import Query\nfrom encino_orm.sqlite import _to_positional\nq = Query('a={00}', [7])\nassert q.sql == 'a=%(parameter_0000)s', q.sql\nassert q.params == {'parameter_0000': 7}, q.params\nassert _to_positional(q.sql, q.params) == ('a=?', [7])\ntry:\n    Query('SELECT 1', [1])\nexcept ValueError as e:\n    print('OK:', e)\nelse:\n    raise SystemExit('FALLO: parámetro sin placeholder aceptado en silencio')"</automated>
  </verify>
  <done>El contrato de cardinalidad se aplica sin carve-out y la compilación usa el índice normalizado: `{00}` no puede producir un `KeyError` en el adaptador y un parámetro declarado sin placeholder lanza `ValueError`.</done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar el doc de diseño publicado y protegerlo con un guard de fuente</name>
  <files>docs/design/0-design.md, tests/test_query.py</files>
  <read_first>
    - `docs/design/0-design.md` líneas 33-110 — §2.1 `Query`: el sketch de implementación (:39-79, que HOY reproduce el carve-out `if indices and ...` en :50 y la compilación con `m.group(1)` crudo en :55) y los ejemplos (:85-105, con el bloque falso de `with_params()` en :100-104 y la nota de migración en :107).
    - `docs/design/0-design.md` líneas 22 y 35 — menciones de `rebind` ya corregidas por 02-03; verificar que siguen correctas (D-05: el sitio publicado no debe documentar una API eliminada).
    - `encino_orm/query.py` — el estado FINAL tras la Task 1 (es el texto que el sketch debe reflejar).
    - `tests/test_query.py` líneas 70-80 — `TestNoRebindInSource`: el patrón de guard de fuente por `pathlib`/`read_text` que ya existe en el repo (mismo estilo para el guard nuevo).
    - `.planning/phases/02-dialect-seam-engine-parity/02-REVIEW.md` — WR-07 (ejemplo falso y por qué `mkdocs build --strict` no lo detecta).
    - `mkdocs.yml` — el sitio se publica desde `docs/`; el build estricto es el gate de sintaxis (no de veracidad).
  </read_first>
  <action>
    **A) Corregir el sketch de implementación (§2.1, bloque `python` de :39-79)** para que coincida EXACTAMENTE con `encino_orm/query.py` tras la Task 1: eliminar el `if indices and` y compilar con `int(m.group(1))`. El doc de diseño se publica: no puede mostrar un contrato que el código no aplica. Mantener el resto del sketch (slots, `object.__setattr__`, properties, `with_params`) tal cual.

    **B) Corregir el ejemplo de `with_params()` (:100-104).** El bloque actual reutiliza la `q` del bloque "Raw SQL (sin placeholders)" — que no tiene placeholders — y afirma que `q2.sql` se convierte en un INSERT. Ambas afirmaciones son falsas: `Query("SELECT * FROM usuarios", []).with_params(["Grupo B", 0]).sql` devuelve el SELECT sin cambios, y tras la Task 1 directamente LANZA. Sustituirlo por un ejemplo autocontenido cuya plantilla SÍ tenga placeholders:
    - `q = Query("insert into grupos (grupo, enabled) values ({0},{1})", ["Grupo A", 1])`
    - `q2 = q.with_params(["Grupo B", 0])`
    - comentarios verdaderos: `q2.sql -> "insert into grupos (grupo, enabled) values (%(parameter_0000)s,%(parameter_0001)s)"`, `q2.params -> {"parameter_0000": "Grupo B", "parameter_0001": 0}`, `q sigue intacta`.
    Añadir además una línea explicando POR QUÉ el bloque anterior no puede reutilizarse así: pasar valores a una plantilla sin `{n}` viola el contrato de cardinalidad y lanza `ValueError` (no descarta los parámetros). Así el doc enseña el patrón correcto y explica el error.

    **C) Mantener la nota de migración** (:107) tal cual: `rebind` se eliminó y su sustituto es `with_params()`. No reintroducir `rebind` como API viva en ninguna parte del fichero (`grep -c "rebind" docs/design/0-design.md` puede seguir siendo > 0 solo por la nota de migración; ninguna aparición puede presentarlo como vigente).

    **D) Guard de fuente en `tests/test_query.py`**, clase nueva `TestDesignDocSync` (mismo estilo que `TestNoRebindInSource`), que hace EJECUTABLE la afirmación del doc:
    1. `test_el_sketch_del_doc_coincide_con_el_codigo` — leer `docs/design/0-design.md` con `encoding="utf-8"` y afirmar que el texto contiene `int(m.group(1))` y NO contiene `indices and`. (Falla ANTES de la Task 1 y del arreglo del doc: el doc hoy muestra `if indices and ...`; pasa después.)
    2. `test_el_ejemplo_de_with_params_del_doc_es_verdadero` — reproducir en código el ejemplo documentado y afirmar los valores EXACTOS que el doc declara: `Query("insert into grupos (grupo, enabled) values ({0},{1})", ["Grupo A", 1]).with_params(["Grupo B", 0])` → `sql == "insert into grupos (grupo, enabled) values (%(parameter_0000)s,%(parameter_0001)s)"` y `params == {"parameter_0000": "Grupo B", "parameter_0001": 0}`, y que el original sigue con `["Grupo A", 1]`.
    3. `test_el_doc_no_documenta_rebind_como_api_viva` — el fichero no contiene `q.rebind(` ni `def rebind(` (D-05: el sitio publicado no documenta una API eliminada).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_query.py -q -k "doc"` pasa y selecciona los 3 tests del guard.
    - `grep -v '^#' docs/design/0-design.md | grep -c "indices and"` == 0 y `grep -c "int(m.group(1))" docs/design/0-design.md` ≥ 1.
    - `uv run mkdocs build --strict` sale 0.
    - Los valores documentados del ejemplo se ejecutan y coinciden (el guard lo prueba, no una revisión a ojo).
    - `git diff --stat docs/design/0-design.md` muestra cambios SOLO en §2.1 (sketch + ejemplo); la nota de migración y el resto del documento quedan intactos.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_query.py -q -k "doc" && uv run mkdocs build --strict</automated>
  </verify>
  <done>El doc de diseño publicado muestra la implementación real de `Query`, un ejemplo de `with_params()` que se ejecuta como está escrito, y ambas afirmaciones quedan protegidas por un guard de fuente que falla si vuelven a divergir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llamador → `Query` | La plantilla SQL y la lista de valores son entrada del llamador; el contrato de cardinalidad es la única defensa contra el descarte silencioso de parámetros. |
| `Query` compilado → traductores del adaptador | El nombre de placeholder compilado debe existir en el dict de params, o el fallo aparece como `KeyError` dentro del driver. |
| doc publicado → usuario | `https://hvalles.github.io/encinorm/` sirve `docs/design/0-design.md`: un ejemplo falso enseña un patrón roto a escala. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-39 | Tampering (Integrity) | `Query.__init__` — guard de cardinalidad con carve-out | mitigate | Se elimina el `indices and`: un `values` no vacío con cero placeholders lanza `ValueError` en vez de almacenar parámetros que todos los `_prepare` descartan. Test de regresión con el caso exacto (`Query("SELECT 1", [1])`) y test de compatibilidad que preserva `Query(sql, [])`. |
| T-02-40 | Denial of Service / Information Disclosure | `{00}` → `KeyError` desde `_to_positional`/`_to_postgres`/`_to_mysql` | mitigate | Compilación desde el índice normalizado (`int(m.group(1))`): validación y compilación no pueden discrepar. Test que ejecuta el traductor de SQLite sobre el Query compilado y exige `("a=?", [7])` sin `KeyError`. |
| T-02-41 | Repudiation | Doc publicado que documenta un resultado inexistente | mitigate | El sketch y el ejemplo se corrigen y quedan protegidos por un guard de fuente (`TestDesignDocSync`) que falla si el doc vuelve a mostrar el carve-out o a documentar `rebind` como API viva. `mkdocs build --strict` sigue siendo el gate de sintaxis; el guard cubre la veracidad. |
| T-02-42 | Tampering | Romper el camino de compatibilidad `Query(sql, [])` al endurecer el contrato | mitigate | Tests dedicados que fijan `Query("SELECT 1", [])` y `Query("SELECT 1")` → `("SELECT 1", {})`, más la suite DB-free completa (migraciones, `list_tables` sin filtro) como oráculo. Comprobación previa de callers obligatoria antes de aplicar el cambio. |
| T-02-SC | Tampering | Instalaciones de paquetes | n/a | Este plan no instala ni actualiza dependencias. |
</threat_model>

<verification>
- `uv run pytest tests/test_query.py -q` pasa (los tests existentes sin editar + las dos clases nuevas).
- `uv run pytest -q -m "not integration and not optional_engine"` pasa.
- `uv run mkdocs build --strict` sale 0.
- `uv run ruff check encino_orm tests` + `uv run ruff format --check encino_orm tests` + `uv run mypy encino_orm` salen 0.
- Las dos reproducciones del informe (`Query('SELECT 1',[1]).params` y `Query('a={00}',[7])` + `_to_positional`) ya no reproducen el fallo: la primera lanza `ValueError`, la segunda compila a `parameter_0000` y resuelve.
</verification>

<success_criteria>
- El contrato de cardinalidad de `Query` se aplica en la construcción, sin carve-outs, tal como lo describen el docstring y el doc de diseño.
- `{00}` (y cualquier índice con ceros iniciales) compila a la misma clave que el dict de params: ningún `KeyError` puede escapar del adaptador por esta causa.
- El camino de compatibilidad `Query(sql, [])` sigue funcionando para migraciones y SQL crudo.
- `docs/design/0-design.md` §2.1 describe la implementación real y un ejemplo de `with_params()` verificado por un test.
</success_criteria>

<output>
Create `.planning/phases/02-dialect-seam-engine-parity/02-07-SUMMARY.md` when done
</output>
