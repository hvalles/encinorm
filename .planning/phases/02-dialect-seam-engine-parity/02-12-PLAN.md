---
phase: 02-dialect-seam-engine-parity
plan: 12
type: execute
wave: 2
depends_on:
  - 02-10
  - 02-11
files_modified:
  - CHANGELOG.md
  - .planning/phases/02-dialect-seam-engine-parity/deferred-items.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - DIAL-02
  - DIAL-05
must_haves:
  truths:
    - "`CHANGELOG.md` registra en `[Unreleased] ### Corregido` (sección YA existente, sin encabezado duplicado) el contrato de cardinalidad de `Query` introducido por 02-07."
    - "`CHANGELOG.md` registra los cambios de comportamiento de CR-01 (validación del nombre de tabla en `QueryBuilder`), CR-02 (fail-closed del render `merge`) y CR-03 (id no fiable en `insert(replace=True)` de MSSQL/Oracle)."
    - "`deferred-items.md` deja de afirmar que no queda ningún item de la Fase 02 sin dueño y asigna dueño explícito a ORA-38104 y a la captura real de `last_id` dentro del insert."
    - "El dueño queda registrado en `ROADMAP.md` (Fase 4 / plan 04-02 / POOL-03) y trazado en `REQUIREMENTS.md`."
    - "Ninguna entrada afirma que el fallback `columns[0]` del MERGE esté corregido: la semántica preexistente sigue documentada como defecto con dueño."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Entradas `[Unreleased] ### Corregido` para el contrato de cardinalidad y para CR-01/CR-02/CR-03"
      contains: "cardinalidad"
    - path: ".planning/phases/02-dialect-seam-engine-parity/deferred-items.md"
      provides: "Dueño explícito de ORA-38104 y de la captura de last_id; sección de la ronda 3"
      contains: "04-02"
    - path: ".planning/ROADMAP.md"
      provides: "El plan 04-02 (Fase 4) declara que posee el fix del SET del MERGE de Oracle (ORA-38104)"
      contains: "ORA-38104"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Trazabilidad del dueño en POOL-03"
      contains: "ORA-38104"
  key_links:
    - from: ".planning/phases/02-dialect-seam-engine-parity/deferred-items.md"
      to: ".planning/ROADMAP.md"
      via: "el dueño asignado (Fase 4 / 04-02 / POOL-03) debe existir y estar redactado en el roadmap"
      pattern: "04-02"
    - from: "CHANGELOG.md"
      to: ".planning/phases/02-dialect-seam-engine-parity/02-07-SUMMARY.md"
      via: "la entrada de cardinalidad describe el cambio observable de 02-07 (commits a953970/c058bba/6433b31, que no tocaron el changelog)"
      pattern: "cardinalidad"
---

<objective>
Cerrar los GAP D / WR-01 y GAP E / WR-02 de `02-VERIFICATION-GAPS.md` (ronda 3): el log de diferidos de la fase MIENTE sobre su propia cobertura y un cambio de comportamiento observable no está documentado, incumpliendo una restricción dura del proyecto.

**WR-01 (WARNING).** `deferred-items.md:10-11` afirma "**Ya NO queda ningún item de la Fase 02 sin dueño**", pero el item `ORA-38104` está marcado PENDIENTE y su "sugerencia para una fase futura" no nombra ninguna fase; ninguna fase 3–8 lo menciona. Además, el defecto CR-02 de `Model.upsert` no estaba registrado en absoluto.

**WR-02 (WARNING).** `Query("SELECT 1", [1])` antes se aceptaba (el valor se descartaba en silencio) y ahora lanza `ValueError`; `{00}` compila a una clave distinta. Los tres commits de 02-07 (`a953970`, `c058bba`, `6433b31`) no tocaron `CHANGELOG.md`. `PROJECT.md`/`AGENTS.md` fijan que, en `0.x`, **todo cambio incompatible DEBE documentarse en `CHANGELOG.md`** — 02-06 y 02-08 añadieron su entrada `### Corregido`; 02-07 no.

Purpose: un log de diferidos que afirma no tener items huérfanos mientras deja uno PENDIENTE sin dueño es peor que no tener log: destruye la confianza en el mecanismo que existe precisamente para no perder trabajo. Y un contrato de comportamiento cambiado sin entrada en el changelog rompe la única promesa que el proyecto hace a sus usuarios en `0.x`.

Output: entradas de changelog para el contrato de cardinalidad y para los tres hallazgos críticos de la ronda (CR-01/CR-02/CR-03); dueño explícito y trazado para ORA-38104 y para la captura real de `last_id`; corrección de la afirmación falsa del encabezado de la ronda anterior.

**Alcance de la documentación (no invade la Fase 8).** La enumeración COMPLETA de cambios incompatibles del milestone (incluida la eliminación de `Query.rebind`/`Query.format`, D-01/D-06) sigue siendo propiedad de la Fase 8 (`08-04`, REL-04) y NO se toca aquí. Este plan añade SOLO las entradas que la propia ronda omitió, siguiendo la práctica que 02-06/02-08 establecieron (entrada inmediata bajo `[Unreleased]`).

**Dependencia real.** `depends_on: [02-10, 02-11]` porque las entradas de changelog deben describir el comportamiento REALMENTE entregado por esos dos planes (y el dueño asignado a ORA-38104 debe ser coherente con el guard de CR-02 y con la decisión de CR-03).
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION-GAPS.md
@.planning/phases/02-dialect-seam-engine-parity/02-REVIEW-GAPS.md
@.planning/phases/02-dialect-seam-engine-parity/02-07-SUMMARY.md
@.planning/phases/02-dialect-seam-engine-parity/02-08-SUMMARY.md
@.planning/phases/02-dialect-seam-engine-parity/deferred-items.md
@CHANGELOG.md

<interfaces>
<!-- Contratos y anclajes que el ejecutor necesita. Extraídos del árbol; no explorar para rederivarlos. -->

`CHANGELOG.md` — estructura ACTUAL de `[Unreleased]` (no crear encabezados duplicados):
```markdown
## [Unreleased]

### Corregido

- `indexes_ddl` valida las columnas de índice no mapeadas … (02-06)
- `QueryBuilder.all()`, `first()` y `exists()` dejan de emitir un `LIMIT` en línea … (02-08)
- `Model.upsert()` en MariaDB emite `ON DUPLICATE KEY UPDATE` … (02-08)
- `Model.insert(replace=True)` usa la clave primaria del modelo como objetivo … (02-08)
- Correcciones de EJECUCIÓN del `MERGE` en MSSQL/Oracle … (02-08)

## [0.2.6] - 2026-09-11
```

`deferred-items.md` — anclajes a corregir:
```text
## Cerrados en la ronda de gap closure                       # :6
… **Ya NO queda ningún item de la Fase 02 sin dueño** …    # :10-11  <- AFIRMACIÓN FALSA
### Oracle — el fallback `merge` actualiza la columna del `ON` (ORA-38104) — PENDIENTE   # :114
… - **Sugerencia para una fase futura:** excluir de `set_sql` …                        # :129-132 <- SIN DUEÑO
```
El fix sugerido (ya escrito en el item) es correcto: excluir de `set_sql` las columnas de `conflict_cols`, como YA hace `build_upsert` con `update_cols`, y regenerar los snapshots de forma visible.

`ROADMAP.md:226` — el plan que hereda el dueño:
```text
- [ ] 04-02: Capture `last_id` **inside** the insert (`RETURNING` / `SCOPE_IDENTITY` /
  immediate `lastrowid`) per connection/task; deprecate post-hoc `last_id()` with a warning — POOL-03
```

`ROADMAP.md:163-178` — narrativa de la ronda de gap closure 2 (donde se añade la de la ronda 3) y `ROADMAP.md:305-328` — Fase 8 (donde NO se toca la enumeración de breaking changes: es de `08-04`/REL-04).

`REQUIREMENTS.md:45` — `POOL-03` (el requisito que se amplía con la nota de trazabilidad).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar en `CHANGELOG.md` el contrato de cardinalidad de `Query` y los cambios de comportamiento de CR-01/CR-02/CR-03</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md:9-47` — `[Unreleased]` completo: el encabezado `### Corregido` YA existe (creado por 02-06) y tiene 5 bullets; hay que EXTENDERLO, nunca añadir un segundo `### Corregido`.
    - `CHANGELOG.md:1-8` — formato declarado: Keep a Changelog + SemVer, y el aviso de que en `0.x` no hay garantía de estabilidad.
    - `.planning/phases/02-dialect-seam-engine-parity/02-07-SUMMARY.md:60-72` — qué cambió exactamente 02-07: se elimina el carve-out `if indices and …`, `Query("SELECT 1", [1])` lanza `ValueError: placeholders [] no cuadran con 1 parámetros`, la compilación usa `int(m.group(1))` y `{00}` → `%(parameter_0000)s`; `Query(sql, [])` se conserva.
    - `.planning/phases/02-dialect-seam-engine-parity/02-REVIEW-GAPS.md` — WR-02 (con la redacción sugerida) y el aviso de que la eliminación de `rebind`/`format` (WR-08) pertenece a la Fase 8.
    - `.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION-GAPS.md` — `gaps[4]` (WR-02) y el `deferred[1]` que confirma que la enumeración completa es de la Fase 8 (SC3).
    - `.planning/phases/02-dialect-seam-engine-parity/02-10-PLAN.md` y `02-11-PLAN.md` — el comportamiento REALMENTE entregado que la entrada debe describir (validación de `_table`; fail-closed del render `merge`; `insert(replace=True)` sin id fiable en MSSQL/Oracle).
    - `PROJECT.md` (Constraints) y `AGENTS.md` — la restricción dura: en `0.x` los cambios incompatibles DEBEN quedar documentados en `CHANGELOG.md`.
  </read_first>
  <action>
    **A) Extender la sección existente, sin duplicarla.** En `CHANGELOG.md`, añadir CUATRO bullets al final de la lista de `### Corregido` dentro de `[Unreleased]` (la que ya existe en :11-46). NO crear un segundo `### Corregido`, NO crear una sección `### Cambiado` nueva y NO tocar `[0.2.6]` ni anteriores. El estilo es el de los bullets existentes: español, una o dos frases, con la nota de ownership cuando el cambio es aditivo respecto a la enumeración del milestone.

    **B) Bullet 1 — contrato de cardinalidad de `Query` (WR-02, cambio observable de 02-07).** Describir que `Query` aplica el contrato de cardinalidad SIN carve-outs: pasar valores a una plantilla sin `{n}` ahora lanza `ValueError` (antes los valores se descartaban en silencio), y `{00}` se normaliza a `parameter_0000` en vez de dejar una clave que el adaptador no resuelve. Mencionar que el camino compatible `Query(sql, [])` se conserva. Cerrar con la nota de ownership ya usada por 02-06: esta entrada es ADITIVA y no prejuzga la enumeración de cambios incompatibles del milestone, que posee la Fase 8 (`08-04`).

    **C) Bullet 2 — CR-01 (validación del nombre de tabla en `QueryBuilder`).** Describir que `QueryBuilder` valida el nombre de tabla del modelo (constructor) y el de cada destino de `join()` con la allowlist estricta, de modo que un `_table` no-identificador (p. ej. de un modelo dinámico/code-generated) lanza `ValueError` ANTES de generar SQL, en vez de interpolarse en el `FROM`/`JOIN` (fail-closed). Mencionar que las posiciones de expresión (`select`/`group_by`/`order_by`) mantienen su allowlist tolerante a puntos, por decisión.

    **D) Bullet 3 — CR-02 (fail-closed del render `merge`).** Describir que, en los dialectos `merge` (MSSQL/Oracle), el objetivo de conflicto de un `MERGE` DEBE ser una columna presente en los datos insertados; si no lo es, el builder lanza `ValueError` con un mensaje accionable en vez de emitir `ON (dst.<col> = src.<col>)` sobre una columna inexistente (antes: MSSQL 207 `Invalid column name` / Oracle `ORA-00904`). Indicar explícitamente que `Model.upsert()` con el conflicto por defecto (PK) sobre un modelo de PK autoincremental ahora falla en voz alta y que el llamador debe pasar `conflict=` con una columna de datos (la PK `id` no viaja en el INSERT). No presentarlo como una mejora de la semántica del upsert: es un fallo cerrado.

    **E) Bullet 4 — CR-03 (`insert(replace=True)` en MSSQL/Oracle).** Describir que `Model.insert(replace=True)` en MSSQL/Oracle se renderiza como `MERGE`, que no expone el id de la fila en la frontera del driver; antes devolvía y asignaba el `last_id()` cacheado, que podía ser el id de OTRA fila (un `update()` posterior apuntaba a la fila equivocada). Ahora devuelve `0` ("id no disponible") y deja `self.id` intacto. Añadir DOS aclaraciones: (1) el camino no-merge (`INSERT` plano y `replace` en PostgreSQL) sigue devolviendo el id real sin cambios; (2) la captura del id DENTRO del insert (`OUTPUT INSERTED.id` / `SCOPE_IDENTITY`) pertenece a la Fase 4 (`04-02`, POOL-03), y el fallback `columns[0]` del MERGE (semántica preexistente, incorrecta) sigue sin corregir y con dueño.

    **F) NO tocar lo ajeno.** No añadir la entrada de la eliminación de `Query.rebind`/`Query.format` (D-01/D-06): es de la Fase 8 (`08-04`). No reordenar, reformatear ni reescribir los bullets existentes. No añadir fechas ni números de versión (`[Unreleased]` se queda sin fecha).
  </action>
  <acceptance_criteria>
    - Exactamente **una** sección `### Corregido` DENTRO de `[Unreleased]` (no hay encabezado duplicado). OJO: `CHANGELOG.md` ya contiene varios `### Corregido` históricos (en `0.2.6` y anteriores), así que el criterio NO puede ser un `grep -c` global: acotarlo a la sección, p. ej. `sed -n '/## \[Unreleased\]/,/## \[0.2.6\]/p' CHANGELOG.md` y contar ahí.
    - `grep -c "cardinalidad" CHANGELOG.md` ≥ 1 y `grep -c "parameter_0000" CHANGELOG.md` ≥ 1.
    - `grep -c "nombre de tabla" CHANGELOG.md` ≥ 1 y `grep -c "no está en el INSERT" CHANGELOG.md` ≥ 1 y `grep -c "id no disponible" CHANGELOG.md` ≥ 1.
    - `grep -c "rebind" CHANGELOG.md` == 0 (la enumeración de la Fase 8 no se adelanta aquí).
    - `git diff --numstat CHANGELOG.md` muestra SOLO adiciones dentro de `[Unreleased]`; `[0.2.6]` y anteriores intactos (verificable con `git diff CHANGELOG.md` e inspección).
    - El changelog sigue siendo markdown válido: `uv run python -c "import pathlib; t=pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8'); assert t.count('## [Unreleased]')==1"`.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pathlib; t = pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8'); unreleased = t.split('## [0.2.6]')[0]; assert unreleased.count('### Corregido') == 1, unreleased.count('### Corregido'); assert 'cardinalidad' in unreleased and 'parameter_0000' in unreleased; assert 'nombre de tabla' in unreleased; assert 'no está en el INSERT' in unreleased; assert 'id no disponible' in unreleased; assert 'rebind' not in t; print('OK: changelog de la ronda 3 sin encabezado duplicado')"</automated>
  </verify>
  <done>`CHANGELOG.md` documenta en la sección `[Unreleased] ### Corregido` ya existente (sin duplicarla) el contrato de cardinalidad de `Query` de 02-07 y los cambios de comportamiento de CR-01/CR-02/CR-03, sin adelantar la enumeración de la Fase 8.</done>
</task>

<task type="auto">
  <name>Task 2: Asignar dueño a ORA-38104 y a la captura de `last_id`, corregir la afirmación falsa y trazarlo en ROADMAP/REQUIREMENTS</name>
  <files>.planning/phases/02-dialect-seam-engine-parity/deferred-items.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/02-dialect-seam-engine-parity/deferred-items.md` completo (135 líneas): el encabezado `## Cerrados en la ronda de gap closure` (:6-11) con la afirmación falsa, los items cerrados (:13-83), la sección `## Encontrados durante 02-08` (:85-135) con el item ORA-38104 PENDIENTE y su fix sugerido (:114-135).
    - `.planning/ROADMAP.md:119-178` — sección de la Fase 2 y la narrativa de la ronda de gap closure 2 (`**Gap-closure waves:** 1 → 02-06, 02-07; 2 → 02-08; 3 → 02-09`).
    - `.planning/ROADMAP.md:204-229` — Fase 4 completa, en particular el bullet `04-02` (:226) y sus Success Criteria 2 ("Concurrent inserts on pooled connections each return their own id").
    - `.planning/ROADMAP.md:305-328` — Fase 8 (SC3 y el plan `08-04`): NO tocar su enumeración de breaking changes; solo se referencia como dueño de la enumeración.
    - `.planning/REQUIREMENTS.md:41-46` — `POOL-03` (el requisito que se amplía con la nota de trazabilidad) y `:157` (la fila de trazabilidad `POOL-03 | Phase 4 | Pending`).
    - `.planning/phases/02-dialect-seam-engine-parity/02-VERIFICATION-GAPS.md` — `gaps[3]` (WR-01) con el `missing` exacto ("Assign an owner (phase/plan ID) to ORA-38104 and to the CR-02 merge-upsert defect, or reword the header") y la nota de que el fix general de CR-03 lo posee la Fase 4 (POOL-03 / SC2).
    - `.planning/phases/02-dialect-seam-engine-parity/02-11-PLAN.md` — las decisiones de CR-02/CR-03 que el log de diferidos debe reflejar (fail-closed; no consumir el id obsoleto).
  </read_first>
  <action>
    **A) Corregir la afirmación falsa del encabezado.** En `deferred-items.md:6-11`, reescribir el párrafo del encabezado `## Cerrados en la ronda de gap closure` para ELIMINAR la frase "**Ya NO queda ningún item de la Fase 02 sin dueño** — este era el último diferido huérfano" (es literalmente falsa). Sustituirla por una afirmación verificable: "Los items de esta sección están cerrados. Los items que quedan abiertos de la fase están en las secciones siguientes y TODOS tienen dueño explícito (Fase 4 / plan 04-02 / POOL-03); no se afirma que no quede ninguno sin dueño."

    **B) Asignar dueño al item ORA-38104.** En el item `### Oracle — el fallback merge actualiza la columna del ON (ORA-38104) — PENDIENTE` (:114-135), cambiar el estado de PENDIENTE a **PENDIENTE — DUEÑO: Fase 4, plan 04-02 (POOL-03)** y añadir un apartado **Dueño** que explique por qué: la Fase 4 captura el id dentro del insert (`RETURNING`/`SCOPE_IDENTITY`/`lastrowid`, `ROADMAP.md:226`) y para que `Model.insert(replace=True)` sea ejecutable Y devuelva un id fiable en Oracle, el `WHEN MATCHED THEN UPDATE SET` no puede actualizar la columna del `ON`; el fix es el ya sugerido (excluir de `set_sql` las columnas de `conflict_cols`, como ya hace `build_upsert` con `update_cols`) y exige regenerar los snapshots de forma visible. Dejar escrito que NO se corrige en esta ronda porque cambiaría el SQL del adaptador pinado byte a byte (golden strings y snapshots) y porque el plan 02-11 solo añade un guard fail-closed que no altera el SQL válido.

    **C) Añadir la sección de la ronda 3.** Añadir al final del fichero una sección `## Encontrados y cerrados en la ronda de gap closure 3 (02-10…02-12)` con:
    1. **CR-01 — `QueryBuilder` interpolaba `_table` sin validar (CERRADO por 02-10).** Causa: `_build_base` interpolaba `self._model_class._table` y cada `join['model_class']._table`; `Model._table` solo se validaba en `_build_column_map()`, que `QueryBuilder` no dispara. Fix: `check_identifier(model_class._table, "nombre de tabla")` en `__init__` y `other._table` en `join()`, con la allowlist estricta; barrido de las posiciones de expresión (allowlist tolerante a puntos, deliberada) y guard de fuente. Sin cambio de SQL válido.
    2. **CR-02 — `Model.upsert()` con el conflicto por defecto emitía `ON (dst.id = src.id)` con `src` sin `id` (CERRADO por 02-11).** Reproducción live: MSSQL 207 / Oracle ORA-00904. Fix: fail-closed en `_merge_sql` (punto único de `build_insert`/`build_upsert`) cuando una columna de conflicto no está en las columnas del INSERT; `Model.upsert()` con PK autoincremental en `merge` exige `conflict=` explícito con una columna de datos. Residual: un upsert por PK autoincremental es INEXPRESABLE en MSSQL/Oracle por construcción (la PK no viaja en el INSERT) — documentado, no un diferido con dueño pendiente.
    3. **CR-03 — MSSQL `insert(replace=True)` devolvía un `last_id()` obsoleto (CERRADO por 02-11).** Regresión introducida por 02-08. Fix: `Model.insert` NO consume `last_id()` ni asigna `self.id` cuando la sentencia ejecutada es un `MERGE`; devuelve `0` ("id no disponible"). **Residual con dueño: Fase 4, plan 04-02 (POOL-03)** para capturar el id DENTRO del insert (`OUTPUT INSERTED.id` / `SCOPE_IDENTITY`). Oracle tiene el mismo defecto de clase (su `execute` solo refresca `_last_id` con `RETURNING`) y el mismo guard lo cubre.
    4. **Byte-identidad de la ronda 3:** ninguna de las tres correcciones cambia el SQL generado para entradas válidas; `tests/__snapshots__/test_sql_snapshots.ambr` y los golden strings por motor no se tocaron. (Verificable con `git diff --stat tests/__snapshots__/`.)
    5. **WR-02 — contrato de cardinalidad de `Query` (CERRADO por 02-12):** registrado en `CHANGELOG.md` `[Unreleased] ### Corregido`; la enumeración completa de cambios incompatibles del milestone sigue siendo de la Fase 8 (`08-04`).
    Mantener el estilo del fichero (español, bullets con **Found in**/**Causa**/**Estado**/**Cobertura** cuando aplique).

    **D) Registrar el dueño en el roadmap.** En `.planning/ROADMAP.md`, extender el bullet del plan `04-02` (:226) para que declare explícitamente que además posee el fix del `SET` del MERGE de Oracle (ORA-38104) que hace ejecutable `Model.insert(replace=True)` en ese motor, con la referencia cruzada al item de `deferred-items.md` y al plan que lo registró (`02-12`). NO renumerar planes, NO cambiar requirements de la Fase 4, NO tocar la Fase 8.

    **E) Añadir la narrativa de la ronda 3.** En la sección de la Fase 2, justo después de la línea `**Gap-closure waves:** 1 → 02-06, 02-07; 2 → 02-08; 3 → 02-09` (:178), añadir un párrafo corto `**Gap closure ronda 3 (02-10…02-12)**` que explique que la re-verificación devolvió `gaps_found` con tres BLOCKER (CR-01/CR-02/CR-03) y dos WARNING (WR-01/WR-02), que 02-10 y 02-11 corren en paralelo (wave 1, ficheros disjuntos) y 02-12 cierra la documentación/ownership en wave 2, y que GAP F (pisos de cobertura por adaptador) sigue diferido pendiente de una corrida verde de `engine-heavy` y NO se planifica aquí. NO editar la lista de planes (`- [x] 02-0N-PLAN.md …`) que ya gestiona el orquestador.

    **F) Trazar en REQUIREMENTS.** En `.planning/REQUIREMENTS.md:45`, añadir al final del bullet de `POOL-03` una nota entre paréntesis: que `04-02` posee también el fix del `SET` del MERGE de Oracle (ORA-38104) que hace ejecutable `Model.insert(replace=True)` allí, registrado por el plan `02-12`. No cambiar el texto del requisito ni su estado en la tabla de trazabilidad (`POOL-03 | Phase 4 | Pending` se queda como está: la Fase 4 no está ejecutada).
  </action>
  <acceptance_criteria>
    - `grep -c "Ya NO queda ningún item" .planning/phases/02-dialect-seam-engine-parity/deferred-items.md` == 0.
    - `grep -c "04-02" .planning/phases/02-dialect-seam-engine-parity/deferred-items.md` ≥ 2 (ORA-38104 + captura de `last_id`).
    - `grep -c "ORA-38104" .planning/ROADMAP.md` ≥ 1 y `grep -c "ORA-38104" .planning/REQUIREMENTS.md` ≥ 1.
    - `grep -c "ronda 3\|Ronda 3" .planning/ROADMAP.md` ≥ 1.
    - `grep -c "02-10\|02-11\|02-12" .planning/phases/02-dialect-seam-engine-parity/deferred-items.md` ≥ 1 (la sección de la ronda 3 existe).
    - `git diff --numstat .planning/ROADMAP.md` solo AÑADE/EDITA líneas de las secciones de Fase 2 y Fase 4; la lista de planes existente y la Fase 8 no se tocan (verificable con `git diff .planning/ROADMAP.md` e inspección).
    - La fila `| POOL-03 | Phase 4 | Pending |` de `.planning/REQUIREMENTS.md` sigue existiendo sin cambios de estado.
    - `uv run pytest -q -m "not integration and not optional_engine"` sigue pasando (este task no toca código; el check confirma que el árbol queda sano).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pathlib; d = pathlib.Path('.planning/phases/02-dialect-seam-engine-parity/deferred-items.md').read_text(encoding='utf-8'); r = pathlib.Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); q = pathlib.Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); assert 'Ya NO queda ningún item' not in d; assert d.count('04-02') >= 2, d.count('04-02'); assert 'ORA-38104' in r and 'ORA-38104' in q; assert '02-10' in d and '02-11' in d and '02-12' in d; assert '| POOL-03 | Phase 4 | Pending |' in q; print('OK: ownership de ORA-38104 y last_id registrado en deferred-items + ROADMAP + REQUIREMENTS')" && uv run pytest -q -m "not integration and not optional_engine"</automated>
  </verify>
  <done>El log de diferidos deja de afirmar algo falso, asigna dueño explícito (Fase 4 / 04-02 / POOL-03) a ORA-38104 y a la captura real de `last_id`, documenta la ronda 3 con sus residuales y dueños, y el dueño queda trazado en `ROADMAP.md` y `REQUIREMENTS.md` sin tocar la enumeración de breaking changes de la Fase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log de diferidos → decisiones de fases futuras | Un log que afirma no tener items huérfanos mientras deja uno PENDIENTE hace que el trabajo diferido se pierda en silencio (el fallo que el propio mecanismo existe para evitar). |
| `CHANGELOG.md` → usuarios en `0.x` | Es la ÚNICA promesa de compatibilidad que el proyecto hace mientras está en `0.x`; un cambio observable sin entrada es un cambio no anunciado. |
| `ROADMAP.md`/`REQUIREMENTS.md` → planificación de la Fase 4 | Un dueño que no existe redactado en el roadmap es un dueño ficticio. |
| changelog/log → veracidad de la ronda | Documentar el fallback `columns[0]` del MERGE como "corregido" sería una afirmación falsa (sigue sin corregir y con dueño). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-62 | Repudiation | `deferred-items.md` afirma que no queda ningún item sin dueño mientras ORA-38104 está PENDIENTE y CR-02 no estaba registrado | mitigate | Se elimina la afirmación literal (`grep` == 0 en el criterio de aceptación), se asigna dueño explícito (Fase 4 / 04-02 / POOL-03) a ORA-38104 y a la captura de `last_id`, y se añade la sección de la ronda 3 con los residuales y sus dueños. |
| T-02-63 | Repudiation | Cambio de comportamiento observable (cardinalidad de `Query`, 02-07) sin entrada en `CHANGELOG.md`, incumpliendo la restricción dura de `0.x` | mitigate | Cuatro bullets añadidos a la sección `[Unreleased] ### Corregido` EXISTENTE (sin encabezado duplicado); criterios de aceptación con `grep` sobre las frases clave y una aserción de que `rebind` NO aparece (la enumeración de la Fase 8 no se adelanta). |
| T-02-64 | Repudiation / Integrity | Un dueño asignado que no existe redactado en el roadmap (dueño ficticio) | mitigate | `ROADMAP.md` (Fase 4, plan `04-02`) y `REQUIREMENTS.md` (POOL-03) declaran el ownership; el criterio de aceptación exige `grep "ORA-38104"` ≥ 1 en ambos. |
| T-02-65 | Repudiation | La documentación presenta el fallback `columns[0]` del MERGE como corregido | mitigate | El bullet de CR-03 y la sección de la ronda 3 declaran explícitamente que la semántica del fallback sigue SIN corregir y que su dueño es la Fase 4 (familia ORA-38104); el plan 02-11 solo añade un guard fail-closed que no altera el SQL válido. |
| T-02-SC | Tampering | Instalaciones de paquetes (npm/pip/cargo) | n/a | Este plan NO instala ni actualiza dependencias. |
</threat_model>

<verification>
- `uv run python -c` con las aserciones de Task 1 (changelog: una sola `### Corregido` en `[Unreleased]`, frases clave presentes, `rebind` ausente) pasa.
- `uv run python -c` con las aserciones de Task 2 (deferred-items sin la afirmación falsa, `04-02` ≥ 2, `ORA-38104` en ROADMAP y REQUIREMENTS, sección de la ronda 3, fila `POOL-03` intacta) pasa.
- `uv run pytest -q -m "not integration and not optional_engine"` sigue pasando (no se toca código).
- `git diff --numstat CHANGELOG.md` solo adiciones dentro de `[Unreleased]`; `[0.2.6]` y anteriores intactos.
- `git diff .planning/ROADMAP.md` solo afecta a la narrativa de la Fase 2 y al bullet `04-02`; la lista de planes y la Fase 8 no se tocan.
- `uv run ruff check encino_orm tests` + `uv run mypy encino_orm` salen 0 (el árbol de código queda sano; `# noqa` sigue en 0).
</verification>

<success_criteria>
- GAP E / WR-02 cerrado: el contrato de cardinalidad de `Query` (02-07) está en `CHANGELOG.md` `[Unreleased] ### Corregido` (sección existente, sin duplicar) junto con los cambios de comportamiento de CR-01/CR-02/CR-03.
- GAP D / WR-01 cerrado: `deferred-items.md` no afirma nada falso y asigna dueño explícito y trazado (Fase 4 / 04-02 / POOL-03) a ORA-38104 y a la captura real de `last_id`.
- La enumeración completa de cambios incompatibles del milestone sigue siendo de la Fase 8 (`08-04`), sin adelantarse.
- La documentación no presenta como corregido el fallback `columns[0]` del MERGE, que sigue con dueño.
</success_criteria>

<output>
Create `.planning/phases/02-dialect-seam-engine-parity/02-12-SUMMARY.md` when done
</output>
