---
phase: 02-dialect-seam-engine-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - encino_orm/query.py
  - encino_orm/dialects/strategies.py
  - encino_orm/dialects/__init__.py
  # Los 6 adaptadores aparecen aquí ÚNICAMENTE por la tarea 2 (DIAL-06: atributos
  # de clase MAX_PARAMS/MAX_ROWS). La tarea 1 (DIAL-05) toca exclusivamente
  # encino_orm/query.py: D-02 prohíbe que el refactor de `Query` modifique un adaptador.
  - encino_orm/sqlite.py
  - encino_orm/mysql.py
  - encino_orm/mariadb.py
  - encino_orm/postgresql.py
  - encino_orm/mssql.py
  - encino_orm/oracle.py
  - encino_orm/pool.py
  - encino_orm/model/model.py
  - docs/design/0-design.md
  - tests/test_query.py
  - tests/test_engine.py
autonomous: true
requirements:
  - DIAL-05
  - DIAL-06
must_haves:
  truths:
    - "`Query` es inmutable: asignar cualquier atributo lanza; el contrato de entrada sigue siendo `Query(sql_con_{n}, [valores])` (D-01, D-02)"
    - "`with_params()` devuelve una copia nueva y deja intacto el original; `rebind` ya no existe (D-01, D-06)"
    - "La compilación detecta los `{n}` REALES por regex (no `enumerate`), acepta duplicados y rechaza cardinalidad inválida con un error claro (D-04)"
    - "`__hash__` lanza `TypeError` explícito ante un parámetro no hashable (D-03)"
    - "Los seis dialectos exponen `MAX_PARAMS`/`MAX_ROWS` con procedencia documentada, y `insert_many` deriva su `chunk` de ellos"
    - "El sitio de docs publica la API nueva: `0-design.md` no documenta `rebind` (D-05)"
  artifacts:
    - path: "encino_orm/query.py"
      provides: "Value object inmutable y hashable, con `sql`/`params`/`fields`/`query` y `with_params()`"
      contains: "def with_params"
    - path: "tests/test_query.py"
      provides: "Inmutabilidad, `with_params`, `{n}` dispersos/duplicados/sin usar/fuera de rango, hash/eq, ausencia de `rebind`"
      min_lines: 80
    - path: "encino_orm/dialects/strategies.py"
      provides: "DialectLimits (max_params/max_rows/procedencia) + LIMITS por dialecto"
      contains: "class DialectLimits"
    - path: "docs/design/0-design.md"
      provides: "Documentación de la API nueva de `Query` + nota de migración `rebind` → `with_params()`"
  key_links:
    - from: "encino_orm/sqlite.py"
      to: "encino_orm/query.py"
      via: "los adaptadores leen `.sql`/`.params` (ya migrados en 02-02); DIAL-05 no toca ningún adaptador"
      pattern: "qry\\.sql|qry\\.params"
    - from: "encino_orm/model/model.py"
      to: "encino_orm/dialects/strategies.py"
      via: "`insert_many` deriva el chunk de MAX_PARAMS/MAX_ROWS del motor resuelto"
      pattern: "MAX_PARAMS"
    - from: "encino_orm/pool.py"
      to: "encino_orm/dialects/strategies.py"
      via: "PoolDb delega MAX_PARAMS/MAX_ROWS al template"
      pattern: "MAX_PARAMS"
---

<objective>
Convertir `Query` en un value object correcto — inmutable, hashable, con compilación por regex de los `{n}` reales, validación de cardinalidad y `with_params()` que sustituye al mutante `rebind` (que se ELIMINA sin shims) — y añadir las constantes `MAX_PARAMS`/`MAX_ROWS` por dialecto.

Purpose: `Query` es el contrato de entrada de todo el camino de ejecución. Hoy su sentinel `sql.find("{0}")` clasifica mal cualquier SQL cuyo primer placeholder no sea `{0}`, `format()` solo reemplaza índices contiguos desde 0, y `rebind` muta el objeto en sitio. Además, sin constantes por dialecto, el `chunk=500` hardcodeado de `insert_many` es una suposición sin procedencia que la Fase 7 consumirá.

Output: `Query` inmutable y hashable; `tests/test_query.py`; `DialectLimits` + `LIMITS` con procedencia; `MAX_PARAMS`/`MAX_ROWS` en los 6 adaptadores y en `PoolDb`; `insert_many` con chunk derivado; `docs/design/0-design.md` actualizado (D-05).

**Nota de alcance (D-02 vs DIAL-06):** los seis adaptadores figuran en `files_modified` **solo** por la tarea 2 (DIAL-06: los atributos de clase `MAX_PARAMS`/`MAX_ROWS`). La tarea 1 (DIAL-05) toca exclusivamente `encino_orm/query.py`: el refactor de `Query` no puede modificar ningún adaptador, porque los adaptadores ya migraron a los accesores `.sql`/`.params` en 02-02 (tarea 3).
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md
@.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md
@.planning/phases/02-dialect-seam-engine-parity/02-PATTERNS.md
@.planning/phases/02-dialect-seam-engine-parity/02-VALIDATION.md
@encino_orm/query.py
@encino_orm/model/filter.py
@encino_orm/model/model.py
@docs/design/0-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescribir `Query` inmutable y crear `tests/test_query.py`</name>
  <files>encino_orm/query.py, tests/test_query.py</files>
  <read_first>
    - `encino_orm/query.py` (completo, 34 líneas): el estado actual — sentinel `sql.find("{0}")` en la línea 7, `format()` con `enumerate(cols)` en 12-26, `rebind` en 28-31.
    - `encino_orm/model/filter.py` (líneas 30-45): el idioma de inmutabilidad del repo (`__slots__` + construcción de solo lectura). **Ojo:** `Filter._rebind_raw` (líneas 197, 218) es un método interno de `Filter` SIN relación con `Query.rebind` — NO tocarlo.
    - `encino_orm/model/model.py` (líneas 78-79): el idioma `object.__setattr__` sancionado para estado interno.
    - `encino_orm/base.py` (líneas 162-178): `paginate` lee `qry.sql_template` y `list(qry.fields)`; debe seguir funcionando.
    - `encino_orm/pool.py` (línea 246): lee `qry.sql_template`.
    - `tests/test_postgresql.py` (líneas 18-30), `tests/test_mssql.py` (líneas 24-32), `tests/test_oracle.py` (líneas 28-36): los 3 tests white-box que leen `q.query[0]`/`q.query[1]` y que deben seguir pasando **sin editarlos** (D-02).
    - `.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md` — decisiones D-01, D-02, D-03, D-04.
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — §Pattern 3, §Code Examples "`Query` — the target shape", Pitfall F y Pitfall G.
  </read_first>
  <behavior>
    - Inmutabilidad: `q.sql = "x"`, `q.fields = []`, `q.ignore_duplicated = True` y `q.atributo_inexistente = 1` lanzan `AttributeError` (slots privados + properties de solo lectura sin setter + ausencia de `__dict__`); en cambio `q.fields.append(...)` NO lanza (mutación in situ fuera de contrato, ver la limitación documentada en `<action>`).
    - `with_params([...])` devuelve un objeto NUEVO, con el mismo `sql_template` y el original sin cambios; revalida la cardinalidad.
    - `not hasattr(Query, "rebind")`.
    - Compilación: `Query("a={1} AND b={0}", [10, 20])` → `.sql == "a=%(parameter_0001)s AND b=%(parameter_0000)s"` y `.params == {"parameter_0000": 10, "parameter_0001": 20}` (orden de aparición textual no importa).
    - Duplicados: `Query("a={0} OR b={0}", [7])` → ambas ocurrencias compilan a `%(parameter_0000)s` con `params == {"parameter_0000": 7}`.
    - Índice fuera de rango: `Query("a={0} AND b={2}", [1, 2])` lanza `ValueError` que nombra el índice máximo y el número de valores.
    - Parámetro sin usar: `Query("a={0}", [1, 2])` lanza `ValueError` que nombra los índices y el número de valores.
    - Sin placeholders: `Query("SELECT 1", [])` → `.sql == "SELECT 1"`, `.params == {}`, `.query == ["SELECT 1", {}]`; `Query("SELECT 1")` (fields omitido) se comporta igual.
    - `sql_template` conserva los `{n}` originales (lo lee `pool.py` y `base.paginate`).
    - `query` es una property de solo lectura que devuelve una lista NUEVA en cada acceso; los 3 tests white-box siguen pasando sin editarse.
    - `ignore_duplicated` es campo de constructor y atributo legible, pero está **EXCLUIDO** de `__eq__` y de `__hash__` (D-03): `Query("t", [], ignore_duplicated=True) == Query("t", [])` y sus hashes son iguales. El flag es intencionalmente ajeno a la clave de hash — ver la nota de cache-key de DATA-07 en `<action>`.
    - Hash: `hash(Query("a={0}", [1])) == hash(Query("a={0}", [1]))`; `Query("a={0}", [[1, 2]])` lanza `TypeError` explícito al hashear; dos Queries con params distintos no colisionan por igualdad.
    - `__eq__` con un objeto de otro tipo devuelve `NotImplemented` (no lanza).
    - `str(q)` sigue concatenando el SQL compilado y los params.
  </behavior>
  <action>
    Reescribir `encino_orm/query.py` como value object inmutable con `__slots__` PRIVADOS y `object.__setattr__` (NO `@dataclass(frozen=True)`: choca con el `__hash__` a medida y con el campo `ignore_duplicated`).

    `__slots__ = ("_sql_template", "_fields", "_ignore_duplicated", "_sql", "_params")` — sin `__dict__`, de modo que una asignación con errata lanza. Los nombres de los slots van SIEMPRE con guion bajo, y la superficie legible se expone con properties: un `__slots__` plano es un descriptor ESCRIBIBLE (así que `q.fields = []` tendría éxito), y declarar a la vez el slot `"fields"` y la property `fields` lanza `ValueError: 'fields' in __slots__ conflicts with class variable` al crear la clase. Slots privados + properties sin setter es la única combinación que da inmutabilidad de atributo real.

    `__init__(self, sql: str, fields: list | None = None, *, ignore_duplicated: bool = False)`:
    - `values = list(fields or [])`.
    - `_PLACEHOLDER_RE = re.compile(r"\{(\d+)\}")` a nivel de módulo.
    - `indices = {int(m) for m in _PLACEHOLDER_RE.findall(sql)}`.
    - Validación de cardinalidad (interpretación explícita de "los placeholders deben cuadrar con la lista de params", Pitfall G): `if indices and indices != set(range(len(values))): raise ValueError(...)`. Esto cubre a la vez el índice fuera de rango y el parámetro sin usar, y acepta índices no contiguos en el TEXTO (p. ej. `{1}` antes de `{0}`) y duplicados. El mensaje debe nombrar los índices ordenados y el número de valores, en español.
    - `params = {f"parameter_000{i}": v for i, v in enumerate(values)}`.
    - `compiled = _PLACEHOLDER_RE.sub(lambda m: f"%(parameter_000{m.group(1)})s", sql)` — sustituye TODAS las ocurrencias, incluidas las duplicadas.
    - Asignar con `object.__setattr__` a los cinco slots privados: `("_sql_template", sql)`, `("_fields", values)`, `("_ignore_duplicated", bool(ignore_duplicated))`, `("_sql", compiled)` y `("_params", params)`. Los nombres `parameter_0000…` se preservan EXACTAMENTE: los `sql_text` ya escritos en `_encino_orm_migrations` no necesitan migración.

    Properties de solo lectura (SIN setter; de ahí que `q.sql = "x"`, `q.fields = []` y `q.ignore_duplicated = True` lancen `AttributeError`): `sql` (compilado), `params` (dict), `query` (lista NUEVA `[self._sql, self._params]` en cada acceso — D-02, para que nadie mute el estado interno a través de ella), y las tres vistas de solo lectura `sql_template` → `_sql_template`, `fields` → `_fields` e `ignore_duplicated` → `_ignore_duplicated`. La property `fields` devuelve la MISMA lista subyacente (no una copia), para que `list(qry.fields)` y `qry.fields == [1, "x"]` sigan funcionando igual que hoy.

    `with_params(self, fields: list) -> "Query"` → `Query(self.sql_template, fields, ignore_duplicated=self.ignore_duplicated)`. Revalida la cardinalidad por construcción. Devuelve una COPIA; nunca muta `self`.

    **Eliminar `rebind()` y `format()`** (D-01: ruptura limpia, sin shims; `format()` solo era usado por `__init__` y `rebind`, ambos reescritos). NO tocar `Filter._rebind_raw` de `encino_orm/model/filter.py`.

    `__eq__(self, other)`: si `not isinstance(other, Query)` → `NotImplemented`; si no, comparar `sql_template` y `fields` (que codifican "plantilla SQL + params", D-03).

    `__hash__(self)`: `hash((self.sql_template, tuple(self.fields)))` dentro de `try/except TypeError` que relanza un `TypeError` EXPLÍCITO en español explicando que hay un parámetro no hashable (list/dict) y cómo evitarlo. **Nota D-03 (registrada, no una omisión):** el usuario fijó el hash sobre `(sql_template, tupla_de_params)`; `ignore_duplicated` NO entra. Consecuencia a documentar en un comentario: dos Queries MSSQL con el mismo texto e `ignore_duplicated` distinto comparan iguales. Hoy no hay caché por `Query` (DATA-07 está diferido), así que no hay bug vivo; **si DATA-07 usa este hash como clave de caché, debe añadir `ignore_duplicated` a la clave**. Registrar esta nota también en el SUMMARY del plan.

    `__str__` → `f"{self._sql}{self._params}"`.

    Docstring en español con el contrato de entrada (`Query("… {0} … {1}", [v0, v1])`), la regla de cardinalidad y DOS limitaciones conocidas, ambas declaradas explícitamente:
    - un `{n}` dentro de un literal de cadena se interpreta como placeholder (no se parsean literales en esta versión);
    - **la inmutabilidad es a nivel de ATRIBUTO, no profunda.** Los slots privados más las properties sin setter impiden REASIGNAR `sql_template`/`fields`/`ignore_duplicated` (y una errata de nombre lanza `AttributeError`, porque no hay `__dict__`), pero `fields` es una lista mutable y `params` expone el dict interno por referencia: `q.fields.append(v)` y `q.params["x"] = v` NO lanzan. Mutar `fields` in situ es un uso NO soportado y además rompe el contrato de `__hash__`, porque el hash se calcula en cada llamada a partir del estado vivo. La property `fields` devuelve la lista subyacente a propósito (no una copia) y `params` no copia el dict en cada acceso: una copia añadiría coste en el camino caliente y haría fallar las aserciones `fields == [1, "x"]` del plan 02-02. La garantía ofrecida es la de un value object de Python: reasignación prohibida, mutación in situ fuera de contrato.

    Sin secciones `Args:`/`Returns:`.

    `tests/test_query.py` (nuevo): tests puros sin BD cubriendo TODO el bloque `<behavior>`. No editar los 3 tests white-box de los ficheros de motor.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_query.py -x -q` pasa.
    - `uv run python -c "from encino_orm import Query; q=Query('a={0} AND b={2}',[1,2])"` falla con `ValueError` y un mensaje que menciona los índices.
    - `uv run python -c "from encino_orm import Query; q=Query('a={0}',[1]); print(hash(q)); q.fields=[]"` imprime un entero y luego falla con `AttributeError`.
    - `uv run python -c "from encino_orm import Query; assert not hasattr(Query,'rebind'); assert not hasattr(Query,'format')"` sale con código 0.
    - `uv run pytest tests/test_postgresql.py tests/test_mssql.py tests/test_oracle.py -q` pasa **sin editar esos ficheros** (los 3 tests white-box de `q.query[0]`/`q.query[1]` siguen funcionando).
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa.
    - `uv run ruff check encino_orm tests` y `uv run mypy encino_orm` salen con código 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_query.py tests/test_postgresql.py tests/test_mssql.py tests/test_oracle.py -q</automated>
  </verify>
  <done>`Query` es inmutable y hashable, compila los `{n}` reales por regex con validación de cardinalidad, expone `.sql`/`.params`/`.query` y las properties de solo lectura `.sql_template`/`.fields`/`.ignore_duplicated` sobre slots privados, ofrece `with_params()`, y `rebind`/`format` han desaparecido sin romper a los adaptadores ni a los tests white-box.</done>
</task>

<task type="auto">
  <name>Task 2: Constantes `MAX_PARAMS`/`MAX_ROWS` por dialecto y chunk derivado en `insert_many`</name>
  <files>encino_orm/dialects/strategies.py, encino_orm/dialects/__init__.py, encino_orm/sqlite.py, encino_orm/mysql.py, encino_orm/mariadb.py, encino_orm/postgresql.py, encino_orm/mssql.py, encino_orm/oracle.py, encino_orm/pool.py, encino_orm/model/model.py, tests/test_engine.py</files>
  <read_first>
    - `encino_orm/dialects/strategies.py`: el módulo donde viven las estrategias (creado en 02-02).
    - `encino_orm/model/model.py` (líneas 511-549): `insert_many` con `chunk: int = 500` hardcodeado.
    - `encino_orm/base.py` (líneas 20-22): dónde vive hoy `dialect: str = ""` en `Db`.
    - `encino_orm/pool.py` (líneas 1-80): la estructura de `PoolDb` y su `_template`.
    - `encino_orm/model/records.py` (líneas 1-30): `MAX_LIMIT = 1000` (referencia de magnitud, NO es lo mismo que `MAX_ROWS`).
    - `.planning/phases/02-dialect-seam-engine-parity/02-RESEARCH.md` — §Standard Stack (asyncpg 32767 verificado en fuente del driver), §Assumptions Log A1/A2/A3 y §Open Questions 1.
    - `tests/test_engine.py` (líneas 1-80): estilo del test a extender.
  </read_first>
  <action>
    **A) `encino_orm/dialects/strategies.py`**: añadir `@dataclass(frozen=True) class DialectLimits` con `max_params: int`, `max_rows: int`, `provenance: str`; y un dict `LIMITS: dict[str, DialectLimits]` con una entrada por dialecto (`"sqlite"`, `"mysql"`, `"mariadb"`, `"postgresql"`, `"mssql"`, `"oracle"`). Cada `provenance` es una frase en español que dice de dónde sale el número y **si está verificado empíricamente o no**:
    - `sqlite`: `max_params=32766` (verificado empíricamente: `SQLITE_MAX_VARIABLE_NUMBER`; 32768 → "too many SQL variables"), `max_rows=1000` (verificado: un multi-row VALUES de 1000 filas / 2000 params fue aceptado en SQLite 3.50.4).
    - `postgresql`: `max_params=32767` (verificado en el código instalado de asyncpg 0.31.0, `prepared_stmt.pyx`), `max_rows=1000`.
    - `mysql` / `mariadb`: `max_params=65535` (límite de protocolo para el contador de placeholders; **NO verificado empíricamente**), `max_rows=1000`.
    - `mssql`: `max_params=2100` (especificación oficial de capacidad de SQL Server; aplica a procedimientos y UDF — el techo ad-hoc **NO verificado empíricamente**), `max_rows=1000`.
    - `oracle`: `max_params=65535` (techo documentado de binds por sentencia; **NO verificado empíricamente**), `max_rows=1000`.
    Los valores son discreción del planner (CONTEXT.md) y la procedencia es obligatoria; la sonda manual del job `engine-heavy` (02-05) los refina.

    **B) `dialects/__init__.py`**: re-exportar `DialectLimits` y `LIMITS`.

    **C) Los 6 adaptadores**: declarar en cada clase `MAX_PARAMS` y `MAX_ROWS` como atributos de clase tomados de `LIMITS` (p. ej. en `SqliteDb`: `MAX_PARAMS = LIMITS["sqlite"].max_params`). `MariadbDb` hereda de `MysqlDb` y debe sobrescribir ambos con los de `"mariadb"` (aunque hoy coincidan) para que el valor sea el del dialecto correcto. Importar `LIMITS` desde `.dialects.strategies`.

    **D) `encino_orm/pool.py`**: exponer `MAX_PARAMS` y `MAX_ROWS` como properties que delegan en `self._template`, para que `PoolDb` no mienta sobre los límites del motor subyacente.

    **E) `encino_orm/model/model.py` — `insert_many`**: cambiar la firma a `async def insert_many(cls, db=None, rows: list[dict] | None = None, *, chunk: int | None = None) -> int`. Si `chunk` es `None`, derivarlo del motor resuelto: `max_params = getattr(db, "MAX_PARAMS", None)` y `max_rows = getattr(db, "MAX_ROWS", None)`; `chunk = min(max_params // max(len(columns), 1), max_rows)` cuando ambos existan, y `500` como último recurso si el objeto no expone las constantes (p. ej. un fake de test). Un `chunk` explícito del llamador se respeta tal cual (los tests existentes con `chunk=2` no cambian). Actualizar el docstring en español para explicar de dónde sale el chunk.

    **F) `tests/test_engine.py`**: extender con un test `-k max_params` que afirme que los 6 adaptadores y `PoolDb` exponen `MAX_PARAMS` y `MAX_ROWS` como enteros positivos, que `PoolDb` delega en su template, y que `LIMITS` cubre exactamente los 6 dialectos. Añadir un test que verifique que `insert_many` sin `chunk` explícito usa un tamaño derivado (`chunk` menor o igual a `MAX_ROWS` y a `MAX_PARAMS // n_columnas`) — se puede comprobar con un fake que capture el tamaño de lote o inspeccionando los `Query` generados.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_engine.py -k max_params -x -q` pasa.
    - `uv run python -c "from encino_orm import SqliteDb, MysqlDb, MariadbDb, PostgresDb, MssqlDb, OracleDb; dbs=[SqliteDb(),MysqlDb(),MariadbDb(),PostgresDb(),MssqlDb(),OracleDb()]; assert all(isinstance(d.MAX_PARAMS,int) and d.MAX_PARAMS>0 and isinstance(d.MAX_ROWS,int) and d.MAX_ROWS>0 for d in dbs)"` sale con código 0.
    - Cada entrada de `LIMITS` tiene un `provenance` no vacío: `uv run python -c "from encino_orm.dialects.strategies import LIMITS; assert len(LIMITS)==6 and all(v.provenance for v in LIMITS.values())"` sale con código 0.
    - `uv run python -c "from encino_orm import Engine; from encino_orm.pool import PoolDb; p=PoolDb(Engine.MYSQL); assert p.MAX_PARAMS==p._template.MAX_PARAMS"` sale con código 0.
    - `uv run pytest tests/test_bulk_upsert.py tests/test_pagination_limits.py tests/test_singleton.py -q` pasa (el `chunk` derivado no rompe `insert_many`, incluido el caso de 1001 filas).
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_engine.py tests/test_bulk_upsert.py tests/test_pagination_limits.py -q</automated>
  </verify>
  <done>Los 6 dialectos y `PoolDb` exponen `MAX_PARAMS`/`MAX_ROWS` con procedencia escrita y `insert_many` deriva su chunk de ellos cuando el llamador no lo fija.</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar `docs/design/0-design.md` a la API nueva (D-05) y blindar la eliminación de `rebind`</name>
  <files>docs/design/0-design.md, tests/test_query.py</files>
  <read_first>
    - `docs/design/0-design.md` (líneas 15-95 y 285-300 y 390-400): la tabla que menciona `rebind` (línea 22), la prosa (35), el boceto de la clase (38-72), los ejemplos (79-89), `qry.query[0]`/`list(qry.query[1].values())` (295-296) y `q_tpl.rebind([nombre, 1])` (397).
    - `encino_orm/query.py` (estado final de la tarea 1): la API que hay que documentar.
    - `.planning/phases/02-dialect-seam-engine-parity/02-CONTEXT.md` — D-05 y D-06.
    - `.planning/codebase/CONVENTIONS.md` — estilo de docs: prosa en español, bloques de código cercados, sin secciones `Args:`/`Returns:`.
  </read_first>
  <action>
    Reescribir en `docs/design/0-design.md` todas las menciones a la API vieja de `Query` (líneas 22, 35, 38-72, 79-89, 295-296, 397) para documentar la API nueva: el objeto inmutable, el contrato de entrada `Query(sql_con_{n}, [valores])`, la regla de cardinalidad, los accesores `.sql` / `.params` / `.fields`, la property de solo lectura `.query` (compatibilidad) y `with_params()` para reutilizar la plantilla con nuevos valores. Añadir una nota de migración explícita: `rebind` se eliminó en 0.3.0 y su sustituto es `with_params()`, que devuelve una copia en vez de mutar. El sitio de docs se publica (`docs.yml` → GitHub Pages), así que no puede quedar documentando una API eliminada.

    NO editar `CHANGELOG.md` ni crear `MIGRATION-0.3.md` en esta fase: la Fase 8 es dueña del release y de ambos artefactos (D-01). Registrar la ruptura en el SUMMARY de este plan para que la Fase 8 la recoja.

    En `tests/test_query.py`, añadir dos guardas: (a) `assert not hasattr(Query, "rebind")`; (b) un guard de fuente que afirme que ningún fichero bajo `encino_orm/` contiene `def rebind` (usar `pathlib.Path.rglob` y normalizar `\` → `/`). El guard debe permitir explícitamente `Filter._rebind_raw` en `encino_orm/model/filter.py`: filtrar por el literal `def rebind(` exacto, no por la subcadena `rebind`.
  </action>
  <acceptance_criteria>
    - `grep -n "def rebind" docs/design/0-design.md` no devuelve nada (exit code 1).
    - `grep -c "with_params" docs/design/0-design.md` es mayor o igual a 2 (documentado en prosa y en un ejemplo).
    - `docs/design/0-design.md` contiene la palabra `rebind` en la nota de migración (se nombra lo eliminado) pero ningún bloque de código que lo invoque como API vigente.
    - `uv run pytest tests/test_query.py -x -q` pasa, incluidos los dos guards nuevos.
    - `uv run mkdocs build --strict` sale con código 0 (la doc sigue construyendo en modo estricto).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_query.py -x -q</automated>
  </verify>
  <done>El sitio de docs documenta la API nueva de `Query` con una nota de migración `rebind` → `with_params()`; un guard automático impide reintroducir `def rebind` en `encino_orm/`; `mkdocs build --strict` pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llamador → `Query(sql, valores)` | La plantilla SQL es entrada del llamador; los valores SIEMPRE se pasan como parámetros ligados. |
| `Query` → adaptadores → driver | La forma compilada (`%(parameter_0000)s`) es el contrato intermedio que cada adaptador traduce a su placeholder nativo. |
| `insert_many` → motor | El tamaño de lote debe respetar el techo de parámetros del motor o el statement falla en runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | `Query.__init__` (compilación de `{n}`) | mitigate | La plantilla NUNCA se interpola con valores: `{n}` se sustituye por nombres de parámetro ligados (`%(parameter_0000)s`). Los valores viajan en el dict de params y los adaptadores los ligan. No hay camino que formatee un valor dentro del SQL. |
| T-02-15 | Tampering | cardinalidad laxa (parámetro sin usar) | mitigate | `set(indices) == set(range(len(values)))` en construcción y en `with_params()`; un parámetro declarado y no usado lanza `ValueError` en vez de ignorarse en silencio (Pitfall 19/G). |
| T-02-16 | Tampering / Repudiation | caché por hash de `Query` (DATA-07, v2) | accept (con nota) | D-03 fija el hash sobre `(sql_template, tupla_de_params)` y EXCLUYE `ignore_duplicated`. No hay caché por `Query` en esta fase, así que no hay bug vivo; la nota se registra en el código y en el SUMMARY para que DATA-07 no use este hash como clave sin añadir el flag. |
| T-02-17 | Denial of Service | `insert_many` con chunk sin procedencia | mitigate | El chunk se deriva de `MAX_PARAMS // n_columnas` acotado por `MAX_ROWS`, con procedencia escrita por dialecto; un `chunk=500` fijo era una suposición no verificada que podía exceder el techo de MSSQL (2100). |
| T-02-18 | Information Disclosure | `Query.__hash__` / `__str__` | accept | `__str__` concatena SQL compilado y params (comportamiento actual). Es una utilidad de depuración del propio llamador, no un log automático de la librería. |
| T-02-19 | Repudiation | doc publicada con API eliminada | mitigate | `0-design.md` se reescribe (D-05) y `mkdocs build --strict` es el gate; un guard de fuente impide reintroducir `def rebind`. |
</threat_model>

<verification>
- `uv run pytest tests/test_query.py tests/test_engine.py tests/test_bulk_upsert.py tests/test_pagination_limits.py tests/test_postgresql.py tests/test_mssql.py tests/test_oracle.py -q` pasa.
- `uv run pytest -q -m "not integration and not optional_engine"` pasa.
- `uv run ruff check encino_orm tests`, `uv run ruff format --check encino_orm tests`, `uv run mypy encino_orm` pasan.
- `uv run mkdocs build --strict` pasa.
- `grep -rn "def rebind" encino_orm/` vacío; `grep -rn "\.query\[0\]\|\.query\[1\]" encino_orm/` vacío.
</verification>

<success_criteria>
- DIAL-05 (redacción corregida por D-06): `Query` inmutable, sin sentinel frágil, hashable, con `with_params()` que devuelve una copia; `rebind` eliminado y la doc actualizada.
- DIAL-06: constantes `MAX_PARAMS`/`MAX_ROWS` por dialecto, expuestas por los 6 adaptadores y por `PoolDb`, con procedencia y con un consumidor real (`insert_many`).
- Ningún adaptador fue tocado por el cambio de `Query` (D-02): los adaptadores ya usaban `.sql`/`.params` desde 02-02.
- La ruptura queda registrada para la Fase 8 (CHANGELOG + MIGRATION-0.3.md), sin escribir esos artefactos aquí.
</success_criteria>

<output>
Create `.planning/phases/02-dialect-seam-engine-parity/02-03-SUMMARY.md` when done
</output>
