---
phase: 05-resilience
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - encino_orm/exceptions.py
  - encino_orm/__init__.py
  - encino_orm/base.py
  - encino_orm/sqlite.py
  - encino_orm/mysql.py
  - encino_orm/postgresql.py
  - encino_orm/mssql.py
  - encino_orm/oracle.py
  - tests/_resilience_helpers.py
  - tests/test_resilience.py
  - tests/test_base.py
  - pyproject.toml
  - docs/engines.md
  - CHANGELOG.md
autonomous: true
requirements: [RESL-04]
must_haves:
  truths:
    - "Existe una taxonomía pública documentada: `ConnectionLostError(ConnectionError)`, `OperationalError(QueryError)`, `IntegrityError(QueryError)`, `ProgrammingError(QueryError)`, exportadas desde `encino_orm` (barrel + `__all__`). `except ConnectionError` y `except QueryError` siguen capturando lo que capturaban."
    - "`Db._translate_exception(exc)` es el punto ÚNICO de traducción: devuelve `exc` sin traducir si `is_lock_error(exc)` es True (para que `retry()` siga reconociendo el error de lock), devuelve `exc` si ya es `EncinoOrmError`, mapea `is_disconnect_error(exc)` a `ConnectionLostError`, y delega en el hook por adaptador `_translate_error(exc)`."
    - "Cada adaptador implementa `_translate_error(exc)` con el mapeo por driver verificado en `05-RESEARCH.md` §Code Examples (IntegrityError/ProgrammingError/OperationalError), reutilizando los helpers existentes (`is_unique_violation` en MSSQL/Oracle, `_native_code`, `_ora_code`). El hook NO construye SQL ni interpola valores."
    - "`_with_reconnect` relanza SIEMPRE la excepción traducida y **preserva la causa del driver** con `raise ... from exc`; esto es una desviación DELIBERADA y documentada de la convención del repo ('`raise ... from` chaining is not used'), justificada por ASVS V7 (diagnóstico del error de driver) y registrada en `docs/engines.md` y `CHANGELOG.md`."
    - "Los errores de lock NUNCA se traducen: `TestD4AutoRetry` sigue verde y `retry()` sigue viendo el tipo/args originales."
    - "Un fallo de `_reconnect()` también se traduce (no escapa una excepción de driver cruda)."
    - "`SqliteDb._reconnect()` para `:memory:` lanza `ConnectionLostError` (subclase de `ConnectionError`; el test de 05-02 con `ConnectionError` sigue verde)."
    - "La documentación (`docs/engines.md`) documenta `is_disconnect_error`, la taxonomía, `pre_ping`/`max_connection_lifetime` como kwargs de `connect()` y el rechazo de `:memory:`; `CHANGELOG.md` `[Unreleased]` registra el CAMBIO DE COMPORTAMIENTO (auto-reconexión, política de escrituras, traducción de excepciones, status HTTP 400 por herencia de `QueryError`, chaining)."
    - "El ratchet de mypy retira `encino_orm.base`/`encino_orm.oracle`/`encino_orm.mysql`/`encino_orm.mssql` SOLO si `uv run mypy encino_orm` queda limpio; si queda un residual ajeno, la entrada se restaura con la causa escrita (precedente 04-04). `encino_orm.pool` NO se toca (residual de Fase 4)."
  artifacts:
    - path: "encino_orm/exceptions.py"
      provides: "las cuatro excepciones nuevas con la jerarquía aditiva"
      contains: "ConnectionLostError"
    - path: "encino_orm/__init__.py"
      provides: "export del barrel + `__all__` de la taxonomía"
      contains: "ConnectionLostError"
    - path: "encino_orm/base.py"
      provides: "`_translate_exception` con cortocircuito de lock + `_translate_error` por defecto + chaining en `_with_reconnect`"
      contains: "_translate_exception"
    - path: "encino_orm/{sqlite,mysql,postgresql,mssql,oracle}.py"
      provides: "hook `_translate_error` por driver + `:memory:` → `ConnectionLostError` en SQLite"
      contains: "_translate_error"
    - path: "tests/test_base.py"
      provides: "asserts de la jerarquía nueva y compatibilidad de `except`"
      contains: "ConnectionLostError"
    - path: "tests/test_resilience.py"
      provides: "bloque RESL-04: traducción por motor, cortocircuito de lock, causa preservada"
      contains: "_translate_exception"
    - path: "docs/engines.md"
      provides: "`is_disconnect_error`, taxonomía, `pre_ping`/lifetime, `:memory:`, nota de chaining"
      contains: "is_disconnect_error"
    - path: "CHANGELOG.md"
      provides: "entrada [Unreleased] del cambio de comportamiento"
      contains: "ConnectionLostError"
    - path: "pyproject.toml"
      provides: "ratchet de mypy reducido (base/oracle/mysql/mssql) o residual documentado"
      contains: "encino_orm.base"
  key_links:
    - from: "encino_orm/base.py::Db._with_reconnect"
      to: "encino_orm/base.py::Db._translate_exception"
      via: "punto único de traducción en el relanzado (chained con `from exc`)"
      pattern: "_translate_exception"
    - from: "encino_orm/base.py::Db._translate_exception"
      to: "encino_orm/base.py::Db.is_lock_error"
      via: "cortocircuito: un lock se devuelve sin traducir para que `retry()` lo reconozca"
      pattern: "is_lock_error"
    - from: "encino_orm/{sqlite,mysql,postgresql,mssql,oracle}.py::_translate_error"
      to: "encino_orm/exceptions.py"
      via: "mapeo driver → OperationalError/IntegrityError/ProgrammingError"
      pattern: "_translate_error"
    - from: "encino_orm/__init__.py"
      to: "encino_orm/exceptions.py"
      via: "barrel + `__all__` (contrato de importación pública)"
      pattern: "ConnectionLostError"
    - from: "pyproject.toml::[[tool.mypy.overrides]]"
      to: "encino_orm/base.py"
      via: "la entrada `encino_orm.base` se retira (o se documenta el residual)"
      pattern: "encino_orm.base"
---

<objective>
Publicar la taxonomía de errores de la librería y el punto único de traducción driver → librería (RESL-04 / Success Criterion 4), y cerrar el ratchet de mypy asignado a esta fase.

Hoy una excepción de driver sube cruda al llamador (`mysql.py:190-202`, `postgresql.py:197-203`, `mssql.py:235-263`, `oracle.py:240-268` no traducen). El usuario no puede distinguir un fallo de integridad de un fallo de programación sin importar el driver — y el contrato de importación diferida (`PROJECT.md`) le prohíbe depender del driver. La traducción debe ser **un único punto** (evitar seis `except` divergentes, como el repo ya centralizó en `http/errors.py:7-22` y en `_warn_last_id_deprecated` `base.py:15-26`) y **nunca** tragarse la excepción (el catch-all está Out of Scope, `REQUIREMENTS.md:124`).

**Decisión de chaining (explícita).** `05-PATTERNS.md` señala la tensión: `CONVENTIONS.md` dice "`raise ... from` chaining is not used", pero el sketch de research usa `raise self._translate_exception(exc) from exc`. Este plan **adopta `from exc`** como excepción deliberada y documentada: la traducción REEMPLAZA el tipo de excepción, y perder la causa del driver destruiría el diagnóstico (ASVS V7). Se registra en `docs/engines.md` y `CHANGELOG.md`. El cortocircuito de lock sigue devolviendo el ORIGINAL (sin traducir), así que `retry()` no se ve afectado.

Purpose: RESL-04 cierra el ciclo — RESL-01 clasifica, RESL-02 recupera, RESL-03 mantiene viva la conexión, y RESL-04 hace que lo que el usuario ve sea una excepción de la librería, no del driver. Además, esta fase es dueña de retirar las entradas `base`/`oracle`/`mysql`/`mssql` del ratchet (`pyproject.toml:272-282`).

Output: `exceptions.py` + barrel; `_translate_exception`/`_translate_error`; traducción cableada en `_with_reconnect`; `SqliteDb._reconnect` → `ConnectionLostError`; tests de taxonomía/traducción/cortocircuito; `docs/engines.md` y `CHANGELOG.md`; ratchet de mypy reducido (o residual documentado).

**Serialización de waves (explícita).** Wave 4 (última), `depends_on: [05-03]`. Toca `base.py` y los seis adaptadores, igual que 05-02/05-03 → van en waves consecutivas, sin solapamiento dentro de una wave. Es el último plan, así que además cierra los gates de la fase (suite, ruff, mypy, `uv lock --check`).

**Sin dependencias nuevas.** No se toca `uv.lock` ni `[project.dependencies]`; el único cambio de `pyproject.toml` es el ratchet de mypy.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-resilience/05-RESEARCH.md
@.planning/phases/05-resilience/05-PATTERNS.md
@encino_orm/exceptions.py
@encino_orm/__init__.py
@encino_orm/base.py
@encino_orm/sqlite.py
@encino_orm/mysql.py
@encino_orm/postgresql.py
@encino_orm/mssql.py
@encino_orm/oracle.py
@encino_orm/http/errors.py
@tests/test_base.py
@pyproject.toml
@docs/engines.md
@CHANGELOG.md

<interfaces>
<!-- Anclajes verificados contra el árbol actual. No explorar más. -->

`encino_orm/exceptions.py` (estado actual, 22 líneas):
- `EncinoOrmError(Exception)` :1; `ConnectionError` :5; `QueryError` :9; `UnsupportedEngineError` :13; `MigrationError` :17; `PoolExhaustedError` :21. Clases planas, cuerpo `pass`, sin `__init__`/`__str__`.

`encino_orm/__init__.py`:
- Import block de excepciones :13-20; `__all__` :41-85 (orden alfabético). Hay que añadir los cuatro nombres en ambos.

`encino_orm/base.py` (tras 05-03):
- `_with_reconnect(self, fn, *, retry)` con `_maybe_recycle()` al entrar, `_is_reconnectable`, `_reconnect`, `_connect_kwargs`/`_connected_at`.
- `is_lock_error` :91-93 (default `False`); `is_disconnect_error` (añadido en 05-01); `retry()` :95-109 (clasifica sobre el ORIGINAL).
- `_last_id_value()` :167-173 — patrón de hook concreto sobreescribible.
- NO importa drivers; importa `check_identifier` y `Query`. Hay que añadir `from .exceptions import ConnectionError, ConnectionLostError, EncinoOrmError, OperationalError` (exceptions.py no importa nada → sin ciclo).

`encino_orm/http/errors.py`:
- `_query_error` :20-22 mapea `QueryError` → HTTP **400**. Al heredar `OperationalError`/`IntegrityError`/`ProgrammingError` de `QueryError`, esos errores pasan de 500 a 400 (cambio de comportamiento → CHANGELOG, A3). `ConnectionError` no tiene handler → sigue 500.

`encino_orm/mssql.py`:
- `is_unique_violation` :65-71 (`sqlstate == "23000" and code in (2601, 2627)`); `_native_code` :55-59.
- `execute`/`execute_insert` traducen nada hoy; solo capturan `ignore_duplicated`.

`encino_orm/oracle.py`:
- `is_unique_violation` :77-79 (`_ora_code(exc) == 1`); `_ora_code` :66-71 (solo `.code`).
- `execute` :240-268 y `execute_insert` :270-305 tienen el `out = cursor.var(...) if returning else None` y el `out.getvalue()` que mypy marca como `Any | None` (errores residuales del ratchet en `:263` y `:299`).

`encino_orm/mysql.py`:
- `is_lock_error` :66-69 (`code = getattr(exc, "args", None)` → `code[0]`), el error residual del ratchet en `:69`.

`pyproject.toml` (ratchet, medido con un config temporal que omite el bloque "cola medida"):
- `encino_orm.base` — 1 error (`base.py:109`, `raise last_exc` con `last_exc = None`).
- `encino_orm.oracle` — 2 errores (`oracle.py:263,299`, `out.getvalue()` con `out` `Any | None`).
- `encino_orm.mysql` — 1 error (`mysql.py:69`, `code[0]` con `code` `Any | None`).
- `encino_orm.mssql` — **medir** (W7): con el bloque de overrides retirado, `uv run mypy encino_orm` decide si la entrada se retira (0 errores) o si se arregla un residual en alcance / se restaura la entrada con causa escrita. NO asumas que está a 0 sin medirlo; si el resultado no es 0, aplica el mismo fallback documentado que para `base`/`oracle`/`mysql`.
- Entradas a retirar :272-282; `encino_orm.pool` :256 NO se toca (residual documentado de 04-04).
- `warn_unused_ignores = true` :222 y 0 `# type: ignore` inline en `encino_orm/`: no se permiten supresiones.

`docs/engines.md` :138-158:
- Bloque "### 2. Implementa la captura del id y el manejo de errores"; `is_lock_error` documentado en :157-158.

`CHANGELOG.md` :9-50:
- `## [Unreleased]` con `### Cambiado` y `### Corregido`; NO crear encabezados duplicados.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Taxonomía en `exceptions.py` + barrel + `_translate_exception`/`_translate_error` + chaining en `_with_reconnect`</name>
  <read_first>
    - `05-RESEARCH.md` §Code Examples ("Taxonomía propuesta" y "Mapeo por driver → taxonomía"), §Pitfall 3 (no traducir lock), §Assumptions A3/A7
    - `05-PATTERNS.md` §`exceptions.py` (jerarquía actual y "Lo que NO se copiar"), §adapters `_translate_error` (analog `http/errors.py:7-22`) y §Shared Patterns → "Chaining (`raise ... from`)"
    - `encino_orm/exceptions.py` (22 líneas), `encino_orm/__init__.py` :13-20,41-85, `encino_orm/http/errors.py` :7-22
    - `encino_orm/base.py` (el `_with_reconnect` de 05-02/05-03)
  </read_first>
  <files>encino_orm/exceptions.py, encino_orm/__init__.py, encino_orm/base.py, encino_orm/sqlite.py, encino_orm/mysql.py, encino_orm/postgresql.py, encino_orm/mssql.py, encino_orm/oracle.py</files>
  <behavior>
    - `issubclass(ConnectionLostError, ConnectionError)` y `issubclass(ConnectionLostError, EncinoOrmError)`; `issubclass(OperationalError, QueryError)`, ídem `IntegrityError`/`ProgrammingError`.
    - `from encino_orm import ConnectionLostError, OperationalError, IntegrityError, ProgrammingError` funciona.
    - `MysqlDb()._translate_exception(pymysql.err.OperationalError(1213, "deadlock"))` devuelve la MISMA instancia (lock, sin traducir).
    - `MysqlDb()._translate_exception(pymysql.err.IntegrityError(1062, "duplicate"))` devuelve `IntegrityError`; `ProgrammingError(1064, "syntax")` → `ProgrammingError`; `OperationalError(2003, "can't connect")` → `OperationalError`.
    - `SqliteDb()._translate_exception(sqlite3.ProgrammingError("Cannot operate on a closed database."))` → `ConnectionLostError`.
    - `PostgresDb()._translate_exception(asyncpg.exceptions.UniqueViolationError("dup"))` → `IntegrityError`; `UndefinedTableError("no table")` → `ProgrammingError`; `DeadlockDetectedError("deadlock")` → la MISMA instancia.
    - `MssqlDb()._translate_exception(_MssqlExc("23000", (2627, "dup")))` → `IntegrityError`; `_MssqlExc("42000", (102, "syntax"))` → `ProgrammingError`.
    - `OracleDb()._translate_exception(_OraExc(1, None))` → `IntegrityError`; `_OraExc(904, None)` → `ProgrammingError`; `_OraExc(60, None)` → la MISMA instancia.
    - Un `FakeResilientDb` con disconnect: `_with_reconnect` lanza `ConnectionLostError` y `exc.__cause__` es la excepción de driver original.
  </behavior>
  <action>
    **A) `exceptions.py` (aditivo, sin renombrar ni re-parentar lo existente).** Añade clases planas con cuerpo `pass`, sin `__init__`/`__str__`: `ConnectionLostError(ConnectionError)` (para que `except ConnectionError` siga capturando); `OperationalError(QueryError)`; `IntegrityError(QueryError)`; `ProgrammingError(QueryError)`. El mensaje se construye al LANZAR (f-string en español), no en la clase.

    **B) Barrel `__init__.py`.** Añade los cuatro nombres al import block :13-20 y a `__all__` :41-85 (orden alfabético; el contrato de importación pública del repo).

    **C) `base.py`.** EXTENDER la línea de import creada en 05-02 (NO añadas un segundo import de `.exceptions`, y conserva el alias para no sombrear el builtin): `from .exceptions import ConnectionError as OrmConnectionError, ConnectionLostError, EncinoOrmError, OperationalError`. `OrmConnectionError` ya lo usan `_is_reconnectable`/`pre_execution` (05-02); `ConnectionLostError`/`EncinoOrmError`/`OperationalError` se usan en `_translate_exception`/`_translate_error`. Añade:
    - `def _translate_error(self, exc) -> Exception:` hook por defecto (concreto, sobreescribible) que devuelve `OperationalError(str(exc))`.
    - `def _translate_exception(self, exc) -> Exception:` PUNTO ÚNICO: si `self.is_lock_error(exc)` → devuelve `exc` SIN traducir (Pitfall 3: `retry()` `base.py:103` necesita el original); si `isinstance(exc, EncinoOrmError)` → devuelve `exc`; si `self.is_disconnect_error(exc)` → `ConnectionLostError(str(exc))`; si no → `self._translate_error(exc)`. NO traga la excepción (mapea y relanza).
    - En `_with_reconnect`, sustituye los relanzados por `raise self._translate_exception(exc) from exc` (y `... from exc2` en el segundo intento). Envuelve `await self._reconnect()` para que un fallo de reconexión también se traduzca: `try: await self._reconnect() except Exception as rexc: raise self._translate_exception(rexc) from rexc`. Actualiza el docstring para documentar el chaining como desviación deliberada y el cortocircuito de lock.
    - Arregla el error de tipado residual `base.py:109`: tipa `last_exc: Exception | None = None` y tras el bucle `if last_exc is None: raise RuntimeError("retry() no ejecutó ningún intento")` antes de `raise last_exc` (sin `assert` y sin `# type: ignore`).

    **D) Hooks `_translate_error` por adaptador** (junto al bloque `# --- errores ---`, sin construir SQL ni interpolar):
    - SQLite: `aiosqlite.IntegrityError` → `IntegrityError`; `aiosqlite.ProgrammingError` → `ProgrammingError`; `aiosqlite.OperationalError`/`DatabaseError` → `OperationalError`; resto → `OperationalError`.
    - MySQL/MariaDB: `pymysql.err.IntegrityError` → `IntegrityError`; `pymysql.err.ProgrammingError` → `ProgrammingError`; `pymysql.err.OperationalError`/`InterfaceError` (que no sean disconnect/lock, ya cortocircuitados) → `OperationalError`; resto → `OperationalError`. MariaDB hereda (no duplicar). Aprovecha para arreglar el residual de mypy en `is_lock_error` (:69) reescribiéndolo con `args = getattr(exc, "args", ())` + guard `if not args: return False` (misma semántica).
    - PostgreSQL: `asyncpg.exceptions.IntegrityConstraintViolationError` → `IntegrityError`; `asyncpg.exceptions.SyntaxOrAccessError`/`UndefinedTableError`/`UndefinedColumnError`/`InvalidNameError` → `ProgrammingError`; `PostgresError` → `OperationalError`; resto → `OperationalError`.
    - MSSQL: si `self.is_unique_violation(exc)` → `IntegrityError`; si el SQLSTATE es de programación (`"42000"`/`"42S02"`/`"42S22"`/`"23000"` no-unique) → `ProgrammingError`; resto → `OperationalError`.
    - Oracle: si `self.is_unique_violation(exc)` o `_ora_code(exc)` en `(1, 2290, 2291, 2292, 1400)` → `IntegrityError`; si `_ora_code(exc)` en `(904, 942, 1722, 1747, 1843, 2290)` → `ProgrammingError` (revisa el solape: la integridad gana); resto → `OperationalError`.
    - **SQLite `:memory:`:** cambia el tipo lanzado por `SqliteDb._reconnect` de `ConnectionError` a `ConnectionLostError` (subclase; el test de 05-02 sigue verde).

    **E) NO toques `is_lock_error`/`is_disconnect_error` semánticamente** (salvo el fix de tipado de MySQL), ni `http/errors.py`, ni el orden del cortocircuito de lock.
  </action>
  <verify>
    <automated>uv run python -c "from encino_orm import ConnectionLostError, OperationalError, IntegrityError, ProgrammingError, ConnectionError, QueryError; assert issubclass(ConnectionLostError, ConnectionError) and issubclass(OperationalError, QueryError) and issubclass(IntegrityError, QueryError) and issubclass(ProgrammingError, QueryError); print('taxonomy OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class ConnectionLostError\|class OperationalError\|class IntegrityError\|class ProgrammingError" encino_orm/exceptions.py` devuelve 4
    - `grep -c "ConnectionLostError\|OperationalError\|IntegrityError\|ProgrammingError" encino_orm/__init__.py` devuelve >= 8 (import + `__all__` de los cuatro)
    - `grep -c "def _translate_exception\|def _translate_error" encino_orm/base.py` devuelve 2
    - `grep -c "def _translate_error" encino_orm/sqlite.py encino_orm/mysql.py encino_orm/postgresql.py encino_orm/mssql.py encino_orm/oracle.py` devuelve >= 1 por fichero
    - `grep -c "from exc\|from exc2\|from rexc" encino_orm/base.py` devuelve >= 3 (chaining en los tres relanzados)
    - `grep -c "is_lock_error" encino_orm/base.py` devuelve >= 2 (cortocircuito en `_translate_exception`)
    - `grep -c "type: ignore" encino_orm/` sigue devolviendo 0
    - `uv run pytest tests/test_d_recommendations.py -k retry -q` pasa (el lock no se traduce)
    - `uv run pytest tests/test_sqlite.py tests/test_base.py -q` pasa
    - `uv run ruff check encino_orm` sale 0
  </acceptance_criteria>
  <done>La taxonomía existe y está exportada; `_translate_exception` es el punto único con cortocircuito de lock; los cinco adaptadores mapean su driver; `_with_reconnect` preserva la causa con `from exc`; el residual de tipado de `base.py`/`mysql.py` queda corregido.</done>
</task>

<task type="auto">
  <name>Task 2: Bloque RESL-04 en `tests/test_resilience.py` + jerarquía en `tests/test_base.py`</name>
  <read_first>
    - `05-RESEARCH.md` §Validation Architecture ("Cómo probar cada criterio de éxito SIN fallos de red reales", punto 4)
    - `05-PATTERNS.md` §`tests/test_base.py` (extend) y §`tests/test_resilience.py`
    - `tests/test_base.py` :30-33 (`TestDbExceptions`), `tests/test_oracle.py` :22-29 (`_FakeError`/`_FakeExc`)
    - `encino_orm/http/errors.py` :20-22 (la herencia `QueryError` → 400 a documentar/probar)
  </read_first>
  <files>tests/test_resilience.py, tests/test_base.py, tests/_resilience_helpers.py</files>
  <action>
    **A) `tests/test_base.py`.** Amplía `TestDbExceptions` para incluir `ConnectionLostError`, `OperationalError`, `IntegrityError`, `ProgrammingError` en el assert de `issubclass(..., EncinoOrmError)` y añade asserts de compatibilidad de `except`: `issubclass(ConnectionLostError, ConnectionError)` y `issubclass(OperationalError, QueryError)` (y las otras dos). Añade un test de que `ConnectionLostError` capturado por `except ConnectionError` no rompe el contrato previo.

    **B) Bloque RESL-04 en `tests/test_resilience.py`** (selector `-k translate`), parametrizado por motor con los builders de `tests/_resilience_helpers`:
    - Traducción de integridad/programación/operacional por motor (SQLite/MySQL/PostgreSQL con tipos reales; MSSQL/Oracle con la forma exacta de `args`).
    - `test_lock_no_se_traduce`: para cada motor, `_translate_exception(lock_exc) is lock_exc` (identidad, no solo tipo).
    - `test_disconnect_se_traduce_a_connection_lost`: `_translate_exception(disconnect_exc)` es `ConnectionLostError`.
    - `test_encinoorm_error_se_devuelve_tal_cual`: un `QueryError` de la librería se devuelve sin re-traducir.
    - `test_with_reconnect_preserva_la_causa`: con `FakeResilientDb` (disconnect) → `pytest.raises(ConnectionLostError)` y `exc.__cause__` es la excepción original del driver.
    - `test_reconnect_fallido_se_traduce`: fake cuyo `connect()` lanza una excepción de driver → la operación lanza una excepción de la taxonomía, no la del driver.
    - `test_sqlite_memory_lanza_connection_lost`: `SqliteDb().connect(database=":memory:")` + `_reconnect()` → `ConnectionLostError` (y sigue siendo capturable como `ConnectionError`).
    - (Opcional, documental) un test que verifique que `OperationalError`/`IntegrityError`/`ProgrammingError` son subclases de `QueryError`, de modo que el handler HTTP las mapea a 400.

    **C) NO** uses `pytest.raises(Exception)` sin `match`; NO toques `http/errors.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_resilience.py -q -k translate && uv run pytest tests/test_base.py -q && uv run pytest tests/test_d_recommendations.py -k retry -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_resilience.py` devuelve >= 27 (RESL-01…04)
    - `uv run pytest tests/test_resilience.py -q -k translate` pasa
    - `grep -c "ConnectionLostError\|OperationalError\|IntegrityError\|ProgrammingError" tests/test_base.py` devuelve >= 4
    - `uv run pytest tests/test_base.py -q` pasa
    - `uv run pytest tests/test_d_recommendations.py -k retry -q` pasa (cortocircuito de lock)
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa
    - `uv run ruff check encino_orm tests` sale 0
  </acceptance_criteria>
  <done>El bloque RESL-04 prueba la traducción por motor, el cortocircuito de lock, la causa preservada y el `:memory:` como `ConnectionLostError`; la jerarquía queda asertada en `test_base.py`.</done>
</task>

<task type="auto">
  <name>Task 3: Documentación (`docs/engines.md`, `CHANGELOG.md`), ratchet de mypy y gates de cierre de fase</name>
  <read_first>
    - `05-RESEARCH.md` §Runtime State Inventory (documentación y ratchet), §Open Question 4, §Assumptions A3/A7
    - `05-PATTERNS.md` §`pyproject.toml` — ratchet (precedente 04-04 :249-256) y §`docs/engines.md` + `CHANGELOG.md`
    - `docs/engines.md` :138-158 y `CHANGELOG.md` :9-60 (`[Unreleased]` existente)
    - `pyproject.toml` :243-283 (ratchet) y :285-315 (dev deps; NO tocar)
    - `.planning/phases/04-pool-correctness-concurrency/04-04-SUMMARY.md` (precedente de "entrada conservada con causa escrita")
  </read_first>
  <files>docs/engines.md, CHANGELOG.md, pyproject.toml</files>
  <action>
    **A) `docs/engines.md`.** En la sección "### 2. Implementa la captura del id y el manejo de errores":
    - Añade `is_disconnect_error(exc)` junto a `is_lock_error` (:157): predicado opcional, obligatorio para que la auto-reconexión funcione; regla de exclusión mutua con `is_lock_error`; el substring solo es refuerzo en MSSQL (`HY000` mid-query) porque los mensajes de ODBC vienen localizados.
    - Documenta la taxonomía pública (`ConnectionLostError`, `OperationalError`, `IntegrityError`, `ProgrammingError`) y su jerarquía, y que `retry()` sigue viendo el error de lock ORIGINAL (no se traduce).
    - Documenta `pre_ping`/`max_connection_lifetime` como kwargs de `connect()` (NO del constructor), su semántica (edad de conexión, opt-in) y el coste de `pre_ping`.
    - Documenta el rechazo de `:memory:` en SQLite y la desviación deliberada del chaining `raise ... from exc` (ASVS V7).
    - Prosa española + bloques fenced; sin `Args:`/`Returns:` (CONVENTIONS.md §Docstrings).

    **B) `CHANGELOG.md`.** Bajo `## [Unreleased]`, amplía las secciones EXISTENTES (no crees encabezados duplicados) con el CAMBIO DE COMPORTAMIENTO de esta fase, en español y con viejo/nuevo:
    1. Una desconexión fuera de transacción reconecta **exactamente una vez**; dentro de transacción relanza. Política de escrituras (A2): las lecturas se re-ejecutan; una escritura cuya sentencia nunca llegó al driver se ejecuta; una escritura que murió mid-statement reconecta pero **no** se re-ejecuta (nunca duplica en silencio). **Lectura literal del Success Criterion 2 (W2):** la mitad "la operación triunfa" se cumple para LECTURAS y para ESCRITURAS pre-ejecución; una ESCRITURA mid-statement reconecta pero **relanza** para no duplicar en silencio. Deja esta distinción escrita en el CHANGELOG, no solo en el código.
    2. Nuevo `is_disconnect_error` por adaptador (RESL-01) y taxonomía pública (RESL-04): las excepciones de driver se traducen a `ConnectionLostError`/`OperationalError`/`IntegrityError`/`ProgrammingError`. **Nota HTTP:** al heredar de `QueryError`, `OperationalError`/`IntegrityError`/`ProgrammingError` se mapean a **400** en `install_error_handlers` (antes escapaban como 500); `ConnectionLostError` hereda de `ConnectionError` y sigue siendo 500.
    3. `pre_ping`/`max_connection_lifetime` opt-in para conexiones directas (RESL-03); SQLite `:memory:` rechaza reconectar (pérdida silenciosa de datos).
    4. Desviación deliberada de la convención "sin chaining": la traducción usa `raise ... from exc` para preservar la causa del driver (ASVS V7).
    Añade la nota de ownership: la entrada es ADITIVA y no prejuzga la enumeración completa del milestone (Fase 8, `08-04`).

    **C) Ratchet de mypy.** Retira `encino_orm.base`, `encino_orm.oracle`, `encino_orm.mysql` y `encino_orm.mssql` del bloque `[[tool.mypy.overrides]]` de la "cola medida" (:272-282). **NO toques `encino_orm.pool`** (:256, residual documentado de 04-04) ni ninguna otra entrada. Ejecuta `uv run mypy encino_orm`:
    - Si sale limpio → deja las entradas retiradas y actualiza el comentario de cabecera del bloque (los conteos cambian; no dejes un comentario mintiendo).
    - Si queda un residual de tipado en un módulo FUERA de `files_modified` de este plan → restaura SOLO esa entrada con la **causa escrita** en el comentario (precedente 04-04 :249-256) y regístralo en el SUMMARY. Nunca borres una entrada sin medir.
    - Si el residual está en `base`/`oracle`/`mysql`/`mssql` (dentro del alcance) → arréglalo sin `# type: ignore` (prohibido inline).

    **D) Gates de cierre de fase.** Ejecuta: `uv run pytest -q -m "not integration and not optional_engine"`, `uv run pytest -q` (si los seis contenedores están arriba), `uv run ruff check encino_orm tests`, `uv run ruff format --check encino_orm tests`, `uv run mypy encino_orm`, `uv lock --check`, `uv run python tools/ci/check_skips.py <junit.xml>` si se generó. Registra en el SUMMARY el estado de cada gate y el diff del ratchet.
  </action>
  <verify>
    <automated>uv run pytest tests/test_resilience.py tests/test_base.py -q && uv run pytest -q -m "not integration and not optional_engine" && uv run ruff check encino_orm tests && uv run ruff format --check encino_orm tests && uv run mypy encino_orm && uv lock --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "is_disconnect_error" docs/engines.md` devuelve >= 1
    - `grep -c "ConnectionLostError" docs/engines.md` devuelve >= 1 y `grep -c "pre_ping" docs/engines.md` devuelve >= 1
    - `sed -n '/## \[Unreleased\]/,/## \[0.2.6\]/p' CHANGELOG.md | grep -ci "connectionlost\|desconexión\|reconect"` devuelve >= 1
    - `sed -n '/## \[Unreleased\]/,/## \[0.2.6\]/p' CHANGELOG.md | grep -c "### Corregido"` devuelve 1 (sin duplicar encabezado)
    - `grep -c '"encino_orm.base"' pyproject.toml` devuelve 0 (o el residual documentado con causa escrita)
    - `grep -c '"encino_orm.oracle"\|"encino_orm.mysql"\|"encino_orm.mssql"' pyproject.toml` devuelve 0 (o residuales documentados)
    - `grep -c '"encino_orm.pool"' pyproject.toml` sigue devolviendo 1 (NO se toca)
    - `uv run mypy encino_orm` sale 0
    - `uv run pytest -q -m "not integration and not optional_engine"` pasa
    - `uv run ruff check encino_orm tests` + `uv run ruff format --check encino_orm tests` salen 0
    - `uv lock --check` sale 0 (no se tocaron dependencias)
    - `grep -c "type: ignore" encino_orm/` sigue devolviendo 0
    - El SUMMARY registra el diff del ratchet y el estado de los gates
  </acceptance_criteria>
  <done>La documentación y el CHANGELOG describen el contrato nuevo y el cambio de comportamiento; el ratchet retira base/oracle/mysql/mssql (o documenta el residual); los gates de cierre de fase pasan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| excepción de driver -> excepción de librería | la traducción decide qué ve el usuario; un mapeo erróneo oculta integridad/programación |
| lock -> traducción | traducir un lock rompe `retry()` (deja de reconocer el original) |
| mensaje traducido -> logs/respuesta | un mensaje que incluya `_connect_kwargs` filtra el password |
| herencia `QueryError` -> HTTP | el status HTTP cambia de 500 a 400 sin que el llamador lo espere |
| ratchet de mypy -> CI | retirar una entrada con errores residuales rompe el job `typecheck` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Repudiation | Un lock traducido deja de reintentarse y el fallo se presenta como error del usuario | mitigate | Cortocircuito en `_translate_exception` (`is_lock_error` → devolver el ORIGINAL); test de identidad por motor; `TestD4AutoRetry` verde |
| T-05-04-02 | Information Disclosure | Mensajes de la taxonomía con `_connect_kwargs` (password) | mitigate | El mensaje se construye con `str(exc)` del driver, nunca con `_connect_kwargs`; `_reconnect` no loguea el dict (05-02); revisión en el test/docs |
| T-05-04-03 | Repudiation | Catch-all que oculta fallos | mitigate | `_translate_error` mapea y RELANZA; el default es `OperationalError` (no `except: pass`); Out of Scope `REQUIREMENTS.md:124` respetado |
| T-05-04-04 | Tampering | Herencia de `QueryError` cambia el status HTTP a 400 sin documentar | mitigate | Nota explícita en `CHANGELOG.md` y `docs/engines.md` (A3); `ConnectionLostError` sigue 500 (hereda `ConnectionError`) |
| T-05-04-05 | Tampering | Pérdida de la causa del driver al traducir | mitigate | `raise ... from exc` en los tres relanzados; test `__cause__` es la excepción original; desviación de convención documentada |
| T-05-04-06 | Tampering | Ratchet retirado con errores residuales rompe el job `typecheck` | mitigate | Retirar solo tras `uv run mypy encino_orm` limpio; si hay residual fuera de alcance, restaurar la entrada con causa escrita (precedente 04-04); `encino_orm.pool` intacto |
| T-05-04-07 | Denial of Service | `# type: ignore` inline como atajo para el ratchet | mitigate | Prohibido por convención y por `warn_unused_ignores = true`; grep de control (`type: ignore` == 0) |
| T-05-04-SC | Tampering | instalaciones de paquetes | n/a | No se toca `[project.dependencies]` ni `uv.lock`; solo el ratchet de `pyproject.toml` |
</threat_model>

<verification>
- `uv run pytest tests/test_resilience.py -q` verde (RESL-01…04)
- `uv run pytest tests/test_base.py -q` verde (jerarquía)
- `uv run pytest tests/test_d_recommendations.py -k retry -q` verde (lock sin traducir)
- `uv run pytest -q -m "not integration and not optional_engine"` verde
- `uv run ruff check encino_orm tests` + `uv run ruff format --check encino_orm tests` salen 0
- `uv run mypy encino_orm` sale 0 con `encino_orm.base`/`oracle`/`mysql`/`mssql` retirados (o residual documentado)
- `uv lock --check` sale 0
- `grep -c "type: ignore" encino_orm/` == 0
- `grep -c "ConnectionLostError" docs/engines.md CHANGELOG.md` >= 1 en cada uno
- `grep -c "is_disconnect_error" docs/engines.md` >= 1
- `grep -c '"encino_orm.pool"' pyproject.toml` == 1 (intacto)
- `git diff --stat encino_orm/pool.py` vacío (PoolDb no cambia en toda la fase)
- `filterwarnings = ["error"]` intacto
</verification>

<success_criteria>
- RESL-04 cubierto: existe una taxonomía pública documentada que traduce excepciones de driver a excepciones de la librería (Success Criterion 4)
- `_translate_exception` es el punto único; el lock se devuelve sin traducir y `retry()` sigue funcionando
- Cada adaptador mapea su driver reutilizando los helpers existentes, sin construir SQL ni tragar excepciones
- La causa del driver se preserva con `raise ... from exc` (desviación documentada)
- `docs/engines.md` y `CHANGELOG.md` reflejan el cambio de comportamiento (0.x: documentado), incluido el status HTTP 400
- El ratchet de mypy retira `base`/`oracle`/`mysql`/`mssql` (o documenta el residual) y `encino_orm.pool` queda intacto
- Los gates de cierre de fase pasan
</success_criteria>

<output>
Create `.planning/phases/05-resilience/05-04-SUMMARY.md` when done
</output>
