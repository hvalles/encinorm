---
phase: 07-performance-and-benchmarks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_benchmarks.py
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
requirements: [PERF-02]
must_haves:
  truths:
    - "CI detecta regresiones de rendimiento >= 1.67x en las unidades síncronas del núcleo (sin ruido de driver)"
    - "Los benchmarks NO contaminan la matriz de 4 patas del job `test` ni el combine de cobertura"
    - "El harness es reproducible sin dependencias nuevas (uv.lock intacto)"
  artifacts:
    - path: "tests/test_benchmarks.py"
      provides: "Unidades de rendimiento con piso numerico (mediana/p95/sigma)"
      contains: "pytest.mark.benchmark"
    - path: ".github/workflows/ci.yml"
      provides: "Job `benchmarks` dedicado (ubuntu, 3.12, sin servicios)"
      contains: "benchmarks"
  key_links:
    - from: ".github/workflows/ci.yml (job test)"
      to: "pytest -m"
      via: "filtro `not optional_engine and not benchmark`"
      pattern: "not benchmark"
    - from: "tests/test_benchmarks.py"
      to: "encino_orm/observability.py::_percentile"
      via: "import para el p95"
      pattern: "_percentile"
---

<objective>
Instalar el harness de benchmarking de unidades SÍNCRONAS del núcleo (sin base
de datos: sin ruido de driver/CI) con piso numérico de rendimiento, cablearlo a
CI en un job dedicado y dejar la disciplina de calibración documentada.

Purpose: PERF-02 — regresiones medibles, no percepciones (constraint de
rendimiento del proyecto). El piso se fija en 0.6× de la línea base medida en
CI: cualquier caída 1.67× o peor falla el job con la mediana en el mensaje.
Output: `tests/test_benchmarks.py` (harness + 5 unidades), cabecera del marker
`benchmark` corregida en `pyproject.toml`, y el job `benchmarks` en
`.github/workflows/ci.yml`.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-and-benchmarks/07-RESEARCH.md

# Q2 (forma del harness) y Q3 (unidades y pisos) son la fuente de este plan. El
# RESEARCH SUSTITUYE el borrador del brief "Query.format/rebind" por las
# unidades medidas: y la fuente real es `Query` + `with_params` + `_to_mysql`.

<interfaces>
Anclas verificadas (líneas actuales al planificar):

- `encino_orm/observability.py`: `def _percentile(samples, p)` (:26-35)
- `encino_orm/query.py`: `class Query` (:8); template `{n}` + params;
  `with_params` devuelve una copia con nuevos params
- `encino_orm/model/query_builder.py`: `class QueryBuilder` (:41);
  `__init__(self, model_class, db=None, alias="mm")` (:47);
  `_build_full()` (:230) devuelve (sql, params) y NO toca la base si no se
  ejecuta; `join`/`where`/`group_by`/`order_by` fluyen sobre el builder
- `encino_orm/mysql.py`: `def _to_mysql(sql: str, params: dict) -> tuple[str, list]` (:56)
- `encino_orm/model/model.py`: fórmula de chunk de `insert_many` (:597-603):
  `max(1, min(max_params // max(len(columns), 1), max_rows))`
- `pyproject.toml`: marker `benchmark` declarado (:79) con texto que menciona
  pytest-codspeed (desactualizado: codspeed NO se usa); `filterwarnings = ["error"]`
- `.github/workflows/ci.yml`: job `test` corre
  `uv run pytest -q -m "not optional_engine" --timeout-method=signal
  --junitxml=junit.xml --cov=encino_orm --cov-branch --cov-report=` (:137-139);
  jobs `lint` (:158), `typecheck` (:182), `deps` (:207, cierra ~:249 con
  `uv run pip-audit -r ...`), `engine-heavy` (:250), `coverage` (:370,
  `needs: [test, engine-heavy]`)

Nota operativa: la suite local completa (`uv run pytest -q`) AHORA incluirá los
benchmarks (~30-60 s extra en el host de desarrollo); las corridas de CI del job
`test` los excluyen por marcador, no por `testpaths` (que los incluiría también
en `engine-heavy` si se ejecutara con selección por archivo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness + 5 unidades con piso + marker corregido + job de CI (un commit atómico)</name>
  <read_first>
    @.planning/phases/07-performance-and-benchmarks/07-RESEARCH.md
    @encino_orm/observability.py
    @encino_orm/query.py
    @encino_orm/model/query_builder.py
    @encino_orm/mysql.py
    @pyproject.toml
    @.github/workflows/ci.yml
  </read_first>
  <files>
    tests/test_benchmarks.py (nuevo)
    pyproject.toml (cabecera del marker `benchmark`, línea ~79)
    .github/workflows/ci.yml (filtro del job `test` + job `benchmarks` nuevo)
  </files>
  <action>
    Crear `tests/test_benchmarks.py` (harness + 5 unidades) y, en el MISMO
    commit, la cabecera del marker y el cableado de CI. UN solo commit — el
    estado intermedio "harness en el repo sin exclusión en CI" correría los
    benchmarks en la matriz de 4 patas (Pitfall 2 del RESEARCH), y el job
    `coverage` combinaría su ruido.

    --- Harness (módulo tests/test_benchmarks.py) ---
    - `pytestmark = [pytest.mark.benchmark, pytest.mark.timeout(60)]` (timeout de
      pytest-timeout, ya presente en CI: el job `test` usa `--timeout-method=signal`).
    - Fixture autouse de módulo: `logging.getLogger("encino_orm").disabled = True`
      (el tracer por defecto degrada el rate; RESEARCH Q2).
    - `from encino_orm.observability import _percentile`.
    - Helper `_measure(fn, *, inner, warmup=3, reps=9) -> tuple[float, float, float]`:
      warmup SIN cronometrar; cada repetición: `gc.collect()` ANTES del bucle
      cronometrado (research Q2); `samples.append(inner / (time.perf_counter() - t0))`.
      Devuelve `(statistics.median(samples), _percentile(samples, 0.95),
      statistics.stdev(samples))`. Ajustar `inner` para que cada repetición
      cronometrada dure >= 0.5 s en el host (semillas del RESEARCH: 2_000 para
      SQL build/Query/with_params/_to_mysql; 200_000 para la fórmula de chunk).
    - Cada test: `assert median > FLOOR, f"<nombre> {median:,.0f} ops/s < {FLOOR} (p95={p95:,.0f}, sigma={std:.0f})"`.
      Un smoke assert FUERA del bucle cronometrado por unidad (SQL resultante /
      tipos devueltos) para que el piso no se mida sobre una operación rota.

    --- Unidades y pisos (0.6x de las líneas base medidas del RESEARCH Q3) ---
    1. `test_sql_build_floor`: `QueryBuilder(_A, db=SqliteDb(":memory:"), alias="mm")`
       con `.select(...).join(_B, "b", on=Filter(...)).where(...).group_by(...)
       .order_by(...)` construido UNA vez fuera del timing; operación medida:
       `qb._build_full()`. Modelos auxiliares: `class _A(Model): _table="a"; x:int`
       y `class _B(Model): _table="b"; y:int` (sin `@pytest.fixture` de módulo que
       los comparta con otros tests). FLOOR = 85_000 ops/s (148,8K medidos).
       Smoke: el SQL devuelto contiene `JOIN` y `WHERE`.
    2. `test_query_construction_floor`: plantilla con ~8 placeholders
       (`"SELECT {0}, {1}, {2} FROM t WHERE a={3} AND b={4} AND c={5} ORDER BY {6}"`);
       operación: `Query(TMPL, list_of_8)`. FLOOR = 140_000 (236K medidos).
       Smoke: `isinstance(q, Query)` y `len(q.params) == 8`.
    3. `test_query_with_params_floor`: `base = Query(TMPL_5_placeholders, [...] )`
       construido fuera del timing; operación: `base.with_params(list_of_5)`.
       FLOOR = 120_000 (209,3K medidos). Smoke: `q2 is not base`.
    4. `test_to_mysql_floor`: `_to_mysql(TMPL_6_placeholders_mysql, {"parameter_0000": 1,
       "parameter_0001": 2, ...})` con el dict construido fuera del timing;
       operación: la llamada. FLOOR = 375_000 (625,1K medidos). Smoke:
       resultado `tuple[str, list]` y `?` sin placeholders sobrantes.
    5. `test_batch_sizing_floor`: operación
       `lambda: max(1, min(32767 // max(5, 1), 1000))` — MISMA fórmula que
       `insert_many` (model.py:597-603). FLOOR = 5_000_000 (9,6M medidos).

    --- pyproject.toml (línea ~79) ---
    Corregir la cabecera del marker `benchmark`: quitar la mención a
    pytest-codspeed (NO se usa) por algo como
    `"benchmark: unidades síncronas del nucleo medidas con time.perf_counter (mediana/p95/sigma); excluidas de corridas normales"`.
    El assert de `tests/test_pytest_config.py:106-107` compara solo NOMBRES de
    markers — la descripción puede cambiar sin romperlo (verificado).

    --- .github/workflows/ci.yml ---
    1. Job `test` (:137-139): filtro a
       `uv run pytest -q -m "not optional_engine and not benchmark" --timeout-method=signal
       --junitxml=junit.xml --cov=encino_orm --cov-branch --cov-report=`
       + comentario: los benchmarks corren en el job dedicado; mantenerlos fuera
       de la matriz de 4 patas (ruido de CI) y del combine de cobertura.
    2. Job nuevo `benchmarks:` DESPUÉS de `deps` (:249) y ANTES de `engine-heavy`
       (:250), SIN `needs:` (paralelo). Mismos pasos base que `deps`
       (checkout@v5, astral-sh/setup-uv@v5 version "0.12.15", `uv sync --group dev`),
       `timeout-minutes: 10`, y:
       `run: uv run pytest -m benchmark -q --timeout-method=signal`
       + comentario del contrato: pisos = 0.6x de una línea base medida UNA vez
       en CI; una regresión >= 1.67x falla el job; recalibración documentada en
       el mismo commit que cambie un piso.
       NO pasar `--cov*` ni `--junitxml` en este job: no debe alimentar el
       combine de `coverage` (needs: [test, engine-heavy] queda intacto).
  </action>
  <verify>
    uv run pytest tests/test_benchmarks.py -q --timeout-method=thread   # 5 unidades verdes en el host
    uv run ruff check tests/test_benchmarks.py && uv run ruff format --check tests/test_benchmarks.py
    uv run pytest -q -m "not optional_engine and not benchmark" -x   # la suite funcional SIGUE verde y excluye benchmarks
    uv run ruff check pyproject.toml 2>/dev/null; uv lock --check
  </verify>
  <done>
    Commit 1 (prefijo `test(benchmarks)` + `ci(benchmarks)`): harness + 5
    unidades con piso, marker corregido, job `benchmarks` en CI y filtro del job
    `test` — los cuatro cambios en el mismo commit (atómico contra el Pitfall 2).
    Localmente: 5 unidades verdes, suite funcional excluyéndolos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Calibración local (dry-run CI) y verificación de empaquetado</name>
  <read_first>
    @tests/test_benchmarks.py
    @.github/workflows/ci.yml
  </read_first>
  <files>
    (ninguno — o, si un piso falla localmente por <=20% vs su línea base, el
    ajuste del constante en tests/test_benchmarks.py en un commit documentado)
  </files>
  <action>
    Reproducir EXACTAMENTE las invocaciones de CI en el host (dry-run):
    1. `uv run pytest -q -m "not optional_engine and not benchmark" --timeout-method=signal`
       → suite funcional completa verde (la corrida local total dura como siempre;
       los benchmarks quedan fuera por el filtro, igual que en CI).
    2. `uv run pytest -m benchmark -q --timeout-method=signal` → las 5 unidades
       verdes; apuntar mediana/p95/σ de cada una en el SUMMARY.
    3. `uv run pytest -q` → la suite COMPLETA local (incluyendo benchmarks) verde:
       este es el comportamiento que verá el host de desarrollo de ahora en adelante.
    4. `mkdocs build --strict` y `uv run mypy encino_orm` siguen verdes (este plan
       no toca `encino_orm/`, pero el commit 1 toca pyproject — confirmar que el
       marker no rompe nada).

    Disciplina de calibración (RESEARCH Q8):
    - Si un piso falla localmente pero la mediana medida está DENTRO de ±20% de
      su línea base del RESEARCH Q3 → el piso está mal sembrado (no hay
      regresión): ajustarlo en un commit dedicado y documentar la razón en el
      SUMMARY. Si la caída es >20%, es una regresión REAL: investigar antes de
      tocar nada.
    - La CALIBRACIÓN FINAL se hace con la primera corrida del job `benchmarks`
      en CI (hardware real): si un piso falla y la mediana de CI está dentro de
      ±20% de la base, ajustar el piso y subirlo en el MISMO commit que el
      ajuste (sin re-pescar silenciosamente pisos después).
    - Verificar que el job `benchmarks` no publica artefactos de cobertura:
      grep del job en ci.yml (sin `--cov`).
    - Empaquetado: `uv build` + listado del wheel
      (`python -c "import zipfile,glob;z=zipfile.ZipFile(sorted(glob.glob('dist/*.whl'))[-1]);assert not [n for n in z.namelist() if 'test_benchmarks' in n or 'benchmark' in n]"`)
      → `tests/` y `benchmarks/` no entran en el wheel (hatchling empaqueta solo
      `packages = ["encino_orm"]`); verificación, sin cambios de config.
  </action>
  <verify>
    uv run pytest -q -m "not optional_engine and not benchmark" -x
    uv run pytest -m benchmark -q
    uv run pytest -q   # completa local (incluye benchmarks) — verde
    uv build && uv run python -c "import zipfile,glob;z=zipfile.ZipFile(sorted(glob.glob('dist/*.whl'))[-1]);assert not [n for n in z.namelist() if 'benchmark' in n]"
  </verify>
  <done>
    Dry-run de CI completo y verde en el host; medianas/p95/σ documentados en el
    SUMMARY; pisos validados (o ajustados con commit documentado); wheel sin
    rastros de benchmarks; `uv lock --check` verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest → núcleo | Las unidades de rendimiento ejercitan código sincrono (`Query`, `QueryBuilder`, `_to_mysql`, fórmula de chunk) SIN base de datos: sin credenciales, sin red. |
| CI job `benchmarks` → runner | Job dedicado sin servicios de DB: solo python + deps del lockfile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Denial of Service | job `benchmarks` | mitigate | `timeout-minutes: 10` + `pytest.mark.timeout(60)` por módulo; sin servicios de DB que arrancar |
| T-07-12 | Tampering | pyproject.toml (marker) | accept | Cabecera descriptiva; el assert de markers compara nombres (`tests/test_pytest_config.py:106-107`) |
| T-07-13 | Information Disclosure | artefactos de cobertura | mitigate | El job `benchmarks` NO pasa `--cov` ni `--junitxml`; `coverage` sigue con `needs: [test, engine-heavy]` |
| T-07-SC | Tampering | instalaciones | mitigate | Cero dependencias nuevas: `uv.lock` intacto, `uv lock --check` verde en el job `deps`; la calibración de pisos jamás añade un paquete de benchmarking |
</threat_model>

<verification>
- Gates completos verdes e idénticos a antes de este plan: `ruff check`/`format
  --check` (encino_orm + tests), `mypy encino_orm`, `mkdocs build --strict`,
  `uv run pytest -q` (CI: filtrado; local: con benchmarks, ~+30-60 s),
  `check_skips.py`, `uv lock --check`, cobertura `fail_under`.
- El job `test` de CI queda con `-m "not optional_engine and not benchmark"` y el
  job `benchmarks` queda independiente (sin `needs:`, sin cov, sin junitxml).
</verification>

<success_criteria>
- Una regresión de rendimiento 1.67x o peor en cualquiera de las 5 unidades
  falla el job `benchmarks` de CI con la mediana en el mensaje del assert.
- Los benchmarks no aparecen en la matriz de 4 patas ni en el combine de
  cobertura (verificado por grep del yaml).
- Sin dependencias nuevas; wheel del paquete sin rastros de tests/benchmarks.
- Primer corrida de CI calibra los pisos (±20%) con ajustes en el mismo commit.
</success_criteria>

<output>
Crear `.planning/phases/07-performance-and-benchmarks/07-02-SUMMARY.md` al terminar:
tabla de las 5 unidades con mediana/p95/σ medidos en el host, invocaciones de
CI reproducidas localmente, resultado del wheel, y (si aplicó) el commit de
calibración con su razón.
</output>