---
phase: 07-performance-and-benchmarks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/profile_workload.py
  - benchmarks/profiles/profile_workload_cprofile_2026-09-19.pstats
  - benchmarks/profiles/profile_workload_cprofile_2026-09-19.txt
  - benchmarks/profiles/profile_workload_manual_2026-09-19.txt
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "Existe una línea base de rendimiento reproducible (profiling) ANTES de optimizar copy_table (PERF-01, plan 07-03)"
    - "El perfil cProfile y el modo manual producen artefactos comparables en el repo"
    - "Los artefactos de profiling no se empaquetan en el wheel publicado"
  artifacts:
    - path: "benchmarks/profile_workload.py"
      provides: "Carga de trabajo reproducible del núcleo (Model CRUD, insert_many, copy_table, Query, fetch)"
      contains: "def main"
    - path: "benchmarks/profiles/profile_workload_manual_2026-09-19.txt"
      provides: "Línea base manual (time.perf_counter) por sección, pre-optimización"
      contains: "copy_table"
    - path: "benchmarks/profiles/profile_workload_cprofile_2026-09-19.pstats"
      provides: "Estadísticas cProfile crudas (reprocesables con pstats)"
  key_links:
    - from: "benchmarks/profile_workload.py"
      to: "encino_orm/transfer.py::copy_table"
      via: "import + llamada (sección copy_table; línea base pre-07-03)"
      pattern: "copy_table"
---

<objective>
Crear la carga de trabajo de profiling reproducible del núcleo y su línea base
comprometida en el repo, ANTES de tocar `copy_table` (07-03, PERF-01). Sin esta
línea base, "la optimización mejora algo" no es verificable: la sección
`copy_table` de este script es la evidencia de comparación.

Purpose: PERF-03 — el artefacto de profiling precede al artefacto de
benchmarking (07-02) y a la optimización (07-03). Fecha los artefactos con la
fecha de ejecución (hoy: 2026-09-19) para que la comparación post-optimización
sea inequívoca.
Output: `benchmarks/profile_workload.py` (script) y
`benchmarks/profiles/profile_workload_{cprofile,manual}_2026-09-19.*`
(artefactos comprometidos).
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-and-benchmarks/07-RESEARCH.md

# Secciones usadas del RESEARCH: Q1 (qué profilar), Q2 (forma del harness — el
# script manual es su predecesor), Q4 (secciones de la carga), Q8 (wheel).

<interfaces>
Anclas verificadas (líneas actuales al planificar):

- `encino_orm/transfer.py`:
  - `async def copy_table(src, dst, table, *, create=False, truncate=False, preserve_ids=True) -> int` (:129)
  - Bucle fila a fila que 07-03 reemplazará por lotes (:152-158)
- `encino_orm/model/model.py`:
  - `insert_many(cls, db=None, rows, *, chunk=500)` (:506); fórmula de chunk
    `max(1, min(max_params // max(len(columns), 1), max_rows))` (:597-603)
- `encino_orm/query.py`: `Query` (:8), `_PLACEHOLDER_RE` (:5)
- `encino_orm/dialects/strategies.py`: `LIMITS` (:106-158) — sqlite
  max_params=32766, max_rows=1000

Dato de diseño: `SqliteDb` y `PostgresDb` NO definen `MAX_PARAMS`/`MAX_ROWS`
(clase `MAX_PARAMS` solo en mariadb.py:17, mssql.py:50, mysql.py:63, oracle.py:77;
propiedad en pool.py:148) → `getattr(db, "MAX_PARAMS", 500)` es el patrón del
núcleo (insert_many). El script NO necesita esto (sqlite no lotea), pero 07-03 sí.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de profiling reproducible (modos cprofile y manual)</name>
  <read_first>
    @encino_orm/transfer.py
    @encino_orm/model/model.py
    @encino_orm/query.py
    @.planning/phases/07-performance-and-benchmarks/07-RESEARCH.md
  </read_first>
  <files>
    benchmarks/profile_workload.py (nuevo; dir benchmarks/ no existe aún)
  </files>
  <action>
    Crear `benchmarks/profile_workload.py`: carga de trabajo DETERMINISTA (sin
    aleatoriedad), solo sqlite :memory:, sin red, sin drivers opcionales. CLI con
    argparse: `--mode {cprofile,manual}` (default `cprofile`), `--scale N`
    (default 1, multiplica iteraciones), `--out PATH` (default
    `benchmarks/profiles/profile_workload_{mode}_{YYYY-MM-DD}`; la fecha se
    obtiene de `datetime.date.today().isoformat()`).

    Secciones (funciones separadas, cada una devuelve (nombre, iteraciones,
    total_s) para el modo manual):

    1. `crud_sqlite(scale)`: modelo declarativo vía `encino_orm.model` con
       `_table`, columnas str/int y un datetime como TEXT (para NO disparar la
       DeprecationWarning del adapter datetime de sqlite3 bajo `-W error`).
       `create_table` + bucle `insert` → `load` → `update` → `delete` de
       500·scale filas. Documentar en un comentario por qué el datetime va como
       TEXT (mismo motivo que `encino_orm/model/model.py:36`, normalización
       UTC-naive).
    2. `insert_many_chunked(scale)`: 5000·scale filas con `insert_many(chunk=500)`
       (camino multi-VALUES existente).
    3. `copy_table_rowwise(scale)`: tabla origen de 2000·scale filas
       sqlite→sqlite con `preserve_ids=True` usando `copy_table` tal como está
       hoy (fila a fila). Es la sección que 07-03 debe mejorar; comentario en el
       script: "línea base pre-07-03 (PERF-03)".
    4. `query_translation(scale)`: 20000·scale iteraciones construyendo `Query`
       con plantilla de ~8 placeholders y su copia con `with_params` (mismos
       patrones que las unidades de 07-02).
    5. `fetch_loop(scale)`: `fetch_all` sobre la tabla creada en (1).

    Al inicio: `logging.getLogger("encino_orm").setLevel(logging.WARNING)` (el
    tracer degradado no debe contaminar la medición).

    Modo `cprofile`: `cProfile.Profile()` alrededor de TODO el workload;
    `pr.dump_stats(out + ".pstats")`; render a texto con
    `pstats.Stats(pr, stream=...)` `sort_stats("cumulative").print_stats(40)`
    (las 40 líneas más costosas acumuladas). El .txt lleva cabecera: argv
    completo, fecha, `platform.platform()`, `sys.version.split()[0]`. El mismo
    texto se imprime a stdout Y se escribe en `out + ".txt"`.

    Modo `manual`: `time.perf_counter()` por sección (sin granularidad media);
    tabla al final: sección | iteraciones | total s | ops/s. Se escribe en
    `out + ".txt"` y se imprime a stdout.

    Gates: `exit 0`, cero warnings con `python -W error`, estilo consistente con
    el repo (línea <= 100; no tocar pyproject — `benchmarks/` no está en
    `[tool.pytest.ini_options] testpaths`, no entra en los gates de ruff/mypy de
    CI, pero el script debe ser limpio igualmente).
  </action>
  <verify>
    uv run python -W error benchmarks/profile_workload.py --mode=manual --scale=1
    uv run python -W error benchmarks/profile_workload.py --mode=cprofile --scale=1
    (ambos exit 0, sin warnings, sin crear archivos fuera de benchmarks/profiles/)
  </verify>
  <done>
    Script comprometido (Commit 1, prefijo scoped `feat(benchmarks)`); se
    ejecuta limpio en ambos modos; cabecera y tabla de secciones presentes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ejecutar ambos modos, comprometer artefactos y verificar el wheel</name>
  <read_first>
    @benchmarks/profile_workload.py
    @pyproject.toml
  </read_first>
  <files>
    benchmarks/profiles/profile_workload_cprofile_2026-09-19.pstats
    benchmarks/profiles/profile_workload_cprofile_2026-09-19.txt
    benchmarks/profiles/profile_workload_manual_2026-09-19.txt
  </files>
  <action>
    Ejecutar el script en el host (scale=1, ambos modos). Comprometer en el
    MISMO commit los tres artefactos (Commit 2, prefijo `benchmarks(07-01)`):

    - cprofile: `.pstats` crudo + `.txt` (top-40 por cumtime). Verificar que la
      sección `copy_table_rowwise` aparece en el top-40 (grep de una seña de la
      sección 3, p. ej. `copy_table`, en el .txt).
    - manual: `.txt` con la tabla de secciones.

    Verificación de empaquetado: `uv build` y listado del wheel con
    `python -c "import zipfile,glob;z=zipfile.ZipFile(sorted(glob.glob('dist/*.whl'))[-1]);print([n for n in z.namelist() if 'benchmark' in n])"`
    → debe imprimir `[]` (hatchling empaqueta solo `packages = ["encino_orm"]`,
    pyproject.toml). Si NO es `[]` (no debería ocurrir), DETENERSE y avisar: el
    fix de pyproject lo coordina el dueño del archivo (07-02), no este plan.
    `dist/` está en .gitignore; no se compromete nada de ahí.
  </action>
  <verify>
    uv run python benchmarks/profile_workload.py --mode=manual --scale=1
    uv run python benchmarks/profile_workload.py --mode=cprofile --scale=1
    uv build
    uv run python -c "import zipfile,glob;z=zipfile.ZipFile(sorted(glob.glob('dist/*.whl'))[-1]);assert not [n for n in z.namelist() if 'benchmark' in n]"
  </verify>
  <done>
    Artefactos comprometidos; sección copy_table visible en el .txt cProfile;
    wheel sin entradas `benchmarks/`; `git status` limpio salvo lo comprometido.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → repo | El script ejecuta código del núcleo contra sqlite :memory:; no cruza red ni recibe input no confiable (solo argparse local). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | benchmarks/profile_workload.py | mitigate | Sin interpolación de input: `--out` valida extensión/directorio; `--scale` entero positivo (argparse type=int, mínimo 1) |
| T-07-02 | Information Disclosure | benchmarks/profiles/*.pstats | accept | Datos sintéticos deterministas; sin credenciales; rutas locales solo en la cabecera |
| T-07-SC | Tampering | instalaciones (sin nuevas deps) | mitigate | Cero dependencias nuevas (stdlib + encino_orm); uv.lock sin cambios: `uv lock --check` sigue verde en CI |
</threat_model>

<verification>
- Gates completos (los corren los jobs de CI/cobertura) y deben seguir verdes:
  `uv run ruff check encino_orm tests`, `uv run ruff format --check encino_orm tests`,
  `uv run mypy encino_orm`, `mkdocs build --strict`, `uv run pytest -q` (la suite
  principal NO incluye `benchmarks/`: no está en `testpaths`),
  `tools/ci/check_skips.py`, `uv lock --check`.
- Este plan NO toca `pyproject.toml` ni `.github/workflows/ci.yml` (dueño: 07-02).
</verification>

<success_criteria>
- `benchmarks/profile_workload.py` existe, es determinista y corre sin warnings
  bajo `-W error` en ambos modos, en Windows (host de desarrollo) y Linux (CI).
- Tres artefactos con fecha comprometidos (2 commits). La sección `copy_table`
  queda registrada como línea base pre-optimización: es la evidencia de 07-03.
- El wheel publicado no contiene `benchmarks/` (verificado, sin cambios de config).
</success_criteria>

<output>
Crear `.planning/phases/07-performance-and-benchmarks/07-01-SUMMARY.md` al terminar,
documentando: tiempos manuales por sección (tabla), top-5 rutas cumtime del perfil
cProfile, resultado del gate del wheel, y qué secciones sirven de línea base para
07-02 (harness de mediana/p95) y 07-03 (copy_table).
</output>