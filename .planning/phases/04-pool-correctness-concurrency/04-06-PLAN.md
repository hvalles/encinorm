---
phase: 04-pool-correctness-concurrency
plan: 06
type: execute
wave: 4
depends_on: [04-01, 04-03, 04-05]
files_modified:
  - tests/test_pool_concurrency.py
autonomous: true
requirements: [POOL-07]
must_haves:
  truths:
    - "Existe `tests/test_pool_concurrency.py` con tests deterministas de concurrencia/estrés que ASSERTAN el comportamiento FINAL del pool (no la caracterización)."
    - "El test de admisión usa `EventBarrier` (construida con `asyncio.Event` desde `tests/_pool_helpers.py`) y `@pytest.mark.timeout(N)`; NO usa `asyncio.Barrier`/`TaskGroup`/`asyncio.timeout` (piso Python 3.10)."
    - "Los tests de barrera llevan el marker `concurrency`; la variante de estrés lleva `stress` + `pytest.mark.repeat(N)` (marker registrado en `pyproject.toml` por 04-05)."
    - "Inserts concurrentes sobre un `PoolDb(\"sqlite\")` real devuelven cada uno SU PROPIO id vía `execute_insert` (sin cruce entre tareas)."
    - "`pool.execute(INSERT)` standalone sigue commiteando y es visible desde otra conexión del pool."
    - "El fichero NO edita `tests/test_pool.py` ni `tests/test_pool_characterization.py` (ficheros de 04-04 en la misma Wave): toda la cobertura nueva vive en el fichero nuevo."
  artifacts:
    - path: "tests/test_pool_concurrency.py"
      provides: "tests deterministas de carrera de admisión, ids concurrentes, commit standalone y variante de estrés"
      contains: "EventBarrier"
  key_links:
    - from: "tests/test_pool_concurrency.py"
      to: "tests/_pool_helpers.py::EventBarrier"
      via: "barrera determinista 3.10-safe compartida (creada por 04-05)"
      pattern: "EventBarrier"
    - from: "tests/test_pool_concurrency.py::test_no_overshoot_under_barrier"
      to: "encino_orm/pool.py::PoolDb.acquire"
      via: "reserva-antes-de-await (04-01): `_size <= max_size` bajo la barrera"
      pattern: "_max_size"
    - from: "tests/test_pool_concurrency.py::test_concurrent_insert_ids_no_crosstalk"
      to: "encino_orm/base.py::Db.execute_insert"
      via: "captura del id dentro de la sentencia (04-02) por conexión/tarea"
      pattern: "execute_insert"
---

<objective>
Entregar los tests deterministas de concurrencia y estrés que ASSERTAN el comportamiento final del pool (POOL-07), usando la infraestructura de 04-05.

La fase ya tiene los cambios de comportamiento (04-01…04-04) y la red de caracterización de Fase 1, pero POOL-07 exige tests que fijen el comportamiento CORRECTO (no el defectuoso): que `acquire()` no supere `max_size` bajo una barrera determinista, que los inserts concurrentes no crucen ids, que el commit standalone siga siendo visible y que exista una variante de estrés repetible. La barrera debe ser `asyncio.Event` a mano porque el piso es Python 3.10 (Research Correction #4).

Purpose: sin estos tests, la corrección del pool queda demostrada solo por la inversión de la caracterización (que mide el defecto) y no por una propiedad afirmada. El Core Value exige que la corrección bajo concurrencia sea verificable, no percibida.

Output: `tests/test_pool_concurrency.py` con la carrera de admisión bajo `EventBarrier` + `pytest.mark.timeout`, la ausencia de cruce de ids en inserts concurrentes, el commit standalone visible, y una variante de estrés `pytest-repeat` tras el marker `stress`.

**Disjoint de Wave 4.** 04-04 (misma Wave) reescribe `encino_orm/pool.py` y los ficheros `tests/test_pool.py`/`tests/test_pool_characterization.py`; este plan SOLO crea `tests/test_pool_concurrency.py`, así que ambos pueden correr en paralelo sin conflicto de ficheros. Los tests de reaper/generación/`close()` retenido son de 04-04 y NO se duplican aquí.

**Dependencias.** `depends_on: [04-01, 04-03, 04-05]`: 04-01 aporta el handle y `acquire()` sin carrera; 04-03 cierra la semántica de liberación (necesaria para que el commit standalone y la reutilización sean estables); 04-05 aporta `EventBarrier`/`FakeDb`/`BlockingFakeDb`, `pytest-timeout`, `pytest-repeat` y el marker `stress`. 04-02 (que introduce `execute_insert`) es dependencia transitiva de 04-03.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-pool-correctness-concurrency/04-RESEARCH.md
@.planning/phases/04-pool-correctness-concurrency/04-PATTERNS.md
@.planning/phases/01-safety-net-ci-gates-test-infrastructure/01-05-SUMMARY.md
@tests/_pool_helpers.py
@tests/test_pool_characterization.py
@tests/test_pool.py
@encino_orm/pool.py

<interfaces>
<!-- Anclajes que el ejecutor necesita. Extraídos del árbol; no explorar más. -->

`tests/_pool_helpers.py` (creado por 04-05) expone: `EventBarrier(parties: int)` con `await barrier.wait()`;
`_CONNECT_BARRIER` (módulo-level, `None` por defecto); `FakeDb` y `BlockingFakeDb(FakeDb)` (el `connect()`
de este último espera a `_CONNECT_BARRIER` si no es `None`). El import es `from tests._pool_helpers import ...`
(`tests/__init__.py` existe).

Registro de la barrera: `monkeypatch.setattr(helpers, "_CONNECT_BARRIER", EventBarrier(parties=5))`, con
`import tests._pool_helpers as helpers`. `parties` DEBE ser igual al número de tareas o la barrera nunca se
libera y el test cuelga (`@pytest.mark.timeout` es la red de seguridad).

Registro de engines fake: `monkeypatch.setitem(encino_orm.pool._ENGINES, "blocking", BlockingFakeDb)` y
`monkeypatch.setitem(encino_orm.pool._ENGINES, "fake", FakeDb)` (patrón de `tests/test_pool_characterization.py:147-156`).

Contrato de `PoolDb` tras 04-01…04-04: `acquire()` devuelve un `PooledConnection` (handle) con `.driver`;
`release(handle)`; `execute_insert(qry)` delega; `PoolDb("sqlite", min_size=1, max_size=N, database=<ruta>)`
para un pool SQLite real (patrón de `tests/test_pool.py:279-285`). `db.insert(tabla, data, returning="id")`
marca el `Query`; `execute_insert` devuelve el id.

Markers: `concurrency` ya registrado (`pyproject.toml:78`); `stress` registrado por 04-05; `timeout` lo aporta
`pytest-timeout`. El timeout global está DESACTIVADO (`timeout = 0`); el límite va por marker.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carrera de admisión determinista bajo `EventBarrier` (POOL-02 afirmado)</name>
  <read_first>
    - `tests/_pool_helpers.py` (EventBarrier, `_CONNECT_BARRIER`, `FakeDb`, `BlockingFakeDb`)
    - `tests/test_pool_characterization.py` :209-230 (la caracterización de la carrera y el uso de la barrera)
    - `encino_orm/pool.py` (`acquire` con reserva-antes-de-await tras 04-01)
    - `.planning/phases/04-pool-correctness-concurrency/04-RESEARCH.md` §1, §Pattern 1, §Pitfall 7 y §Code Examples ("Configuración de pytest-timeout/repeat")
  </read_first>
  <files>tests/test_pool_concurrency.py</files>
  <behavior>
    - Con `BlockingFakeDb`, `PoolDb("blocking", min_size=0, max_size=2)` y `EventBarrier(parties=5)`, las 5 tareas `acquire()` terminan y `p._size <= p._max_size` (propiedad afirmada, no caracterizada).
    - Cada tarea recibe un handle distinto y `len(p._checked_out) == 2` (las 2 con cupo) mientras las otras 3 esperan.
    - El test está marcado `@pytest.mark.concurrency` y `@pytest.mark.timeout(10)`.
    - Si `parties` se desajusta a propósito (test negativo separado, con su propio timeout corto), el test NO cuelga la suite: `pytest-timeout` lo interrumpe. Este test negativo se marca `xfail`/se omite por diseño para no matar la corrida (documentarlo) o se valida como un caso con `asyncio.wait_for` externo — elige la opción que no dependa de matar el proceso.
  </behavior>
  <action>
    Crea `tests/test_pool_concurrency.py` con la cabecera en español que explique: tests deterministas de concurrencia/estrés (POOL-07), barrera `asyncio.Event` (piso 3.10), y que sustituyen la caracterización de Fase 1 por propiedades afirmadas.

    Añade el test `test_no_overshoot_under_barrier`:
    - `import tests._pool_helpers as helpers`; `monkeypatch.setitem(pool_module._ENGINES, "blocking", helpers.BlockingFakeDb)`.
    - `p = PoolDb("blocking", min_size=0, max_size=2)`; `await p.connect()`.
    - `monkeypatch.setattr(helpers, "_CONNECT_BARRIER", helpers.EventBarrier(parties=5))`.
    - `tasks = [asyncio.create_task(p.acquire()) for _ in range(5)]`; `handles = await asyncio.gather(*tasks)`.
    - `assert p._size <= p._max_size`; `assert p._size == 2`; `assert len(set(handles)) == 5` (cada tarea recibió un handle; las 3 de más esperaron y obtuvieron cupo tras las liberaciones si el test libera) — AJUSTA: si las 5 tareas adquieren de golpe con `max_size=2`, las 3 sin cupo quedarían esperando en la cola y el `gather` colgaría. Para que sea determinista y no cuelgue: usa `parties=5` SOLO sobre `connect()`, y en el cuerpo libera los handles adquiridos para que las tareas en espera progresen, o usa `asyncio.wait_for` sobre el `gather` con un límite. La forma CORRECTA y no colgante: `parties=5` en la barrera de `connect()`, las 5 tareas pasan la comprobación de cupo a la vez, y el `gather` completa porque todas las que no obtienen cupo esperan en `_idle.get()` y son servidas al liberar las que sí lo obtienen — implementa el cuerpo como: `await asyncio.gather(*tasks)` donde cada tarea hace `h = await p.acquire(); await asyncio.sleep(0); await p.release(h)` (la liberación de las 2 primeras despierta a las siguientes). Documenta el porqué en un comentario.
    - Cierra el pool en un `finally`.
    - Decoradores: `@pytest.mark.concurrency` + `@pytest.mark.timeout(10)`.

    Añade `test_barrier_releases_only_with_matching_parties` como test NO colgante: crea `EventBarrier(parties=2)`, lanza `wait()` en una sola tarea y asserta con `asyncio.wait_for(task, timeout=0.5)` que NO se libera (lanza `TimeoutError`), demostrando el requisito `parties == nº de tareas`. Cancela la tarea al final. NO uses un `parties` desajustado en un test que dependa de que pytest-timeout lo mate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pool_concurrency.py -q -m concurrency</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EventBarrier" tests/test_pool_concurrency.py` devuelve >= 1
    - `grep -c "@pytest.mark.timeout" tests/test_pool_concurrency.py` devuelve >= 1
    - `grep -c "@pytest.mark.concurrency" tests/test_pool_concurrency.py` devuelve >= 1
    - El chequeo AST no encuentra identificadores de código `Barrier`/`TaskGroup` ni la cadena `asyncio.timeout` (docstring-safe; `pytest.mark.timeout` sí está permitido): `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_pool_concurrency.py').read_text(encoding='utf-8')); names={n.id for n in ast.walk(t) if isinstance(n,ast.Name)}|{n.attr for n in ast.walk(t) if isinstance(n,ast.Attribute)}; bad=[x for x in ('Barrier','TaskGroup') if x in names]; asto=any(isinstance(n,ast.Attribute) and n.attr=='timeout' and isinstance(n.value,ast.Name) and n.value.id=='asyncio' for n in ast.walk(t)); assert not bad and not asto, (bad, asto); print('OK: sin primitivas 3.11 en codigo')"`
    - `uv run pytest tests/test_pool_concurrency.py -q -m concurrency` pasa y termina en < 30 s
    - `uv run pytest tests/test_pool_concurrency.py -q` pasa bajo `--timeout-method=signal` (o el default local) sin colgarse
  </acceptance_criteria>
  <done>La carrera de admisión está cubierta por una barrera determinista con `pytest.mark.timeout`, afirmando `_size <= max_size` y sin colgar la suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ids concurrentes sin cruce y commit standalone visible (POOL-03/POOL-04 afirmados)</name>
  <read_first>
    - `tests/_pool_helpers.py` (dobles) y `tests/test_pool.py` :279-329 (patrón de pool SQLite real y `TestPoolAutocommit`)
    - `encino_orm/pool.py` (`execute_insert`, `execute`, `_run`, `release` tras 04-01…04-04)
    - `encino_orm/base.py` (`execute_insert` y el `returning` opt-in de 04-02)
    - `.planning/phases/04-pool-correctness-concurrency/04-RESEARCH.md` §2 (last_id), §Validation Architecture (POOL-03 → `concurrent_insert_ids`) y §Pattern 2
  </read_first>
  <files>tests/test_pool_concurrency.py</files>
  <behavior>
    - `test_concurrent_insert_ids_no_crosstalk`: `PoolDb("sqlite", min_size=1, max_size=3, database=str(tmp_path/"c.db"))`; crear tabla con PK AUTOINCREMENT; lanzar N=5 tareas, cada una `q = pool.insert("t", {"nombre": f"n{i}"}, returning="id")` y `rid = await pool.execute_insert(q)`; assertar que los 5 ids son DISTINTOS, que `set(ids) == set(range(1, 6))` y que cada `nombre` mapea a su id (leer las filas y comprobar la correspondencia).
    - `test_standalone_commit_visible_across_connections`: `await pool.execute(pool.insert("t", {"nombre": "a"}))`; `rows = await pool.fetch_all(Query("SELECT * FROM t", []))`; assert `len(rows) == 1` y `rows[0]["nombre"] == "a"` (preserva el comportamiento de `TestPoolAutocommit` desde un fichero nuevo).
    - `test_execute_insert_inside_transaction_uses_held_connection`: dentro de `async with pool.transaction():`, `execute_insert` captura el id en la conexión retenida y dos inserts consecutivos devuelven ids correlativos.
  </behavior>
  <action>
    Añade los tres tests a `tests/test_pool_concurrency.py` con un fixture local `sqlite_pool(tmp_path)` que cree `PoolDb("sqlite", min_size=1, max_size=3, database=str(tmp_path / "concurrency.db"))`, `await p.connect()`, `yield p`, `await p.close()` (patrón de `tests/test_pool.py:279-285`).

    En `test_concurrent_insert_ids_no_crosstalk`, marca `@pytest.mark.concurrency` y `@pytest.mark.timeout(20)`; usa N=5 (pequeño, determinista); recoge `asyncio.gather(*tasks)` y asserta unicidad y mapeo nombre→id. Comenta que SQLite serializa las escrituras con su `busy_timeout` y que el objetivo del test es la captura del id DENTRO de la sentencia (04-02), no el paralelismo del motor.

    En `test_standalone_commit_visible_across_connections`, NO marques `concurrency` (no usa barrera); el commit explícito de 04-03 lo hace determinista.

    En `test_execute_insert_inside_transaction_uses_held_connection`, usa `pool.transaction()` y asserta la correlación de ids.

    NO edites `tests/test_pool.py` ni `tests/test_pool_characterization.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pool_concurrency.py -q -k "concurrent_insert_ids or standalone_commit or inside_transaction"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "execute_insert" tests/test_pool_concurrency.py` devuelve >= 2
    - `uv run pytest tests/test_pool_concurrency.py -k "concurrent_insert_ids" -q` pasa y asserta 5 ids distintos
    - `uv run pytest tests/test_pool_concurrency.py -k "standalone_commit" -q` pasa
    - `uv run pytest tests/test_pool_concurrency.py -q` pasa
    - `git diff --numstat tests/test_pool.py tests/test_pool_characterization.py` está VACÍO (este plan no los toca)
    - El SUMMARY registra el valor RED si aplica (con el pool anterior, los ids podían cruzarse) y GREEN con el pool de 04-01…04-04
    - `uv run ruff check encino_orm tests` sale 0
  </acceptance_criteria>
  <done>Los inserts concurrentes devuelven cada uno su id sin cruce, el commit standalone es visible entre conexiones y `execute_insert` usa la conexión retenida dentro de `transaction()`.</done>
</task>

<task type="auto">
  <name>Task 3: Variante de estrés `pytest-repeat` tras el marker `stress`</name>
  <read_first>
    - `tests/test_pool_concurrency.py` (los tests de Tasks 1-2 a reutilizar)
    - `pyproject.toml` (markers `concurrency`/`stress` y `timeout = 0`; añadidos por 04-05)
    - `.planning/phases/04-pool-correctness-concurrency/04-RESEARCH.md` §Code Examples ("Configuración de pytest-timeout/repeat") y §Validation Architecture (POOL-07 → `-m stress --count=5`)
  </read_first>
  <files>tests/test_pool_concurrency.py</files>
  <action>
    Añade `test_no_overshoot_repeated` a `tests/test_pool_concurrency.py`: la misma comprobación de admisión que Task 1 pero SIN barrera (o con ella si el coste es bajo) y con N repeticiones pequeñas, marcada `@pytest.mark.stress` + `@pytest.mark.repeat(5)`. Debe pasar también en la corrida normal (el `repeat` la ejecuta 5 veces sin `--count`); documenta en un comentario que el soak real es `uv run pytest -m stress --count=100` y que en CI la corrida de estrés es opcional/pequeña (NO añadirla a `ci.yml` en este plan).

    Añade un test unitario de que la barrera es reutilizable (varios ciclos `wait()` con las mismas `parties` no se liberan antes de tiempo) o, si `EventBarrier` no soporta reutilización por diseño, documenta la limitación y crea una barrera nueva por ciclo. NO cambies `tests/_pool_helpers.py` (es de 04-05): si necesitas una variante, defínela localmente en este fichero.

    Verifica que el marker `stress` está registrado (lo hizo 04-05) ejecutando la selección; si `--strict-markers` falla, DETENTE y reporta que 04-05 no aterrizó.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pool_concurrency.py -q -m stress && uv run pytest tests/test_pool_concurrency.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@pytest.mark.stress" tests/test_pool_concurrency.py` devuelve >= 1
    - `grep -c "pytest.mark.repeat" tests/test_pool_concurrency.py` devuelve >= 1
    - `uv run pytest tests/test_pool_concurrency.py -q -m stress` pasa sin fallar por `--strict-markers`
    - `uv run pytest tests/test_pool_concurrency.py -q` pasa (el `repeat` corre en la corrida normal sin colgar)
    - `uv run pytest tests/test_pool_concurrency.py -q -m "not stress"` pasa
    - `uv run ruff check encino_orm tests` sale 0
  </acceptance_criteria>
  <done>La variante de estrés existe tras el marker `stress` con `pytest-repeat` y pasa en la corrida normal; el soak con `--count=100` queda como comando local documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tareas concurrentes -> `PoolDb.acquire()` | la barrera fuerza la carrera; un `parties` mal ajustado cuelga la suite |
| tareas concurrentes -> id de inserción | dos tareas insertando a la vez no deben observar el id de la otra |
| test de estrés -> duración de CI | una repetición alta puede agotar el `timeout-minutes` del job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Denial of Service | barrera con `parties` desajustado cuelga el test | mitigate | `@pytest.mark.timeout` en cada test de barrera; test negativo NO colgante con `asyncio.wait_for`; `--timeout-method=signal` en CI (04-05) |
| T-04-06-02 | Tampering (Integrity) | cruce de ids entre tareas concurrentes | mitigate | `execute_insert` captura el id DENTRO de la sentencia por conexión/tarea (04-02); test que asserta ids distintos y mapeo nombre→id |
| T-04-06-03 | Repudiation | cobertura que solo caracteriza el defecto | mitigate | Los tests AFIRMAN el comportamiento final (`_size <= max_size`, ids propios, commit visible), no el baseline defectuoso |
| T-04-06-04 | Denial of Service | variante de estrés que agota CI | mitigate | `repeat(5)` por defecto; el soak `--count=100` es local y NO se añade a `ci.yml` en este plan |
| T-04-06-05 | Tampering | primitivas 3.11 coladas en los tests | mitigate | `grep` de `asyncio.Barrier`/`asyncio.timeout`/`TaskGroup`/`except*` == 0 como criterio de aceptación |
| T-04-06-SC | Tampering | instalaciones de paquetes | n/a | Este plan no instala ni actualiza dependencias (lo hace 04-05 tras el checkpoint humano) |
</threat_model>

<verification>
- `uv run pytest tests/test_pool_concurrency.py -q` verde
- `uv run pytest tests/test_pool_concurrency.py -q -m concurrency` verde y en < 30 s
- `uv run pytest tests/test_pool_concurrency.py -q -m stress` verde
- `uv run pytest -q -m "not integration and not optional_engine"` verde
- El chequeo AST de `tests/test_pool_concurrency.py` no encuentra `Barrier`/`TaskGroup` ni `asyncio.timeout` en CÓDIGO (docstring-safe; `pytest.mark.timeout` permitido)
- `git diff --numstat tests/test_pool.py tests/test_pool_characterization.py` VACÍO (disjoint de Wave 4)
- `uv run ruff check encino_orm tests` sale 0
- `filterwarnings = ["error"]` intacto en `pyproject.toml`
</verification>

<success_criteria>
- POOL-07 cubierto: tests deterministas de concurrencia/estrés con barrera `asyncio.Event` (sin primitivas 3.11), `pytest-timeout` por marker y variante de estrés `pytest-repeat` tras el marker `stress`
- La carrera de admisión, el no-cruce de ids y el commit standalone quedan afirmados como propiedades, no como caracterización
- `tests/test_pool_concurrency.py` es un fichero nuevo y no toca los ficheros de 04-04 (Wave 4 sin conflicto)
- Los tests de barrera no pueden colgar la suite (timeout por marker + test negativo no colgante)
</success_criteria>

<output>
Create `.planning/phases/04-pool-correctness-concurrency/04-06-SUMMARY.md` when done
</output>
