---
phase: 04-pool-correctness-concurrency
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .github/workflows/ci.yml
  - tests/_pool_helpers.py
autonomous: false
requirements: [POOL-07]
must_haves:
  truths:
    - "`pytest-timeout==2.4.0` y `pytest-repeat==0.9.4` están en `[dependency-groups].dev` con pin exacto y `uv.lock` regenerado; `uv lock --check` pasa."
    - "El marker `stress` está registrado en `[tool.pytest.ini_options].markers`, de modo que `--strict-markers` no falla al usar `@pytest.mark.stress`."
    - "El job `test` de CI (Linux) ejecuta pytest con `--timeout-method=signal`; el job `engine-heavy` también."
    - "Existe `tests/_pool_helpers.py` con `EventBarrier` (construida SOLO con `asyncio.Event`, sin `asyncio.Barrier`/`TaskGroup`/`asyncio.timeout`), `FakeDb` y `BlockingFakeDb`, listos para que 04-06 los importe."
    - "Este plan NO añade ninguna aserción de comportamiento del pool: solo habilita la infraestructura (deps, marker, CI, helper)."
  artifacts:
    - path: "pyproject.toml"
      provides: "dev deps pytest-timeout/pytest-repeat con pin exacto + marker stress"
      contains: "pytest-timeout"
    - path: "uv.lock"
      provides: "lock regenerado con las dos dev deps nuevas"
      contains: "pytest-timeout"
    - path: ".github/workflows/ci.yml"
      provides: "método signal del timeout en los jobs que corren pytest"
      contains: "--timeout-method=signal"
    - path: "tests/_pool_helpers.py"
      provides: "EventBarrier 3.10-safe + dobles FakeDb/BlockingFakeDb compartidos"
      contains: "class EventBarrier"
  key_links:
    - from: "pyproject.toml::[dependency-groups].dev"
      to: "uv.lock"
      via: "uv add --dev regenera el lock; el job deps de CI corre uv lock --check"
      pattern: "pytest-timeout"
    - from: "tests/_pool_helpers.py::EventBarrier"
      to: "asyncio.Event"
      via: "liberación en la última llegada, sin temporizador (piso Python 3.10)"
      pattern: "asyncio\\.Event"
    - from: ".github/workflows/ci.yml::test"
      to: "pytest-timeout"
      via: "--timeout-method=signal permite continuar la corrida tras un timeout"
      pattern: "--timeout-method=signal"
---

<objective>
Habilitar la infraestructura de tests deterministas de concurrencia/estrés de POOL-07, SIN escribir todavía ninguna aserción de comportamiento del pool (eso es 04-06).

La fase necesita dos plugins de test que hoy no están instalados: `pytest-timeout` (una barrera con `parties` mal ajustado cuelga el test para siempre, `04-RESEARCH.md` Pitfall 7) y `pytest-repeat` (variante de estrés). Ambos son `[ASSUMED]` porque `slopcheck` no está disponible en la máquina de research (`04-RESEARCH.md` §Package Legitimacy Audit), así que la instalación va precedida de un `checkpoint:human-verify` `blocking-human` NO auto-aprobable. Además, la barrera determinista ya existe localmente en `tests/test_pool_characterization.py:126-144` y debe extraerse a un helper compartido para que 04-06 lo reutilice sin duplicar.

Purpose: 04-06 (variante b) no puede escribir `@pytest.mark.timeout`/`@pytest.mark.repeat` ni importar la barrera si esta infraestructura no existe antes. Se separa en su propio plan para que el checkpoint humano no bloquee el trabajo de pool de 04-01 y para que la instalación de dependencias sea un cambio aislado y revisable.

Output: `pyproject.toml` con las dos dev deps pinadas y el marker `stress`; `uv.lock` regenerado; `.github/workflows/ci.yml` con `--timeout-method=signal`; `tests/_pool_helpers.py` con `EventBarrier`/`FakeDb`/`BlockingFakeDb`.

**Restricción dura (Research Correction #4):** piso Python 3.10. Prohibido `asyncio.Barrier`, `asyncio.timeout`, `TaskGroup` y `except*` en el helper y en cualquier test. `EventBarrier` se construye solo con `asyncio.Event`.

**Alcance (NO comportamiento).** Este plan no añade tests que asserten el comportamiento del pool ni modifica `encino_orm/`. Su verificación es de infraestructura: el lock, el registro del marker y la carga de los plugins.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-pool-correctness-concurrency/04-RESEARCH.md
@.planning/phases/04-pool-correctness-concurrency/04-PATTERNS.md
@pyproject.toml
@.github/workflows/ci.yml
@tests/test_pool_characterization.py
@tests/test_pool.py

<interfaces>
<!-- Anclajes que el ejecutor necesita. Extraídos del árbol; no explorar más. -->

`pyproject.toml`:
- `[tool.pytest.ini_options]` :41-80: `asyncio_mode = "auto"`, `addopts = "-ra --strict-markers"`,
  `xfail_strict = true`, `filterwarnings = ["error", "ignore:The default datetime adapter..."]`,
  `markers = ["integration", "optional_engine", "concurrency", "benchmark"]` (:75-80).
- `[dependency-groups].dev` :265-286: pines exactos de `ruff==0.16.8` y `syrupy==6.1.1` con comentario-racional
  (copiar ese estilo para las dos deps nuevas).
- `[[tool.mypy.overrides]]` :230-239 (NO tocar aquí; el ratchet de `encino_orm.pool` es 04-04).
- `[tool.ruff.lint.per-file-ignores]` :127-131 (NO tocar aquí; la decisión sobre PT011/B017 es 04-04).

`.github/workflows/ci.yml`:
- Job `test` :102-136: `run: > uv run pytest -q -m "not optional_engine" --junitxml=junit.xml --cov=... --cov-report=`.
- Job `engine-heavy` :353-356: `run: > uv run pytest -q tests/test_mssql.py tests/test_oracle.py --junitxml=junit.xml ... --cov-fail-under=0`.
- Job `deps` :219-225: `uv lock --check` (por eso hay que regenerar el lock en el mismo commit).

Helper a extraer — forma exacta a preservar (`tests/test_pool_characterization.py:106-144`):
- `_CONNECT_BARRIER = None` a nivel de módulo; `BlockingFakeDb(FakeDb)` cuyo `connect()` hace
  `barrier = _CONNECT_BARRIER; if barrier is not None: await barrier.wait(); await super().connect(**kwargs)`.
- `EventBarrier.__init__(self, parties: int)` con `_arrived` y `asyncio.Event`; `wait()` incrementa,
  hace `set()` en la última llegada y `await self._event.wait()`.
- `FakeDb` (`:29-104`): registra cada llamada en `self.calls`, `transaction()` como `@asynccontextmanager`,
  `execute` pone `self._last=42` y `self._in_tx=True`, `last_id()` devuelve `self._last`.
  IMPORTANTE: 04-02 añadirá `execute_insert` al contrato; el helper debe declarar `execute_insert` como
  `async def execute_insert(self, qry)` que devuelva `self._last` (o `None`) para no romperse en 04-02.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificar la legitimidad de los paquetes [ASSUMED] antes de instalar</name>
  <files>pyproject.toml, uv.lock</files>
  <action>
    Ejecuta la comprobación automática de resolución en PyPI definida como la verificación de esta tarea y pega su salida en el SUMMARY. Después DETENTE y presenta el checkpoint: esta tarea no instala nada ni modifica ningún fichero. Solo cuando el humano apruebe podrá Task 2 ejecutar `uv add --dev "pytest-timeout==2.4.0" "pytest-repeat==0.9.4"`. Si el humano nombra un paquete que falla, no instales nada y reporta el bloqueo. Este checkpoint NUNCA es auto-aprobable (`workflow.auto_advance` debe ignorarse para él).
  </action>
  <what-built>
    Pre-chequeo automático de legitimidad de los dos paquetes que este plan añade a `[dependency-groups].dev`: `pytest-timeout` y `pytest-repeat`.

    La auditoría de legitimidad de paquetes de `04-RESEARCH.md` no pudo ejecutar `slopcheck` (herramienta ausente), así que ambos quedan `[ASSUMED]` y no pueden aprobarse automáticamente. Evidencia adicional recogida en research (no sustituye al gate): ambos son plugins de la organización `pytest-dev`, con repositorio público, sin vulnerabilidades reportadas, y ambos cargaron y funcionaron bajo `pytest==9.1.1` + `pytest-asyncio==1.4.0` en un venv aislado Python 3.10.18 (`repeat(3)` produjo 3 ejecuciones; `timeout(2)` interrumpió un `await` colgado).

    Antes de este checkpoint el ejecutor DEBE haber ejecutado el chequeo automático y pegado su salida en el SUMMARY. Versiones esperadas según research: `pytest-timeout 2.4.0` y `pytest-repeat 0.9.4` (últimas publicadas en PyPI a 2026-09-18).
  </what-built>
  <how-to-verify>
    1. Confirma que la salida del chequeo automático muestra ambos paquetes resolviendo en PyPI.
    2. Abre cada página del registro y confirma que el proyecto es el esperado (no un typosquat):
       - https://pypi.org/project/pytest-timeout/
       - https://pypi.org/project/pytest-repeat/
    3. Confirma que el enlace "Repository"/"Source" de cada página apunta al upstream canónico:
       `github.com/pytest-dev/pytest-timeout` y `github.com/pytest-dev/pytest-repeat`.
    4. Confirma que aceptas añadirlos a `[dependency-groups].dev` con pin exacto (`==`), como `ruff`/`syrupy`.

    Si algún paquete no coincide, DETENTE y reporta — no ejecutes `uv add`.
  </how-to-verify>
  <read_first>
    - `.planning/phases/04-pool-correctness-concurrency/04-RESEARCH.md` §Package Legitimacy Audit (la tabla `[ASSUMED]` y la nota de `slopcheck` ausente), §Standard Stack (versiones esperadas) y §Environment Availability
    - `pyproject.toml` `[dependency-groups].dev` (para confirmar que ninguno de los dos está ya presente)
  </read_first>
  <verify>
    <automated>uv run python -c "import json,urllib.request as u; [print(p, json.load(u.urlopen(f'https://pypi.org/pypi/{p}/json'))['info']['version'], json.load(u.urlopen(f'https://pypi.org/pypi/{p}/json'))['info']['project_urls'].get('Source') or json.load(u.urlopen(f'https://pypi.org/pypi/{p}/json'))['info']['home_page']) for p in ['pytest-timeout','pytest-repeat']]"</automated>
  </verify>
  <acceptance_criteria>
    - El chequeo automático imprime una versión y una URL de upstream canónico para `pytest-timeout` y `pytest-repeat`
    - Las URLs canónicas son `github.com/pytest-dev/pytest-timeout` y `github.com/pytest-dev/pytest-repeat`
    - `pyproject.toml` `[dependency-groups].dev` NO cambia en esta tarea — no se instala nada aquí
    - El humano reanuda con aprobación explícita; el checkpoint nunca es auto-aprobable (`workflow.auto_advance` ignorado)
    - La salida del chequeo automático queda pegada en `04-05-SUMMARY.md`
  </acceptance_criteria>
  <resume-signal>Escribe "approved" para permitir `uv add --dev "pytest-timeout==2.4.0" "pytest-repeat==0.9.4"`, o nombra el paquete que falló</resume-signal>
  <done>Ambos paquetes resuelven en PyPI al upstream canónico y el humano ha aprobado añadirlos a `[dependency-groups].dev`.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar las dev deps, registrar el marker `stress` y añadir `--timeout-method=signal` a CI</name>
  <read_first>
    - `pyproject.toml` :41-80 (markers y `filterwarnings`) y :265-286 (`[dependency-groups].dev`, estilo de pin exacto con comentario)
    - `.github/workflows/ci.yml` :102-136 (job `test`) y :353-356 (job `engine-heavy`)
    - `.planning/phases/04-pool-correctness-concurrency/04-RESEARCH.md` §Code Examples ("Configuración de pytest-timeout/repeat") y §Pitfall 10 (regenerar el lock)
    - `.planning/phases/04-pool-correctness-concurrency/04-PATTERNS.md` §`pyproject.toml` y §`.github/workflows/ci.yml`
  </read_first>
  <files>pyproject.toml, uv.lock, .github/workflows/ci.yml</files>
  <action>
    Ejecuta `uv add --dev "pytest-timeout==2.4.0" "pytest-repeat==0.9.4"` (requiere la aprobación de Task 1). `uv add` regenera `uv.lock` en el mismo paso; NO edites el lock a mano. Conserva el estilo de pin exacto y añade un comentario-racional en español junto a las dos entradas, copiando el patrón de `syrupy==6.1.1` (`pyproject.toml:281-285`): la salida/efecto está versionada y un rango abierto podría resolver una versión más nueva y romper un PR no relacionado.

    En `[tool.pytest.ini_options]`:
    - Añade el marker `stress` a `markers` con descripción en español ("variante de estrés con pytest-repeat; se corre con --count=N"), junto a los cuatro existentes (`integration`, `optional_engine`, `concurrency`, `benchmark`). Sin esto, `--strict-markers` (en `addopts`) falla la colección al usar `@pytest.mark.stress`.
    - Añade `timeout = 0` explícito con un comentario que explique que el timeout global está DESACTIVADO a propósito: el job `engine-heavy` (Oracle 60-120 s) no debe morir por un límite global; el timeout se aplica por marker SOLO a los tests de barrera.
    - NO toques `filterwarnings` (sigue `["error", ...]`).

    En `.github/workflows/ci.yml`:
    - Añade `--timeout-method=signal` al comando de pytest del job `test` (Linux). El método *signal* permite que la corrida continúe tras un timeout; el método *thread* (default en Windows) mata el proceso y pierde el JUnit XML (Pitfall 7).
    - Añade `--timeout-method=signal` también al comando del job `engine-heavy` (mismo razonamiento, Linux).
    - NO añadas una corrida `-m stress --count=N` a CI en este plan: la variante de estrés es soak local; si entra en CI será una decisión posterior. El marker queda inerte hasta que 04-06 lo use.
  </action>
  <verify>
    <automated>uv lock --check && uv run python -c "import pytest_timeout, pytest_repeat; print('plugins OK')" && uv run pytest --collect-only -q -m stress 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pytest-timeout==2.4.0' pyproject.toml` devuelve 1 y `grep -c 'pytest-repeat==0.9.4' pyproject.toml` devuelve 1
    - `grep -c '"stress: ' pyproject.toml` devuelve 1 (marker registrado con descripción)
    - `grep -c -- "--timeout-method=signal" .github/workflows/ci.yml` devuelve >= 1
    - `uv lock --check` sale 0 (lock regenerado por `uv add`)
    - `uv run pytest --collect-only -q -m stress` NO falla por `--strict-markers` (el marker está registrado; puede no seleccionar ningún test todavía)
    - `uv run pytest -q -m "not integration and not optional_engine"` sigue verde (ninguna dependencia nueva rompe la suite)
    - La salida de `uv run python -c "import pytest_timeout, pytest_repeat"` y la versión de cada plugin quedan en el SUMMARY
  </acceptance_criteria>
  <done>Las dos dev deps están pinadas y el lock regenerado; el marker `stress` está registrado; CI usa `--timeout-method=signal` en los dos jobs que corren pytest.</done>
</task>

<task type="auto">
  <name>Task 3: Extraer `EventBarrier` y los dobles a `tests/_pool_helpers.py`</name>
  <read_first>
    - `tests/test_pool_characterization.py` :29-156 (los dobles `FakeDb`/`BlockingFakeDb`, `_CONNECT_BARRIER` y `EventBarrier` a extraer)
    - `tests/test_pool.py` :11-92 (`FakeDb` de test_pool, con `insert` que registra `conflict`/`schema`; variante más completa)
    - `.planning/phases/04-pool-correctness-concurrency/04-PATTERNS.md` §Tests (barrera 3.10-safe y fakes a mano) y §Shared Patterns (Fakes deterministas + monkeypatch)
    - `.planning/phases/04-pool-correctness-concurrency/04-RESEARCH.md` §Pattern (barrera determinista 3.10) y Research Correction #4
  </read_first>
  <files>tests/_pool_helpers.py</files>
  <action>
    Crea `tests/_pool_helpers.py` (el prefijo `_` evita que pytest lo colecte) con:
    - `EventBarrier(parties: int)`: `asyncio.Event` + contador `_arrived`; `wait()` incrementa, hace `set()` cuando `_arrived >= parties` y `await self._event.wait()`. Docstring en español que explique que el piso es Python 3.10 (la clase `Barrier` de `asyncio` no existe allí), que la liberación es determinista (última llegada, nunca un temporizador) y que `parties` DEBE ser igual al número de tareas o el test se cuelga.
    - `_CONNECT_BARRIER = None` a nivel de módulo, `FakeDb` y `BlockingFakeDb(FakeDb)`: copia la forma de `tests/test_pool_characterization.py:29-144` (registro de llamadas en `self.calls`, `transaction()` como `@asynccontextmanager`, `execute` que pone `self._last = 42` y `self._in_tx = True`, `last_id()` que devuelve `self._last`, `in_transaction`/`commit`/`rollback`/`save_point`, `is_alive`). `BlockingFakeDb.connect()` espera a `_CONNECT_BARRIER` si no es `None`.
    - Añade `async def execute_insert(self, qry)` al `FakeDb` del helper: devuelve `self._last` (o `None`) y registra `("execute_insert", qry)`. Existe para que 04-02 (que introduce `execute_insert` en el contrato de `Db`) no rompa los tests de 04-06; en este plan no se usa.
    - NO uses `unittest.mock` (convención del repo: fakes a mano + `monkeypatch`). NO uses las primitivas de concurrencia añadidas en 3.11 (la clase `Barrier` de `asyncio`, `asyncio.timeout`, `TaskGroup`): el piso es 3.10 y el gate de esta tarea es un chequeo AST sobre identificadores de CÓDIGO, así que el docstring PUEDE nombrarlas para explicar la restricción (los docstrings son `ast.Constant` y no generan nodos `Name`/`Attribute`).

    NO modifiques `tests/test_pool_characterization.py` ni `tests/test_pool.py` en este plan: la migración de esos ficheros a importar del helper la hace 04-06 (y 04-01 los conserva con definiciones locales para no depender de un plan paralelo). Esto evita un solapamiento de ficheros en la Wave 1.
  </action>
  <verify>
    <automated>uv run python -c "import ast,pathlib; src=pathlib.Path('tests/_pool_helpers.py').read_text(encoding='utf-8'); tree=ast.parse(src); names=set(); [names.add(n.id) for n in ast.walk(tree) if isinstance(n,ast.Name)]; [names.add(n.attr) for n in ast.walk(tree) if isinstance(n,ast.Attribute)]; bad=[t for t in ('Barrier','TaskGroup','timeout') if t in names]; assert not bad, bad; print('helper OK: sin primitivas 3.11 en codigo')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class EventBarrier" tests/_pool_helpers.py` devuelve 1
    - `grep -c "class FakeDb" tests/_pool_helpers.py` devuelve 1 y `grep -c "class BlockingFakeDb" tests/_pool_helpers.py` devuelve 1
    - El chequeo AST no encuentra ningún identificador de código `Barrier`/`TaskGroup`/`timeout` (los docstrings pueden nombrarlos; el AST no los ve)
    - `grep -c "unittest.mock" tests/_pool_helpers.py` devuelve 0
    - `uv run python -c "import ast, pathlib; ast.parse(pathlib.Path('tests/_pool_helpers.py').read_text(encoding='utf-8'))"` sale 0
    - `uv run pytest tests/test_pool.py tests/test_pool_characterization.py -q` sigue verde sin editar esos ficheros (no hay regresión por el helper añadido)
    - `uv run ruff check encino_orm tests` sale 0
  </acceptance_criteria>
  <done>`tests/_pool_helpers.py` ofrece `EventBarrier`, `FakeDb` y `BlockingFakeDb` 3.10-safe, sin primitivas prohibidas, listos para que 04-06 los importe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> entorno de CI/desarrollo | dos paquetes nuevos entran en `[dependency-groups].dev`; `slopcheck` no pudo auditarlos |
| `uv.lock` -> job `deps` de CI | un lock desincronizado de `pyproject.toml` rompe `uv lock --check` |
| helper de test -> tests de concurrencia | una barrera con `parties` incorrecto cuelga la suite en vez de fallar |
| CI Linux -> método de timeout | el método *thread* en Linux/Windows puede matar el proceso y perder el JUnit XML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-SC | Tampering (supply chain) | `uv add --dev pytest-timeout==2.4.0 pytest-repeat==0.9.4` | mitigate | `slopcheck` no disponible → ambos `[ASSUMED]`. Compuerta `checkpoint:human-verify` `blocking-human` ANTES de instalar, pin exacto `==`, verificación manual de PyPI + repositorio `pytest-dev`. La compuerta nunca es auto-aprobable |
| T-04-05-01 | Denial of Service | `EventBarrier` con `parties` desajustado | mitigate | Docstring que exige `parties == nº de tareas`; `pytest-timeout` con `--timeout-method=signal` en CI mata el test colgado sin perder el XML; el timeout se aplica por marker, no global |
| T-04-05-02 | Tampering | marker `stress` no registrado | mitigate | Registrar `stress` en `markers`; `--strict-markers` falla la colección si falta, de modo que no puede quedar inerte en silencio |
| T-04-05-03 | Repudiation | lock desincronizado presentado como cambio aislado | mitigate | `uv add` regenera `uv.lock` en el mismo commit; el job `deps` corre `uv lock --check` como gate bloqueante |
| T-04-05-04 | Tampering | dependencias de runtime contaminadas por deps de test | accept | Solo se toca `[dependency-groups].dev`; `[project.dependencies]` y los extras quedan intactos, así que el contrato de importación diferida del núcleo no cambia |
</threat_model>

<verification>
- `uv lock --check` sale 0
- `uv run python -c "import pytest_timeout, pytest_repeat"` sale 0 y las versiones son `2.4.0` / `0.9.4`
- `uv run pytest -q -m "not integration and not optional_engine"` verde (sin regresiones)
- `uv run pytest --collect-only -q -m stress` no falla por `--strict-markers`
- `grep -c -- "--timeout-method=signal" .github/workflows/ci.yml` >= 1
- `grep -c '"stress: ' pyproject.toml` == 1
- El chequeo AST de `tests/_pool_helpers.py` no encuentra identificadores de código `Barrier`/`TaskGroup`/`timeout` (docstring-safe)
- `uv run ruff check encino_orm tests` sale 0
- `filterwarnings = ["error"]` intacto en `pyproject.toml`
- Ninguna aserción de comportamiento del pool se añade en este plan (el helper `execute_insert` no se usa todavía)
</verification>

<success_criteria>
- POOL-07 (parte de infraestructura) cubierto: `pytest-timeout` y `pytest-repeat` instalados con pin exacto y lock regenerado, marker `stress` registrado y `--timeout-method=signal` en CI
- `tests/_pool_helpers.py` ofrece `EventBarrier`/`FakeDb`/`BlockingFakeDb` 3.10-safe, sin primitivas prohibidas ni `unittest.mock`
- La compuerta de legitimidad de paquetes queda superada por aprobación humana explícita (nunca auto-aprobable)
- El contrato de importación diferida del núcleo no se toca (solo dev deps)
</success_criteria>

<output>
Create `.planning/phases/04-pool-correctness-concurrency/04-05-SUMMARY.md` when done
</output>
